f
- Remaining function/class names (getZootopiaFirestore, PgFirestore, shouldUseFirestore) will need future cleanup once migration fully complete
- Do not restore Firebase-era deployment assumptions; Vercel is sole active deployment target
- Do not re-add references to non-existent `/firebase/` or `/scripts/firebase/` folders unless they are actually created
- Reserved for future work: Renaming the remaining misleading function names (getZootopiaFirestore -> getZootopiaPgAdapter) and class name (PgFirestore -> PgPostgresAdapter) once cascading function call sites can be safely updated

2026-04-11 (FINAL PASS) - Guide-Files Deprecation Warnings + Ledger Reference Corrections

Scope
- Mark Guide-Files containing Firebase-era assumptions as reference-only to prevent agent misinterpreting them as current architecture
- Update ledger reference from "primary architecture guide" to clarified "reference-only" designation
- Correct PowerShell guide to explicitly state App Hosting is NO LONGER ACTIVE

Action: Guide-Files marking
- `Guide-Files/Zootopia_Club_Next_16_2_2_Simplified_Monorepo_Guide_2026_WORKSPACE_UPDATED.md`: Added prominent DEPRECATION NOTICE at top explaining this is Firebase-era reference, not current truth; current uses Supabase+Auth.js+Vercel
- `Guide-Files/powershell-script-commands-guide.md`: Changed title from "Firebase App Hosting Secrets" to "LEGACY REFERENCE ONLY"; updated deprecation status to clearly state "ACTIVE APPS NO LONGER USE FIREBASE APP HOSTING"; Vercel+Supabase is current deployment
- `Guide-Files/AI_Agent_Execution_Instruction_Guide_2026.md`: Fixed line 350 from "Firebase App Hosting or the current real deployment path" to "Vercel (current real deployment path) or legacy Firebase App Hosting"

Action: Ledger reference updates
- Changed "Primary architecture guide" designation to "Architecture reference (DEPRECATED Firebase-era; see Current Truth Snapshot for active stack)"
- This signals agents to treat the guide as historical, not authoritative

Verification results
- `npm run lint --workspace @zootopia/web`: PASSED
- `npm run build --workspace @zootopia/web`: PASSED (47 routes, 10.1s compile)
- Git commit: Successfully recorded

Architecture cleanup completeness assessment
- File renamed: zootopia-firestore-pg.ts -> zootopia-postgres-adapter.ts (file name mismatch resolved)
- Imports updated: 2 direct references corrected
- Documentation: All Firebase terminology labeled as legacy/reference-only where it appears
- Active code wording: No remaining misleading "current" or "active" Firebase claims in runtime/active surfaces
- Environment files: Properly commented with "Legacy Firebase script compatibility"
- Guide-Files: Marked as reference-only with deprecation warnings
- Ledger accuracy: Corrected and refreshed with 200+ lines of historical context + new cleanup changelog

All user requirements from original request are now COMPLETE.
Next agents should reference the ledger "Current Truth Snapshot" as the authority, not the deprecated guides.

2026-04-11 | Migration-First Database System Implementation

Root Cause
- Error: `relation "zc_entities" does not exist` at `PgQuery._getWithSql`
- Cause: Migration files existed in `supabase/migrations/` but had NEVER been applied
  to the actual Supabase Postgres database. The code assumed the table existed at
  runtime, but no migration runner had been executed.
- `zc_entities` IS the intended main table -- it is a generic JSON document store
  (EAV pattern) used by the Postgres adapter for all logical collections
  (documents, assessments, infographics, credits, idempotency, admin logs, users).

Whether zc_entities Was Truly Intended
- YES. It is intentionally designed as a generic JSONB document store that mirrors
  former Firestore collections. The `collection` column acts as a namespace, `id` as
  the document key, `owner_uid` as the indexed owner, and `body` as the full JSONB
  payload. This is the active persistence model for the current codebase.

Exact Migrations Added/Changed (4 total, all pre-existing but never applied)
1. `20260410120000_zootopia_platform_entities.sql`
   - Creates `public.zc_entities` table (collection, id, owner_uid, body jsonb, updated_at)
   - Primary key: (collection, id)
   - Indexes: owner_collection_idx, body_expires_idx (for documents)
   - RLS enabled
   - Creates storage bucket `zootopia-private`

2. `20260410180000_phase1_firestore_parity_tables.sql`
   - Creates 8 relational tables:
     - `user_profiles` -- user profile metadata (Firestore: users)
     - `documents` -- upload metadata (Firestore: documents)
     - `assessment_generations` -- assessment generation records (Firestore: assessmentGenerations)
     - `infographic_generations` -- infographic records (Firestore: infographicGenerations)
     - `admin_activity_logs` -- audit trail (Firestore: adminActivityLogs)
     - `assessment_generation_idempotency` -- duplicate-click protection
     - `assessment_daily_credits` -- UTC day ledger
     - `assessment_credit_accounts` -- per-user credit wallet settings
     - `assessment_credit_grants` -- time-bounded admin grants
   - All tables have RLS enabled, owner-scoped indexes, and proper constraints

3. `20260410180001_phase1_storage_bucket.sql`
   - Creates storage bucket `zootopia-app` (idempotent)

4. `20260410213000_phase1_owner_rls_and_storage_policies.sql`
   - Creates RLS policies for all owner-scoped tables (select/insert/update/delete)
   - Creates storage RLS policies for `zootopia-private` bucket
   - Idempotent policy creation (checks pg_policies before creating)

Exact Runtime Files Changed
- NONE. No runtime code was modified. The fix was purely operational: applying
  the existing migrations to the actual database.

Exact Tables/Indexes/Constraints Created
Tables:
  - `zc_entities` (PK: collection+id, indexes on owner_uid+collection, body->expiresAt)
  - `user_profiles` (PK: uid, index on lower(email))
  - `documents` (PK: id, index on owner_uid+created_at)
  - `assessment_generations` (PK: id, index on owner_uid+created_at)
  - `infographic_generations` (PK: id, index on owner_uid+created_at)
  - `admin_activity_logs` (PK: id, index on created_at)
  - `assessment_generation_idempotency` (PK: id, index on owner_uid)
  - `assessment_daily_credits` (PK: id, index on owner_uid+day_key)
  - `assessment_credit_accounts` (PK: owner_uid)
  - `assessment_credit_grants` (PK: id, index on owner_uid)
  - `_migration_history` (PK: migration_name) -- migration tracking table

Storage Buckets:
  - `zootopia-private` (private)
  - `zootopia-app` (private)

RLS Policies:
  - user_profiles: self_select, self_insert, self_update
  - All owner-scoped tables: owner_select, owner_insert, owner_update, owner_delete
  - admin_activity_logs: owner_select (covers owner_uid, actor_uid, target_uid)
  - Storage: zootopia_private_owner select/insert/update/delete

How to Bootstrap a Fresh Database from Zero
1. Ensure `DATABASE_URL` or `SUPABASE_DATABASE_URL` is set in `.env.local`
2. Run: `npm run db:apply`
3. All pending migrations are applied in order inside transactions
4. Verify: `npm run db:apply:dry` should report "All migrations are already applied"

Local Workflow Commands
- `npm run db:apply` -- Apply all pending migrations
- `npm run db:apply:dry` -- Preview what would be applied (no changes)
- `npm run supabase:migrations:list` -- List migration files
- `npm run supabase:migrations:validate` -- Validate migration file ordering
- `npm run supabase:check` -- Validate + list

Production Workflow Commands (Vercel + Supabase)
- Migrations are NOT auto-applied on deploy. They must be run manually or via CI/CD.
- Option A (manual): Run `node scripts/supabase/apply-migrations.mjs` with production
  DATABASE_URL set (use Vercel CLI env pull or run locally with prod connection string)
- Option B (CI/CD): Add a pre-deploy step in your CI pipeline that runs the migration
  script against the production database before deploying the app
- Option C (Supabase Dashboard): Paste migration SQL into Supabase SQL editor manually
- IMPORTANT: Always run migrations BEFORE deploying code that depends on new tables

Migration System Design
- Script: `scripts/supabase/apply-migrations.mjs`
- Uses `postgres` npm package (already installed)
- Loads `.env.local` via dotenv for standalone operation
- Creates `_migration_history` table to track applied migrations
- Each migration runs inside a transaction (atomic)
- Failed migrations stop execution and report the error
- Idempotent: re-running skips already-applied migrations

Environment-Specific Notes
1. Local Integrated Development:
   - Migrations applied via `npm run db:apply`
   - Uses `DATABASE_URL` from `.env.local`
   - Pointing to Supabase pooler: `aws-0-eu-west-1.pooler.supabase.com:5432`

2. Vercel Production Deployment:
   - Migrations must be applied separately from deployment
   - Use production DATABASE_URL from Vercel env vars
   - Run migration script locally or in CI before deploy
   - No auto-migration on Vercel deploy (intentional safety)

3. Supabase Remote Database:
   - Migrations are the schema source of truth under `supabase/migrations/`
   - Do NOT use Supabase Dashboard schema editor as primary source
   - Do NOT rely on runtime auto-creation of tables
   - Schema drift is prevented by the migration history tracking

Verification Results
- `npm run lint`: PASSED
- `npm run typecheck`: PASSED
- `npm run build`: PASSED (8.8s compile, 21+ routes confirmed)

Files Added
- `scripts/supabase/apply-migrations.mjs` -- Migration apply script

Files Modified
- `package.json` -- Added `db:apply` and `db:apply:dry` npm scripts

Safety and Scope Notes
- No runtime code was changed
- No tables were dropped or altered
- All migrations are idempotent and safe to re-run
- Migration history is tracked in `_migration_history` table
- The `zc_entities` table is confirmed as the active persistence model

---
2026-04-11 — Qwen Assessment Runtime Centralization + Error Message Fix

Root Cause of "The selected model runtime is not configured on the server yet.":
The error is thrown at `execution.ts:1340-1345` (`ASSESSMENT_PROVIDER_NOT_CONFIGURED`) when
`resolveProviderRuntime("qwen3.5-flash").apiKey` returns null/falsy, i.e. when
`process.env.DASHSCOPE_API_KEY` is not loaded in the server process.

The code path for qwen3.5-flash:
1. UI sends `{ modelId: "qwen3.5-flash" }` to POST `/api/assessment`
2. Validation via `isModelSupportedForTool("assessment", "qwen3.5-flash")` — PASSES (model is in catalog with toolScopes: ["assessment"])
3. `generateAssessment()` → `getModelById("qwen3.5-flash")` → provider: "qwen" — CORRECT
4. `resolveProviderRuntime("qwen3.5-flash")` → checks `DASHSCOPE_API_KEY` + base URL — SOURCE OF FAILURE
5. `executeAssessmentModel()` → switch case "qwen3.5-flash" → `executeQwenAssessmentModel()` — CORROUTING
6. `!input.runtime.apiKey` → throws `ASSESSMENT_PROVIDER_NOT_CONFIGURED` — FAILURE POINT

The most common trigger: server process lacks `DASHSCOPE_API_KEY` in `process.env`
(e.g. server started before env was set, or Vercel env not configured).

Qwen Connection Strategy Chosen:
OpenAI-compatible chat/completions path via `{DASHSCOPE_BASE_URL}/chat/completions`.
This is the correct fit because the assessment flow is text-first (JSON Q&A generation)
and does not require DashScope's native multimodal APIs.

Canonical env keys (documented in provider-runtime.ts):
- `DASHSCOPE_API_KEY` — API key for DashScope/Alibaba Model Studio
- `DASHSCOPE_BASE_URL` — Base URL for OpenAI-compatible mode (default: `https://dashscope-intl.aliyuncs.com/compatible-mode/v1`)
- Legacy aliases: `DASHSCOPE_COMPATIBLE_BASE_URL`, `ALIBABA_MODEL_STUDIO_BASE_URL`
- `QWEN_MODEL` — model name override (not used for qwen3.5-flash direct selection)

Changes Made:
1. Centralized Qwen client factory (`createQwenClient`) in `provider-runtime.ts` alongside
   env key constants, types, and readiness checks. This is now the single source of truth
   for Qwen/DashScope OpenAI-compatible connection logic.
2. Removed duplicated Qwen client factory and types from `execution.ts` — now imports
   `createQwenClient`, `QwenOpenAiCompatibleChatCompletionResponse` from provider-runtime.
3. Fixed Google "not configured" error message: changed from "The selected Google model is
   not configured on the server yet." to "The selected model runtime is not configured on the
   server yet." — aligns with the i18n key `assessmentFieldProviderUnavailable`.

Files Modified
- `apps/web/lib/server/ai/provider-runtime.ts` — Added Qwen client factory, request/response
  types, and comprehensive env contract documentation header.
- `apps/web/lib/server/ai/execution.ts` — Removed Qwen client/types duplication; imports
  from provider-runtime. Fixed Google error message. Updated client call to `createQwenClient`.

Verification Results
- `npx tsc --noEmit`: PASSED (zero errors)
- `npx eslint` on changed files: PASSED
- `npx next build`: PASSED (all routes including /api/assessment confirmed)

Admin/User Isolation
- No changes to auth, session, or owner-scoped checks. Provider-runtime fix does not
  bypass any existing session checks, admin boundaries, or assessment export guards.

All Assessment Models Verified
- qwen3.5-flash: routes to executeQwenAssessmentModel (temperature 0.25) — requires DASHSCOPE_API_KEY + DASHSCOPE_BASE_URL
- gemini-3.1-flash-lite-preview: routes to executeGoogleAssessmentModel (temperature 0.2) — requires GOOGLE_AI_API_KEY
- gemini-2.5-pro: routes to executeGoogleAssessmentModel (temperature 0.3) — requires GOOGLE_AI_API_KEY
- gemini-2.5-flash: routes to executeGoogleAssessmentModel (temperature 0.25) — requires GOOGLE_AI_API_KEY
- Legacy aliases (qwen-balanced, qwen-flash-us, google-balanced, google-advanced, gemini-2.5-flash-lite): all map to canonical lanes

---
2026-04-11 — Assessment Runtime Determinism Hardening (Two-File Surgical Pass)

Scope
- Limited strictly to:
  - `apps/web/lib/server/ai/execution.ts`
  - `apps/web/lib/server/ai/provider-runtime.ts`

Objective
- Remove model/runtime split-brain risk so assessment generation uses one canonical model id and one resolved runtime path for readiness, execution, and persisted identity.

Changes Applied
- `apps/web/lib/server/ai/execution.ts`
  - Canonicalizes `request.modelId` once near the start of `generateAssessment`.
  - Resolves `model` + `providerRuntime` once from the canonical id and reuses them for:
    - provider readiness (`providerConfigured` in prompt orchestration)
    - execution routing (`executeAssessmentModel`)
    - persistence identity (`request.modelId`, `modelId`, meta fields, prompt preview inputs).
  - Simplified `executeAssessmentModel` to route by already-canonical `input.model.id` and consume the runtime passed in, removing duplicate internal canonicalization/runtime resolution.

- `apps/web/lib/server/ai/provider-runtime.ts`
  - Re-established explicit DashScope env contract exports used by execution:
    - primary: `DASHSCOPE_BASE_URL`
    - legacy fallback aliases: `DASHSCOPE_COMPATIBLE_BASE_URL`, `ALIBABA_MODEL_STUDIO_BASE_URL`
    - exported ordered key list: `QWEN_BASE_URL_ENV_KEYS`
  - Centralized base URL resolution to primary-first with legacy fallback behavior and returned `baseUrlEnvKey` from the resolver.

Regression Encountered and Resolved During Verification
- Initial `typecheck` failed because `execution.ts` imported `QWEN_BASE_URL_ENV_KEYS` while the current `provider-runtime.ts` state no longer exported it.
- Fixed by restoring the export and centralized base URL resolver contract in `provider-runtime.ts`.

Verification Results
- `npm run lint --workspace @zootopia/web` PASSED
- `npm run typecheck --workspace @zootopia/web` PASSED
- `npm run build --workspace @zootopia/web` PASSED

Risk Note
- No model catalog, route, UI, or auth changes in this pass.
- Runtime behavior change is intentionally narrow: canonical model/runtime alignment and DashScope base URL resolution contract consistency.

---
2026-04-11 - Assessment Runtime Diagnostics Hardening + Deterministic QA Proof

Scope
- Completed an assessment runtime diagnostics hardening pass and captured deterministic end-to-end QA proof for admin/user lane boundaries, provider-path execution, owner separation, and missing-config machine-code behavior.

Runtime Files Changed (hardening implementation)
- `apps/web/lib/server/ai/execution.ts`
- `apps/web/lib/server/ai/provider-runtime.ts`
- `apps/web/app/api/assessment/route.ts`
- `apps/web/components/assessment/assessment-studio.tsx`

Hardening Summary
- `apps/web/lib/server/ai/execution.ts`
  - Added explicit provider machine codes for missing API key/base URL, model unavailability, and provider route mismatch.
  - Introduced structured execution context (`layer`, `subsystem`, `operation`, `route`, `flow`, model/provider/lane metadata) and unified error creation through `createAssessmentExecutionError(...)`.
  - Propagated contextual metadata through provider execution, timeout classification, parser failure handling, and unsupported-model handling.
  - Threaded canonical/requested model and lane context through `executeAssessmentModel(...)` and `generateAssessment(...)`.
- `apps/web/lib/server/ai/provider-runtime.ts`
  - Added runtime model override resolution via model `runtimeEnvKey`.
  - Surfaced `missingEnvKeys` diagnostics for Google and Qwen runtimes, including missing-base-url hints for Qwen.
- `apps/web/app/api/assessment/route.ts`
  - Added route-level diagnostic response wrapper to standardize machine-code API errors with context.
  - Added explicit user-lane enforcement (`ASSESSMENT_USER_LANE_REQUIRED`) and contextualized error paths for validation, idempotency, credits, generation, and finalization.
  - Preserved owner-scoped behavior; cleanup failure paths are now best-effort and non-fatal.
- `apps/web/components/assessment/assessment-studio.tsx`
  - Added client mapping for newly introduced provider machine codes and user-lane-required code.

Runtime QA Artifacts And Script Fixes
- Primary QA script: `.tmp/assessment-runtime-supabase-qa.mjs`
  - Fixed profile payload to satisfy live validation constraints (4-part full name + valid 7-digit university code + valid E.164 phone).
  - Updated generation success handling to treat HTTP `201` as successful creation (in addition to `200`).
  - Expanded profile-failure diagnostics to include response payload/text for faster triage.

Deterministic Runtime Setup Used For Proof
- Baseline server: `http://127.0.0.1:3045` with `ZOOTOPIA_ADMIN_EMAILS=qa-admin-runtime-20260411@example.com`
- Missing-config server: `http://127.0.0.1:3046` with same admin allowlist and `DASHSCOPE_API_KEY` intentionally blank
- Health verification:
  - `3045` runtime flags: `googleAi=true`, `qwen=true`
  - `3046` runtime flags: `googleAi=true`, `qwen=false`

QA Root Causes Resolved During This Pass
- Profile completion failed with HTTP 400 because the script payload violated server validators:
  - full name must include at least 4 meaningful parts
  - university code must be exactly 7 digits with valid cohort prefix
- Qwen path and owner-separation checks initially failed because successful creation returned HTTP `201`, while script logic accepted only HTTP `200`.

Final QA Evidence
- Final report path:
  - `output/playwright/assessment-runtime-diagnostics/2026-04-11T18-03-40-658Z/assessment-runtime-diagnostics-report.json`
- Final checks (all pass):
  - `adminLaneRejected: true`
  - `qwenPathVerified: true`
  - `googlePathVerified: true`
  - `ownerLaneSeparationVerified: true`
  - `missingConfigCodeVerified: true`

Build/Static Verification
- `npm run lint --workspace @zootopia/web`: PASSED
- `npm run typecheck --workspace @zootopia/web`: PASSED
- `npm run build --workspace @zootopia/web`: PASSED

Safety Notes
- Runtime authority and owner-scoped checks remain server-side; no client-side privilege broadening was introduced.
- QA helper files under `.tmp/` and generated reports under `output/` are diagnostics artifacts and not production source-of-truth.

---
2026-04-11 — Admin Assessment Access Bypass (Admin 403 Fix)

Root Cause
- Admin accounts received HTTP 403 "Assessment generation is disabled for this account" when
  attempting to generate assessments, despite having admin role.
- Two independent blocks prevented admin access:

  1. Route handler (`apps/web/app/api/assessment/route.ts`, line ~251):
     `if (user.role !== "user")` returned 403 with code `ASSESSMENT_USER_LANE_REQUIRED`.
     This blocked ALL non-user roles including admins.

  2. Repository (`apps/web/lib/server/repository.ts`, lines ~3266 and ~3433):
     `if (account.assessmentAccess === "disabled")` threw `ASSESSMENT_ACCESS_DISABLED`
     in both the Firestore transaction path and the memory store path.
     Admin accounts with `assessmentAccess: "disabled"` were blocked identically to normal users.

  The UI in `assessment-studio.tsx` maps both error codes to the same generic message:
  "Assessment generation is disabled for this account. Contact an administrator to restore access."

Files Changed
- `apps/web/app/api/assessment/route.ts`
  - Replaced `if (user.role !== "user")` block with explicit admin bypass logic:
    - `isAdmin = user.role === "admin"` and `isAccountActive = user.status === "active"` computed.
    - Admin hard block: only blocks inactive admin accounts (new code `ASSESSMENT_ACCESS_DENIED_ADMIN_HARD_BLOCK`).
    - Normal-user lane gate: only applies when `!isAdmin && user.role !== "user"`.
    - Structured authorization diagnostics logged at every decision point:
      - `assessment-authorization-check` (every request)
      - `assessment-admin-hard-blocked` (inactive admin denied)
      - `assessment-user-lane-required` (non-admin non-user denied)
      - `assessment-admin-bypass-granted` (admin allowed through)

- `apps/web/lib/server/repository.ts`
  - Firestore path (`saveAssessmentGenerationWithCreditCommit`, ~line 3266):
    - Added `isAdmin = input.user.role === "admin"` check.
    - Only throws `ASSESSMENT_ACCESS_DISABLED` when `!isAdmin && account.assessmentAccess === "disabled"`.
    - Added structured warning log: `[assessment-credit-commit] assessment-access-disabled-firestore`.

  - Memory store path (`saveAssessmentGenerationWithCreditCommit`, ~line 3433):
    - Added `isMemoryStoreAdmin = (input.user.role as UserRole) === "admin"` check.
    - Only throws `ASSESSMENT_ACCESS_DISABLED` when `!isMemoryStoreAdmin && account.assessmentAccess === "disabled"`.
    - Added structured warning log: `[assessment-credit-commit] assessment-access-disabled-memory`.
    - Cast to `UserRole` needed because callers currently only pass non-admin users (admins are
      exempted earlier via `isAssessmentDailyCreditsExempt`), so TypeScript narrows the type to `"user"`.

Design Decisions
- Admin bypass is explicit and auditable: every authorization decision emits a structured log.
- Normal-user restrictions remain fully intact — credits, tool-disable, lane gating all unchanged.
- Admin hard block exists as a separate code path so future admin-only denials can be added
  without touching normal-user gates.
- The `ASSESSMENT_ACCESS_DISABLED` error is still thrown for normal users with disabled access.

Verification Results
- `npx tsc --noEmit` (apps/web): PASSED (zero errors)
- `npx eslint` on changed files: PASSED (zero errors)

Safety and Scope Notes
- Only two files modified: route handler and repository.
- No changes to auth, session, owner-scoped checks, or credit consumption logic.
- Admin bypass applies only to assessment generation; other admin boundaries remain unchanged.
- Structured logs use `[assessment-auth]` and `[assessment-credit-commit]` prefixes for easy filtering.

---

## 2026-04-11 — Surgical 2026-Grade Cleanup: Remove Active Firestore/Firebase Dependence, Naming Cleanup

### Root Causes Found
1. **Misleading "Firestore" naming in active runtime code** — Despite the adapter being 100% Supabase Postgres, the class was named `PgFirestore` and the factory function was `getZootopiaFirestore()`. These names implied Firebase/Firestore reality when the actual backend is Supabase Postgres via the `postgres` npm package.
2. **Legacy function name `listAllFirebaseAuthUsers()`** — This function enumerates Supabase Auth users, not Firebase Auth users.
3. **Legacy log event name `assessment-access-disabled-firestore`** — The event name referenced Firestore when the actual persistence is Supabase Postgres.
4. **No actual Firebase SDK imports in active code** — The codebase was already migrated; only naming remnants remained.

### Files Changed
1. **`apps/web/lib/server/zootopia-postgres-adapter.ts`**
   - Renamed `class PgFirestore` → `class PgDatabase`
   - Renamed `function getZootopiaFirestore()` → `function getZootopiaDatabase()`
   - Added deprecated aliases for backward compatibility:
     - `export const getZootopiaFirestore = getZootopiaDatabase;`
     - `export const PgFirestore = PgDatabase;`
   - Rewrote file header comment to accurately describe Supabase Postgres reality
   - Removed misleading "Firestore naming exists because..." migration note

2. **`apps/web/lib/server/repository.ts`**
   - Updated import: `getZootopiaDatabase` + deprecated alias `getZootopiaFirestore`
   - Renamed `function shouldUseFirestore()` → `function shouldUseDatabase()`
   - Added deprecated alias: `const shouldUseFirestore = shouldUseDatabase;`
   - Renamed `function listAllFirebaseAuthUsers()` → `function listAllAuthUsers()`
   - Updated call site: `const authUsers = await listAllAuthUsers();`
   - Fixed log event name: `assessment-access-disabled-firestore` → `assessment-access-disabled-database`

### Firebase/Firestore Active-Runtime Paths Removed or Relabeled
| Old Name | New Name | File |
|----------|----------|------|
| `class PgFirestore` | `class PgDatabase` | zootopia-postgres-adapter.ts |
| `getZootopiaFirestore()` | `getZootopiaDatabase()` | zootopia-postgres-adapter.ts |
| `shouldUseFirestore()` | `shouldUseDatabase()` | repository.ts |
| `listAllFirebaseAuthUsers()` | `listAllAuthUsers()` | repository.ts |
| `assessment-access-disabled-firestore` | `assessment-access-disabled-database` | repository.ts |

### How Normal-User Assessment Generation Now Works
- Normal users can generate assessments when: authenticated, profile complete, account active, assessment access enabled, credits available, provider configured.
- Normal users are blocked only for: unauthenticated session, suspended account, assessment tool disabled, insufficient credits, provider misconfiguration, provider auth failure, runtime timeout, invalid model, document validation failure.
- Credits deduction logic remains intact and explicit.
- Idempotency and request safety preserved.

### How Admin Control Is Now Separated from User Rules
- Admin bypass logic in route handler (`/api/assessment/route.ts`) grants admins explicit bypass of normal-user restrictions.
- Admin hard block exists as separate code path (only blocks inactive admins).
- Admin permissions resolved through: Auth.js session → repository user record → role field.
- `ZOOTOPIA_ADMIN_EMAILS` used as allowlist input; role stored in user record and session.

### Structured Error Codes/Logging
- Existing error codes preserved: `UNAUTHENTICATED`, `PROFILE_INCOMPLETE`, `ASSESSMENT_ACCESS_DENIED_ADMIN_HARD_BLOCK`, `ASSESSMENT_USER_LANE_REQUIRED`, `ASSESSMENT_DAILY_CREDITS_EXHAUSTED`, `ASSESSMENT_ACCESS_DISABLED`, etc.
- Log event name fixed: `assessment-access-disabled-database` (was `assessment-access-disabled-firestore`).
- Structured logs use `[assessment-auth]` and `[assessment-credit-commit]` prefixes.

### Credits/Quota Logic Status
- Credits deduction logic: INTACT
- Daily credit reservation: INTACT
- Grant credit consumption: INTACT
- Manual credits: INTACT
- Admin credit mutation: INTACT

### Admin Hard-Block Logic Status
- Admin hard block: INTACT (blocks inactive admins only)
- Admin bypass: INTACT (active admins pass through normal-user gates)

### Lint/Typecheck/Build Results
- `npx tsc --noEmit` (apps/web): PASSED (zero errors)

### Ledger Entry
- Renamed `PgFirestore` → `PgDatabase` in zootopia-postgres-adapter.ts
- Renamed `getZootopiaFirestore()` → `getZootopiaDatabase()` in zootopia-postgres-adapter.ts
- Added deprecated aliases for backward compatibility
- Renamed `shouldUseFirestore()` → `shouldUseDatabase()` in repository.ts
- Renamed `listAllFirebaseAuthUsers()` → `listAllAuthUsers()` in repository.ts
- Fixed log event name: `assessment-access-disabled-firestore` → `assessment-access-disabled-database`
- No actual Firebase SDK imports found in active code (already migrated)
- All changes are backward-compatible via deprecated aliases

---

2026-04-11 — Runtime Boundary Hardening (Admin/User Lane Alignment + Active Naming Truth)

Scope
- Completed a read-first audit of active runtime boundaries before edits, covering auth/session callbacks, server auth guards, admin mutation routes, upload/document ownership, assessment generation, infographic generation, assessment export routes, repository ownership checks, and storage path enforcement.
- Applied minimal, deterministic changes only where active runtime behavior or naming truth was still inconsistent with Supabase/Auth.js architecture.

Audited Files/Routes (high-impact)
- Auth/session and role resolution:
  - `apps/web/auth.ts`
  - `apps/web/proxy.ts`
  - `apps/web/lib/server/session.ts`
  - `apps/web/lib/server/admin-auth.ts`
  - `apps/web/lib/server/supabase-admin.ts`
  - `apps/web/lib/server/auth-types.ts`
- User/admin route boundaries:
  - `apps/web/app/api/assessment/route.ts`
  - `apps/web/app/api/assessment/[id]/route.ts`
  - `apps/web/app/api/assessment/results/[id]/route.ts`
  - `apps/web/app/api/assessment/linked-document/route.ts`
  - `apps/web/app/api/assessment/credits/route.ts`
  - `apps/web/app/api/assessment/export/**/route.ts`
  - `apps/web/app/api/uploads/route.ts`
  - `apps/web/app/api/uploads/[id]/route.ts`
  - `apps/web/app/api/infographic/route.ts`
  - `apps/web/app/api/infographic/[id]/route.ts`
  - `apps/web/app/api/admin/users/route.ts`
  - `apps/web/app/api/admin/users/[uid]/credits/route.ts`
  - `apps/web/app/api/admin/users/[uid]/status/route.ts`
  - `apps/web/app/api/admin/users/[uid]/role/route.ts`
- Ownership/repository/storage core:
  - `apps/web/lib/server/repository.ts`
  - `apps/web/lib/server/zootopia-postgres-adapter.ts`
  - `apps/web/lib/server/assessment-export-route-context.ts`
  - `apps/web/scripts/normalize-standalone.mjs`

Findings (actual issues fixed this pass)
1) Assessment lane context mismatch (admin requests were tagged as user lane in execution metadata)
- Root cause:
  - `apps/web/app/api/assessment/route.ts` called `generateAssessment(...)` with `sessionLane: "user"` and `requestLane: "user"` even when `user.role === "admin"`.
- Why it matters:
  - Broke deterministic session-to-execution lane alignment in diagnostics and provider/runtime traces.
  - Increased risk of confusing admin-vs-user incident triage under concurrency.
- Fix:
  - Added explicit `executionLane` derived from authenticated role and passed it to `generateAssessment(...)`.

2) Active repository path still used Firestore-era helper names in runtime call sites
- Root cause:
  - `apps/web/lib/server/repository.ts` still executed all persistence path checks through `shouldUseFirestore()` and `getZootopiaFirestore()` identifiers.
- Why it matters:
  - Active runtime naming contradicted Supabase Postgres architecture truth and produced role/storage diagnostics with legacy cognitive drift.
- Fix:
  - Repointed active call sites to `shouldUseDatabase()` and `getZootopiaDatabase()`.
  - Removed deprecated alias import and deprecated local alias usage from active repository flow.

3) Active token mapping still emitted Firebase-shaped provider claim field
- Root cause:
  - `apps/web/lib/server/supabase-admin.ts` generated `decodedToken.firebase.sign_in_provider` as active provider claim output.
  - `apps/web/lib/server/admin-auth.ts` read `decodedToken.firebase` directly in provider resolution fallback.
- Why it matters:
  - Active auth runtime still presented Firebase-era claim naming despite Supabase/Auth.js authority.
- Fix:
  - Added architecture-neutral provider claim shape: `auth_provider.signInProvider`.
  - Updated provider resolution to prefer `app_metadata.provider`, then `auth_provider.signInProvider`, then legacy `firebase.sign_in_provider` fallback.
  - Updated `DecodedAuthToken` type contract accordingly.

4) Active standalone script comment still referenced Firebase App Hosting
- Root cause:
  - `apps/web/scripts/normalize-standalone.mjs` comment referenced "Firebase App Hosting/Cloud Run" as active packaging context.
- Fix:
  - Reworded comment to runtime-neutral container context without Firebase-era active deployment wording.

Files Changed
- `apps/web/app/api/assessment/route.ts`
  - Lane metadata now derives from authenticated role and no longer hardcodes user lane.
- `apps/web/lib/server/repository.ts`
  - Active persistence call sites now use database-truth helper names (`shouldUseDatabase`, `getZootopiaDatabase`).
  - Removed deprecated Firestore alias import and local alias usage from active execution paths.
- `apps/web/lib/server/supabase-admin.ts`
  - Token mapping now emits `auth_provider.signInProvider`.
- `apps/web/lib/server/admin-auth.ts`
  - Provider resolution now supports architecture-neutral claim shape first, with explicit legacy fallback.
- `apps/web/lib/server/auth-types.ts`
  - Added `auth_provider` claim contract and retained legacy `firebase` typing as fallback-only.
- `apps/web/scripts/normalize-standalone.mjs`
  - Removed Firebase App Hosting wording from active runtime script comment.
- `apps/web/lib/server/zootopia-postgres-adapter.ts`
  - Strengthened deprecated alias comments to mark Firestore-named exports as compatibility-only.

Admin/User Separation Outcome
- Admin mutation/control routes remain explicit and protected by `getAdminSessionUser()`.
- Normal-user owner-only routes remain owner-scoped server-side (`get...ForOwner(...)` patterns).
- Assessment generation now keeps admin lane identity consistent through execution diagnostics.
- No admin power leak into user lanes and no user access to admin mutation lanes was introduced.

Ownership/Isolation Outcome
- Verified unchanged and still enforced server-side:
  - Upload ownership from session UID only.
  - Owner-scoped document/generation lookups (`...ForOwner`).
  - Storage path scoping and assertion enforcement in upload/document pipeline.
  - Assessment/infographic/export reads remain owner-scoped in user-facing endpoints.

Legacy Firebase References Remaining (intentional)
- `apps/web/lib/server/zootopia-postgres-adapter.ts`
  - `getZootopiaFirestore` and `PgFirestore` remain as deprecated compatibility aliases only.
- `apps/web/lib/server/auth-types.ts` and `apps/web/lib/server/admin-auth.ts`
  - `firebase.sign_in_provider` remains as legacy token-compatibility fallback only.
- These are explicitly legacy-only compatibility surfaces, not active architecture authority.

Verification
- `npm run lint --workspace @zootopia/web`: PASSED
- `npm run typecheck --workspace @zootopia/web`: PASSED
- `npm run build --workspace @zootopia/web`: PASSED
- `next build` route manifest includes all critical auth/admin/assessment/upload/infographic routes after changes.

Safety Notes
- No database schema, migration, or storage bucket policy changes in this pass.
- No broad architectural redesign.
- No relaxation of owner checks.
- No widening of user access permissions.

---

2026-04-11 — Strict Admin Isolation + Admin Wiring Verification + Alias Removal Pass

Scope
- Completed another read-first hardening pass focused on admin isolation, admin control integrity, frontend-to-backend wiring truth, owner isolation, and removal of dead Firebase/Firestore-era runtime aliases.
- Started with full ledger read, then executed a dedicated Explore subagent audit, then performed direct source verification before edits.

Audited Admin Surfaces
- Admin pages/layouts/guards:
  - `apps/web/app/(protected)/admin/layout.tsx`
  - `apps/web/app/(protected)/admin/page.tsx`
  - `apps/web/app/(protected)/admin/users/page.tsx`
  - `apps/web/proxy.ts`
  - `apps/web/lib/server/session.ts`
- Admin API routes:
  - `apps/web/app/api/admin/overview/route.ts`
  - `apps/web/app/api/admin/users/route.ts`
  - `apps/web/app/api/admin/users/export/route.ts`
  - `apps/web/app/api/admin/users/[uid]/role/route.ts`
  - `apps/web/app/api/admin/users/[uid]/status/route.ts`
  - `apps/web/app/api/admin/users/[uid]/credits/route.ts`
- Frontend admin mutation wiring:
  - `apps/web/components/admin/users-table.tsx`
- Owner/lane-sensitive routes:
  - `apps/web/app/api/uploads/route.ts`
  - `apps/web/app/api/uploads/[id]/route.ts`
  - `apps/web/app/api/assessment/route.ts`
  - `apps/web/app/api/infographic/route.ts`
  - `apps/web/lib/server/document-runtime.ts`

Verified Before Edit (safe surfaces)
- All admin API mutation/read endpoints were already protected with `getAdminSessionUser()` and returned 403 for non-admins.
- Admin pages remained server-guarded by `requireAdminUser()` at layout/page level plus proxy redirect gate.
- Frontend admin controls in `users-table.tsx` were already wired to real protected admin routes and only updated local UI state after successful API responses (no false success optimistic writes).
- Admin capability coverage already present and wired:
  - role mutation
  - status mutation
  - assessment access enable/disable
  - manual credits adjust/set
  - daily override set/clear
  - grant add/revoke
  - user listing + export
- Owner isolation and file/storage scoping remained server-enforced via owner-scoped lookups and path assertions.

Issues Fixed In This Pass
1) Dead Firestore alias exports still present in active adapter file
- File: `apps/web/lib/server/zootopia-postgres-adapter.ts`
- Fix: Removed dead compatibility alias exports:
  - `getZootopiaFirestore`
  - `PgFirestore`
- Verification: no remaining runtime references in `apps/web/**/*.{ts,tsx,mjs}`.

2) Legacy Firebase token-provider fallback still in active admin provider resolution
- Files:
  - `apps/web/lib/server/admin-auth.ts`
  - `apps/web/lib/server/auth-types.ts`
- Fix:
  - Removed `firebase.sign_in_provider` fallback parse path from `getDecodedSignInProvider(...)`.
  - Removed legacy `firebase` field from `DecodedAuthToken` contract.
- Result: active provider resolution now stays on current-truth claims (`app_metadata.provider`, `auth_provider.signInProvider`).

3) Admin-only infographic route lacked denied-access/success audit events
- File: `apps/web/app/api/infographic/route.ts`
- Fix:
  - Added denied non-admin access audit log (`infographic-admin-access-denied`) before 403.
  - Added successful generation admin audit log (`infographic-generated`) after persistence.

4) Proxy role normalization lacked explicit diagnostics for malformed role claims
- File: `apps/web/proxy.ts`
- Fix:
  - Added fail-closed role normalization diagnostics log for unexpected role claim values while preserving default user-lane behavior.

Admin Isolation Outcome
- Normal authenticated users cannot load admin pages or execute admin API mutations.
- Admin-only route protections now exist across proxy redirect gate, server page/layout guard, and API-level guard.
- No admin bypass for normal users was introduced.
- No user-access widening was introduced.

Frontend/Backend Wiring Outcome
- Admin dashboard user management UI remains connected to protected backend mutations via `/api/admin/users/{uid}/{action}` and `/api/admin/users/{uid}/credits`.
- State refresh after admin actions remains response-driven from backend payloads; no synthetic success path detected.
- Export flow remains backend-authoritative through `/api/admin/users/export`.

Ownership/Isolation Outcome
- Verified unchanged and still strict:
  - upload owner derived from authenticated session UID only
  - owner-scoped document read/delete paths
  - deterministic storage path format: `documents/{ownerUid}/{documentId}/{fileName}`
  - storage path assertion gate (`assertOwnerScopedStoragePath`) remains enforced
  - assessment and infographic owner read/write paths remain owner-scoped

Legacy Naming Status After This Pass
- Removed from active runtime code in this pass:
  - `getZootopiaFirestore` alias export
  - `PgFirestore` alias export
  - `firebase.sign_in_provider` provider-resolution fallback
- Remaining Firebase/Firestore wording is now limited to historical/reference context and non-runtime artifacts.

Verification
- `npm run lint --workspace @zootopia/web`: PASSED
- `npm run typecheck --workspace @zootopia/web`: PASSED
- `npm run build --workspace @zootopia/web`: PASSED
- Changed-file diagnostics: no errors in updated files.

Safety Notes
- No schema changes.
- No migration changes.
- No storage policy changes.
- No broad refactor; edits were surgical and bounded to admin diagnostics/guards and runtime naming truth.

---

2026-04-11 — Strict Admin Control Verification + Real Admin Delete User Flow

Scope
- Completed mandatory read-first pass (full ledger read, then subagent exploration, then direct source verification) before edits.
- Verified admin user-management controls end-to-end and implemented a real destructive admin-only delete-user flow for the active Supabase/Auth.js runtime.
- Added structured mutation diagnostics across admin mutation routes.
- Removed remaining stale Firebase-era wording from active runtime comments.

Verified Admin Controls (Frontend -> Backend -> Repository)
- `Make admin` / `Make user`
  - UI: `apps/web/components/admin/users-table.tsx` (`patchUser(..., "role", ...)`)
  - API: `PATCH /api/admin/users/[uid]/role`
  - Guard: `getAdminSessionUser()`
  - Backend mutation: `setUserRole(...)` + claim persistence + token revocation
- `Block user` / `Unblock user`
  - UI: `patchUser(..., "status", ...)`
  - API: `PATCH /api/admin/users/[uid]/status`
  - Guard: `getAdminSessionUser()`
  - Backend mutation: `setUserStatus(...)` + Supabase disable/enable + token revocation
- `Assessment access toggle`
  - UI: `patchCredits(..., { action: "set_access" })`
  - API: `PATCH /api/admin/users/[uid]/credits`
  - Guard: `getAdminSessionUser()` + self-mutation block
  - Backend mutation: `applyAdminAssessmentCreditMutation(...)`
- `Daily override save / clear`
  - UI: `set_daily_override` / `clear_daily_override`
  - API: same credits route
  - Backend mutation: same repository mutation path
- `Manual credits add / subtract / set`
  - UI actions: `add_manual_credits`, `subtract_manual_credits`, `set_manual_credits`
  - API: same credits route
  - Backend mutation: same repository mutation path
- `Temporary grant credits` / `Revoke grant`
  - UI actions: `grant_credits`, `revoke_grant`
  - API: same credits route
  - Backend mutation: same repository mutation path
- `Export users`
  - UI: `handleExportUsers()`
  - API: `GET /api/admin/users/export`
  - Guard: `getAdminSessionUser()`
  - Backend: `listUsers()`, auth metadata join, credit-state join, workbook generation, admin log append

New Destructive Admin-Only Delete Flow
- Added API route: `DELETE /api/admin/users/[uid]`
  - File: `apps/web/app/api/admin/users/[uid]/route.ts`
  - Enforces admin guard via `getAdminSessionUser()`
  - Validates target uid
  - Blocks self-delete (`ADMIN_SELF_DELETE_FORBIDDEN`)
  - Requires explicit confirmation text matching target uid or target email
  - Emits structured mutation diagnostics and returns structured success payload
- Added centralized repository/service deletion path:
  - File: `apps/web/lib/server/repository.ts`
  - New function: `deleteUserAccountAsAdmin(...)`
  - Strategy selected: mixed hard-delete with safe sequence and explicit partial-failure reporting
    1. Freeze account first (disable + revoke refresh tokens)
    2. Delete owner-scoped storage artifacts (best-effort, idempotent)
    3. Delete owner-scoped data records from active persistence collections
    4. Delete Supabase Auth user account (hard delete)
  - Returns `AdminUserDeletionSummary` including auth/database/storage results and failure point/reason
  - Logs success/failure via `appendAdminLog(...)` with structured metadata

Delete Scope Implemented
- Supabase Auth
  - Hard delete user via new adapter path (`deleteSupabaseAuthUser`) exposed through `getServerAuthAdmin().deleteUser(...)`
- Postgres-backed application records
  - Owner-scoped deletes for:
    - `documents`
    - `assessmentGenerations`
    - `infographicGenerations`
    - `assessmentCreditGrants`
    - `assessmentDailyCredits`
    - `assessmentGenerationIdempotency`
  - Direct document delete for:
    - `assessmentCreditAccounts/{uid}`
    - legacy `users/{uid}` if present
- Storage
  - Deletes document binaries for owner records
  - Deletes assessment artifact storage objects referenced by owner generations
  - Cleanup is explicitly marked best-effort/idempotent in diagnostics summary

Admin Mutation Diagnostics Added
- Added structured route-level diagnostics for:
  - `PATCH /api/admin/users/[uid]/role`
  - `PATCH /api/admin/users/[uid]/status`
  - `PATCH /api/admin/users/[uid]/credits`
  - `DELETE /api/admin/users/[uid]`
- Each log now includes:
  - action name
  - target uid
  - acting admin uid
  - route hit
  - backend mutation result (`started|success|failed`)
  - failure reason when failed

Frontend Admin UI Update
- File: `apps/web/components/admin/users-table.tsx`
- Added visible destructive button: `Delete user permanently`
- Uses destructive styling (`variant="destructive"`)
- Requires explicit prompt confirmation including target email or uid
- Sends `DELETE /api/admin/users/[uid]` with confirmation payload
- Reconciles UI from backend truth by replacing users list with server-returned `users`

Runtime Naming Cleanup (Active Paths)
- `apps/web/next.config.ts`
  - Removed stale Firebase-era wording in canonical env-source comment
- `apps/web/lib/server/zootopia-postgres-adapter.ts`
  - Removed stale Firebase/Firestore wording from active adapter header comment

Shared Contracts Added
- File: `packages/shared-types/src/admin.ts`
- Added:
  - `AdminUserDeletionStorageSummary`
  - `AdminUserDeletionDatabaseSummary`
  - `AdminUserDeletionSummary`
  - `AdminDeleteUserResponse`

Localization Updates
- Files:
  - `apps/web/messages/en.json`
  - `apps/web/messages/ar.json`
- Added admin delete-user labels/prompts/errors for explicit destructive confirmation UX.

Files Changed
- `apps/web/lib/server/supabase-admin.ts`
- `apps/web/lib/server/server-auth.ts`
- `apps/web/lib/server/repository.ts`
- `apps/web/app/api/admin/users/[uid]/route.ts` (new)
- `apps/web/app/api/admin/users/[uid]/role/route.ts`
- `apps/web/app/api/admin/users/[uid]/status/route.ts`
- `apps/web/app/api/admin/users/[uid]/credits/route.ts`
- `apps/web/components/admin/users-table.tsx`
- `packages/shared-types/src/admin.ts`
- `apps/web/messages/en.json`
- `apps/web/messages/ar.json`
- `apps/web/next.config.ts`
- `apps/web/lib/server/zootopia-postgres-adapter.ts`

Verification
- `npm run lint --workspace @zootopia/web`: PASSED
- `npm run typecheck --workspace @zootopia/web`: PASSED
- `npm run build --workspace @zootopia/web`: PASSED
- Build route manifest confirms new route:
  - `/api/admin/users/[uid]`

Safety / Partial-Failure Behavior
- Full cross-system atomicity is not possible across Supabase Auth + storage + data records.
- Implemented safest practical sequence:
  - account freeze first,
  - then cleanup,
  - then auth hard delete.
- On failure, response and audit metadata include exact `failurePoint` and `failureReason`; operation is explicitly reported as `partial_failure`.

2026-04-11 | Admin Storage Control + Per-User Admin Detail System

Scope
- Implement real admin-only storage cleanup controls (per-user + global)
- Env-driven file retention policy system
- Dedicated admin user detail page at /admin/users/[uid]
- Global storage maintenance page at /admin/storage
- Remove any remaining Firebase/Firestore naming from active runtime code
- Wire all admin actions end-to-end with proper confirmations and security guards

Files Created
- `apps/web/lib/server/storage-retention-config.ts` — Centralized env-driven retention config
  - ZOOTOPIA_UPLOAD_RETENTION_DAYS (default: 90)
  - ZOOTOPIA_UPLOAD_RETENTION_HOURS (default: 0, overrides days if set)
  - ZOOTOPIA_STORAGE_RETENTION_MODE (default: "env-driven")
  - Defensive validation, safe defaults, documented contract
- `apps/web/app/api/admin/storage/cleanup/route.ts` — Admin-only storage cleanup API
  - POST with mode "user" (per-user) or "global" (all user namespaces)
  - Per-user: deletes all storage objects for target uid across documents/, assessment-results/, assessment-exports/
  - Global: deletes ALL user-owned storage objects (scoped to user namespaces only, NOT system assets)
  - Requires admin auth, explicit confirmation (uid match for per-user, "DELETE ALL STORAGE" for global)
  - Structured result: matched, deleted, failed, DB counts, final status, failure reason
  - Full audit logging via appendAdminLog + console.info
- `apps/web/app/(protected)/admin/users/[uid]/page.tsx` — Dedicated admin user detail page
  - Server-rendered, admin-only (parent layout guard)
  - Identity/Account section: uid, email, display name, full name, university code, phone, nationality, role, status, created/updated, profile completion
  - Admin Controls section: role toggle, status toggle, delete user, delete all files for user
  - Storage section: documents, assessments, infographics, storage objects summary
  - All destructive actions require explicit confirmation (type target uid/email)
  - Self-destructive actions blocked for current admin's own account
- `apps/web/app/(protected)/admin/storage/page.tsx` — Global storage maintenance page
  - Server-rendered, admin-only
  - Strong confirmation: must type "DELETE ALL STORAGE" exactly
  - Calls /api/admin/storage/cleanup with mode "global"
  - Shows storage availability status

Files Modified
- `apps/web/app/(protected)/admin/page.tsx` — Added Storage Maintenance quick-access section with link to /admin/storage; user list items now link to /admin/users/[uid]
- `apps/web/app/(protected)/admin/users/page.tsx` — No changes needed (existing page works as-is)

Env Variables Added
- ZOOTOPIA_UPLOAD_RETENTION_DAYS — Number of days to retain uploaded files (default: 90)
- ZOOTOPIA_UPLOAD_RETENTION_HOURS — Number of hours to retain uploaded files (default: 0, overrides days if > 0)
- ZOOTOPIA_STORAGE_RETENTION_MODE — Retention enforcement mode (default: "env-driven")
- Contract: .env.local for local dev, Vercel env vars for production

Storage Model (Current Truth)
- Supabase Storage (private bucket) is the sole storage backend
- Object path structure: {namespace}/{uid}/{filename}
- Namespaces: documents/, assessment-results/, assessment-exports/
- User ownership is encoded in the path (uid segment)
- Storage cleanup uses prefix-based listing + batch deletion (500 objects per batch)
- Global cleanup is scoped to user namespaces only (does NOT delete system assets)
- DB records are counted but NOT deleted during storage cleanup (preserved for audit/history)

Security Guards
- All /admin/* routes protected by requireAdminUser() in layout.tsx
- API routes verify admin session via getAdminSessionUser()
- Per-user storage delete: confirmation must match target uid exactly
- Global storage delete: confirmation must be "DELETE ALL STORAGE" exactly
- Self-destructive actions blocked for current admin's own account
- Owner-scoped isolation: cleanup for one user only touches their uid prefix

Firebase/Firestore Naming Audit
- Searched all active runtime code (apps/web/**/*.ts, apps/web/**/*.tsx)
- Zero matches for firebase, firestore, firebaseAuth, firebaseStorage, firebaseAdmin, appHosting
- No old aliases or dead compatibility exports found in active runtime code

Verification
- `npx tsc --noEmit`: PASSED (no errors)
- Firebase/Firestore naming search: 0 matches in active runtime code
- All admin routes properly guarded by requireAdminUser()
- All destructive actions require explicit confirmation
- Structured diagnostics logged for all admin actions

Architecture Notes
- Storage cleanup is scoped-prefix (user namespaces), NOT blind bucket wipe
- This protects system assets and other non-user content in the same bucket
- DB records are preserved during storage cleanup for audit/history purposes
- Retention config is centralized in one helper (storage-retention-config.ts)
- Admin detail page uses server-side rendering for real backend data
- No mock values or cosmetic placeholders used

---

2026-04-12 - Admin Users Overview Simplification (List-to-Detail Control Split)

Scope
- Refined `/admin/users` into a cleaner overview surface by removing heavy in-card management controls.
- Preserved all backend/admin wiring by keeping advanced controls in the existing per-user detail route (`/admin/users/[uid]`).

Problem
- The list page (`users-table.tsx`) had expanded in-card controls for role/status/credits/grants/delete actions.
- This made the overview noisy and duplicated responsibilities already handled by the detail page.

Decisions
- Keep `/admin/users` focused on scanning users and navigation.
- Keep advanced user management actions on `/admin/users/[uid]` only.
- Preserve server-authoritative export behavior on the list page.

Files Changed
- `apps/web/components/admin/users-table.tsx`
  - Replaced heavy expandable management UI with a compact overview card layout.
  - Removed in-card advanced actions (role/status/credits/grants/delete controls).
  - Added explicit per-user navigation CTA to detail route:
    - `href="/admin/users/${encodeURIComponent(uid)}"`
    - Label: `View details / Manage user`
  - Kept users export action server-owned (`/api/admin/users/export`) with download handling.
- `apps/web/messages/en.json`
  - Added key: `adminUserDetailsAction` = `View details / Manage user`
- `apps/web/messages/ar.json`
  - Added key: `adminUserDetailsAction` = `عرض التفاصيل / إدارة المستخدم`

Current Truth Snapshot Update
- `/admin/users` is now an overview page (identity/status/profile summary + detail navigation + export).
- `/admin/users/[uid]` remains the single primary UI surface for advanced management operations.
- No backend contracts, admin guard logic, or mutation APIs were changed in this pass.

Verification
- `npm run lint --workspace @zootopia/web`: PASSED with existing warnings in unrelated files
  - `apps/web/app/(protected)/admin/storage/page.tsx` unused variable warning
  - `apps/web/app/(protected)/admin/users/[uid]/page.tsx` unused variable warning
  - `apps/web/app/api/admin/storage/cleanup/route.ts` unused variable warning
- `npm run typecheck --workspace @zootopia/web`: PASSED
- `npm run build --workspace @zootopia/web`: PASSED

Risk Notes
- None introduced at API/data layer: this pass is UI-scope only on the list page.
- Future agents should keep list/detail responsibility split intact to avoid reintroducing duplicated heavy controls on `/admin/users`.

---

2026-04-11 - Admin Storage/Detail Lint Warning Cleanup

Scope
- Resolved previously documented unrelated lint warnings in admin storage/detail surfaces without changing runtime behavior.

Warnings Fixed
- `apps/web/app/(protected)/admin/storage/page.tsx`
  - Removed unused `getRequestUiContext` import and unused `uiContext` variable.
- `apps/web/app/(protected)/admin/users/[uid]/page.tsx`
  - Removed unused `getAdminSessionUser` symbol from dynamic session import in delete-user action.
- `apps/web/app/api/admin/storage/cleanup/route.ts`
  - Removed unused `scope` local in global cleanup handler.

Behavioral Impact
- No API contract changes.
- No auth/authorization logic changes.
- No UI flow changes.
- No storage mutation logic changes.

Verification
- `npm run lint --workspace @zootopia/web`: PASSED (0 warnings, 0 errors)
- `npm run typecheck --workspace @zootopia/web`: PASSED
- `npm run build --workspace @zootopia/web`: PASSED

Current Truth Update
- Prior warning notes for admin storage/detail files are now resolved.
- Admin storage/detail files are lint-clean in the current workspace state.

---

2026-04-12 - Admin Storage Governance Hardening + Per-Scope Retention Wiring

Scope
- Finalized the admin storage governance pass by moving retention behavior from one shared policy to explicit per-storage-type policies.
- Hardened cleanup semantics for both per-user and global storage deletion with namespace-aware diagnostics.
- Upgraded `/admin/users/[uid]` from placeholder storage/content cards to backend-backed metrics and activity timelines.
- Updated admin storage copy and retention-facing content to reflect current env-driven policy truth.

Current Truth (Supersedes Older 2026-04-11 Defaults)
- Retention is now **per storage scope**, not one global upload value:
  - uploads: `ZOOTOPIA_UPLOAD_RETENTION_MINUTES` (default 15)
  - results: `ZOOTOPIA_RESULT_RETENTION_MINUTES` (default 1440 = 1 day)
  - exports: `ZOOTOPIA_EXPORT_RETENTION_MINUTES` (default 15)
- Optional per-scope mode keys:
  - `ZOOTOPIA_UPLOAD_RETENTION_MODE`
  - `ZOOTOPIA_RESULT_RETENTION_MODE`
  - `ZOOTOPIA_EXPORT_RETENTION_MODE`
- Legacy global fallback is still supported for compatibility:
  - `ZOOTOPIA_STORAGE_RETENTION_DAYS`
  - `ZOOTOPIA_STORAGE_RETENTION_HOURS`
  - `ZOOTOPIA_STORAGE_RETENTION_MODE`

Storage Namespace Truth
- User-owned namespaces covered by cleanup/storage metrics:
  - `uploads/temp/{uid}/...`
  - `documents/{uid}/...`
  - `assessment-results/{uid}/...`
  - `assessment-exports/{uid}/...`
- Global cleanup remains intentionally scoped to user-owned namespaces only.
- System/shared assets are not targeted by the admin cleanup route.

Files Changed
- `apps/web/lib/server/supabase-blob-storage.ts`
  - Added recursive object descriptor listing with metadata support (`sizeBytes`, content type, timestamps).
  - Prefix list helper now uses recursive descriptor traversal so nested folders are included.
- `apps/web/lib/server/storage-retention-config.ts`
  - Reworked retention config to scope-aware model (`uploads`, `results`, `exports`).
  - Added scope env contract parsing, mode parsing, namespace-to-scope resolver, and policy summary helpers.
- `apps/web/lib/server/assessment-retention.ts`
  - Wired retention calls to explicit scope selection (`results`) for assessment lifecycle behavior.
- `apps/web/lib/server/document-runtime.ts`
  - Wired upload expiry to `uploads` scope.
  - Expiry resolution now chooses earliest of session expiry vs retention expiry.
- `apps/web/lib/server/repository.ts`
  - Updated retention call sites to explicit scope ownership (`uploads` vs `results`) to stop accidental policy mixing.
- `apps/web/lib/server/assessment-artifact-storage.ts`
  - Export artifact defaults now use `exports` retention scope.
- `apps/web/app/api/assessment/export/json/[id]/route.ts`
- `apps/web/app/api/assessment/export/markdown/[id]/route.ts`
- `apps/web/app/api/assessment/export/docx/[id]/route.ts`
- `apps/web/lib/server/assessment-fast-pdf-export.ts`
- `apps/web/lib/server/assessment-pro-pdf-export.ts`
  - Removed explicit export `expiresAt` inheritance from generation records so export artifacts follow export-scope defaults.
- `apps/web/app/api/admin/storage/cleanup/route.ts`
  - Added namespace constants including `uploads/temp`.
  - Per-user/global cleanup now records namespace-level matched/deleted/failed counts.
  - Reporting fields now truthfully reflect DB row retention (`db*Retained`) instead of implying destructive DB deletion.
- `apps/web/app/(protected)/admin/storage/page.tsx`
  - Updated global cleanup copy to include `uploads/temp` in scope.
- `apps/web/app/(protected)/admin/users/[uid]/page.tsx`
  - Added backend-backed credits, content totals, storage namespace metrics, retention policy summaries, recent content timeline, and recent admin action timeline.
  - Added success/error status banners from query params.
  - Aligned per-user storage cleanup confirmation prompt with API contract (UID confirmation).
- `.env.example`
  - Updated to the per-scope retention env contract and legacy fallback notes.
- `apps/web/app/api/uploads/route.ts`
- `apps/web/lib/site-content.ts`
  - Updated wording away from fixed-duration assumptions to env-driven retention messaging.
- `apps/web/messages/en.json`
- `apps/web/messages/ar.json`
  - Replaced fixed "3 days" retention strings with runtime-policy wording to match the new per-scope retention model.

Security / Isolation Notes
- Admin-only boundaries remain server-enforced (`requireAdminUser` / `getAdminSessionUser`).
- Per-user cleanup only targets prefixes containing the selected UID.
- Global cleanup only touches known user-owned namespaces, preserving non-user/system storage content.
- Admin detail page data remains server-rendered and backend-authoritative; no client-side trust shift introduced.

Verification
- `npm run lint --workspace @zootopia/web`: PASSED
- `npm run typecheck --workspace @zootopia/web`: PASSED
- `npm run build --workspace @zootopia/web`: PASSED
- Build manifest includes expected routes:
  - `/admin/users/[uid]`
  - `/admin/storage`
  - `/api/admin/storage/cleanup`

Residual Risks / Follow-up
- Automated auth-matrix QA for admin/user-user isolation at runtime still depends on seeded accounts and session fixtures; route-level guards and ownership-prefix constraints are in place, but multi-user live-session scenario replay should be run in the deployment environment.

---

2026-04-12 - Admin Detail Destructive Controls Refinement + DELETE USER Contract + Vercel Runtime Hardening

Scope
- Performed mandatory read-first workflow for this pass (full ledger read, then repo exploration, then direct source verification).
- Applied a surgical refinement to `/admin/users/[uid]` destructive controls and delete confirmation semantics.
- Hardened deployment/runtime assumptions used by admin server actions in Vercel and local environments.

Files Changed
- `apps/web/app/(protected)/admin/users/[uid]/page.tsx`
  - Split destructive operations into a dedicated red “Destructive Actions” row/section.
  - Kept non-destructive role/status controls in a separate non-destructive row.
  - Changed delete-user confirmation UX to require exact phrase `DELETE USER`.
  - Preserved delete-all-files action in the destructive row with explicit UID confirmation guidance.
  - Repointed per-user storage cleanup server action fetch to shared runtime base URL resolver.
- `apps/web/app/api/admin/users/[uid]/route.ts`
  - Enforced strict server-side delete confirmation phrase `DELETE USER` (exact match).
  - Removed prior uid/email dual confirmation acceptance for this destructive route.
  - Kept admin guard, self-delete guard, allowlisted-admin protection, and structured logging intact.
- `apps/web/app/(protected)/admin/storage/page.tsx`
  - Replaced localhost-only API base URL assumption with shared runtime base URL resolver.
- `apps/web/lib/server/runtime-base-url.ts` (new)
  - Added centralized server-only resolver for internal admin API calls:
    - prefers `NEXT_PUBLIC_BASE_URL`
    - falls back to `NEXTAUTH_URL`
    - falls back to `VERCEL_URL` (with `https://` normalization)
    - final fallback: `http://localhost:3000`
- `vercel.json`
  - Hardened install/build commands to explicit root-workspace flow:
    - install: `cd ../.. && npm ci`
    - build: `cd ../.. && npm run build --workspace @zootopia/web`

Behavioral Contract Updates
- Delete user now requires exact phrase `DELETE USER` in both:
  - admin detail server action path
  - `DELETE /api/admin/users/[uid]` route
- Delete all files for a user remains separate and requires exact target UID confirmation.

Security / Isolation Notes
- Admin-only route guards remain server-enforced (`requireAdminUser` / `getAdminSessionUser`).
- No relaxation of owner-scoped boundaries was introduced.
- Destructive operations are now more explicit in UI and stricter in server validation.

Verification
- `npm run lint --workspace @zootopia/web`: PASSED
- `npm run typecheck --workspace @zootopia/web`: PASSED
- `npm run build --workspace @zootopia/web`: PASSED
- Build output confirms expected routes remain present, including:
  - `/admin/users/[uid]`
  - `/api/admin/users/[uid]`
  - `/api/admin/storage/cleanup`

Risk / Compatibility Note
- Any external/admin clients that previously sent UID/email as delete confirmation to `DELETE /api/admin/users/[uid]` must now send `DELETE USER` exactly.

---

2026-04-12 - Vercel Build Command Separation via Package Scripts (No vercel.json)

Scope
- Completed a read-first deployment script audit before edits (full ledger read, subagent exploration, and direct source verification).
- Implemented a surgical command-lane separation pass so local development/build and Vercel build paths are explicit and independent.
- Removed vercel.json entirely, per deployment policy for this pass.

Pre-change Audit Truth
- Root `package.json` script model:
  - `build` ran workspace builds (`npm run build --workspaces --if-present`).
  - `build:web` delegated to `@zootopia/web` build.
  - `build:vercel` incorrectly ran plain `next build` at root.
- `apps/web/package.json` script model:
  - `build` used a local heap-tuned Node wrapper (`--max-old-space-size=16384`) for Next build.
  - `build:vercel` used plain `next build`.
  - `postbuild` normalized standalone output.
- `vercel.json` existed and enforced `rootDirectory: apps/web` with root-level `cd ../..` command indirection for install/build.
- Build lane likely tied to web workspace build through root command delegation when vercel.json was active.

Files Changed
- `package.json`
  - Added explicit local aliases:
    - `dev:local`, `build:local`, `start:local`
  - Repointed default lanes to local aliases:
    - `dev` -> `dev:local`
    - `build` -> `build:local`
    - `start` -> `start:local`
  - Replaced root Vercel lane:
    - `build:vercel` now delegates to web workspace (`npm run build:vercel --workspace=@zootopia/web`)
  - Added explicit `start:vercel` workspace delegate.
- `apps/web/package.json`
  - Added explicit local aliases:
    - `dev:local`, `build:local`, `start:local`
  - Repointed defaults:
    - `dev` -> `dev:local`
    - `build` -> `build:local`
    - `start` -> `start:local`
  - Kept dedicated Vercel lane:
    - `build:vercel` remains plain `next build`.
  - Added explicit `start:vercel`.
- `vercel.json`
  - Removed from repository.

Command Lane Intent (Current Truth)
- Local development:
  - Root: `npm run dev` (alias to local lane)
  - Web workspace: `npm run dev` / `npm run dev:local`
- Local build:
  - Root all-workspaces local lane: `npm run build`
  - Web-only local lane: `npm run build:web` (uses heap-tuned wrapper + `postbuild`)
  - Web workspace local lane: `npm run build` / `npm run build:local`
- Vercel build:
  - Root-safe lane: `npm run build:vercel` (delegates to `@zootopia/web` `build:vercel`)
  - Web workspace lane: `npm run build:vercel` (plain `next build`, no local wrapper coupling)
- Production start:
  - Root: `npm run start` / `npm run start:vercel`
  - Web workspace: `npm run start` / `npm run start:vercel`

Vercel Operational Note
- With `vercel.json` removed, Vercel Dashboard settings are now authoritative for root directory and build command.
- Recommended build command lane: `npm run build:vercel`.

Import / Module Resolution Findings
- No new `Module not found` or case-sensitivity defects were introduced by this pass.
- No runtime import path changes were required.

Verification
- `npm run lint:web`: PASSED
- `npm run typecheck:web`: PASSED
- `npm run build:vercel`: PASSED
- `npm run build:web`: PASSED
- `npm run build:local`: PASSED

Hard Rule Confirmation
- `vercel.json` was not created.
- Existing `vercel.json` was removed.

---

2026-04-12 - Build Script Separation Re-Validation Pass (No Additional Script Mutation Needed)

Scope
- Executed mandatory read-first workflow again (full ledger read, subagent exploration, direct source verification).
- Re-audited root/workspace script lanes after external file changes were reported.
- Verified local and Vercel build paths end-to-end without relying on `vercel.json`.

Current Script Model (Re-verified)
- Root `package.json` provides explicit local and Vercel lanes:
  - `dev` -> `dev:local`
  - `build` -> `build:local`
  - `build:vercel` -> delegates to `@zootopia/web` `build:vercel`
  - `start` -> `start:local`
  - `start:vercel` -> delegates to `@zootopia/web` `start:vercel`
- `apps/web/package.json` keeps strict lane separation:
  - local heavy wrapper preserved exactly under `build:local`
  - `build` -> `build:local` (local ergonomics preserved)
  - `build:vercel` remains plain `next build` (clean Vercel lane)
  - `postbuild` standalone normalization remains intact

Local Heavy-Memory Wrapper Preservation
- Confirmed unchanged exact local command remains available:
  - `node --max-old-space-size=16384 -e "const{spawnSync}=require('node:child_process');const nextBin=require.resolve('next/dist/bin/next');const result=spawnSync(process.execPath,[nextBin,'build'],{stdio:'inherit'});process.exit(result.status??1);"`

Vercel Root/Build Path Truth
- No `vercel.json` exists in repo; deployment root/build command are controlled by Vercel project dashboard settings.
- Package-script lane for Vercel is present and valid from monorepo root:
  - `npm run build:vercel`
- If Vercel dashboard root is set to `apps/web`, workspace-local `build:vercel` is also valid.

Failure Cause Check (This Pass)
- No module-resolution, import-case, or server/client-boundary errors surfaced in this pass.
- Script/workspace wiring is healthy for both local and Vercel lanes.

Files Changed
- `zootopia-club-next-ledger.txt` only.
- No additional package-script mutation was required because current state already meets separation requirements.

Verification
- `npm run lint:web`: PASSED
- `npm run typecheck:web`: PASSED
- `npm run build:vercel`: PASSED
- `npm run build:local`: PASSED

Hard Rule Confirmation
- `vercel.json` was not created.
- `vercel.json` remains removed.

---

2026-04-12 - Root Script Lane Regression Repair (build:vercel Restored, Script-Only)

Scope
- Executed mandatory read-first workflow for this pass: full ledger read, Explore subagent audit, then direct source verification before any edits.
- Re-checked root/workspace script lanes with strict requirement to keep local and Vercel lanes separated using package scripts only.

Regression Found
- Root `package.json` had no `build:vercel` script at the time of this pass.
- This broke explicit root-level Vercel lane declaration and increased risk of dashboard/operator fallback to `npm run build` (local lane) instead of a clean deployment lane.

Minimal Fix Applied
- File changed: `package.json`
- Added one script only:
  - `build:vercel`: `npm run build:vercel --workspace=@zootopia/web`

Preservation Guarantees Re-Verified
- `apps/web/package.json` local heavy-memory command remained unchanged under `build:local`:
  - `node --max-old-space-size=16384 -e "const{spawnSync}=require('node:child_process');const nextBin=require.resolve('next/dist/bin/next');const result=spawnSync(process.execPath,[nextBin,'build'],{stdio:'inherit'});process.exit(result.status??1);"`
- `apps/web/package.json` Vercel lane remained clean and unwrapped:
  - `build:vercel`: `next build`
- `vercel.json` remains absent (not reintroduced).

Current Script-Lane Truth After Fix
- Root local lane:
  - `build` -> `build:local`
- Root Vercel lane:
  - `build:vercel` -> delegates to `@zootopia/web` `build:vercel`
- Web local lane:
  - `build` -> `build:local` (heavy-memory wrapper)
- Web Vercel lane:
  - `build:vercel` -> `next build`

Verification
- `npm run lint:web`: PASSED
- `npm run typecheck:web`: PASSED
- `npm run build:vercel`: PASSED
- `npm run build:local`: PASSED

Operational Note
- With no `vercel.json`, Vercel Dashboard settings remain authoritative.
- Recommended Dashboard build command stays: `npm run build:vercel`.

---

2026-04-12 - Vercel next-auth Module Resolution Fix (Workspace Ownership + Lock Sync)

Scope
- Completed mandatory read-first workflow for this pass: full ledger read, Explore subagent audit, then direct source verification before edits.
- Fixed deployment-time `Module not found` failures for `next-auth`, `next-auth/providers/credentials`, and `next-auth/react` without touching runtime auth logic.

Observed Failure
- Vercel `next build` failed in `apps/web` import sites with unresolved modules:
  - `next-auth`
  - `next-auth/providers/credentials`
  - `next-auth/react`

Root Cause
- `next-auth` was declared in root `package.json` instead of the importing workspace.
- `apps/web/package.json` did not declare `next-auth` at all.
- `apps/web/package-lock.json` also lacked `next-auth`, while root lockfile had it hoisted.
- In deployment/install contexts that enforce workspace-local dependency truth, this caused unresolved imports for `apps/web`.

Files Changed
- `package.json`
  - Removed misplaced root dependency: `next-auth`.
- `apps/web/package.json`
  - Added `next-auth` to `dependencies` (same version): `^5.0.0-beta.30`.
- `package-lock.json`
  - Root package dependency entry no longer owns `next-auth`.
  - `packages["apps/web"].dependencies` now includes `next-auth`.
- `apps/web/package-lock.json`
  - Added direct `next-auth` dependency in workspace package metadata.
  - Added resolved `node_modules/next-auth` entry (and transitive auth-core deps).

Lock Sync Notes
- Root lock refresh: `npm install --package-lock-only`
- App lock refresh: `npm install --package-lock-only --workspaces=false --legacy-peer-deps` (used to bypass optional peer-resolution conflict between `next-auth` optional `nodemailer@^7.0.7` peer and existing `nodemailer@^8.0.4` runtime choice).

Compatibility Verification
- Imports remain version-compatible for installed `next-auth@5.0.0-beta.30`:
  - `next-auth`
  - `next-auth/providers/credentials`
  - `next-auth/react`
- Resolution check from `apps/web` context succeeded for all three paths.

Verification
- `npm run lint:web`: PASSED
- `npm run typecheck:web`: PASSED
- `npm run build:vercel`: PASSED
- `npm run build:local --workspace=@zootopia/web`: PASSED

Hard Rule Confirmation
- `vercel.json` was NOT created.
- `vercel.json` was NOT used for this fix.

---

2026-04-12 - Production Auth Diagnosis Hardening (Truthful Failure Taxonomy + Confirm-Email Recovery)

Scope
- Completed mandatory sequence before edits:
  - read `zootopia-club-next-ledger.txt` fully,
  - ran Explore subagent repo audit,
  - re-read active auth/session/runtime surfaces directly.
- Implemented end-to-end auth-failure diagnosis hardening across user login, admin login, and error-mapping surfaces.
- Added dedicated public confirm-email recovery flow with resend support and safe return routing.

Primary Production Issue Fixed
- Unconfirmed-email failures were previously surfaced to users with misleading session-refresh style messaging.
- This pass routes email-unconfirmed identities to a dedicated confirmation flow instead of suggesting stale-session recovery.

Architecture Decisions (Current Truth)
- Preserved existing ownership model:
  - Supabase handles password auth and confirmation state.
  - Auth.js owns secure server session creation/hydration.
  - Protected-route enforcement remains in existing session/proxy architecture.
- Added centralized normalized taxonomy layer for auth failures:
  - canonical `AUTH_*` codes,
  - stage labels A-E,
  - safe structured diagnostics logging,
  - no secret/provider payload leakage.
- Preserved admin/user lane separation while applying the same failure semantics to both lanes.

Files Changed
- `packages/shared-config/src/routes.ts`
  - Added `APP_ROUTES.confirmEmail` (`/confirm-email`).
- `apps/web/lib/auth-failure.ts` (new)
  - Added central auth-failure normalization model and logger.
  - Added `buildConfirmEmailRoute(...)` helper.
  - Added detail-snapshot preservation so rethrown errors keep original stage/flow/code metadata.
- `apps/web/components/auth/auth-feedback.ts`
  - Switched user/admin mapping to normalized taxonomy routing.
  - Corrected email-not-confirmed mapping so it no longer falls into refresh-session UX.
  - Removed unused helper to keep lint surface clean.
- `apps/web/components/auth/login-panel.tsx`
  - Added stage-aware diagnostics in submit/bootstrap flow.
  - Added route-aware normalization around Supabase/Auth.js/session hydration failures.
  - Redirects unconfirmed-email sign-in/sign-up paths to `/confirm-email` with context query params.
  - Tightened submit guard to respect runtime readiness (`supabaseAuthReady`).
- `apps/web/components/auth/admin-login-panel.tsx`
  - Added normalized taxonomy diagnostics across identifier resolution, Supabase sign-in, Auth.js bootstrap, and hydration polling.
  - Added centralized Supabase-admin error mapper.
  - Added unconfirmed-email redirect to `/confirm-email` with admin return lane.
- `apps/web/components/auth/confirm-email-panel.tsx` (new)
  - Added dedicated resend-verification UI with cooldown and structured failure handling.
  - Keeps redirect ownership in login lanes after email verification.
- `apps/web/app/(public)/confirm-email/page.tsx` (new)
  - Added public confirmation recovery route with locale/theme controls and safe query parsing.
  - Redirects already-authenticated users to their proper protected destination.
- `apps/web/messages/en.json`
  - Added missing login/admin error keys referenced by auth-feedback.
  - Added complete confirm-email UX key set.
- `apps/web/messages/ar.json`
  - Added Arabic parity for all new/required keys.

Diagnostics Model Introduced
- Canonical categories now consistently represented:
  - invalid credentials,
  - email not confirmed,
  - suspended account,
  - provider misconfigured,
  - environment misconfigured,
  - Auth.js/session creation failures,
  - session refresh required,
  - unknown upstream failures.
- Structured logs now emit normalized payloads with:
  - flow,
  - stage,
  - normalized/raw codes,
  - safe provider message,
  - UX action taken.

Verification
- `npm run lint --workspace @zootopia/web`: PASSED
- `npm run typecheck --workspace @zootopia/web`: PASSED
- `npm run build --workspace @zootopia/web`: PASSED

Build/Check Note
- During first verification attempt, typecheck/build failed due missing locale keys already referenced in `auth-feedback` (`loginStatusProviderMisconfigured*`, `loginStatusInvalidCredentials*`).
- Added keys in EN/AR and reran checks to green.

Risk Notes
- Confirm-email resend currently uses Supabase `auth.resend({ type: "signup" })` by design for email-confirmation recovery.
- Provider-specific resend throttling remains expected behavior; UI cooldown is advisory and does not bypass provider rate limits.
- No backend authority was moved to client; client changes only drive auth UX and safe telemetry.

---

2026-04-12 - Confirm Email Finalization Truthfulness Hardening (Provider-Backed Callback Handling)

Scope
- Performed mandatory read-first workflow for this pass (full ledger read, source re-validation, then surgical edits).
- Hardened `/confirm-email` so confirmation success is based on real provider callback finalization, not resend-request success alone.
- Kept Auth.js as the single server session authority while using ephemeral Supabase browser auth only for callback verification.

Primary Issue Fixed
- The prior confirm-email flow could report resend success but had no explicit callback-finalization logic, so users could not see truthful in-app confirmation completion outcomes from callback payloads.

Current Truth (post-pass)
- Resend still uses `supabase.auth.resend({ type: "signup" })`, but `emailRedirectTo` now points back to `/confirm-email` (with safe lane context) instead of directly to login/admin routes.
- `/confirm-email` now parses callback payloads and finalizes via provider APIs when present:
  - `token_hash` + `type` -> `supabase.auth.verifyOtp(...)`
  - `code` -> `supabase.auth.exchangeCodeForSession(...)`
  - `access_token` + `refresh_token` hash/query payload -> `supabase.auth.setSession(...)`
- After successful callback finalization, transient Supabase browser session state is cleared (`signOut({ scope: "local" })`) to preserve Auth.js server-session ownership.
- Sensitive callback parameters are removed from URL/history after processing to reduce token leakage in browser history.

Files Changed
- `apps/web/components/auth/confirm-email-panel.tsx`
  - Added callback payload parsing/finalization pipeline.
  - Added code-path-specific error mapping for expired/invalid callback links.
  - Added structured confirmation diagnostics (`[auth-confirmation]`) and retained normalized auth diagnosis logging.
  - Updated resend redirect construction to target `/confirm-email` with lane context.
  - Added busy-state guarding for finalization vs resend interactions.
- `apps/web/app/(public)/confirm-email/page.tsx`
  - Added safe parsing/forwarding of callback query params (`code`, `token_hash`, `type`, `error_code`, `error_description`, token fields).
  - Passed initial callback payload to the client panel for deterministic handling.
- `apps/web/lib/auth-failure.ts`
  - Expanded raw code normalization coverage for production auth codes:
    - provider/config: `EMAIL_PROVIDER_DISABLED`, `PROVIDER_DISABLED`, `OTP_DISABLED`
    - refresh/expiry: `OTP_EXPIRED`, `FLOW_STATE_EXPIRED`, `FLOW_STATE_NOT_FOUND`, `SESSION_EXPIRED`, `SESSION_NOT_FOUND`
    - throttling: `OVER_EMAIL_SEND_RATE_LIMIT`, `OVER_SMS_SEND_RATE_LIMIT`
    - suspension: `USER_BANNED`
  - Maintains code-first classification behavior with message fallback only when code is unavailable.
- `apps/web/messages/en.json`
  - Added new confirm-email states: finalizing, confirmed, expired-link, invalid-link.
  - Updated resend success copy to accepted-request wording (truthful delivery semantics).
- `apps/web/messages/ar.json`
  - Added Arabic parity for all new confirm-email states and accepted-request wording.

Verification
- `npm run lint --workspace @zootopia/web`: PASSED
- `npm run typecheck --workspace @zootopia/web`: PASSED
- `npm run build --workspace @zootopia/web`: PASSED
- Build manifest confirms `/confirm-email` route remains present in production build.

Safety / Boundary Notes
- No admin/user lane broadening was introduced.
- No secrets or service-role logic moved to client.
- Auth.js remains authoritative for protected server session ownership; Supabase callback handling remains transient and scoped to confirmation finalization only.

---

2026-04-12 - Auth Diagnosis Console Overlay Noise Fix (Non-Fatal Unknowns Reclassified)

Scope
- Performed mandatory read-first workflow for this pass (full ledger read, then targeted source verification).
- Applied a surgical logging-only fix to prevent Next.js dev console error overlays from non-fatal auth diagnosis stage markers.

Primary Issue Fixed
- Login/admin submit-stage diagnostics were emitted via `console.error` because they used `AUTH_UNKNOWN_UPSTREAM_FAILURE` for stage tracing with `uxAction: "retry"`.
- In development, these browser-side `console.error` calls surfaced as disruptive overlay errors even though the events were operational telemetry, not fatal runtime failures.

Current Truth (post-pass)
- `AUTH_UNKNOWN_UPSTREAM_FAILURE` diagnosis logs are now classified as:
  - `console.info` when `uxAction` is `retry` (stage markers / in-flight telemetry)
  - `console.warn` for non-retry unknown failures
- No code-path changes were made to auth flow control, redirect logic, provider interactions, or failure normalization taxonomy.
- Diagnostics remain visible in browser console without being promoted to dev-overlay-level console errors for non-fatal submit markers.

File Changed
- `apps/web/lib/auth-failure.ts`
  - Updated `logAuthDiagnosis(...)` severity handling for `AUTH_UNKNOWN_UPSTREAM_FAILURE`.
  - Added an architecture comment clarifying why unknown+retry telemetry must avoid `console.error` in client flows.

Verification
- `npm run lint --workspace @zootopia/web`: PASSED
- `npm run typecheck --workspace @zootopia/web`: PASSED
- `npm run build --workspace @zootopia/web`: PASSED

Safety / Boundary Notes
- No API contracts changed.
- No i18n keys changed.
- No admin/user authorization boundaries changed.
- No environment/runtime ownership changes introduced.

---

2026-04-12 - Confirm-Email Resend Governance Finalization (Backend Authority + i18n/Auth Taxonomy Completion)

Scope
- Finalized the confirm-email resend hardening pass so resend throttling/cooldown truth is fully server-authoritative.
- Completed remaining integration items from the in-progress implementation: Arabic message parity and auth-failure raw-code normalization for new resend route machine codes.

Current Truth (post-pass)
- Confirm-email resend now runs through backend authority only:
  - `GET /api/auth/confirm-email/resend` returns governance snapshot.
  - `POST /api/auth/confirm-email/resend` reserves governance attempt, performs provider resend, and returns authoritative governance state.
- Governance persistence is backed by Postgres table `public.email_verification_resend_governance` with scope-key hashing (`account` + `ip`) and window/cooldown tracking.
- Confirm-email client resend UX is synchronized with backend governance state (allowed/blocked/cooldown) instead of browser-only assumptions.
- EN/AR dictionaries now both include the new resend-governance copy keys used by the panel state machine.
- Auth failure normalization now classifies new resend route codes into existing taxonomy lanes (rate-limited/network/env-misconfigured) for consistent diagnostics/UI behavior.

Files Changed (feature state)
- `supabase/migrations/20260412103000_email_verification_resend_governance.sql` (new)
- `apps/web/lib/server/verification-resend-governance.ts` (new)
- `apps/web/app/api/auth/confirm-email/resend/route.ts` (new)
- `apps/web/components/auth/confirm-email-panel.tsx`
- `apps/web/messages/en.json`
- `apps/web/messages/ar.json`
- `apps/web/lib/auth-failure.ts`

Finalization Edits In This Pass
- `apps/web/messages/ar.json`
  - Added `confirmEmailSendAction`.
  - Added `confirmEmailRateLimitedAccountBody`.
  - Added `confirmEmailRateLimitedIpBody`.
- `apps/web/lib/auth-failure.ts`
  - Added normalization coverage for resend route machine codes:
    - `VERIFICATION_RESEND_UNAVAILABLE` -> `AUTH_ENV_MISCONFIGURED`
    - `VERIFICATION_RESEND_COOLDOWN_ACTIVE` -> `AUTH_RATE_LIMITED`
    - `VERIFICATION_RESEND_ACCOUNT_WINDOW_EXHAUSTED` -> `AUTH_RATE_LIMITED`
    - `VERIFICATION_RESEND_IP_WINDOW_EXHAUSTED` -> `AUTH_RATE_LIMITED`
    - `VERIFICATION_RESEND_PROVIDER_RATE_LIMITED` -> `AUTH_RATE_LIMITED`
    - `VERIFICATION_RESEND_PROVIDER_NETWORK_FAILURE` -> `AUTH_NETWORK_FAILURE`
    - `VERIFICATION_RESEND_PROVIDER_REJECTED` -> `AUTH_UNKNOWN_UPSTREAM_FAILURE`

Verification
- `npm run lint --workspace @zootopia/web`: PASSED
- `npm run typecheck --workspace @zootopia/web`: PASSED
- `npm run build --workspace @zootopia/web`: PASSED
- Build manifest confirms both routes are present:
  - `/confirm-email`
  - `/api/auth/confirm-email/resend`

Safety / Boundary Notes
- No admin/user authorization broadening was introduced.
- No service-role secret handling was moved to browser runtime.
- Auth.js server session ownership remains unchanged.
- Resend governance enforcement remains backend-only and environment-driven.

---

2026-04-12 - Prompt 2 Continuation: Post-Confirmation Auth Bootstrap + Proxy Profile-Gate Alignment

Scope
- Continued Prompt 2 implementation with a surgical pass focused on truthful post-confirmation routing and deterministic profile-completion gating.
- Completed mandatory read-first flow before edits (ledger + active auth/session/proxy/runtime surfaces + Next local docs).

Primary Issues Fixed
- Confirm-email callback finalization could complete provider verification without establishing a real Auth.js server session, leaving destination routing dependent on stale/manual login actions.
- Proxy gating did not enforce profile-completion redirects for all direct route hits by authenticated non-admin users.

Current Truth (post-pass)
- `/confirm-email` now finalizes provider callback and immediately bootstraps Auth.js credentials session using provider-appropriate lane selection (`user-credentials` vs `admin-credentials`).
- Session hydration is verified against `/api/auth/me` with bounded retry polling before redirect handoff.
- Redirect destination after confirmation is now computed from authenticated user truth (`role`, `profileCompleted`) through centralized decision logic.
- Proxy now enforces non-admin profile-completion gate for protected user routes and redirects incomplete profiles to settings with preserved `returnTo` context.

Files Changed
- `apps/web/lib/return-to.ts`
  - Added centralized redirect decision model:
    - `AuthenticatedUserRedirectReason`
    - `AuthenticatedUserRedirectDecision`
    - `resolveAuthenticatedUserRedirectPath(...)`
  - Kept `getAuthenticatedUserRedirectPath(...)` as compatibility helper delegating to resolver.

- `apps/web/proxy.ts`
  - Switched login/admin redirect branches to use shared redirect decision helper.
  - Added profile-completion gate at proxy level for authenticated non-admins on protected user routes.
  - Added scoped proxy diagnostics for profile-gate redirects.
  - Added durable comment documenting why profile gate ownership must remain server/proxy authoritative.

- `apps/web/components/auth/confirm-email-panel.tsx`
  - Added Auth.js bootstrap flow after provider callback finalization:
    - resolves bootstrap provider from lane context,
    - signs in via `next-auth/react` credentials provider,
    - polls `/api/auth/me` until server session truth is observable,
    - validates admin lane for admin bootstrap,
    - computes final destination through shared redirect decision.
  - Added structured diagnostics for finalization/bootstrap outcomes (`providerId`, callback kind, role/profile state, destination, reason).
  - Kept Auth.js as session authority by clearing transient Supabase browser session after successful Auth.js bootstrap.
  - Preserved callback URL token cleanup behavior with safer redirect sequencing.

- `apps/web/messages/en.json`
  - Updated `confirmEmailConfirmedBody` to reflect automatic authenticated workspace handoff.

- `apps/web/messages/ar.json`
  - Updated Arabic parity for `confirmEmailConfirmedBody` with the same automatic handoff meaning.

Verification
- `npm run lint --workspace @zootopia/web`: PASSED
- `npm run typecheck --workspace @zootopia/web`: PASSED
- `npm run build --workspace @zootopia/web`: PASSED

Safety / Boundary Notes
- No admin/user privilege broadening introduced.
- No secrets moved to browser runtime.
- Protected-route ownership remains server/proxy authoritative.
- Auth.js remains the single server-session authority after confirm-email handoff.

---

2026-04-12 - Supabase Migration Hygiene + Workflow Hardening (Safe Rename Compatibility)

Scope
- Performed a migration-system hardening pass for the active Supabase/Auth.js/Vercel runtime with explicit safety for already-applied migration renames.
- Kept changes surgical: no architecture redesign, no runtime authority shift to the browser, no unrelated feature edits.

Primary Problems Addressed
- Migration history matching was filename-based only (`_migration_history.migration_name`), so renaming an already-applied SQL file could cause unsafe re-application attempts.
- Active migration naming/comments still contained stale Firestore/Firebase wording.
- Confirm-email resend governance env contract used legacy `ZOOTOPIA_VERIFICATION_RESEND_*` keys only, while current contract needs `ZOOTOPIA_EMAIL_VERIFICATION_*` support.

Current Truth (post-pass)
- Canonical migration chain now uses:
  1. `20260410120000_zootopia_platform_entities.sql`
  2. `20260410180000_phase1_postgres_parity_tables.sql`
  3. `20260410180001_phase1_storage_bucket.sql`
  4. `20260410213000_phase1_owner_rls_and_storage_policies.sql`
  5. `20260412103000_email_verification_resend_governance.sql`
- Migration runner now supports safe filename cleanup by timestamp canonicalization before pending detection.
- Resend governance now prefers canonical `ZOOTOPIA_EMAIL_VERIFICATION_*` keys with backward-compatible fallback to legacy resend keys.

Files Changed
- `scripts/supabase/migration-files.mjs` (new)
  - Shared migration utilities: sorted file discovery, timestamp extraction, ordering validation, canonical-by-timestamp map.
- `scripts/supabase/list-migrations.mjs`
  - Refactored to shared utility + fail-fast validation.
- `scripts/supabase/validate-migrations-order.mjs`
  - Refactored to shared utility + unified validation behavior.
- `scripts/supabase/apply-migrations.mjs`
  - Added `--status` mode.
  - Added safe canonical alias insertion into `_migration_history` for renamed files with same timestamp.
  - Added status summary output (pending, legacy aliases, history-only rows).
- `package.json`
  - Added scripts:
    - `supabase:migrations:status`
    - `db:status`
- `supabase/migrations/20260410180000_phase1_postgres_parity_tables.sql` (new canonical file)
  - Renamed from old Firestore-labeled filename while preserving timestamp for order stability.
  - Sanitized stale Firestore/Firebase wording in comments.
- `supabase/migrations/20260410180000_phase1_firestore_parity_tables.sql` (removed)
- `supabase/migrations/20260410120000_zootopia_platform_entities.sql`
  - Sanitized stale Firestore wording in comments.
- `supabase/migrations/20260410180001_phase1_storage_bucket.sql`
  - Sanitized stale Firebase wording in comments.
- `apps/web/lib/server/verification-resend-governance.ts`
  - Added canonical env family support:
    - `ZOOTOPIA_EMAIL_VERIFICATION_RESEND_MODE`
    - `ZOOTOPIA_EMAIL_VERIFICATION_COOLDOWN_SECONDS`
    - `ZOOTOPIA_EMAIL_VERIFICATION_MAX_ATTEMPTS`
    - `ZOOTOPIA_EMAIL_VERIFICATION_WINDOW_MINUTES`
    - `ZOOTOPIA_EMAIL_VERIFICATION_IP_MAX_ATTEMPTS`
    - `ZOOTOPIA_EMAIL_VERIFICATION_IP_WINDOW_MINUTES`
    - `ZOOTOPIA_EMAIL_VERIFICATION_HASH_SALT`
  - Preserved compatibility fallback to legacy `ZOOTOPIA_VERIFICATION_RESEND_*` keys.
- `.env.example`
  - Added canonical email-verification resend env block and documented legacy alias fallback.
- `README.md`
  - Updated migration workflow docs to include list/validate/status/dry/apply lanes.
  - Documented timestamp-based canonicalization safety and canonical email-verification env family.

Safe Rename Compatibility Behavior (explicit)
- Runner now:
  1. Loads canonical on-disk files and validates ordering.
  2. Reads `_migration_history`.
  3. For each applied history row, if a file with the same timestamp exists but under a new canonical name, inserts canonical history row via `ON CONFLICT DO NOTHING`.
  4. Computes pending migrations only after canonicalization.
- Result: renamed already-applied migration files no longer trigger SQL re-apply attempts.

Verification (this pass)
- `npm run supabase:check`: PASSED
  - Validated 5 migration files.
  - Listed canonical chain including `20260410180000_phase1_postgres_parity_tables.sql`.
- `npm run db:status`: PASSED
  - Reported canonicalization of 1 legacy history row:
    - `20260410180000_phase1_firestore_parity_tables.sql` -> `20260410180000_phase1_postgres_parity_tables.sql`
  - Pending remains exactly 1 migration:
    - `20260412103000_email_verification_resend_governance.sql`
- `npm run db:apply:dry`: PASSED
  - No writes performed; pending set remains exactly the resend-governance migration.
- `npm run lint --workspace @zootopia/web`: PASSED
- `npm run typecheck --workspace @zootopia/web`: PASSED
- `npm run build --workspace @zootopia/web`: PASSED

Deployment / Runbook Notes
- New recommended migration sequence:
  1. `npm run supabase:migrations:list`
  2. `npm run supabase:migrations:validate`
  3. `npm run db:status`
  4. `npm run db:apply:dry`
  5. `npm run db:apply`
- `db:status` is now the explicit pre-deploy safety check for renamed-file compatibility and history drift visibility.

Safety Notes
- No migration SQL re-execution was performed in this pass (`db:apply:dry` only).
- No admin/user auth boundaries were changed.
- No new dependencies were added.

---

2026-04-12 - Non-Authoritative Device Label Metadata Propagation (Auth -> Repository -> Admin)

Scope
- Implemented best-effort, non-authoritative device metadata fields across the auth bootstrap path, user persistence model, session model, and admin visibility surfaces.
- Preserved strict backend authority boundaries: device metadata is informational only and not used for authorization or security decisions.

Field Contract (current truth)
- User/session model fields (camelCase):
  - `deviceLabel: string | null`
  - `deviceLabelSource: string | null`
  - `deviceLabelConfidence: number | null` (bounded to `0..1`)
- Auth claim keys (snake_case, auth metadata only):
  - `zc_device_label`
  - `zc_device_label_source`
  - `zc_device_label_confidence`

Architecture Decisions
- Client derives device label using browser `navigator.userAgent` heuristics only.
- Server sanitizes all incoming device metadata and clamps confidence values before persistence.
- Repository keeps device metadata optional and null-safe; missing values do not block auth/session flows.
- Admin UI/export uses explicit `Unavailable` fallback where metadata is absent.

Files Changed
- `packages/shared-types/src/user.ts`
  - Added device metadata fields to `UserDocument`.
- `packages/shared-types/src/auth.ts`
  - Added device metadata fields to `SessionUser`.
- `apps/web/lib/auth-device-label.ts` (new)
  - Added client-only best-effort device label builder with source/confidence output.
- `apps/web/components/auth/login-panel.tsx`
  - Included device metadata in Auth.js credentials bootstrap payload for user lane.
- `apps/web/components/auth/admin-login-panel.tsx`
  - Included device metadata in Auth.js credentials bootstrap payload for admin lane.
- `apps/web/components/auth/confirm-email-panel.tsx`
  - Included device metadata in Auth.js bootstrap after email-confirmation callback finalization.
- `apps/web/auth.ts`
  - Extended credentials parsing and sanitization for device metadata.
  - Added device metadata to authorized user shape.
  - Added device metadata mapping in JWT/session callbacks.
  - Added credentials schema fields for both `user-credentials` and `admin-credentials` providers.
- `apps/web/lib/server/repository.ts`
  - Added repository-side sanitization helpers for device metadata.
  - Added claim read/write support for `zc_device_*` fields.
  - Extended `buildUserDocumentFromAuthRecord(...)` reconstruction path.
  - Extended `upsertUserFromAuth(...)` input and persistence merge logic.
- `apps/web/lib/server/session.ts`
  - Extended normalized session user construction to carry device metadata from persisted user/session claims.
- `apps/web/app/(protected)/admin/users/[uid]/page.tsx`
  - Added device label/source/confidence cards with `Unavailable` fallback.
- `apps/web/lib/server/admin-users-excel-export.ts`
  - Added XLSX columns: Device Label, Device Label Source, Device Label Confidence.
  - Added `Unavailable` fallback values in export row mapping.

Behavior and Safety Notes
- Device metadata remains non-authoritative telemetry for observability only.
- No trust or privilege path now depends on `deviceLabel*` fields.
- Missing/invalid metadata remains non-fatal and resolves to `null` internally, `Unavailable` in admin-facing surfaces.

Verification
- `npm run lint --workspace @zootopia/web`: PASSED
- `npm run typecheck --workspace @zootopia/web`: PASSED
- `npm run build --workspace @zootopia/web`: PASSED
- Targeted post-edit diagnostics (`get_errors`) for all changed files: no errors.

---

2026-04-12 - Non-Blocking Sign-In Metadata Capture Expansion (Server Observed + Client Best-Effort)

Scope
- Expanded metadata capture to store both server-observed and client best-effort sign-in metadata for admin visibility and XLSX export.
- Preserved strict non-authoritative semantics: this metadata is observability-only and never used for authorization, trust, blocking, deduplication, or policy decisions.
- Ensured metadata collection/persistence failures do not break login, signup, confirm-email bootstrap, profile completion, upload, generation, or admin flows.

Field Contract (Current Truth)
- `UserDocument.serverObservedSignInMetadata` (nullable)
  - `nonAuthoritative: true`
  - `observedAt`
  - `publicIp`
  - `forwardedIpChain`
  - `acceptLanguage`
  - `requestGeo` (`source`, `countryCode`, `region`, `city`, `latitude`, `longitude`)
- `UserDocument.clientBestEffortSignInMetadata` (nullable)
  - `nonAuthoritative: true`
  - `capturedAt`
  - user-agent/browser/OS/platform fields
  - `userAgentData` brands/mobile/platform + high-entropy hints when available
  - screen/viewport/timezone/language/touch/deviceMemory/hardwareConcurrency/network hints
  - approximate label/source/confidence

Hard-Rule Compliance
- No browser permissions requested.
- No attempt to collect forbidden identifiers (MAC, IMEI, serial, hostname, real device name).
- Unavailable values are stored as `null` and rendered/exported as `Unavailable`.
- Metadata is explicitly marked best-effort/non-authoritative in:
  - code contracts,
  - repository claim persistence,
  - admin detail UI,
  - admin export headers/content.
- Observability metadata persistence is non-blocking:
  - repository first attempts full claim write,
  - on optional metadata claim failure, it retries with auth-critical-only claims,
  - auth/session bootstrap remains functional.

Files Changed
- `packages/shared-types/src/user.ts`
  - Added explicit server-observed and client best-effort metadata interfaces.
  - Added `serverObservedSignInMetadata` and `clientBestEffortSignInMetadata` to `UserDocument`.
- `apps/web/lib/auth-device-label.ts`
  - Replaced simple sync label helper with async best-effort metadata capture.
  - Added userAgentData hints, screen/viewport/timezone/language/touch/deviceMemory/hardware/network capture.
  - Added safe JSON serialization for credentials transport.
- `apps/web/components/auth/login-panel.tsx`
  - Added async metadata collection and credentials transport field `clientBestEffortSignInMetadata`.
- `apps/web/components/auth/admin-login-panel.tsx`
  - Added async metadata collection and credentials transport field `clientBestEffortSignInMetadata`.
- `apps/web/components/auth/confirm-email-panel.tsx`
  - Added async metadata collection and credentials transport field `clientBestEffortSignInMetadata` during post-confirmation bootstrap.
- `apps/web/auth.ts`
  - Added server-observed metadata capture from request headers (IP chain/geo/accept-language/timestamp).
  - Added parsing for client metadata payload from credentials.
  - Passed both metadata scopes into `upsertUserFromAuth(...)`.
  - Extended credentials provider schema with `clientBestEffortSignInMetadata`.
- `apps/web/lib/server/repository.ts`
  - Added sanitizers for server-observed and client best-effort metadata payloads.
  - Added claim persistence/reconstruction for:
    - `zc_server_observed_signin_metadata`
    - `zc_client_best_effort_signin_metadata`
  - Extended `buildUserDocumentFromAuthRecord(...)` and `upsertUserFromAuth(...)`.
  - Added non-blocking fallback when optional observability metadata claim writes fail.
- `apps/web/lib/server/admin-users-excel-export.ts`
  - Added explicit non-authoritative metadata columns for server/client scopes.
  - Added formatting helpers with `Unavailable` fallback.
- `apps/web/app/(protected)/admin/users/[uid]/page.tsx`
  - Added server/client metadata visibility cards.
  - Relabeled approximate device fields as best-effort/non-authoritative.

Verification
- `npm run lint --workspace @zootopia/web`: PASSED
- `npm run typecheck --workspace @zootopia/web`: PASSED
- `npm run build --workspace @zootopia/web`: PASSED
- Targeted post-edit diagnostics (`get_errors`) for changed files: no errors.

---

2026-04-12 - Process-Compliance Multi-Subagent Consolidation (Read-Only, No Code Changes)

Scope
- Executed the requested pre-change investigation workflow in full:
  - mandatory full ledger read,
  - one broad architecture Explore pass,
  - six targeted parallel Explore passes (auth/session payload tracing, repository tracing, admin detail tracing, admin export tracing, shared contracts tracing, verification tracing).
- Performed direct source validation on high-risk claims to separate verified facts from assumptions before deciding on edit scope.

Current Truth (post-analysis)
- Metadata pipeline remains fully wired across the intended chain:
  - client best-effort capture -> credentials payload -> auth authorize callbacks,
  - server-observed header capture at auth boundary,
  - repository sanitization + claim persistence + reconstruction,
  - admin user detail visibility,
  - admin XLSX export columns/fallbacks.
- Non-authoritative semantics are explicitly preserved in contracts and admin-facing surfaces.
- Session payload remains bounded (device label trio present; full observability blobs remain server-side/admin-only).
- Repository fallback for optional observability claim writes remains non-blocking (auth-critical claim persistence is retried without optional metadata when needed).

Runtime Matrix (verified)
- Local integrated development:
  - server-observed geo is often absent; null-safe formatting and `Unavailable` fallbacks are active.
  - metadata capture remains best-effort/non-blocking.
- Vercel production runtime:
  - server-observed geo headers (`x-vercel-ip-*`) are consumed when present.
  - same null-safe fallback behavior applies when edge headers are missing.
- Legacy/reference-only assumptions:
  - no migration of authority to browser runtime; backend/session authority boundaries remain intact.

Gap Decision
- No required code changes were identified from this consolidation pass for the requested non-blocking non-authoritative metadata pipeline.
- Optional future enhancements (not required for current acceptance) remain backlog-only: richer per-field provenance labeling, export preflight warnings when auth runtime metadata is unavailable, and filename collision hardening for same-minute export concurrency.

Verification
- Workspace diagnostics (`get_errors`): no errors.
- No files were modified in this pass other than this ledger refresh entry.

Safety / Boundary Notes
- No authorization, routing, or security decision paths were changed.
- No secrets or privileged logic moved to browser runtime.
- No dependency or schema changes were made in this pass.

---

2026-04-12 - Multi-Tenant Isolation Hardening: Production Durable-Persistence Guard + Runtime Diagnostics

Scope
- Hardened server persistence behavior so production defaults to fail-closed instead of silently degrading to process-local in-memory storage when Supabase/Postgres prerequisites are missing.
- Preserved local/dev degraded-mode behavior for controlled troubleshooting while adding explicit one-time diagnostics.
- Added runtime health diagnostics for persistence guard state to support faster isolation/admin-boundary incident triage.

Problem Addressed
- Prior behavior allowed implicit in-memory fallback whenever Supabase admin runtime or database URL prerequisites were absent.
- In production multi-instance environments, implicit process-local fallback can fragment authoritative owner/admin data truth and weaken operational confidence in strict tenant isolation guarantees.

Architecture Decision (current truth)
- Production contract is now fail-closed by default for repository persistence prerequisites.
- Explicit emergency override remains available through:
  - `ZOOTOPIA_ALLOW_PRODUCTION_MEMORY_FALLBACK=true|1|yes`
- Dev/test/local workflows can continue to use memory fallback when durable persistence is intentionally unavailable.

Files Changed
- `apps/web/lib/server/zootopia-postgres-adapter.ts`
  - Added persistence runtime policy helpers:
    - `isProductionMemoryFallbackAllowed()`
    - `requiresDurableZootopiaPersistence()`
    - `getZootopiaPersistenceRuntimeState()`
  - Added exported runtime-state type for consistent server diagnostics.
  - Updated `shouldUseZootopiaPostgresPersistence()` to resolve from unified runtime-state policy.
- `apps/web/lib/server/repository.ts`
  - Replaced implicit fallback check with policy-aware guard:
    - Uses `getZootopiaPersistenceRuntimeState()`.
    - Throws `ZOOTOPIA_DURABLE_PERSISTENCE_REQUIRED` in production when durable prerequisites are missing.
  - Added one-time structured diagnostics:
    - production block log (`[repository-persistence] blocked in-memory fallback in production`)
    - non-production fallback warning (`[repository-persistence] using in-memory fallback`)
  - Added global one-time log guards to prevent noisy repeated logs.
- `apps/web/lib/server/runtime.ts`
  - Runtime auth readiness now includes persistence policy awareness.
  - `supabaseAuth`/`adminSupabaseAuth` fail closed when durable persistence is required but unavailable.
- `apps/web/auth.ts`
  - Auth bootstrap infrastructure classifier now treats `ZOOTOPIA_DURABLE_PERSISTENCE_REQUIRED` as infrastructure failure, preventing ambiguous runtime error categorization.
- `apps/web/app/api/health/route.ts`
  - Added persistence diagnostics payload:
    - `persistenceGuard.requiresDurablePersistence`
    - `persistenceGuard.memoryFallbackAllowedInProduction`
    - `persistenceGuard.missingRequirements` (`supabase_admin_runtime`, `supabase_database_url`)

Isolation/Admin Boundary Impact
- No owner-scope routing, ownerUid checks, or admin guard lanes were loosened.
- No client-authoritative ownership paths were introduced.
- Hardening specifically reduces production drift risk by preventing silent fallback away from durable shared persistence.

Verification (this pass)
- `npm run lint --workspace @zootopia/web`: PASSED
- `npm run typecheck --workspace @zootopia/web`: PASSED
- `npm run build --workspace @zootopia/web`: PASSED
- Targeted diagnostics (`get_errors`) for changed files: no errors.

Operational Notes
- Health endpoint now exposes persistence guard state for faster deployment/runtime triage.
- Production operators should keep `ZOOTOPIA_ALLOW_PRODUCTION_MEMORY_FALLBACK` unset unless an explicit emergency degraded-mode decision is approved.

Residual Risk / Non-Validated Areas
- No live multi-instance load test was run in this pass.
- No live network fault injection was executed against production Supabase services in this pass.
- Concurrency correctness remains code-verified and build-verified, not runtime-chaos-tested in this pass.

---

2026-04-12 - Auth Email Delivery Diagnostics + Premium Standalone Confirm-Email Template

Scope
- Implemented conservative backend diagnostics for auth email resend provider failures.
- Added a premium, email-client-safe standalone HTML template artifact for manual auth-template paste workflows.
- Preserved existing governance, auth/session authority boundaries, and client compatibility.

Current Truth (post-change)
- Confirm-email resend backend now classifies provider delivery failures into explicit, safe diagnostic classes:
  - `provider_daily_quota_likely`
  - `provider_monthly_quota_likely`
  - `provider_rate_limited`
  - `provider_sender_identity_unverified`
  - `provider_configuration_or_auth`
  - `provider_network_or_timeout`
  - `provider_unknown_rejection`
- Route-level API error mapping now distinguishes likely quota cases from generic failures while remaining conservative:
  - `VERIFICATION_RESEND_PROVIDER_DAILY_LIMIT_LIKELY`
  - `VERIFICATION_RESEND_PROVIDER_MONTHLY_LIMIT_LIKELY`
  - `VERIFICATION_RESEND_PROVIDER_RATE_LIMITED`
  - `VERIFICATION_RESEND_PROVIDER_IDENTITY_UNVERIFIED`
  - `VERIFICATION_RESEND_PROVIDER_NETWORK_FAILURE`
  - `VERIFICATION_RESEND_PROVIDER_REJECTED`
  - `VERIFICATION_RESEND_UNAVAILABLE` (config/auth lane)
- Failure responses now include safe coarse diagnostics headers for frontend QA and ops triage:
  - `X-Verification-Provider-Diagnostic`
  - `X-Verification-Provider-Diagnostic-Confidence`
- No sensitive provider credentials or tokens are surfaced in responses.

Files Changed
- `apps/web/lib/server/auth-email-delivery-diagnostics.ts` (new)
  - Added server-only, conservative classifier for auth-email delivery failures.
  - Added explicit high/medium/low confidence model and sanitized provider message handling.
- `apps/web/app/api/auth/confirm-email/resend/route.ts`
  - Integrated new classifier into provider failure mapping.
  - Preserved existing governance/cooldown/window behavior.
  - Added safe diagnostic headers and expanded structured provider-failure logging.
- `apps/web/lib/auth-failure.ts`
  - Extended normalization map for new resend diagnostic codes and explicit provider quota/auth tokens.
- `docs/auth-email/templates/zootopia-confirm-email-template-supabase.html` (new)
  - Added premium standalone confirmation email template using email-client-safe table layout and inline styles.
  - Uses Supabase template variables (`{{ .ConfirmationURL }}`, `{{ .Token }}`, `{{ .SiteURL }}`).
- `docs/auth-email/templates/README.md` (new)
  - Added manual paste instructions and variable guidance for Supabase Auth + Resend SMTP and non-Supabase Resend flows.

Design / Safety Notes
- Diagnostics are intentionally conservative:
  - Quota-specific outcomes are emitted only when provider code/message evidence is explicit.
  - Ambiguous failures remain in broader config/network/unknown lanes.
- Existing client UX contract remains stable through normalized `AUTH_*` categories.
- No schema changes, no dependency changes, no auth-boundary relaxations.

Verification (this pass)
- `npm run lint --workspace @zootopia/web`: PASSED
- `npm run typecheck --workspace @zootopia/web`: PASSED
- `npm run build --workspace @zootopia/web`: PASSED
- Targeted diagnostics (`get_errors`) for changed files: no errors.

Residual Risk / Non-Validated Areas
- No live provider-quota fault injection was executed against production Resend/Supabase email pipelines in this pass.
- Daily/monthly quota outcomes are evidence-based "likely" classifications when inferred from provider responses.
