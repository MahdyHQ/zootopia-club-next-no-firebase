================================================================================
ZOOTOPIA CLUB NEXT — PROJECT LEDGER
Authoritative Encyclopedia & Master Project Memory
================================================================================

Last Updated: 2026-04-15 (Assessment non-admin rights panel overflow-safe responsive typography refinement)
Repository: https://github.com/MahdyHQ/zootopia-club-next-no-firebase.git
Current Commit: 58e639b4e42c9f46f6405074b4bed019d2605fa2
Framework: Next.js 16.2.2 (App Router) / React 19.2.4 / TypeScript 5
Runtime: Node.js >= 20.9.0

================================================================================
TABLE OF CONTENTS
================================================================================

  1.  Project Identity
  2.  Current Truth Snapshot
  3.  Project Purpose & Platform Direction
  4.  Architecture Style
  5.  Monorepo Structure
  6.  Dependency & Runtime Baseline
  7.  Active Deployment Model
  8.  Environment Strategy
  9.  Route Map
  10. Route Ownership Clarification
  11. Backend Runtime & Service Ownership
  12. Auth & Session Architecture
  13. Admin Authority Model
  14. Profile Completion & User Gating Model
  15. Upload / Document / Storage Flow
  16. Assessment Runtime / Result / Export Flow
  17. Infographic Flow
  18. Database / Migrations / Storage / Persistence Notes
  19. AI / Model / Provider Orchestration
  20. Redirect / Callback / Confirmation Behavior
  21. Email Verification & Resend Governance
  22. Security Assumptions & Trust Boundaries
  23. Important Folders & Files
  24. Sensitive & High-Risk Files
  25. Important Commands & Scripts
  26. Current Feature Set
  27. Deferred / Disabled / Legacy Areas
  28. Future Agent Guidance
  29. Historical Change Log

================================================================================
1. PROJECT IDENTITY
================================================================================

Name:           zootopia-club-next
Version:        0.1.0
Private:        Yes
Type:           Monorepo (npm workspaces)
Primary App:    @zootopia/web (Next.js 16.2.2)
Secondary App:  @zootopia/api (placeholder only — inactive)
Shared Packages:
  - @zootopia/shared-config   (constants, routes, env keys, feature flags)
  - @zootopia/shared-types    (TypeScript interfaces, DTOs, shared contracts)
  - @zootopia/shared-utils    (pure utilities: validation, cn, dates, RTL)

Git Remote:     origin https://github.com/MahdyHQ/zootopia-club-next-no-firebase.git
Branch:         main (primary)

Product Name:   Zootopia Club
Domain Purpose: AI-powered document analysis, assessment generation, and
                infographic creation platform with admin governance.

================================================================================
2. CURRENT TRUTH SNAPSHOT
================================================================================

As of 2026-04-15, the project is a Next.js 16.2.2 monorepo deployed on Vercel,
using Supabase as the unified backend provider (Postgres database, Auth, Storage).

Key verified facts:
- Firebase has been fully replaced by Supabase (no Firebase runtime dependencies)
- Auth.js (NextAuth v5) with JWT strategy manages sessions
- Supabase Auth provides identity (email/password only)
- Two credential providers: user-credentials and admin-credentials
- Admin access controlled by email allowlist (ZOOTOPIA_ADMIN_EMAILS)
- Auth bootstrap now fails closed when Supabase still reports email as unconfirmed
  (user + admin flows), and confirm-email callback success UI now requires
  provider-confirmed email state before Auth.js session handoff
- Regular-user password lifecycle now includes server-authoritative forgot-password,
  recovery reset, and in-account password-change flows with explicit admin-lane
  separation and password-security metadata persistence
- Public auth surfaces now include `/forgot-password` and `/reset-password`, and
  signup/reset/settings password entry points share one policy validator contract
- Profile completion gating blocks incomplete profiles from protected features
- AI providers: Google AI (Gemini) and DashScope (Qwen/Alibaba)
- Tool default models are now server-authoritative per real tool via
  `ZOOTOPIA_DEFAULT_MODEL_ASSESSMENT` and `ZOOTOPIA_DEFAULT_MODEL_INFOGRAPHIC`,
  with strict tool-scoped fallback validation
- Assessment Gemini model catalog now treats `gemini-2.5-flash-lite` and
  `gemini-3.1-flash-lite-preview` as active lanes, with
  `gemini-2.5-flash-lite` as the built-in default; legacy
  `gemini-2.5-flash` requests normalize to `gemini-2.5-flash-lite`
- Assessment export/result artifact metadata now mirrors into
  `assessmentArtifactMetadata` documents for owner/kind/status/expiry queries
- Upload records now persist normalized `fileExtension` and `contentSha256`
  metadata for durable reporting and retention tooling
- Document uploads stored with configurable retention (default 15 min)
- Assessment results stored with configurable retention (default 1440 min / 1 day)
- Export artifacts stored with configurable retention (default 15 min)
- Assessment credits limit AI generation with env-configurable UTC renewal windows
  (default 24h, daily behavior preserved)
- Admin credit mutations now persist durable per-user before/after history
  snapshots and are rendered on `/admin/users/[uid]`
- Protected shell credit header refresh now coalesces concurrent trigger paths
  (focus/visibility/event/interval) and skips hidden-tab interval polling to
  reduce duplicate `/api/assessment/credits` requests
- Protected header credit badge now keeps a compact always-visible balance value
  beside the wallet icon and includes an adjacent non-mutating plus action that
  opens an Arabic urgent-credit help popover (Elmahdy Abdallah contact guidance)
  with outside-click/Escape close behavior
- Assessment prompt feature now uses a durable admin-managed per-user
  entitlement gate (`enabled|disabled`) with server-authoritative enforcement,
  while preserving the existing env-password unlock mechanism for entitled
  users and keeping admin permanently exempt
- Assessment setup non-admin model-replacement panel now uses compact Arabic
  rights ownership copy and a subtle donation/support hint with small icons,
  preserving premium visual balance in the top setup row across breakpoints
- Assessment setup non-admin rights panel now enforces overflow-safe responsive
  wrapping/sizing so Arabic rights text stays fully contained on mobile, tablet,
  desktop, and large-width surfaces without escaping the panel container
- Global maintenance mode now supports an env-controlled proxy guard that
  redirects non-admin auth/public/site/protected traffic to `/maintenance`,
  while preserving admin bypass and `/admin/login` availability
- Admin overview/users API routes now explicitly declare dynamic runtime cache
  behavior (`dynamic = "force-dynamic"`, `revalidate = 0`) in addition to
  response-level no-store headers
- Protected header and settings surfaces now share a compact local avatar policy
  (`admin` role override → managed `gender` asset → initial fallback)
- Request-locale bootstrap now supports env-seeded first-load language via
  `ZOOTOPIA_DEFAULT_LANGUAGE` (`en|ar`, invalid/missing fails closed to `en`)
- Home page stat counters now render in a lower workspace snapshot section so
  the hero title lane remains clean across breakpoints
- Home protected page spacing is now slightly tighter on small screens with a
  centered fold-discovery cue that keeps subtle motion and honors reduced-motion
  preferences
- Home protected page platform-story CTA now keeps its elevated interaction cue
  visible in touch-first production contexts, and its roadmap viewer renders
  through a viewport-owned portal with a more visible close control so the Home
  hero's overflow/blur shell cannot clip the popup on Vercel/mobile browsers
- Journey page light-mode surfaces/text now use coherent high-contrast daytime
  styling
- Upload hero now presents a single premium intake surface with unified internal
  layering and a restrained upper-left depth hover reaction, replacing the prior
  redundant route-level title shell
- Assessment hero/premium surfaces now use slightly higher translucency while
  preserving contrast for controls and text
- Detached assessment preview/result pages now place their interactive export
  controls in a dedicated full-width action panel beneath metadata; preview
  footers now render through a dedicated preview-only component while the
  Fast and Pro PDF lanes each own their own server-side renderer file
  (`assessment-fast-print-renderer.ts` / `assessment-pro-print-renderer.ts`),
  and now preserve the old shared renderer content inside lane-owned files
  without hidden final-renderer coupling; Pro keeps the absolute-base-url /
  non-interactive capture contract while Fast stays browser-print-first
- Web workspace `typecheck` and `build` now clear stale `.next/dev` cache state
  before invoking Next typegen/build so malformed dev-only route helper files do
  not poison later TypeScript or production build runs
- Assessment Pro PDF export now traces the hoisted Chromium Brotli payload from
  the workspace root for Vercel, uses explicit `maxDuration = 120`, and emits
  stage-classified internal diagnostics without exposing raw failure details to
  the user-facing response
- Protected Home/Assessment/Infographic/Settings/Upload/History wrappers now
  share a lighter translucency baseline so background ambience reads through
  consistently without reducing form/control legibility
- Exhausted daily-credit messaging now includes localized EN/AR escalation
  guidance to contact developer/admin Elmahdy Abdallah for private or urgent
  credits, rendered only in the exhausted-credit state
- Public auth pages now include tighter mobile overlap guards and restrained
  overlay/shadow density for cleaner stacking behavior
- Email verification via Supabase with Resend provider support
- Admin panel includes user management, storage management, governance dashboard
- Database uses Supabase Postgres with RLS policies
- Migration management via scripts/supabase/apply-migrations.mjs
- 11 migration files tracked in supabase/migrations (latest adds user password security event/state persistence)

================================================================================
3. PROJECT PURPOSE & PLATFORM DIRECTION
================================================================================

Zootopia Club is an AI-powered platform that provides:

1. Document Upload & Processing
   - Users upload documents (PDF, images, text files)
   - Server-side extraction and processing pipeline
   - Configurable retention policies per storage type

2. AI Assessment Generation
   - Users generate assessments from uploaded documents
   - AI models analyze content and produce structured assessments
   - Credit-limited system (daily caps)
   - Export to PDF, JSON, and other formats

3. Infographic Generation
   - AI-powered infographic creation from document content
   - Admin-only feature (currently gated)
   - Multiple modes and styles

4. Admin Governance
   - User management and oversight
   - Storage management and cleanup
   - System governance controls
   - Audit logging

Platform Direction:
- Migrated from Firebase to Supabase (complete)
- Monorepo structure prepared for future API extraction
- Provider-agnostic AI layer (Google + Alibaba/DashScope)
- Enterprise-grade security with RLS, admin allowlists, rate limiting

================================================================================
4. ARCHITECTURE STYLE
================================================================================

Pattern:        Next.js App Router with layered server architecture
Style:          Monorepo with workspace isolation
Session:        Stateless JWT (Auth.js / NextAuth v5)
Database:       Supabase Postgres (provider-managed)
Storage:        Supabase Storage buckets (provider-managed)
AI:             Multi-provider orchestration (Google AI + DashScope)

Layered Architecture:
```
Route Handlers (apps/web/app/api/)
  → Session/Auth Layer (lib/server/session.ts)
  → Repository Layer (lib/server/repository.ts)
  → AI Execution Layer (lib/server/ai/execution.ts)
  → Storage Layer (lib/server/supabase-blob-storage.ts)
  → Database Layer (lib/server/zootopia-postgres-adapter.ts)
```

Boundaries:
- Frontend: Client components in (site), (public), (protected)/(completed)
- Backend: Route handlers, lib/server/*, auth.ts, proxy.ts
- Shared: packages/shared-* (types, config, utils)
- Infrastructure: supabase/migrations/, scripts/, vercel.json
- Provider-Managed: Supabase (Auth, Postgres, Storage), Vercel (Hosting), Resend (Email)

Same-Origin vs Provider-Managed:
- Next.js app runs on Vercel (same-origin for all /api/* routes)
- Supabase Auth: cross-origin provider (browser → Supabase CDN)
- Supabase Postgres: server-side only (connection string, not browser-accessible)
- Supabase Storage: cross-origin provider (browser + server)
- Google AI API: cross-origin provider (server-side calls)
- DashScope API: cross-origin provider (server-side calls)
- Resend/SMTP: server-side only

================================================================================
5. MONOREPO STRUCTURE
================================================================================

Root: zootopia-club-next/
├── apps/
│   ├── web/                    # Next.js 16.2.2 application (PRIMARY/ACTIVE)
│   │   ├── app/                # App Router routes and layouts
│   │   │   ├── (protected)/    # Authenticated user workspace
│   │   │   ├── (public)/       # Public auth pages
│   │   │   ├── (site)/         # Public marketing/info pages
│   │   │   └── api/            # API route handlers
│   │   ├── components/         # React components
│   │   ├── lib/                # Library code (client + server)
│   │   │   └── server/         # Server-side services
│   │   ├── messages/           # AI prompt templates / i18n messages
│   │   ├── public/             # Static assets
│   │   ├── scripts/            # Build/postbuild scripts
│   │   ├── auth.ts             # Auth.js configuration
│   │   ├── proxy.ts            # Auth.js proxy helpers
│   │   ├── next.config.ts      # Next.js configuration
│   │   └── package.json        # @zootopia/web manifest
│   └── api/                    # Future API extraction target (INACTIVE)
│       └── (placeholder only — no active code)
├── packages/
│   ├── shared-config/          # Constants, routes, env keys, feature flags
│   ├── shared-types/           # TypeScript interfaces, DTOs
│   └── shared-utils/           # Pure utility functions
├── scripts/
│   └── supabase/               # Migration management scripts
├── supabase/
│   └── migrations/             # SQL migration files
├── docs/                       # Architecture documentation
│   └── auth-email/             # Auth email templates
├── Guide-Files/                # Agent/CLI guides (reference only)
├── skills/                     # Agent skill definitions
│   ├── supabase/
│   └── supabase-postgres-best-practices/
├── package.json                # Root workspace manifest
├── tsconfig.json               # Root TypeScript config
├── tsconfig.base.json          # Shared TypeScript base config
├── eslint.config.mjs           # ESLint configuration
├── .env.example                # Environment variable template
├── .env.local                  # Local environment (gitignored)
└── zootopia-club-next-ledger.txt  # This file

================================================================================
6. DEPENDENCY & RUNTIME BASELINE
================================================================================

Root Dependencies:
- @supabase/ssr: ^0.10.2
- @supabase/supabase-js: ^2.103.0
- framer-motion: 12.38.0
- lucide-react: ^1.7.0
- pnpm: ^10.33.0
- @tailwindcss/postcss: ^4
- tailwindcss: ^4
- typescript: ^5
- eslint: ^9
- eslint-config-next: 16.2.2

@zootopia/web Dependencies (additional):
- next: 16.2.2
- next-auth: ^5.0.0-beta.30
- react: 19.2.4
- react-dom: 19.2.4
- @radix-ui/react-slot: ^1.2.4
- radix-ui: ^1.4.3
- class-variance-authority: ^0.7.1
- clsx: ^2.1.1
- tailwind-merge: ^3.5.0
- tw-animate-css: ^1.4.0
- shadcn: ^4.2.0
- motion: ^12.38.0
- docx: ^9.5.1
- exceljs: ^4.4.0
- libphonenumber-js: ^1.12.41
- react-phone-number-input: ^3.4.16
- qrcode: ^1.5.4
- nodemailer: ^8.0.4
- postgres: ^3.4.5
- puppeteer-core: ^24.35.0
- @sparticuz/chromium: ^143.0.0

Node Engine: >= 20.9.0
Module Type: ESM (apps/web has "type": "module")

================================================================================
7. ACTIVE DEPLOYMENT MODEL
================================================================================

Primary Target: Vercel
- Build command: npm run build:vercel (→ next build)
- Start command: npm run start:vercel (→ next start)
- Node max old space: 16384 MB for build
- Postbuild: scripts/normalize-standalone.mjs

Local Development:
- Dev command: npm run dev (→ npm run dev:local → next dev --turbopack)
- Start command: npm run start:local (→ next start)

Supabase:
- Provider-managed Postgres, Auth, Storage
- Migrations applied via scripts/supabase/apply-migrations.mjs
- Uses Supabase Management API for migration application
- Project ID from SUPABASE_PROJECT_ID env var

Email:
- Resend provider for email verification (Supabase-managed)
- SMTP relay for contact form (optional)

Legacy/Reference-Only (NOT active):
- Firebase (fully migrated away)
- Cloud Run
- Netlify
- Docker/Cloud Build
- Single-app server layouts

================================================================================
8. ENVIRONMENT STRATEGY
================================================================================

Environment Variables (from .env.example):

Required:
- AUTH_SECRET: Auth.js session crypto (generate with: npx auth secret)
- NEXT_PUBLIC_BASE_URL: Canonical app origin for server callbacks/internal fetches
- NEXTAUTH_URL: Optional Auth.js origin fallback when canonical base URL is unset
- NEXT_PUBLIC_SUPABASE_URL: Supabase project URL (browser + server)
- NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY: Supabase anon key (browser)
- NEXT_PUBLIC_SUPABASE_ANON_KEY: Browser auth alias (compatibility)
- SUPABASE_URL: Server auth config (can reuse NEXT_PUBLIC_SUPABASE_URL)
- SUPABASE_SERVICE_ROLE_KEY: Server admin key for Supabase
- SUPABASE_DATABASE_URL: Postgres connection string (pooler URI)
- GOOGLE_AI_API_KEY: Google AI provider key
- DASHSCOPE_API_KEY: DashScope (Alibaba/Qwen) provider key

Admin & Governance:
- ZOOTOPIA_ADMIN_EMAILS: Comma-separated admin email allowlist
- ZOOTOPIA_MAINTENANCE_SECRET: Optional maintenance endpoint secret
- ZOOTOPIA_MAINTENANCE_MODE_ENABLED: Optional global maintenance-mode toggle
  (`1|true|yes` enables; missing/invalid values fail closed to disabled)

Session & Credits:
- ZOOTOPIA_SESSION_TTL_SECONDS: Session TTL (default 3600)
- ZOOTOPIA_DEFAULT_DAILY_ASSESSMENT_CREDITS: Daily AI credit cap (default 3)
- ZOOTOPIA_ASSESSMENT_CREDIT_RENEWAL_WINDOW_HOURS: Credit renewal window in UTC hours
  (default 24, valid range 1..168)
- ZOOTOPIA_ASSESSMENT_PROMPT_LOCK_ENABLED: Optional lock toggle for normal-user
  assessment prompt access (true/false)
- ZOOTOPIA_ASSESSMENT_PROMPT_UNLOCK_PASSWORD: Server-only unlock password for
  normal-user assessment prompt access
- ZOOTOPIA_DEFAULT_THEME_MODE: Initial theme fallback (`dark` or `light`, invalid falls back to `dark`)
- ZOOTOPIA_DEFAULT_LANGUAGE: Initial locale fallback (`en` or `ar`, invalid falls back to `en`)

Tool Default Models:
- ZOOTOPIA_DEFAULT_MODEL_ASSESSMENT: server-authoritative default for assessment
- ZOOTOPIA_DEFAULT_MODEL_INFOGRAPHIC: server-authoritative default for infographic
- Missing, invalid, unsupported, or cross-tool values fall back to the built-in
  tool default instead of leaking across tool scopes

Redirects:
- NEXT_PUBLIC_ZOOTOPIA_AUTH_USER_DEFAULT_REDIRECT: Post-login redirect (default /)
- NEXT_PUBLIC_ZOOTOPIA_AUTH_ADMIN_DEFAULT_REDIRECT: Admin post-login redirect (default /)

Email Verification:
- ZOOTOPIA_EMAIL_VERIFICATION_RESEND_MODE: provider (default)
- ZOOTOPIA_EMAIL_VERIFICATION_COOLDOWN_SECONDS: 30
- ZOOTOPIA_EMAIL_VERIFICATION_MAX_ATTEMPTS: 5
- ZOOTOPIA_EMAIL_VERIFICATION_WINDOW_MINUTES: 60
- ZOOTOPIA_EMAIL_VERIFICATION_IP_MAX_ATTEMPTS: 20
- ZOOTOPIA_EMAIL_VERIFICATION_IP_WINDOW_MINUTES: 60
- ZOOTOPIA_EMAIL_VERIFICATION_HASH_SALT: Salt for attempt hashing

Storage Retention:
- ZOOTOPIA_UPLOAD_RETENTION_MINUTES: 15 (documents/uploads)
- ZOOTOPIA_RESULT_RETENTION_MINUTES: 1440 (1 day, assessment results)
- ZOOTOPIA_EXPORT_RETENTION_MINUTES: 15 (export artifacts)
- ZOOTOPIA_UPLOAD_RETENTION_MODE: expiry
- ZOOTOPIA_RESULT_RETENTION_MODE: expiry
- ZOOTOPIA_EXPORT_RETENTION_MODE: expiry

AI Configuration:
- DASHSCOPE_BASE_URL: https://dashscope-us.aliyuncs.com/compatible-mode/v1
- DASHSCOPE_COMPATIBLE_BASE_URL: (legacy alias)
- ALIBABA_MODEL_STUDIO_BASE_URL: (legacy alias)
- GOOGLE_AI_MODEL: (optional override)
- GOOGLE_AI_ADVANCED_MODEL: (optional override)
- QWEN_MODEL: (optional override)

SMTP/Contact (optional):
- SMTP_HOST: smtp.gmail.com (default)
- SMTP_PORT: 587 (default)
- SMTP_SECURE: false (default)
- SMTP_USER, SMTP_PASS, EMAIL_FROM, CONTACT_FORM_TO

PDF Generation (optional):
- ASSESSMENT_PDF_BROWSER_EXECUTABLE_PATH: Local Chrome/Chromium override
- PUPPETEER_EXECUTABLE_PATH: Puppeteer Chrome override

Legacy (commented out, not used):
- FIREBASE_* variables (all legacy Firebase config)
- ZOOTOPIA_ADMIN_LOGIN_PASSWORD (deprecated admin password gate)
- ZOOTOPIA_VERIFICATION_RESEND_* (legacy aliases, still accepted)
- ZOOTOPIA_STORAGE_RETENTION_* (legacy global fallback)
- DATABASE_URL (optional alias)

Environment File Strategy:
- .env.example: Template (committed)
- .env.local: Local development secrets (gitignored)
- Vercel: Environment variables configured in Vercel dashboard
- Supabase: Provider-managed, no env file needed

================================================================================
9. ROUTE MAP
================================================================================

Route Groups:
- (protected): Requires authentication + profile completion
- (public): Public pages (login, forgot/reset password, confirm email)
- (site): Public marketing/info pages

Complete Route Tree:

Root:
  /                           → Home dashboard (protected, completed profile)

Protected Routes (requires auth):
  /upload                     → Document upload workspace
  /assessment                 → AI assessment generation studio
  /infographic                → Infographic generation (admin-only)
  /history                    → Upload & assessment history viewer
  /settings                   → User profile settings
  /admin                      → Admin dashboard
  /admin/users/[uid]          → User detail management
  /admin/storage              → Storage management dashboard
  /admin/governance           → Governance controls (if present)

Public Routes (no auth):
  /login                      → Login page
  /forgot-password            → Password reset email request page
  /reset-password             → Password recovery completion page
  /confirm-email              → Email confirmation page
  /maintenance                → Maintenance-mode holding page
  /coming-soon                → Feature preview placeholder

Site Routes (public marketing):
  /about                      → About page (if present)
  /contact                    → Contact page (if present)
  /privacy                    → Privacy policy (if present)
  /terms                      → Terms of service (if present)

API Routes:
  /api/auth/[...nextauth]     → Auth.js handler
  /api/auth/confirm-email/resend  → Email verification resend
  /api/auth/password/forgot   → Request password reset email (regular users)
  /api/auth/password/recovery → Validate/complete recovery token flow
  /api/auth/password/change   → In-account password change (regular users)
  /api/assessment             → POST: Create assessment
  /api/assessment/[id]        → GET: Get single assessment
  /api/assessment/credits     → GET: Daily credit status
  /api/assessment/prompt-unlock → POST: Verify unlock password for assessment prompt
  /api/assessment/export/*    → GET: Export as PDF/JSON
  /api/assessment/linked-document → GET: Resolve linked document
  /api/assessments            → GET: List user assessments
  /api/documents              → GET: List user documents
  /api/documents/[id]         → GET/DELETE: Document operations
  /api/uploads                → POST: File upload
  /api/uploads/[id]           → GET/DELETE: Upload operations
  /api/infographic            → POST: Generate infographic
  /api/admin/users            → GET: List users (admin)
  /api/admin/users/[uid]      → GET/PATCH: User management (admin)
  /api/admin/storage          → GET: Storage stats (admin)
  /api/admin/storage/cleanup  → POST: Storage cleanup (admin)
  /api/admin/governance       → GET/PATCH: Governance controls (admin)
  /api/admin/logs             → GET: Admin audit logs
  /api/contact                → POST: Contact form (SMTP relay)
  /api/internal/maintenance/expired-uploads → POST: Secret-gated upload cleanup sweep

================================================================================
10. ROUTE OWNERSHIP CLARIFICATION
================================================================================

Frontend-Owned (Client Pages):
- apps/web/app/(protected)/(completed)/page.tsx — Home dashboard
- apps/web/app/(protected)/(completed)/upload/page.tsx — Upload workspace
- apps/web/app/(protected)/(completed)/assessment/page.tsx — Assessment studio
- apps/web/app/(protected)/(completed)/infographic/page.tsx — Infographic page
- apps/web/app/(protected)/(completed)/history/page.tsx — History viewer
- apps/web/app/(protected)/settings/page.tsx — Settings page
- apps/web/app/(protected)/admin/page.tsx — Admin dashboard
- apps/web/app/(protected)/admin/users/[uid]/page.tsx — User management
- apps/web/app/(protected)/admin/storage/page.tsx — Storage management
- apps/web/app/(public)/login/page.tsx — Login page
- apps/web/app/(public)/forgot-password/page.tsx — Forgot-password page
- apps/web/app/(public)/reset-password/page.tsx — Reset-password page
- apps/web/app/(public)/confirm-email/page.tsx — Email confirmation
- apps/web/app/(public)/maintenance/page.tsx — Maintenance-mode page
- apps/web/app/(site)/** — Marketing pages

Backend-Owned (API Routes):
- apps/web/app/api/auth/** — Auth.js and email verification
- apps/web/app/api/assessment/** — Assessment CRUD and export
- apps/web/app/api/assessments — Assessment listing
- apps/web/app/api/documents/** — Document operations
- apps/web/app/api/uploads/** — Upload operations
- apps/web/app/api/infographic — Infographic generation
- apps/web/app/api/admin/** — Admin operations
- apps/web/app/api/contact — Contact form relay
- apps/web/app/api/internal/maintenance/expired-uploads — Internal maintenance sweep endpoint

Shared Ownership:
- apps/web/auth.ts — Auth.js config (server-side, but affects client sessions)
- apps/web/proxy.ts — Auth.js proxy helpers (server-side)
- apps/web/lib/server/** — Server services (backend authority)
- apps/web/lib/** (non-server) — Client utilities (frontend authority)

================================================================================
11. BACKEND RUNTIME & SERVICE OWNERSHIP
================================================================================

Server-Side Services (apps/web/lib/server/):

Database Layer:
- zootopia-postgres-adapter.ts — Postgres connection and query helpers
  Owns: Database connection pool, query execution, type-safe adapters

Repository Layer:
- repository.ts — Data access abstraction
  Owns: CRUD operations for all entities, transaction boundaries

Storage Layer:
- supabase-blob-storage.ts — Supabase Storage bucket operations
  Owns: Upload, download, delete, URL generation, bucket management
- storage-retention-config.ts — Retention policy configuration
  Owns: Per-scope retention parsing from env vars, expiry calculation

Document Processing:
- document-runtime.ts — Document upload and processing pipeline
  Owns: File validation, metadata extraction, storage persistence

Assessment System:
- assessment-retention.ts — Assessment lifecycle and cleanup
  Owns: Assessment expiry, result cleanup, credit management

AI Execution:
- ai/execution.ts — AI model orchestration
  Owns: Provider selection, prompt construction, response parsing

Email & Verification:
- verification-resend-governance.ts — Email verification rate limiting
  Owns: Cooldown tracking, attempt counting, IP-based limits

Session Management:
- session.ts — Session utilities
  Owns: Session validation, user context extraction

Admin Services:
- Admin-specific repository and governance helpers
  Owns: Admin queries, audit logging, user management

Runtime Flow:
1. Request arrives at API route handler
2. Handler validates session via session.ts
3. Handler calls repository.ts for data access
4. Repository uses zootopia-postgres-adapter.ts for database queries
5. For uploads: supabase-blob-storage.ts handles storage
6. For AI: ai/execution.ts orchestrates model calls
7. Response is returned with proper error handling

================================================================================
12. AUTH & SESSION ARCHITECTURE
================================================================================

Framework: Auth.js (NextAuth v5)
Strategy: JWT (stateless sessions)
Identity Provider: Supabase Auth (email/password only)

Credential Providers:
1. user-credentials — Regular user login
2. admin-credentials — Admin login (separate provider)

User Login Flow:
1. Client signs in via Supabase browser client → receives idToken
2. Client calls signIn("user-credentials", { idToken, ...deviceMetadata })
3. Server (auth.ts → authorizeUserCredentials):
   a. Runtime check: Supabase admin runtime + allowlist must be configured
   b. Rate limit: 20 requests/minute per IP
   c. Token verification: verifySupabaseAccessToken() via Supabase getUser()
   d. Recency check: auth_time must be within 5 minutes
   e. Admin block: If email in ZOOTOPIA_ADMIN_EMAILS, reject with ADMIN_LOGIN_REQUIRED
   f. Provider check: Must be password provider
  g. Email confirmation check: token/user must be confirmed (fail-closed AUTH_EMAIL_NOT_CONFIRMED)
  h. User upsert: upsertUserFromAuth() creates/updates profile in Postgres
  i. Status check: User must be active (not suspended)
4. JWT callback populates token with uid, role, status, profile completion, and confirmation claims
5. Session callback syncs token to session object and projects emailVerified from JWT claims
6. Client receives session cookie

Admin Login Flow:
1. Admin signs in via Supabase browser client
2. Client calls signIn("admin-credentials", { idToken })
3. Server checks email against ZOOTOPIA_ADMIN_EMAILS allowlist
4. If not in allowlist, reject
5. If in allowlist, proceed with same verification as user flow, including email-confirmation enforcement
6. JWT populated with role/status/profile and confirmation claims

Regular User Password Lifecycle (2026-04-14 hardening):
1. Forgot-password request enters `/api/auth/password/forgot` (email validation + IP/account throttling)
2. Admin/allowlisted identities are blocked from user reset lane (`ADMIN_PASSWORD_FLOW_UNSUPPORTED`)
3. Recovery callback flow finalizes token/session on `/reset-password`, then `/api/auth/password/recovery` validates token identity server-side
4. Password recovery completion records security metadata (`recovery_reset`) without storing raw password/hash data
5. In-account password changes run through `/api/auth/password/change` with current-password verification and policy enforcement
6. Settings password change enforces an immediate re-authentication boundary after success

Session Configuration:
- Strategy: JWT (no database sessions)
- Max age: ZOOTOPIA_SESSION_TTL_SECONDS (default 3600 = 1 hour)
- Cookie: httpOnly, secure, sameSite: lax

Key Files:
- apps/web/auth.ts — Auth.js configuration
- apps/web/proxy.ts — Auth.js proxy helpers
- apps/web/lib/auth-*.ts — Auth utilities
- apps/web/lib/server/session.ts — Server session utilities

================================================================================
13. ADMIN AUTHORITY MODEL
================================================================================

Admin Access Control:
- Email allowlist: ZOOTOPIA_ADMIN_EMAILS environment variable
- Separate credential provider: admin-credentials
- Admin users cannot log in via user-credentials provider (blocked)
- Regular users cannot access admin routes (role check)

Admin Routes:
- /api/admin/** — All require admin role
- /admin/** — All require admin role + active status

Admin Capabilities:
- User management: view, update, suspend users
- Storage management: view stats, run cleanup
- Governance controls: system configuration
- Audit logging: view admin action logs
- Assessment override: bypass credit limits (if implemented)

Admin Panel Pages:
- /admin — Dashboard with system overview
- /admin/users/[uid] — User detail and management
- /admin/storage — Storage statistics and cleanup
- /admin/governance — Governance controls (if present)

Admin Server-Side Guards:
- Role check in API route handlers
- Email allowlist check in auth.ts
- Separate admin credential provider
- Admin-specific redirect after login

================================================================================
14. PROFILE COMPLETION & USER GATING MODEL
================================================================================

Profile Completion Gating:
- Users must complete their profile before accessing protected features
- Incomplete profiles are redirected to /settings to finish setup
- Profile completion status stored in JWT session token
- Gating applied at:
  - Route level (middleware or layout guards)
  - API level (server-side checks in route handlers)

Gating Rules:
1. User logs in → session created
2. Session checked for profile completion
3. If incomplete → redirect to /settings
4. If complete → access to protected routes
5. Admin users bypass profile completion gating

Profile Fields (typical):
- Display name
- Phone number (validated with libphonenumber-js)
- Preferences (theme, locale)
- Any required onboarding fields

Implementation:
- Profile completion check in session token
- Server-side guards in API routes
- Client-side guards in protected layouts
- Redirect logic in auth flow

================================================================================
15. UPLOAD / DOCUMENT / STORAGE FLOW
================================================================================

Upload Flow:
1. User selects file on /upload page
2. Client validates file type and size
3. File sent to POST /api/uploads
4. Server validates file, extracts metadata
5. File stored in Supabase Storage bucket
6. Metadata saved to Postgres via repository
7. Upload record created with owner_uid
8. Response includes upload ID and metadata

Document Processing:
- document-runtime.ts handles processing pipeline
- Metadata extraction (file type, size, name)
- Storage path generation
- Retention policy application

Storage Retention:
- Uploads/documents: 15 minutes (configurable via ZOOTOPIA_UPLOAD_RETENTION_MINUTES)
- Mode: expiry (automatic deletion after retention period)
- Configurable per scope via env vars

Storage Operations:
- supabase-blob-storage.ts handles all storage operations
- Upload, download, delete, URL generation
- Bucket management and cleanup
- Admin storage cleanup endpoint: POST /api/admin/storage/cleanup

File Types Supported:
- PDF documents
- Images (PNG, JPG, etc.)
- Text files
- Other document formats

================================================================================
16. ASSESSMENT RUNTIME / RESULT / EXPORT FLOW
================================================================================

Assessment Generation Flow:
1. User navigates to /assessment
2. User selects document and configures assessment options
3. Client sends POST /api/assessment
4. Server performs checks:
   a. Authentication check
   b. Profile completion check
   c. Admin bypass (if admin)
   d. Idempotency check
   e. Credit reservation check (daily limit)
   f. Tool-scoped default-model resolution and request model validation
  g. Assessment-prompt lock enforcement for normal users when prompt text is submitted
5. AI generation via ai/execution.ts
6. Assessment result saved to Postgres
7. Admin log entry created
8. Response includes assessment data

Assessment Model Defaults:
- Backend default model resolves through `apps/web/lib/server/ai/default-models.ts`
- Env key: `ZOOTOPIA_DEFAULT_MODEL_ASSESSMENT`
- Frontend initial selected model is passed from the server page using the same
  resolver, but backend request validation still re-normalizes and re-validates
  the submitted model id
- Fallback when env is missing/invalid/unsupported: `gemini-2.5-flash-lite`

Credit System:
- Daily credit limit: ZOOTOPIA_DEFAULT_DAILY_ASSESSMENT_CREDITS (default 3)
- Renewal window: ZOOTOPIA_ASSESSMENT_CREDIT_RENEWAL_WINDOW_HOURS (default 24h UTC)
- GET /api/assessment/credits returns current status with explicit no-store headers
- Credit reservation before AI generation
- Credit consumed on success, released on failure
- Protected shell credit badge now refreshes on explicit assessment events plus focus,
  visibility, and periodic sync to reduce admin/user cross-session drift

Assessment Retrieval:
- GET /api/assessment/[id] — Single assessment
- GET /api/assessments — List user assessments
- GET /api/assessment/linked-document — Resolve linked document context

Export Flow:
- GET /api/assessment/export/* — Export assessment
- Formats: PDF, JSON, Markdown, DOCX
- Locale and theme variants supported
- `/api/assessment/export/pdf/[id]` is compatibility-only and redirects to the
  explicit Fast or Pro lane using canonical runtime origin
- `/api/assessment/export/pdf/fast/[id]` stays the lightweight owner-scoped
  browser-print HTML lane and now owns
  `apps/web/lib/server/assessment-fast-print-renderer.ts`
- `/api/assessment/export/pdf/pro/[id]` is the owner-scoped Puppeteer lane for
  real downloadable PDFs, runs on Node.js with `maxDuration = 120`, owns
  `apps/web/lib/server/assessment-pro-print-renderer.ts`, and now traces the
  hoisted workspace-root `@sparticuz/chromium/bin/**/*` payload so Vercel
  lambdas receive the packaged browser binary even when npm workspaces do not
  create `apps/web/node_modules`
- Pro PDF failures are now logged with internal stage classification
  (`route-context`, `artifact-load`, `qr-build`, `html-render`, `pdf-buffer`,
  `artifact-store`, `generation-save`, `audit-log`) while the UI still returns
  the same professional generic failure message
- PDF generation uses Puppeteer (puppeteer-core + @sparticuz/chromium)
- Export retention: 15 minutes (configurable)

Detached Preview/Footer Ownership:
- `/assessment/preview/[id]` and `/assessment/results/[id]` still share
  `AssessmentPreviewShell` and `AssessmentResultViewer` for page chrome/body.
- Preview-only footer ownership now branches inside
  `apps/web/components/assessment/assessment-result-viewer.tsx` by `view`.
- Preview route uses
  `apps/web/components/assessment/assessment-preview-page-footer.tsx`.
- Result route keeps `apps/web/components/assessment/assessment-file-footer.tsx`
  as its shared footer owner.
- Assessment preview/result/export grouping is now owned by normalized
  `questionSections` from `apps/web/lib/server/assessment-preview.ts`; extra
  provider-returned question types are tolerated in raw generation output but
  filtered out of final grouped display/export surfaces.
- Type-specific preview/result card ownership now lives in
  `apps/web/components/assessment/renderers/*` with the grouped page shell in
  `apps/web/components/assessment/assessment-grouped-view.tsx` and the switch
  boundary in `apps/web/components/assessment/question-renderer-switch.tsx`.
- Fast export HTML/footer ownership now lives in
  `apps/web/lib/server/assessment-fast-print-renderer.ts`.
- Pro export HTML/footer ownership now lives in
  `apps/web/lib/server/assessment-pro-print-renderer.ts`.

Assessment Retention:
- Results: 1440 minutes (1 day, configurable)
- Mode: expiry (automatic cleanup)
- assessment-retention.ts handles lifecycle management

================================================================================
17. INFOGRAPHIC FLOW
================================================================================

Infographic Generation:
- POST /api/infographic — Generate infographic
- Admin-only feature (currently gated)
- Non-admin users see "coming soon" on /infographic page
- Backend now validates infographic model selection against infographic-scoped
  catalog entries before execution

Flow:
1. User/admin navigates to /infographic
2. Non-admin → sees coming-soon message
3. Admin → can configure and generate
4. AI processes document content for infographic data
5. Infographic rendered on client
6. Export options available

Modes:
- Multiple infographic modes supported
- English language guide available (Guide-Files/Zootopia_Infographic_Modes_English_Guide.md)

Infographic Model Defaults:
- Backend default model resolves through `apps/web/lib/server/ai/default-models.ts`
- Env key: `ZOOTOPIA_DEFAULT_MODEL_INFOGRAPHIC`
- Frontend initial selected model is passed from the server page using the same
  resolver, but backend still validates submitted model ids
- Fallback when env is missing/invalid/unsupported or cross-tool:
  `google-balanced`
- Invalid infographic request model ids now fail closed with
  `INFOGRAPHIC_MODEL_UNSUPPORTED`, and execution has a second infographic-scoped
  guard to prevent accidental fallback into assessment defaults

================================================================================
18. DATABASE / MIGRATIONS / STORAGE / PERSISTENCE NOTES
================================================================================

Database: Supabase Postgres (provider-managed)

Migrations (supabase/migrations/):
1. 20260410120000_zootopia_platform_entities.sql
  - Base zc_entities table + storage bucket bootstrap
2. 20260410180000_phase1_postgres_parity_tables.sql
  - Phase 1 parity relational tables
3. 20260410180001_phase1_storage_bucket.sql
  - Additional phase-1 storage bucket wiring
4. 20260410213000_phase1_owner_rls_and_storage_policies.sql
  - Owner-scoped RLS and storage policy hardening
5. 20260412000000_phase2_internal_table_hardening.sql
  - Internal table hardening
6. 20260412103000_email_verification_resend_governance.sql
  - Resend governance persistence tables/functions
7. 20260412164000_internal_operational_table_lockdown.sql
  - Internal operational table lockdown
8. 20260413193000_fix_zc_entities_created_at_and_index.sql
  - Idempotent repair for missing created_at metadata column + supporting index
9. 20260414103000_storage_owner_policies_unified_user_root.sql
  - Rebuilds storage object owner policies to allow canonical users/{uid}/... paths while keeping legacy compatibility
10. 20260414153000_assessment_artifact_metadata_index_and_document_extension.sql
  - Adds assessment artifact metadata query indexes + backfills assessmentArtifactMetadata mirror rows
  - Backfills missing document fileExtension metadata and adds file-extension query index
11. 20260414230000_user_password_security_events.sql
  - Adds `user_password_security_events` append-only telemetry table for password reset/change events
  - Adds `user_password_security_state` latest-state table with backend-only deny-all RLS for anon/authenticated

Migration Application:
- scripts/supabase/apply-migrations.mjs
- Uses postgres npm client against SUPABASE_DATABASE_URL / DATABASE_URL
- Maintains public._migration_history for canonical timestamp-safe replay tracking
- Applies pending migrations sequentially inside per-file transactions

Current Metadata Persistence Notes:
- Assessment generations still persist canonical `artifacts` map payloads.
- `assessmentArtifactMetadata` now mirrors each artifact into independently queryable
  owner-scoped records (for retention/admin/reporting queries).
- Upload documents now persist `fileExtension` and `contentSha256` fields for
  durable format/digest metadata lookup.

NPM Scripts:
- npm run supabase:migrations:list — List migrations
- npm run supabase:migrations:validate — Validate migration order
- npm run supabase:migrations:status — Check migration status
- npm run supabase:check — Validate + list
- npm run db:apply — Apply migrations
- npm run db:apply:dry — Dry run
- npm run db:status — Check status

Storage:
- Supabase Storage buckets (provider-managed)
- Bucket names and paths configured in supabase-blob-storage.ts
- Retention policies applied per scope
- Admin cleanup endpoint for manual cleanup

RLS Policies:
- Admin: full access to all records
- User: access only to own records (owner_uid match)
- Policies defined in initial migration and hardened in later migrations

================================================================================
19. AI / MODEL / PROVIDER ORCHESTRATION
================================================================================

Providers:
1. Google AI (Gemini)
   - API Key: GOOGLE_AI_API_KEY
   - Model: configurable via GOOGLE_AI_MODEL, GOOGLE_AI_ADVANCED_MODEL
   - Used for: assessment generation, infographic data extraction

2. DashScope (Qwen/Alibaba)
   - API Key: DASHSCOPE_API_KEY
   - Base URL: DASHSCOPE_BASE_URL (default: https://dashscope-us.aliyuncs.com/compatible-mode/v1)
   - Model: configurable via QWEN_MODEL
   - Legacy aliases: DASHSCOPE_COMPATIBLE_BASE_URL, ALIBABA_MODEL_STUDIO_BASE_URL

Orchestration:
- ai/execution.ts handles provider selection and execution
- Provider selection based on task type and availability
- Prompt construction from messages/ directory templates
- Response parsing and validation
- Error handling and fallback logic

Tool Model Catalog & Defaults:
- Real tool scopes currently present in the repo: `assessment`, `infographic`
- Shared catalog lives in `packages/shared-config/src/models.ts`
- Built-in assessment default: `gemini-2.5-flash-lite`
- Built-in infographic default: `google-balanced`
- Server-authoritative per-tool env resolver lives in
  `apps/web/lib/server/ai/default-models.ts`
- Provider runtime env overrides (`GOOGLE_AI_MODEL`,
  `GOOGLE_AI_ADVANCED_MODEL`, `QWEN_MODEL`) remain separate from tool-default
  selection and still resolve provider-side model ids after tool selection

Prompt Templates:
- Stored in apps/web/messages/ directory
- Organized by task type
- May include i18n variants

PDF Generation:
- Uses Puppeteer (puppeteer-core)
- @sparticuz/chromium for serverless Chrome
- Vercel Pro PDF tracing must include the workspace-root Chromium Brotli payload
  because this repo hoists the package to root `node_modules`
- Local executable override: ASSESSMENT_PDF_BROWSER_EXECUTABLE_PATH
- PUPPETEER_EXECUTABLE_PATH for local development

================================================================================
20. REDIRECT / CALLBACK / CONFIRMATION BEHAVIOR
================================================================================

Post-Login Redirects:
- User default: NEXT_PUBLIC_ZOOTOPIA_AUTH_USER_DEFAULT_REDIRECT (default /)
- Admin default: NEXT_PUBLIC_ZOOTOPIA_AUTH_ADMIN_DEFAULT_REDIRECT (default /)
- Using NEXT_PUBLIC_ prefix for server/client alignment in Next App Router

Email Confirmation:
- /confirm-email page handles email verification
- Supabase sends confirmation email via Resend
- User clicks link → redirected to /confirm-email
- Finalization now verifies provider-confirmed email state before Auth.js bootstrap
- Only confirmed callbacks proceed to role/profile redirect resolution

Password Recovery Callbacks:
- `/forgot-password` requests reset emails through `/api/auth/password/forgot` with privacy-preserving success responses
- `/reset-password` validates callback tokens, enforces user-only lane ownership, and completes password updates with password-security metadata persistence

Auth Flow Redirects:
- Login success → default redirect or profile completion check
- Profile incomplete → redirect to /settings
- Admin login → admin default redirect
- Email confirmation → user default redirect

Runtime-Origin Authority (2026-04-12 hardening):
- Server-only redirects/callback URLs now use getServerRuntimeOrigin() instead of request origin
- Canonical resolution order: NEXT_PUBLIC_BASE_URL → NEXTAUTH_URL → VERCEL_URL → localhost
- Protocol-less env values are normalized (https by default, http for localhost variants)
- Confirm-email resend callback and PDF export compatibility redirect no longer trust request host
- Pro PDF Puppeteer asset base URL now uses canonical runtime origin (host-header spoof resistant)

================================================================================
21. EMAIL VERIFICATION & RESEND GOVERNANCE
================================================================================

Email Provider:
- Supabase Auth manages email sending via Resend (provider mode)
- SMTP relay available for contact form (optional)

Verification Resend Governance:
- verification-resend-governance.ts handles rate limiting
- Cooldown: 30 seconds (ZOOTOPIA_EMAIL_VERIFICATION_COOLDOWN_SECONDS)
- Account max attempts: 5 per window (ZOOTOPIA_EMAIL_VERIFICATION_MAX_ATTEMPTS)
- Account window: 60 minutes (ZOOTOPIA_EMAIL_VERIFICATION_WINDOW_MINUTES)
- IP max attempts: 20 per window (ZOOTOPIA_EMAIL_VERIFICATION_IP_MAX_ATTEMPTS)
- IP window: 60 minutes (ZOOTOPIA_EMAIL_VERIFICATION_IP_WINDOW_MINUTES)
- Hash salt: ZOOTOPIA_EMAIL_VERIFICATION_HASH_SALT

API Endpoint:
- GET /api/auth/confirm-email/resend?email=... — Return governance snapshot for resend availability polling
- POST /api/auth/confirm-email/resend — Trigger resend with governance

Confirm-Email Query/UX Hardening (2026-04-12):
- Confirm-email page now resolves duplicate query keys using first non-empty value (for example email=&email=user@example.com)
- Resend GET route now resolves duplicate email params using first non-empty value before validation
- Resend action label remains truthful per email session: starts as "Send verification email" and only flips to
  "Resend verification email" after a real provider-accepted send has been observed

Resend Governance Correctness Hardening (2026-04-13):
- Provider-rejected resend attempts are now rolled back so account/IP attempt budgets are not permanently consumed
  when upstream delivery was never accepted.
- Rollback remains best-effort: route still returns provider-classified errors if rollback itself fails.
- Confirm-email panel now disables submit while governance is initializing and during governance-blocked states
  (cooldown/account/ip/unavailable), preventing misleading early submits.
- Reserve flow now marks reservation-accepted sends so the same request is not immediately rejected by its own
  newly-created cooldown; cooldown still applies to subsequent attempts.

Email Templates:
- docs/auth-email/templates/zootopia-confirm-email-template-supabase.html
- Custom branded confirmation email template

Legacy Aliases (still accepted):
- ZOOTOPIA_VERIFICATION_RESEND_* (all variants)

================================================================================
22. SECURITY ASSUMPTIONS & TRUST BOUNDARIES
================================================================================

Trust Boundaries:
- Browser → Supabase Auth: trusted provider (cross-origin)
- Browser → Next.js API: same-origin (trusted)
- Next.js Server → Supabase Postgres: server-side only (connection string)
- Next.js Server → Supabase Storage: server + browser (provider-managed)
- Next.js Server → Google AI API: server-side only (API key)
- Next.js Server → DashScope API: server-side only (API key)
- Next.js Server → SMTP: server-side only (credentials)

Security Measures:
- JWT sessions (stateless, httpOnly cookies)
- Rate limiting on auth endpoints (20 req/min per IP)
- Admin email allowlist (ZOOTOPIA_ADMIN_EMAILS)
- RLS policies on all database tables
- Profile completion gating
- Credit limits on AI generation
- Email verification rate limiting
- Token recency checks (auth_time within 5 minutes)
- Provider checks (password only)
- Status checks (active vs suspended)

Sensitive Data:
- AUTH_SECRET: Session encryption key
- SUPABASE_SERVICE_ROLE_KEY: Full admin access to Supabase
- SUPABASE_DATABASE_URL: Database connection string
- GOOGLE_AI_API_KEY: Google AI provider key
- DASHSCOPE_API_KEY: DashScope provider key
- SMTP credentials: Email relay credentials
- ZOOTOPIA_ADMIN_EMAILS: Admin allowlist

Never Expose to Browser:
- SUPABASE_SERVICE_ROLE_KEY
- SUPABASE_DATABASE_URL
- GOOGLE_AI_API_KEY
- DASHSCOPE_API_KEY
- SMTP credentials
- AUTH_SECRET
- Any private keys or credentials

================================================================================
23. IMPORTANT FOLDERS & FILES
================================================================================

Root Configuration:
- package.json — Root workspace manifest
- tsconfig.json — Root TypeScript configuration
- tsconfig.base.json — Shared TypeScript base config
- eslint.config.mjs — ESLint configuration
- .env.example — Environment variable template
- .env.local — Local environment (gitignored)
- AGENTS.md — Agent instructions

Application (apps/web/):
- auth.ts — Auth.js configuration
- proxy.ts — Auth.js proxy helpers
- next.config.ts — Next.js configuration
- app/ — App Router routes and layouts
- components/ — React components
- components/assessment/assessment-preview-page-footer.tsx — preview-route-only
  footer surface for `/assessment/preview/[id]` (kept separate from result/export)
- lib/ — Library code
- lib/server/ — Server-side services
- messages/ — AI prompt templates / i18n messages
- public/ — Static assets
- scripts/ — Build/postbuild scripts
- package.json — @zootopia/web manifest

Shared Packages:
- packages/shared-config/ — Constants, routes, env keys, feature flags
- packages/shared-types/ — TypeScript interfaces, DTOs
- packages/shared-utils/ — Pure utility functions

Database & Migrations:
- supabase/migrations/ — SQL migration files
- scripts/supabase/ — Migration management scripts

Documentation:
- docs/ — Architecture documentation
- docs/auth-email/ — Auth email templates
- Guide-Files/ — Agent/CLI guides (reference only)
- zootopia-club-next-ledger.txt — This file

================================================================================
24. SENSITIVE & HIGH-RISK FILES
================================================================================

Critical Secret Files:
- .env.local — Contains all local secrets (gitignored)
- apps/web/auth.ts — Contains auth logic, session configuration
- apps/web/lib/server/zootopia-postgres-adapter.ts — Database connection handling
- apps/web/lib/server/supabase-blob-storage.ts — Storage operations with service key

High-Risk API Routes:
- /api/admin/** — Admin operations (user management, storage cleanup)
- /api/assessment — Assessment generation (credit consumption)
- /api/uploads — File upload (storage consumption)
- /api/auth/confirm-email/resend — Email resend (rate-limited)

High-Risk Server Files:
- apps/web/lib/server/ai/execution.ts — AI provider calls (API key usage)
- apps/web/lib/server/session.ts — Session validation
- apps/web/lib/server/repository.ts — Data access (all entities)

Migration Files:
- supabase/migrations/*.sql — Database schema changes (irreversible if destructive)
- scripts/supabase/apply-migrations.mjs — Migration application

Build Scripts:
- apps/web/scripts/normalize-standalone.mjs — Postbuild normalization

Agent Instruction Files:
- AGENTS.md — Contains agent behavior rules
- Guide-Files/*.txt, *.md — Reference guides (not runtime code)

================================================================================
25. IMPORTANT COMMANDS & SCRIPTS
================================================================================

Development:
- npm run dev — Start local development server (with Turbopack)
- npm run dev:local — Same as dev
- npm run dev:web — Same as dev

Build:
- npm run build — Build all workspaces
- npm run build:local — Build all workspaces locally
- npm run build:vercel — Build for Vercel deployment
- npm run build:web — Build web app only
- npm run rebuild:web — Clean and rebuild web app

Start:
- npm run start — Start local production server
- npm run start:local — Start local production server
- npm run start:web — Start web app
- npm run start:vercel — Start web app (Vercel mode)

Lint & Type Check:
- npm run lint — Lint all workspaces
- npm run lint:web — Lint web app
- npm run typecheck — Type check all workspaces
- npm run typecheck:web — Type check web app
- npm run check — Lint + typecheck + build
- npm run check:web — Lint + typecheck + build web app

Clean:
- npm run clean:web — Remove .next build cache

Database:
- npm run supabase:migrations:list — List migrations
- npm run supabase:migrations:validate — Validate migration order
- npm run supabase:migrations:status — Check migration status
- npm run supabase:check — Validate + list migrations
- npm run db:apply — Apply migrations
- npm run db:apply:dry — Dry run migrations
- npm run db:status — Check migration status

Auth:
- npx auth secret — Generate AUTH_SECRET

================================================================================
26. CURRENT FEATURE SET
================================================================================

Implemented & Active:
- [x] User authentication (email/password via Supabase)
- [x] Admin authentication (separate provider, email allowlist)
- [x] Email verification with resend governance
- [x] Regular-user forgot-password and recovery reset flow
- [x] In-account regular-user password change with forced re-authentication handoff
- [x] Unified regular-user password policy enforcement across sign-up/reset/settings
- [x] Profile completion gating
- [x] Document upload with retention policies
- [x] AI assessment generation with credit limits
- [x] Assessment history viewer
- [x] Assessment export (PDF, JSON)
- [x] User settings page
- [x] Admin dashboard
- [x] Admin user management
- [x] Admin storage management
- [x] Infographic page (admin-only, others see coming-soon)
- [x] Dark/light mode support
- [x] Localization support
- [x] Responsive design (mobile to ultra-wide)
- [x] Supabase Postgres database with RLS
- [x] Supabase Storage with retention policies
- [x] Multi-provider AI orchestration (Google + DashScope)
- [x] Session management with JWT
- [x] Rate limiting on auth endpoints
- [x] Admin audit logging
- [x] Admin-managed per-user assessment prompt entitlement grant/revoke controls
- [x] Env-controlled global maintenance mode with proxy-enforced non-admin redirects
- [x] Secret-gated internal expired-upload maintenance cleanup endpoint

Partially Implemented:
- [~] Infographic generation (UI present, AI flow may be incomplete)
- [~] Contact form (SMTP relay configured, may need activation)
- [~] Governance controls (admin page may be partial)

Not Implemented / Placeholder:
- [ ] @zootopia/api extraction (placeholder only)
- [ ] Firebase integration (fully migrated away)

================================================================================
27. DEFERRED / DISABLED / LEGACY AREAS
================================================================================

Legacy/Deprecated:
- Firebase integration: Fully migrated to Supabase. Legacy env vars commented out.
- ZOOTOPIA_ADMIN_LOGIN_PASSWORD: Deprecated admin password gate. Replaced by allowlist.
- ZOOTOPIA_VERIFICATION_RESEND_*: Legacy aliases for email verification. Still accepted.
- ZOOTOPIA_STORAGE_RETENTION_*: Legacy global retention fallback. Still accepted.
- FIREBASE_* env vars: Commented out, not used by current runtime.

Reference-Only:
- Guide-Files/: Historical agent guides, deployment guides, classification guides.
  These are reference material, not active runtime code.
- docs/legacy-reference/: Legacy documentation.
- apps/code-backup.txt: Historical code backup.

Inactive:
- apps/api/: Placeholder for future API extraction. No active code.
- Single-app server layouts (server.ts, server/*): Not present in current repo.
- Docker/Cloud Build/Netlify: Not present in current repo.

Deferred:
- Infographic AI generation: UI present, may need AI flow completion.
- Contact form: SMTP configured, may need activation.
- Governance controls: May need completion.

================================================================================
28. FUTURE AGENT GUIDANCE
================================================================================

For Future AI Agents and Human Developers:

1. Always read this ledger first before making changes.
2. Treat it as the primary historical memory of the project.
3. Compare the ledger against the real current codebase before editing.
4. Never rebuild from scratch unless explicitly requested.
5. Never redesign the architecture unless explicitly requested.
6. Never remove working features unless explicitly requested.
7. Make only minimal, surgical, backward-compatible changes.
8. Read all relevant files fully before editing.
9. Trace imports, exports, route ownership, shared contracts, and state flow.
10. Preserve the current live runtime ownership.
11. Keep backend authority on the backend.
12. Never expose secrets, API keys, credentials, or privileged logic to the browser.
13. Always check official documentation for every framework, library, or platform.
14. Prefer official conventions over ad-hoc patterns.
15. Keep shared state separate from shared page UI.
16. Preserve dark mode, light mode, localization, and current design language.
17. Always consider responsive behavior across all screen sizes.
18. Run lint, typecheck, and build after meaningful changes.
19. Refresh this ledger thoroughly after every task.
20. Record all meaningful changes, decisions, risks, and verification results.

Architecture Boundaries to Respect:
- Frontend: apps/web/app/(site)/**, apps/web/app/(public)/**, client components
- Backend: apps/web/app/api/**, apps/web/lib/server/**, auth.ts, proxy.ts
- Shared: packages/shared-*/**
- Infrastructure: supabase/**, scripts/**, vercel.json
- Provider-Managed: Supabase, Vercel, Resend, Google AI, DashScope

Sensitive Areas Requiring Extra Care:
- auth.ts: Session and authentication configuration
- lib/server/**: Server-side business logic
- supabase/migrations/**: Database schema changes
- .env.local: Secrets (never commit)
- API routes with admin access
- AI provider configuration
- Storage retention policies

================================================================================
29. HISTORICAL CHANGE LOG
================================================================================

2026-04-14 — Assessment Pro PDF Vercel Chromium Trace Fix + Stage Diagnostics
==============================================================================
Type: Production bug fix / Vercel serverless compatibility / export diagnostics hardening
Scope: owner-scoped Pro PDF export route, shared browser-backed PDF capture path, Next standalone trace config

Summary:
- Completed mandatory ledger-first analysis, current-code comparison, Next.js docs review, Vercel project/log inspection, screenshot review, and subagent exploration before editing.
- Verified the failing path is the premium Puppeteer-backed Pro lane (`/api/assessment/export/pdf/pro/[id]`), while the generic alias route and Fast browser-print lane remain separate and owner-scoped.
- Fixed the Vercel deployment incompatibility by tracing the hoisted workspace-root `@sparticuz/chromium/bin/**/*` payload into the Pro route bundle instead of assuming a nested `apps/web/node_modules` install.
- Added internal stage-classified diagnostics so production logs now distinguish browser-launch, render, PDF-capture, artifact, and audit/save failures without leaking internals to the UI.
- Preserved the existing user-safe response contract: failed exports still return `ASSESSMENT_PDF_EXPORT_FAILED` with the professional generic message only.

Root Cause (code-verified):
- `apps/web/next.config.ts` already attempted to trace Chromium binaries for `/api/assessment/export/pdf/pro/[id]`, but the include path only targeted `./node_modules/@sparticuz/chromium/bin/**/*` relative to `apps/web`.
- In this npm workspace, `@sparticuz/chromium` is hoisted to the monorepo root (`../../node_modules/...` from `apps/web`), so the deployed Pro route bundle on Vercel did not reliably receive the packaged Brotli Chromium payload (`chromium.br`, `al2023.tar.br`, `fonts.tar.br`, `swiftshader.tar.br`).
- The Pro lane depends on that payload when no local browser executable exists, which is exactly the Vercel serverless case. That made the Pro route fail during browser startup/executable resolution even though preview/data/auth flows remained intact.

Files Changed:
- apps/web/next.config.ts
- apps/web/app/api/assessment/export/pdf/pro/[id]/route.ts
- apps/web/lib/server/assessment-pdf-download.ts
- apps/web/lib/server/assessment-pro-pdf-export.ts
- zootopia-club-next-ledger.txt

Behavioral Changes:
- Vercel trace fix:
  - Pro route output-file tracing now includes both the app-local and hoisted workspace-root Chromium bin paths
  - Pro route standalone/serverless bundles now keep the packaged Chromium payload even when npm workspaces hoist the dependency
- Pro route runtime:
  - `runtime = "nodejs"` preserved
  - `maxDuration = 120` added explicitly for the browser-backed premium lane
- Internal diagnostics:
  - unexpected shared export-context failures now log as `route-context` before returning the same safe 500 response
  - Pro route failure logs now emit structured stage details via `describeAssessmentProPdfFailure(...)`
  - browser-backed PDF capture now classifies `browser-launch`, `page-create`, `surface-render`, and `pdf-generate`
  - Pro export orchestration now classifies `artifact-load`, `qr-build`, `html-render`, `pdf-buffer`, `artifact-store`, `generation-save`, and `audit-log`
  - successful Pro export paths now emit concise lifecycle logs for context resolution, fresh artifact generation, and cache reuse
- Safety boundaries preserved:
  - owner/session/export context validation flow unchanged
  - generic alias route and Fast lane behavior unchanged
  - UI still receives the same clean failure message with no sensitive internals

Verification:
- Source verification:
  - confirmed `apps/web/node_modules/@sparticuz/chromium/bin` does not exist in this workspace
  - confirmed root `node_modules/@sparticuz/chromium/bin` contains the required Brotli payload files
- Vercel verification:
  - inspected the live Vercel project/logs for `zootopia-club-next-no-firebase-web`
  - confirmed repeated production 500s on the Pro export route before the fix
- Build verification:
  - `npm run lint`: pass with 2 pre-existing warnings in `apps/web/components/upload/upload-workspace.tsx`
  - `npm run typecheck`: pass
  - `npm run build`: pass
  - post-build trace verification: `apps/web/.next/server/app/api/assessment/export/pdf/pro/[id]/route.js.nft.json` now contains the hoisted Chromium payload entries (`al2023.tar.br`, `chromium.br`, `fonts.tar.br`, `swiftshader.tar.br`)

Risks / Notes:
- This pass intentionally avoids broad export-architecture changes and keeps the existing Pro/Fast lane separation intact.
- A live authenticated end-to-end production click on the fixed route was not executed from this session because the route is owner-scoped and the workspace contains unrelated in-progress changes that should not be deployed blindly.
- The strongest deployment-safe proof in this pass is the combination of live Vercel investigation, code-path verification, and post-build route-trace confirmation that the previously missing Chromium payload is now bundled for the Pro lambda.

2026-04-14 — Assessment Preview Action-Panel Reflow + Mobile Footer Readability Fix (UI-Only)
================================================================================================
Type: UI polish / responsive layout hardening / detached preview-result surface cleanup
Scope: detached assessment preview/result shell actions placement, preview-only export grid sizing, shared file footer mobile readability

Summary:
- Completed mandatory ledger-first, docs-first, and source-first analysis, plus an Explore subagent pass, before editing.
- Verified that `/assessment/preview/[id]` only renders `AssessmentPreviewShell`; the real responsive ownership lives in the shared shell, shared export-actions CSS, and shared file footer.
- Replaced the unstable right-side preview action rail with a dedicated full-width action panel rendered below the metadata cards, keeping all export/theme behavior intact.
- Scoped preview-only export-grid overrides so action buttons and inline status/error feedback have stable horizontal space without altering history-page action layouts.
- Reduced the visual weight of the shared file footer on phones by shrinking the side anchors and removing mobile text balancing, preventing the Arabic attribution from collapsing into one-word vertical fragments.

Root Issue (code-verified):
- `apps/web/components/assessment/assessment-preview-shell.tsx` placed `AssessmentExportActions` in an `xl` right-side rail (`assessment-preview-shell__actions-rail`) next to the title/summary lane, which left the action block too narrow for multiple export buttons plus `AuthStatus` feedback.
- Shared export-actions CSS in `apps/web/app/globals.css` assumed a capped-width action grid (`width: min(100%, 34.75rem)`) and preview-lane buttons that span full rows, which worked in cards/history contexts but felt cramped inside the detached preview shell.
- `apps/web/components/assessment/assessment-file-footer.tsx` kept large left/right ornaments at all breakpoints while using `text-balance` in the center attribution span, causing phone layouts to squeeze the Arabic footer copy into awkward narrow wrapping.

Files Changed:
- apps/web/components/assessment/assessment-preview-shell.tsx
- apps/web/components/assessment/assessment-file-footer.tsx
- apps/web/app/globals.css
- zootopia-club-next-ledger.txt

Behavioral Changes:
- Preview/result action area:
  - removed the detached preview/result right-side action rail
  - introduced a dedicated bordered actions panel beneath the generated/expires metadata cards
  - kept the theme toggle, export buttons, and inline status/error feedback together inside that full-width panel
- Preview-only export layout:
  - preview/result shell action blocks now force the export grid to use the full available panel width
  - preview/result shell action blocks switch to a stable two-column grid from `md` upward
  - preview/result shell action blocks allow preview-lane and formerly full-width buttons to sit in the grid instead of stacking as a narrow sidebar column
  - preview/result shell `AuthStatus` feedback now inherits the full action-panel width, preventing tall narrow message strips
- Shared file footer:
  - mobile side anchors now step down in size while desktop/export-grade sizing remains on larger breakpoints
  - footer container spacing and page-number typography are lighter on phones
  - mobile footer attribution text no longer uses `text-balance`; wrapping stays normal on phones and balanced on `sm+`

Verification:
- `npm run lint:web`: pass with 2 pre-existing warnings in `apps/web/components/upload/upload-workspace.tsx` (`title` and `description` unused)
- `npm run typecheck:web`: pass
- `npm run build:web`: pass
- Runtime smoke:
  - `npx next start --hostname 127.0.0.1 --port 3020` served successfully and `/api/health` returned `ok=true`
  - unauthenticated request to `/assessment/preview/layout-qa` redirected to `/login` (307), confirming the detached preview route is auth-gated in this session
- Responsive verification status:
  - source-level verification completed for large-screen action-panel placement, preview-only grid behavior, full-width status-card scope, and mobile footer sizing/wrapping guards
  - full authenticated browser visual QA of a real preview record was not possible in this session because the preview route remained owner-auth gated

Risks / Notes:
- This pass intentionally does not touch backend/export/auth/business logic; all export URLs, theme query handling, and PDF fetch behavior remain unchanged.
- Preview-only action-grid overrides are scoped under the detached preview shell so history-card export actions keep their prior layout.
- `AssessmentFileFooter` is shared by preview, result, and support pages, so the lighter mobile footer geometry now applies consistently across all detached file-surface pages.

2026-04-12 — Verification Baseline Reconfirm (Current)
=======================================================
Type: Verification / Ledger refresh
Scope: lint, typecheck, build, commit metadata, runtime-origin hardening re-validation

Summary:
- Re-read the full ledger and revalidated current source for runtime-origin hardening paths.
- Confirmed canonical-origin routing is already present in:
  - apps/web/app/api/auth/confirm-email/resend/route.ts
  - apps/web/app/api/assessment/export/pdf/[id]/route.ts
  - apps/web/lib/server/runtime-base-url.ts
- Ran workspace verification tasks:
  - web-lint-post-polish: pass
  - web-typecheck-post-polish: pass
  - web-build-post-polish: pass
- No source code changes were required.
- Updated ledger commit metadata to current HEAD.

Notes:
- task shell:web-start-3025 is currently misconfigured (passes positional args to
  next start and fails on invalid project directory). It was not changed in this
  verification-only pass because runtime code changes were not required.

Files Changed:
- zootopia-club-next-ledger.txt

Files NOT Changed:
- All application/runtime source files.
- No migrations or environment templates.

---

2026-04-12 — Redirect Authority + Auth Runtime Hardening
=========================================================
Type: Security hardening / auth flow consistency / production guardrails
Scope: redirect decision consistency, auth bootstrap runtime gating, proxy fail-closed behavior, auth tracing clarity

Summary:
- Consolidated parallel auth/runtime/admin traces against current source before edits.
- Fixed redirect authority drift by routing login handoff through the centralized resolver.
- Hardened auth bootstrap to fail closed when production requires durable persistence but Postgres runtime is unavailable.
- Hardened proxy active-session classification with durable-runtime readiness checks to reduce stale-session protected-route loops.
- Improved Auth.js JWT trace context labeling so existing admin sessions are no longer mislabeled as user-credentials flow in callback logs.

Key Findings Captured:
- Redirect decisions were already centralized in apps/web/lib/return-to.ts, but two login handoff paths still used hardcoded destinations.
- Production 500 risk remains strongly environment-coupled where durable persistence prerequisites are missing or degraded.
- Auth trace logs on admin paths could show flow=user/provider=user-credentials for callback refresh cycles because JWT trace context defaulted from transient user payload instead of persisted token claims.

Files Changed:
- apps/web/components/auth/login-panel.tsx
- apps/web/components/auth/admin-login-panel.tsx
- apps/web/auth.ts
- apps/web/proxy.ts
- zootopia-club-next-ledger.txt

Behavioral Changes:
- Login panel now resolves final destination via resolveAuthenticatedUserRedirectPath(...), preserving env-driven post-auth redirect authority.
- Admin login panel now resolves final destination via resolveAuthenticatedUserRedirectPath(...), preserving centralized admin lane semantics.
- Auth bootstrap now blocks early with DB_REPOSITORY_UNAVAILABLE when durable persistence is required and Postgres runtime is not active.
- Proxy now treats active sessions as valid only when durable-runtime prerequisites are satisfied in production fail-closed mode.
- JWT callback tracing now infers flow/provider from persisted token claims when user payload is absent, improving admin-path observability fidelity.

Verification:
- web-lint-post-polish: pass
- web-typecheck-post-polish: pass
- web-build-post-polish: pass

Risks / Notes:
- This change set improves guardrails and consistency but cannot repair deployment environments with invalid/missing DB connectivity; Vercel env correctness remains required.
- Existing stale browser sessions may still require re-authentication after runtime/env corrections.

---

2026-04-12 — Runtime Origin Hardening + Admin Boundary Verification (Historical)
=======================================================================
Type: Security Hardening / Runtime Debugging / Documentation Alignment
Scope: Runtime origin handling, redirect/callback safety, admin-boundary verification

Summary:
- Spawned parallel subagents for:
  1. ledger + current-truth architecture tracing
  2. server-components runtime error tracing
  3. auth/session/admin-boundary tracing
  4. origin/host/redirect/allowed-origin tracing
  5. admin route + admin API tracing
  6. repository/data-access/admin privilege tracing
  7. build/lint/typecheck baseline verification
- Consolidated findings before edits and verified current runtime truth directly in code.
- Implemented canonical runtime-origin hardening at minimal insertion points.
- Preserved existing backend-authoritative admin guards and verified admin profile-gating exemption behavior remains role-scoped and server enforced.

Root Cause Findings (code-verified):
- Request-origin derived URL construction created brittle runtime behavior and security risk surfaces:
  - confirm-email resend callback URL used request-derived origin
  - PDF compatibility redirect used request-derived origin
  - Pro PDF document base URL used request-derived origin
  - export route context parsed request.url without canonical base fallback
- These paths could drift across proxy/host contexts and contribute to provider callback "origin not allowed" failures, plus host-header influenced redirect/origin behavior.

Files Changed:
- .env.example
- apps/web/lib/server/runtime-base-url.ts
- apps/web/lib/server/assessment-export-route-context.ts
- apps/web/lib/server/assessment-pro-pdf-export.ts
- apps/web/app/api/assessment/export/pdf/[id]/route.ts
- apps/web/app/api/auth/confirm-email/resend/route.ts

Security/Runtime Changes:
- Added runtime URL normalization in runtime-base-url.ts:
  - supports protocol-less env values safely
  - exports getServerRuntimeOrigin()
  - exports resolveRequestUrlWithServerBase()
- confirm-email resend callback now pins redirect origin to canonical server runtime origin.
- assessment export compatibility redirect now builds redirect target from canonical server runtime origin.
- Pro PDF lane now uses canonical runtime origin for documentBaseUrl during Puppeteer rendering.
- export route context now uses resilient URL parsing with canonical base fallback.

Admin Boundary Verification (no widening of privileges):
- Admin routes remain server-protected via requireAdminUser()/getAdminSessionUser().
- Admin APIs under /api/admin/** remain server-gated and normal users remain 403-blocked.
- Profile completion gating remains user-only (isProfileCompletionRequired: role !== admin).
- No changes made that grant admin powers to non-admin users.
- No owner-scope protections for normal users were removed.

Verification:
- npm run check:web
  - lint: pass
  - typecheck: pass
  - build: fail (pre-existing environment/network issue fetching Google Fonts in sandbox)
- Build failure remains unrelated to this change set and matches prior baseline behavior.

Environment Path Clarification:
- Local integrated development:
  - Frontend: Next app pages/components
  - Backend: Next route handlers + server libs
  - Provider-managed: Supabase Auth/Postgres/Storage
  - Origin authority: localhost fallback if no canonical env URL configured
- Vercel production runtime:
  - Frontend/backend co-hosted on Vercel
  - Origin authority: NEXT_PUBLIC_BASE_URL preferred, then NEXTAUTH_URL, then VERCEL_URL
  - Request host is no longer the callback/redirect authority for hardened paths
- Supabase runtime:
  - Auth provider callback URLs now derive from canonical server runtime origin
  - Reduces callback-origin mismatch drift versus request-derived host reconstruction
- Legacy/reference-only assumptions:
  - Firebase-era assumptions remain legacy only; no runtime restoration was introduced

Risks / Unverified Edge Cases:
- Full live Vercel + Supabase redirect allowlist verification cannot be completed in this sandbox.
- Deploy-time env correctness remains critical:
  - NEXT_PUBLIC_BASE_URL should match intended canonical deployment origin.
  - Supabase redirect allowlist must include intended callback origin(s).

---

2026-04-12 — End of Day Ledger Consolidation
======================================================
Type: Documentation / Architecture Maintenance
Scope: zootopia-club-next-ledger.txt (full rebuild from empty state)

Summary:
- Ledger file was found empty (0 lines). Full rebuild required.
- Spawned 5 parallel subagents for comprehensive repository exploration:
  1. Architecture and file-ownership tracing
  2. Frontend/UI and route inventory tracing
  3. Backend/API/runtime/service tracing
  4. Auth/session/admin/environment/deployment tracing
  5. Database/storage/schema/migrations + AI/model/provider tracing
- All 5 subagents completed successfully (189 total tool calls).
- Read and verified key configuration files:
  - package.json (root workspace manifest)
  - apps/web/package.json (@zootopia/web manifest)
  - .env.example (environment variable template)
- Built comprehensive ledger from scratch with verified findings.
- Ledger now contains 29 sections covering all major project dimensions.

Sections Created:
1. Project Identity
2. Current Truth Snapshot
3. Project Purpose & Platform Direction
4. Architecture Style
5. Monorepo Structure
6. Dependency & Runtime Baseline
7. Active Deployment Model
8. Environment Strategy
9. Route Map
10. Route Ownership Clarification
11. Backend Runtime & Service Ownership
12. Auth & Session Architecture
13. Admin Authority Model
14. Profile Completion & User Gating Model
15. Upload / Document / Storage Flow
16. Assessment Runtime / Result / Export Flow
17. Infographic Flow
18. Database / Migrations / Storage / Persistence Notes
19. AI / Model / Provider Orchestration
20. Redirect / Callback / Confirmation Behavior
21. Email Verification & Resend Governance
22. Security Assumptions & Trust Boundaries
23. Important Folders & Files
24. Sensitive & High-Risk Files
25. Important Commands & Scripts
26. Current Feature Set
27. Deferred / Disabled / Legacy Areas
28. Future Agent Guidance
29. Historical Change Log

Verified Architectural Facts Added:
- Next.js 16.2.2 monorepo with npm workspaces
- Supabase as unified backend (Postgres, Auth, Storage)
- Auth.js (NextAuth v5) with JWT strategy
- Two credential providers (user + admin)
- Admin email allowlist model
- Profile completion gating
- Multi-provider AI (Google AI + DashScope)
- Configurable storage retention policies
- Daily assessment credit system
- Email verification with resend governance
- 10+ Supabase migrations (initial schema through operational lockdown)
- Vercel deployment target
- Complete route inventory with ownership clarification
- Complete server-side service map
- Complete environment variable documentation

Legacy/Historical Context Preserved:
- Firebase migration history (fully migrated away)
- Legacy environment variable aliases
- Reference-only Guide-Files documentation
- Inactive apps/api placeholder
- Deprecated admin password gate

Files Changed:
- zootopia-club-next-ledger.txt: Full rebuild (was empty, now comprehensive)

Files NOT Changed:
- All runtime code left untouched.
- No configuration files modified.
- No database changes.

Unresolved Ambiguity:
- Some route handlers may have additional sub-routes not fully enumerated.
- Infographic AI generation flow completeness not fully verified.
- Contact form SMTP relay activation status not confirmed.
- Governance controls page completeness not verified.
- The exact Supabase migration file count may differ (7 base + 3 phase migrations verified, but additional migrations may exist).
- apps/api/ directory existence confirmed but contents not deeply explored (placeholder only per subagent findings).

---

2026-04-12 — Admin Users Detail/Export Reliability Hardening (Minimal / Backward-Compatible)
==============================================================================================
Type: Runtime Stability / Admin Observability / Fail-Safe Hardening
Scope: /admin/users/[uid], /api/admin/users/export, admin credit-state read path

Summary:
- Traced intermittent 500 behavior to all-or-nothing async fan-out on non-critical admin detail/export data dependencies.
- Added a role-aware optimization path to admin credit-state reads to avoid redundant auth-user lookups in hot admin flows.
- Hardened admin export so one problematic user record no longer fails the entire workbook response.
- Hardened admin user detail page so non-critical sections fail soft with safe defaults while identity and admin authorization remain strict.
- Preserved backend-authoritative admin boundary (no privilege widening, no client-side service-role exposure).

Root Cause (code-verified):
- `/admin/users/[uid]` loaded several independent datasets in one `Promise.all(...)`; any transient repository/storage/auth failure could throw and collapse the whole server-rendered page to 500.
- `/api/admin/users/export` previously used route-wide fan-out behavior where one per-user credit lookup failure could fail the full export generation path.
- Repeated owner-role resolution in credit-state paths introduced avoidable extra auth admin lookups under load.

Files Changed:
- apps/web/lib/server/repository.ts
- apps/web/app/(protected)/admin/users/[uid]/page.tsx
- apps/web/app/api/admin/users/export/route.ts
- apps/web/app/api/admin/users/[uid]/credits/route.ts
- zootopia-club-next-ledger.txt

Behavioral Changes:
- `getAdminAssessmentCreditStateForUser(...)` now accepts optional `ownerRole` to reuse already-known role context and skip redundant `getUserByUid(...)` calls.
- `/admin/users/[uid]` now:
  - resolves target user first as a strict gate (still 404 when missing), then
  - loads surrounding analytics/activity sections through guarded best-effort loaders with explicit fallbacks and non-sensitive warning logs.
- `/api/admin/users/export` now:
  - treats auth metadata enrichment as best-effort,
  - processes per-user credit state with per-user error isolation,
  - logs sanitized error-code telemetry,
  - returns a generic export failure message instead of surfacing internal error details.
- `/api/admin/users/[uid]/credits` GET now passes known `user.role` into credit-state resolution for the same role-aware optimization.

Security / Boundary Confirmation:
- Admin route/API guards remain server enforced through existing session/admin checks.
- No changes moved privileged logic to the browser.
- `SUPABASE_SERVICE_ROLE_KEY` usage remains server-only.
- Normal users remain blocked from admin-only routes/APIs.

Verification:
- web-lint-post-polish: pass
- web-typecheck-post-polish: pass
- web-build-post-polish: pass

Risks / Notes:
- When non-critical data dependencies are temporarily unavailable, `/admin/users/[uid]` now intentionally renders with partial/empty informational sections instead of returning 500.
- Export now prioritizes completing workbook generation even if some users temporarily lack computed credit columns due to transient backend faults.
- Observed an unrelated pre-existing working-tree change in `apps/web/lib/server/zootopia-postgres-adapter.ts`; left untouched in this pass.

---

2026-04-12 — Supabase Pooler Env + Global Singleton Typings Visibility
=======================================================================
Type: Runtime Configuration Hardening / TypeScript Contract Visibility
Scope: local env pooler URI contract, globalThis singleton declaration visibility

Summary:
- Updated active local env contracts to use Supabase transaction pooler mode for Postgres (`:6543` + `pgbouncer=true`).
- Added explicit global singleton declaration file so TypeScript always sees the `globalThis.__zootopia_sql_singleton__` contract independent of module traversal order.
- Kept transaction semantics unchanged; `runTransaction(...)` remains backed by `postgres(...).begin(...)`, which preserves one connection per transaction scope.

Files Changed:
- .env.local (local file, gitignored)
- apps/web/.env.local (local file, gitignored)
- apps/web/global.d.ts
- .env.example
- zootopia-club-next-ledger.txt

Behavioral / Contract Changes:
- `SUPABASE_DATABASE_URL` in both active local env files now follows transaction pooler format (`:6543` with `?pgbouncer=true`).
- Added `apps/web/global.d.ts` with:
  - `declare global { var __zootopia_sql_singleton__: ReturnType<typeof postgres> | undefined; }`
  - ensures the singleton declaration is visible to TypeScript during app compilation.
- `.env.example` now explicitly documents the recommended transaction pooler URI format for `SUPABASE_DATABASE_URL`.

Verification:
- web-typecheck-post-polish: pass
- web-build-post-polish: pass

Risks / Notes:
- Local env files are gitignored; values were changed in-place for this workstation only.
- During the edit pass, malformed intermediate local values were detected and immediately restored from `Guide-Files/env-backup.txt` before final validation.

---

2026-04-12 — Confirm-Email Resend False Invalid-Email Fix (Duplicate Param Parsing)
==============================================================================
Type: Auth UX Correctness / Query Parsing Hardening / Minimal Backward-Compatible Fix
Scope: /confirm-email page searchParam hydration, /api/auth/confirm-email/resend GET parser, resend CTA label resilience

Summary:
- Fixed a reproducible false-negative path where valid links containing duplicate email params
  (for example `email=&email=user@example.com`) were treated as invalid because only the first value was used.
- Hardened page-level query hydration and backend GET parsing to resolve the first non-empty email candidate.
- Preserved truthful CTA wording after a real successful send by making local `hasAcceptedSend` session state sticky
  for the current email even if a subsequent governance poll reports a transient stale snapshot.

Root Cause (code + runtime verified):
- `apps/web/app/(public)/confirm-email/page.tsx` selected the first array value for duplicate params,
  so `email=&email=valid@example.com` hydrated as empty.
- `apps/web/app/api/auth/confirm-email/resend/route.ts` GET used the first email value from query parsing,
  so the same duplicate pattern returned `VERIFICATION_RESEND_INVALID_EMAIL` (HTTP 400) despite a valid trailing value.
- `apps/web/components/auth/confirm-email-panel.tsx` could regress CTA wording after success if a later poll snapshot
  returned `hasAcceptedSend=false` transiently.

Files Changed:
- apps/web/app/(public)/confirm-email/page.tsx
- apps/web/app/api/auth/confirm-email/resend/route.ts
- apps/web/components/auth/confirm-email-panel.tsx
- zootopia-club-next-ledger.txt

Behavioral Changes:
- Confirm-email page search-param helper now picks the first non-empty string in duplicate-value arrays,
  then falls back to the first raw value only when all candidates are empty.
- Resend GET route now scans duplicate `email` params and normalizes the first non-empty candidate before validation.
- Resend panel governance-sync path now keeps `hasAcceptedSend` as `previous || snapshot.hasAcceptedSend`
  so successful send state is not lost by transient polls for the same email.

Verification:
- web-lint-post-polish: pass (1 pre-existing warning in apps/web/lib/server/zootopia-postgres-adapter.ts)
- web-typecheck-post-polish: pass
- web-build-post-polish: pass
- Runtime probe (3026):
  - GET `/api/auth/confirm-email/resend?email=&email=elmahdyabdulla208%40gmail.com` now returns HTTP 200 with governance payload
  - Page probe `/confirm-email?...&email=&email=...` now hydrates input with the valid email value
- Mocked browser resend-flow probe:
  - Before submit label: "Send verification email"
  - After mocked provider-accepted submit + follow-up governance poll: "Resend verification email"

Risks / Notes:
- Existing governance throttling/limits, cooldown windows, and backend authority are unchanged.
- No secret handling, auth boundary, or provider credentials flow was altered.
- Start task argument behavior remains unchanged (`npm run start --workspace @zootopia/web -- --hostname ... --port ...` still routes to the known script limitation).

---

2026-04-13 — Resend Attempt Rollback + Governance-Truthful Submit Hardening
============================================================================
Type: Auth Resend Governance Correctness / UX Safety / Backward-Compatible Hardening
Scope: resend POST provider-failure branch, governance counter lifecycle helper, confirm-email panel submit-state gating

Summary:
- Implemented server-side rollback for resend attempts when the Supabase/Resend provider rejects delivery.
- Preserved backend-authoritative governance while preventing permanent attempt-budget loss on provider non-acceptance.
- Hardened confirm-email submit UX so button state mirrors governance truth and blocks first-submit races while
  governance state is still initializing.

Root Cause (code-verified):
- POST resend path reserved attempts before calling provider; provider rejection branch returned classified errors
  without reversing the already-reserved account/IP counters.
- Confirm-email panel disabled state did not include governance-blocked snapshots and allowed a governance-null
  initialization window where submit could proceed before first poll completed.

Files Changed:
- apps/web/lib/server/verification-resend-governance.ts
- apps/web/app/api/auth/confirm-email/resend/route.ts
- apps/web/components/auth/confirm-email-panel.tsx
- zootopia-club-next-ledger.txt

Behavioral Changes:
- Added `rollbackVerificationResendAttempt({ request, email })` in governance module:
  - no-op when mode is disabled
  - decrements account attempt count (floor 0)
  - decrements IP attempt count only when a real IP was detected
  - keeps cooldown semantics unchanged (throttle pressure remains intact)
- Provider error branch in resend POST now:
  - executes best-effort rollback
  - logs warning on rollback failure
  - refreshes governance snapshot for response headers after rollback attempt
  - still returns existing provider-classified failure response contract
- Confirm-email panel now:
  - tracks governance initialization readiness per active email
  - disables submit while governance is priming
  - disables submit when governance snapshot is blocked (`allowed=false`)
  - forces pre-submit governance refresh when snapshot is missing or blocked
  - keeps existing backend-authoritative status/error mapping and label logic

Verification:
- web-lint-post-polish: pass (1 pre-existing warning in apps/web/lib/server/zootopia-postgres-adapter.ts)
- web-typecheck-post-polish: pass
- web-build-post-polish: pass
- Runtime rollback probe (provider failure, cooldown forced to 0 for deterministic branch reach):
  - server env override: invalid service-role key + `ZOOTOPIA_EMAIL_VERIFICATION_COOLDOWN_SECONDS=0`
  - before POST: accountUsed=0, ipUsed=0
  - POST result: 503 `VERIFICATION_RESEND_UNAVAILABLE` (provider config/auth classification)
  - after POST: accountUsed=0, ipUsed=0 (attempt rollback confirmed)
- Runtime success-path probe (normal service key, cooldown forced to 0):
  - before POST: accountUsed=0, ipUsed=0
  - POST result: 200 `ok=true`, `providerAccepted=true`
  - after POST: accountUsed=1, ipUsed=1 (normal consumption preserved)
- Headless UI probe (mocked governance responses):
  - priming window: submit button disabled early; Enter press produced 0 POST requests before poll completed
  - blocked snapshot: submit button disabled, cooldown label visible, POST count remained 0

Risks / Notes:
- Governance/account/IP model and provider error classification remain intact; no security boundary was loosened.
- Rollback is best-effort by design; provider error responses are returned even if rollback logging indicates a failure.
- Deterministic provider-branch verification required a temporary local cooldown override (`=0`) to ensure POST reaches
  provider branch before cooldown enforcement in this environment.

---

2026-04-13 — First-Click Cooldown Self-Block Fix + Strict Resend Preflight
============================================================================
Type: Auth Resend Correctness / Backend Governance Flow Hardening / UX Preflight Tightening
Scope: reserve→rejection sequencing in resend POST, rollback branch telemetry, confirm-email submit preflight and disabled guards

Summary:
- Fixed the observed network pattern `GET ready -> POST 429 cooldown` on first valid resend click.
- Preserved provider-failure rollback semantics while adding explicit rollback telemetry on provider rejection.
- Tightened panel preflight so every submit re-checks backend governance truth before POST, with cooldown explicitly treated as a disabled state.

Root Cause (code + runtime verified):
- `reserveVerificationResendAttempt()` incremented counters and set cooldown inside the same request.
- Route-level `mapGovernanceRejection()` then interpreted the post-reservation cooldown as a block for that same request,
  returning `VERIFICATION_RESEND_COOLDOWN_ACTIVE` before provider send.
- Panel pre-check logic did not force a governance refresh on every submit attempt and did not explicitly include
  cooldownSeconds in disabled-state derivation.

Files Changed:
- apps/web/lib/server/verification-resend-governance.ts
- apps/web/app/api/auth/confirm-email/resend/route.ts
- apps/web/components/auth/confirm-email-panel.tsx
- zootopia-club-next-ledger.txt

Behavioral Changes:
- Governance snapshot now carries internal `reservationAccepted` metadata when reservation succeeds in the current request.
- Resend POST now rejects governance only when block state was present before reservation (not from same-request cooldown creation).
- Provider rejection branch keeps best-effort rollback and now logs `attemptRolledBack: true` for explicit observability.
- Panel disabled-state now includes `cooldownSeconds > 0` directly.
- Panel submit pre-check now always refreshes governance (`suppressStatus: false`) before deciding whether to POST.

Verification:
- web-lint-post-polish: pass (1 pre-existing warning in apps/web/lib/server/zootopia-postgres-adapter.ts)
- web-typecheck-post-polish: pass
- web-build-post-polish: pass
- Runtime replay (default cooldown):
  - GET `/api/auth/confirm-email/resend?email=...` => 200 `VERIFICATION_RESEND_READY`
  - POST `/api/auth/confirm-email/resend` => 200 `ok=true, providerAccepted=true`
  - follow-up snapshot => cooldown active with consumed attempt counters (expected for subsequent attempts)
- Runtime forced provider rejection (invalid service-role key):
  - before POST counters: account/ip 0/0
  - POST => 503 provider config/auth failure
  - after POST counters: account/ip 0/0 (rollback preserved budget)

Risks / Notes:
- Cooldown remains intentionally preserved even on provider rejection to avoid rapid-fire provider hammering.
- `reservationAccepted` is internal optional metadata on server snapshot shape and is not relied on by client contracts.

---

2026-04-13 — Hash Salt Dependency Audit + One-User Governance Cleanup Runbook
===============================================================================
Type: Operational Safety Audit / Hash-Key Contract Clarity / Minimal Hardening
Scope: resend-governance hash salt resolution path, admin cleanup boundary, environment contract notes

Summary:
- Completed a six-track architecture/safety audit focused on the resend-governance hash/salt dependency chain,
  one-user cleanup safety, and backend-authoritative controls.
- Confirmed the hashed governance model is intact (`scope|salt|normalized_value` via SHA-256) and no raw email
  keys are persisted in governance storage.
- Added non-breaking runtime diagnostics for missing/legacy hash salt env usage plus env-template guidance to keep
  hash salt stable across environments.

Audit Findings (code-verified):
- Salt/key dependency:
  - `getVerificationResendGovernanceConfig()` resolves canonical key
    `ZOOTOPIA_EMAIL_VERIFICATION_HASH_SALT` with legacy alias fallback.
  - `hashGovernanceSubject()` deterministically hashes `scope|salt|value`.
  - Missing salt previously fell back silently to empty-string salt; this pass adds explicit warning telemetry.
- Persistence boundary:
  - `public.email_verification_resend_governance` primary key remains `(key_scope, key_hash)` only.
  - RLS + revoke policies keep table backend-only for browser roles.
- One-user cleanup safety:
  - Admin internal route remains email-driven server-side hash lookup/clear for account scope only.
  - Account-scope-only clearing is intentional; IP scope may represent shared networks and must not be bulk-cleared
    from account-targeted operational actions.

Files Changed:
- apps/web/lib/server/verification-resend-governance.ts
- .env.example
- zootopia-club-next-ledger.txt

Behavioral Changes:
- Governance config now logs a one-time warning when canonical hash salt is unset.
- Governance config now logs a one-time warning when a legacy hash-salt alias is used instead of canonical key.
- Added an architecture comment documenting why account cleanup intentionally does not clear IP-scope rows.
- Env template now explicitly warns to keep hash salt stable after rows exist.

Safe One-User Cleanup (no mass delete):
- Preferred operational path:
  - Use admin internal endpoint `DELETE /api/admin/internal/verification-resend-governance?email=<target>`
    under authenticated admin session.
- SQL-only fallback (when direct DB ops are required and salt is known):
  - Wrap in a transaction and scope strictly to account key for one email.
  - Pattern:
    `delete from public.email_verification_resend_governance`
    `where key_scope = 'account'`
    `and key_hash = encode(digest('account' || '|' || <salt> || '|' || lower(trim(<email>)), 'sha256'), 'hex');`

Verification:
- web-lint-post-polish: pass (1 pre-existing warning in apps/web/lib/server/zootopia-postgres-adapter.ts)
- web-typecheck-post-polish: pass
- web-build-post-polish: pass

Risks / Notes:
- Hash-salt warnings are observability-only and do not change runtime governance math.
- Rotating hash salt without a coordinated governance-row migration/clear will make prior rows unreachable by new
  derived hashes.
- One-user cleanup remains account-scoped by design to avoid collateral network-wide state deletion.

---

2026-04-13 — Required Managed Gender Profile Contract + Redirect/Admin Visibility Alignment
=============================================================================================
Type: Profile Contract Hardening / Auth Claims Propagation / Admin Visibility Expansion / Env Contract Clarification
Scope: shared profile types + validation, repository claim persistence, session/JWT propagation, settings form/API, admin users UI/export, redirect env examples

Summary:
- Added a required managed `gender` field (controlled values) to the profile completion contract for non-admin users.
- Kept redirect authority centralized and server-owned: no redirect architecture redesign, only data-contract extension.
- Extended admin visibility surfaces (user cards, detail page, Excel export) so gender appears where profile state is reviewed.
- Updated redirect env template comments with explicit role-safe route examples tied to current route map.

Files Changed:
- packages/shared-types/src/auth.ts
- packages/shared-types/src/user.ts
- packages/shared-utils/src/validation.ts
- apps/web/lib/server/repository.ts
- apps/web/auth.ts
- apps/web/lib/server/session.ts
- apps/web/app/api/users/me/profile/route.ts
- apps/web/components/settings/profile-settings-form.tsx
- apps/web/app/(protected)/settings/page.tsx
- apps/web/components/admin/users-table.tsx
- apps/web/app/(protected)/admin/users/[uid]/page.tsx
- apps/web/lib/server/admin-users-excel-export.ts
- apps/web/messages/en.json
- apps/web/messages/ar.json
- apps/web/lib/site-content.ts
- .env.example
- zootopia-club-next-ledger.txt

Behavioral Changes:
- Shared profile contract now includes required `gender` input and field error channel.
- Controlled gender values are validated as: `male`, `female`, `prefer_not_to_say`.
- `evaluateProfileCompletion()` now requires valid gender for non-admin users; admin bypass remains intact.
- Repository claim persistence now includes `zc_gender` and reconstructs gender from claims during user hydration.
- Auth.js JWT/session pass-through now carries `gender` end-to-end.
- Settings self-profile form now renders a managed gender selector and submits validated gender in PATCH payload.
- `/api/users/me/profile` now validates and persists gender in the same server-authoritative update path.
- Admin users surfaces now show gender in:
  - overview cards
  - user detail identity section
  - Excel export workbook column
- Privacy copy (EN/AR) and profile-gate explanatory copy now reference gender in required profile fields.
- `.env.example` redirect comments now list valid role-safe route examples for user vs admin defaults.

Redirect/Role/Sidebar Audit Outcome (this pass):
- Confirm-email finalization redirect logic remains centralized in `resolveAuthenticatedUserRedirectPath()` and unchanged.
- Settings-save redirect path remains `requestedReturnTo || getAuthenticatedUserRedirectPath(updatedUser)`.
- Admin vs user lane separation remains unchanged (allowlist + role claims + admin route gate).
- Sidebar wiring remained direct and route-constant based (`Link href={APP_ROUTES.*}`); no route rewiring required.

Verification:
- web-lint-post-polish: pass (1 pre-existing warning in `apps/web/lib/server/zootopia-postgres-adapter.ts`)
- web-typecheck-post-polish: pass
- web-build-post-polish: pass
- Source verification:
  - confirm-email redirect decision path: still role/profile-complete aware
  - settings save redirect path: still resolver-backed
  - admin/user route separation and proxy gating: unchanged
  - sidebar links: direct route constants, no indirection regression introduced

Risks / Notes:
- Existing non-admin users without gender data will now evaluate as profile-incomplete until gender is set.
- Controlled gender validation is fail-closed; unknown/legacy string values normalize to incomplete/null.
- Runtime browser flow validation for every viewport/theme/locale combination was not executed in this pass;
  compile-time/lint/build checks and source-trace verification were completed.

---

2026-04-13 — Gender Persistence Storage Audit (No Schema Migration Required)
=============================================================================
Type: Persistence audit / schema verification / build validation
Scope: user profile gender durability path, auth/session propagation, admin visibility/export, migrations

Summary:
- Completed mandatory full-ledger read first, then ran seven parallel read-only traces for:
  1. user persistence schema
  2. profile settings save path
  3. repository storage model
  4. Supabase migrations/schema
  5. auth/session claim propagation
  6. admin display/export surfaces
  7. lint/typecheck/build verification baselines
- Cross-validated subagent findings directly in source before any edits.
- Determined that `gender` is durably persisted in the active runtime model via Supabase Auth custom claims (`zc_gender`), not via a dedicated SQL column.

Persistence Verdict (code-verified):
- Write path:
  - `apps/web/app/api/users/me/profile/route.ts` validates and forwards `gender` to `updateUserProfile(...)`.
  - `apps/web/lib/server/repository.ts` (`updateUserProfile`) builds `nextUser.gender` and persists via `persistAuthCriticalUserClaims(...)`.
  - `persistAuthCriticalUserClaims(...)` writes `zc_gender` through `getServerAuthAdmin().setCustomUserClaims(...)`.
- Readback path:
  - `getUserByUid(...)` loads auth record via `getServerAuthAdmin().getUser(...)`.
  - `buildUserDocumentFromAuthRecord(...)` reconstructs `gender` from `zc_gender` claims.
  - `apps/web/lib/server/session.ts` hydrates `SessionUser.gender` from persisted user (or normalized token fallback).

Storage Model Findings:
- Active user profile authority is Supabase Auth claim metadata, not relational `user_profiles` writes.
- `supabase/migrations/20260410180000_phase1_postgres_parity_tables.sql` has no `gender` column in `public.user_profiles`.
- `supabase/migrations/20260410120000_zootopia_platform_entities.sql` confirms schema-flexible `zc_entities` JSONB storage exists, but active user profile read/write paths currently do not depend on `collection("users")` for profile authority.
- No pre-existing gender-specific migration exists.

Decision:
- No new migration added.
- Rationale: current production user-profile persistence contract is claim-backed and already stores/re-hydrates `gender` durably through Supabase Auth metadata.

Coverage Checks:
- Settings load: `apps/web/app/(protected)/settings/page.tsx` reads `user.gender` and passes `initialGender`.
- Settings save: `apps/web/components/settings/profile-settings-form.tsx` validates and submits `gender` to PATCH.
- Profile completion: `packages/shared-utils/src/validation.ts` includes `gender` in `validateRequiredUserProfile(...)` and `evaluateProfileCompletion(...)`.
- Auth/session hydration: `apps/web/auth.ts` maps `gender` into JWT/session; `apps/web/lib/server/session.ts` normalizes/hydrates it.
- Admin table: `apps/web/components/admin/users-table.tsx` renders localized gender.
- Admin detail: `apps/web/app/(protected)/admin/users/[uid]/page.tsx` renders stored gender.
- Admin export: `apps/web/lib/server/admin-users-excel-export.ts` includes Gender column + value mapping.

Verification:
- web-lint-post-polish: pass (1 pre-existing warning in `apps/web/lib/server/zootopia-postgres-adapter.ts`)
- web-typecheck-post-polish: pass
- web-build-post-polish: pass

Files Changed:
- zootopia-club-next-ledger.txt

Files Not Changed:
- No runtime/source files changed.
- No Supabase migration files changed.

---

2026-04-13 — Settings Save Feedback + Proxy Profile-Gate Drift Fix
==================================================================
Type: Redirect correctness / profile-completion gate hardening / settings UX polish
Scope: proxy protected-route handling, returnTo whitelist parity, settings save action feedback, env redirect docs

Summary:
- Completed full-ledger read and source-level trace validation before edits.
- Fixed the reported "completed profile still redirected to settings" behavior by removing stale-claim
  profile-completion redirects from proxy and preserving completion authority in server route guards.
- Added protected-route/returnTo parity for `/history` so route matching, sanitization, and env examples stay aligned.
- Improved settings save UX with truthful near-button pending/success/error feedback tied to actual PATCH outcomes,
  plus light spacing/rhythm polish without redesigning the page architecture.

Root Cause (code-verified):
- `apps/web/proxy.ts` enforced non-admin profile completion using JWT/session claims available in proxy context.
- After successful profile updates, token claim state can lag persisted user state, producing false redirects for routes
  covered by proxy matchers (`/`, `/upload`, `/assessment`, etc.).
- `/history` asymmetry exposed this drift: it existed in app routes/nav but was absent from proxy/user returnTo matcher sets,
  so behavior differed across protected pages.

Files Changed:
- apps/web/proxy.ts
- apps/web/lib/return-to.ts
- apps/web/components/settings/profile-settings-form.tsx
- apps/web/app/(protected)/settings/page.tsx
- apps/web/messages/en.json
- apps/web/messages/ar.json
- .env.example
- zootopia-club-next-ledger.txt

Behavioral Changes:
- Proxy changes:
  - Added `/history` to `USER_PROTECTED_MATCHERS` and `config.matcher` coverage.
  - Removed proxy-level profile-completion redirect branch that used potentially stale claims.
  - Kept unauthenticated/login/admin protections in proxy, but made authenticated login redirect selection role-based
    (completion-agnostic) to avoid claim-lag redirects back to settings.
- Completion-gate ownership:
  - Profile completion now remains enforced by server route ownership (`requireCompletedUser`) using persisted
    session-backed user data, preventing claim-lag false redirects while preserving backend authority.
- Return-to parity:
  - Added `APP_ROUTES.history` to `USER_RETURN_MATCHERS` in `apps/web/lib/return-to.ts`.
  - Updated `.env.example` user redirect examples to include `/history`.
- Settings UX:
  - Added submit feedback state in profile settings form (`idle | pending | success | error`).
  - Surfaced pending/success/error text directly beside the submit action rail to keep save truth visible near the button.
  - Added localized message keys:
    - `settingsProfileSavePendingStatus`
    - `settingsProfileSaveSuccessStatus`
  - Applied minimal spacing adjustments in settings page and settings form to improve grouping/rhythm while preserving
    existing visual language.

Verification:
- web-lint-post-polish: pass (1 pre-existing warning in `apps/web/lib/server/zootopia-postgres-adapter.ts`)
- web-typecheck-post-polish: pass
- web-build-post-polish: pass
- Build output confirms `/history` route remains active and proxy is compiled.

Risks / Notes:
- This pass intentionally avoids reintroducing completion gating in proxy to prevent claim-lag regressions.
- Server-side completion checks already remain authoritative on completed-route pages.
- Confirm-email redirect/finalization logic was reviewed in source during this pass and left unchanged.

---

2026-04-13 — Home Hero Polish Utilities (globals.css Additive Insert)
=====================================================================
Type: UI styling additive update / home-page-only utilities
Scope: apps/web/app/globals.css insertion immediately before Custom Scrollbars section

Summary:
- Added the requested Home polish CSS block as a surgical insertion only.
- Inserted exactly before the `/* Custom Scrollbars */` marker with no refactors or removals.
- Kept changes route-scoped by class naming (`home-*`) so no existing non-home layout contracts are affected.

Files Changed:
- apps/web/app/globals.css
- zootopia-club-next-ledger.txt

Behavioral Additions:
- Added `@keyframes home-scroll-hint-bounce` and `.home-scroll-hint`.
- Added `.home-zone-enter` and `@keyframes home-zone-fade-up`.
- Added `.home-hero-divider` with dark/system theme variants.
- Added `.home-stat-card` hover transitions with dark/system variants.
- Added `.home-nav-card` directional hover rail with RTL flip support.
- Added `.home-trust-pill` hover behavior.
- Added `prefers-reduced-motion` overrides for all new animated/hover transforms.

Verification:
- web-lint-post-polish: pass (1 pre-existing warning in `apps/web/lib/server/zootopia-postgres-adapter.ts`)
- web-typecheck-post-polish: pass
- web-build-post-polish: pass

Risks / Notes:
- CSS language-service diagnostics for Tailwind custom at-rules in `globals.css` are pre-existing workspace behavior and unrelated to this insertion.
- No runtime/auth/data-flow logic changed in this pass.

---

2026-04-13 — Postgres Adapter PendingQuery Type Preservation Fix
===============================================================
Type: TypeScript build fix / adapter typing hardening / minimal compatibility patch
Scope: apps/web/lib/server/zootopia-postgres-adapter.ts query-builder helper typing

Summary:
- Fixed root workspace build failure caused by an incompatible `PendingQuery` row mutability type in the Postgres adapter query builder.
- Kept runtime SQL behavior unchanged and applied a surgical type-surface adjustment only.
- Removed stale lint suppression comment tied to `declare global` singleton typing.

Root Cause (code-verified):
- `PgQuery._appendWhereClauses(...)` and `PgQuery._appendOrderBy(...)` accepted/returned `ReturnType<SqlExecutor>` directly.
- During chained template composition, `postgres.js` inferred a narrower `PendingQuery<readonly ...>` variant in one branch.
- Reassignment in `_getWithSql` then failed type-check against the broader mutable `PendingQuery<Row[]>` expectation.

Files Changed:
- apps/web/lib/server/zootopia-postgres-adapter.ts
- zootopia-club-next-ledger.txt

Behavioral Changes:
- Updated internal helper signatures to preserve caller query type with generics:
  - `_appendWhereClauses<TQuery>(..., query: TQuery): TQuery`
  - `_appendOrderBy<TQuery>(..., query: TQuery): TQuery`
- Added internal cast boundaries inside both helpers to keep type-preserving chaining while avoiding `PendingQuery` variance conflicts.
- Removed unused `// eslint-disable-next-line no-var` comment above `declare global` singleton declaration.

Verification:
- Root `npm run build`: pass
  - @zootopia/api build: pass
  - @zootopia/web build: pass
  - @zootopia/shared-config build: pass
  - @zootopia/shared-types build: pass
  - @zootopia/shared-utils build: pass
- web-lint-post-polish: pass

Risks / Notes:
- This is a type-only stabilization patch; SQL text, query semantics, and transaction behavior are unchanged.
- Generic helper return typing is intentionally local to query construction to avoid leaking postgres.js internal generic variance to callers.

---

2026-04-13 — Confirm-Email Resend Label Boundary Hardening
===========================================================
Type: Auth UX correctness fix / confirm-email resend action-copy state guard
Scope: confirm-email panel client state ownership for Send vs Resend label transitions

Summary:
- Fixed a confirm-email CTA copy regression where governance polling could flip the button label to "Resend verification email"
  before the user had made a successful resend attempt on the current page.
- Kept backend governance logic and API contracts unchanged.
- Applied a minimal client-only state boundary fix to preserve cooldown/blocked/priming behavior.

Root Cause (code-verified):
- `syncGovernanceState(...)` merged persisted backend `governance.hasAcceptedSend` into local label state during GET polling.
- That backend value is intentionally cross-session and cross-device, so a first page load for an email with historical accepted sends
  could pre-switch the label to "Resend" even before any current-page submit.

Files Changed:
- apps/web/components/auth/confirm-email-panel.tsx
- zootopia-club-next-ledger.txt

Behavioral Changes:
- Added page-scoped label state `hasAcceptedSendOnPage` with explicit confirm-email ownership comments.
- Removed polling-driven label mutation in `syncGovernanceState(...)`.
- Kept governance snapshot/cooldown refresh intact so throttle truth remains backend-driven.
- Flip to resend label now occurs only after POST resend succeeds with provider acceptance.
- Page-scoped resend-label state resets when the email becomes invalid/unset and when user edits the email input.

Verification:
- web-lint-post-polish: pass
- web-typecheck-post-polish: pass
- web-build-post-polish: pass

Risks / Notes:
- This pass intentionally scopes only button copy transition logic; no auth/session/bootstrap behavior changed.
- Existing governance blocked/cooldown labels and disablement gates remain unchanged.

---

2026-04-13 — Home Hall of Honor Wrapper Visual Neutralization
==============================================================
Type: UI polish / home section visual cleanup / surgical class-level edit
Scope: Hall of Honor block in completed home page route

Summary:
- Removed the decorative parent-layer radial gradient from the Hall of Honor block so the wrapper is visually neutral.
- Kept the inner Hall of Honor card treatment (border/background/hover/padding/radius) as the primary polished surface.
- Preserved existing responsive layout behavior across mobile/tablet/desktop with no structural changes.

Files Changed:
- apps/web/app/(protected)/(completed)/page.tsx
- zootopia-club-next-ledger.txt

Behavioral Changes:
- Removed Hall of Honor inline decorative overlay:
  - `pointer-events-none absolute inset-0 rounded-[1.25rem] bg-[radial-gradient(...)]`
- Left wrapper spacing (`px-6 pt-5 md:px-10 md:pt-6`) and card spacing/radius (`p-5`, `rounded-[1.25rem]`) unchanged to keep balanced alignment with surrounding home sections.

Verification:
- web-lint-post-polish: pass
- web-typecheck-post-polish: pass
- web-build-post-polish: pass

Risks / Notes:
- No business logic, routing, data flow, or component ownership changed.
- Patch is component-local and does not touch shared/global styling contracts.

---

2026-04-13 — Admin/User Production Render Hardening + Extension-Noise Separation
===============================================================================
Type: Runtime stability hardening / role-boundary preservation / fallback rendering safety
Scope: protected server-component data loading, admin API read-path error handling, session lookup resilience

Summary:
- Traced the reported production "Something went wrong" path from first-party source and separated it from browser-extension noise.
- Confirmed the console `inpage.js` / `toncenter.com` "Origin not allowed" message is external injected-script noise and not Zootopia-owned code.
- Hardened protected/admin server components and admin read routes to fail soft with fallback UI/data instead of crashing the full page on transient repository/runtime faults.
- Preserved strict role separation and server-authoritative admin gates; no privilege widening was introduced.

Root Cause (first-party, code-verified):
- The dominant failure mode was fail-fast server rendering from unguarded async chains (`Promise.all`) and uncaught repository exceptions.
- Admin and protected pages previously allowed non-critical data loaders to throw and bubble to global error boundary (`Something went wrong`).
- Admin read APIs also had unwrapped repository calls in selected handlers, resulting in unstructured 500 failures during lookup/read turbulence.

Noise vs Fault Separation:
- External noise (not root cause): injected browser extension `inpage.js` call to `https://toncenter.com/api/v2/jsonRPC` with `Origin not allowed`.
- Real app fault: unhandled first-party runtime/read failures in protected/admin render/data paths.

Files Changed:
- apps/web/lib/server/session.ts
- apps/web/app/(protected)/admin/page.tsx
- apps/web/app/(protected)/admin/users/page.tsx
- apps/web/app/(protected)/admin/users/[uid]/page.tsx
- apps/web/app/(protected)/(completed)/page.tsx
- apps/web/app/(protected)/(completed)/upload/page.tsx
- apps/web/app/(protected)/(completed)/assessment/page.tsx
- apps/web/app/(protected)/(completed)/history/page.tsx
- apps/web/app/(protected)/(completed)/infographic/page.tsx
- apps/web/app/(protected)/(completed)/assessment/preview/[id]/page.tsx
- apps/web/app/(protected)/(completed)/assessment/results/[id]/page.tsx
- apps/web/app/api/admin/overview/route.ts
- apps/web/app/api/admin/users/route.ts
- apps/web/app/api/admin/users/[uid]/credits/route.ts
- apps/web/app/api/admin/users/[uid]/route.ts
- apps/web/app/api/admin/storage/cleanup/route.ts
- zootopia-club-next-ledger.txt

Behavioral Changes:
- Session resilience:
  - `getVerifiedSessionContext()` now catches repository user-lookup failures and falls back to signed session claims for this request instead of crashing protected rendering.
- Protected/admin page resilience:
  - Added guarded best-effort loaders with safe fallbacks for dashboard/user-workspace datasets.
  - Added non-blocking fallback notices when data is degraded, while preserving page shell and route access rules.
  - Admin user detail storage namespace summary now fails soft with fallback counts and warning state.
  - History preview list now skips malformed records safely instead of failing the whole page.
  - Assessment preview/result pages now gracefully return unavailable cards if generation fetch/preview-model construction fails.
- Admin API resilience:
  - Added structured error handling and stable 503 responses for transient read-path failures in overview/users/credits/user-lookup/storage cleanup lookup flows.

Security / Role Boundary Verification:
- Admin-only route ownership remains enforced by `requireAdminUser()` and admin route layout.
- User/admin redirect and lane separation remain unchanged.
- No backend authority moved to client.
- No changes to `apps/web/lib/server/zootopia-postgres-adapter.ts` in this pass; adapter was analyzed as a dependency path only.

Verification:
- web-lint-post-polish: pass
- web-typecheck-post-polish: pass
- web-build-post-polish: pass

Risks / Notes:
- Fallback UI may show empty-state data during transient backend/read turbulence by design, prioritizing render continuity over hard-fail screens.
- Existing unrelated workspace change in `.env.example` was detected and left untouched.

---

2026-04-13 — zc_entities created_at Drift Repair + Adapter Resilience Completion
===============================================================================
Type: Schema compatibility repair / Postgres adapter hardening / production upload-credit unblock
Scope: zc_entities migration alignment, ensureSchema legacy self-heal, batch transient retry behavior

Summary:
- Audited adapter feature completeness against current runtime implementation and confirmed the enhanced Postgres adapter surface is present
  (operators, nested JSON traversal, create/update/add, batch/transaction APIs, count/offset/first, snapshot helpers, metadata, retry, debug, schema helpers).
- Identified live schema drift: base migration created `zc_entities` without `created_at`, while adapter read/write paths require `created_at` metadata.
- Added an idempotent repair migration to add missing `created_at` plus supporting index.
- Hardened `ensureZootopiaSchema()` to self-heal legacy tables by adding missing timestamp columns and created_at index even when table already exists.
- Upgraded `PgWriteBatch.commit()` to run through retry-with-backoff so transient deadlocks/serialization failures are automatically retried.

Root Cause (code-verified):
- `apps/web/lib/server/zootopia-postgres-adapter.ts` reads `created_at` in snapshot queries and writes `created_at` in insert paths.
- `supabase/migrations/20260410120000_zootopia_platform_entities.sql` originally created `zc_entities` without `created_at`.
- This mismatch can surface as `column "created_at" does not exist`, cascading into upload and credit endpoint failures.

Files Changed:
- apps/web/lib/server/zootopia-postgres-adapter.ts
- supabase/migrations/20260413193000_fix_zc_entities_created_at_and_index.sql
- zootopia-club-next-ledger.txt

Behavioral Changes:
- New migration adds:
  - `created_at timestamptz not null default now()` (if missing)
  - `idx_zc_entities_created` on `(collection, created_at desc)` (if missing)
  - column comment documenting adapter metadata contract
- `ensureZootopiaSchema()` now includes defensive `ALTER TABLE ... ADD COLUMN IF NOT EXISTS` for `created_at` and `updated_at`.
- `ensureZootopiaSchema()` now creates `idx_zc_entities_created` if missing.
- `PgWriteBatch.commit(retryOptions?)` now wraps commit execution with the same transient retry strategy used by other public operations.

Verification:
- web-lint-post-polish: pass
- web-typecheck-post-polish: pass
- web-build-post-polish: pass
- `npm run supabase:migrations:validate`: pass (`Validated 8 migration file(s).`)

Risks / Notes:
- Schema repair SQL is idempotent and safe for both fresh and already-provisioned environments.
- Upload/credits production recovery still requires applying pending migration files to the target database.
- No auth/admin role boundaries were changed in this pass.

---

2026-04-13 — Unified Owner-Scoped Storage Layout + Metadata Contract Hardening
===============================================================================
Type: Storage architecture hardening / owner-scope canonicalization / backward-compatible metadata normalization
Scope: owner path helpers, upload/result/export artifact metadata, admin cleanup/reporting, storage policy migration

Summary:
- Implemented a canonical owner-scoped storage layout for all new writes under `users/{ownerUid}/...`.
- Preserved backward compatibility for historical object paths (`documents/{ownerUid}/...`, `assessment-results/{ownerUid}/...`, `assessment-exports/{ownerUid}/...`, `uploads/temp/{ownerUid}/...`) so existing records remain readable/deletable.
- Added authoritative storage metadata fields on persisted document and assessment artifact records.
- Hardened assessment artifact normalization to reject artifacts whose storage paths are not owner-scoped to the generation owner UID.
- Updated admin storage visibility/cleanup to aggregate canonical + legacy prefixes without widening any user/admin authority boundaries.

Key Architectural Decisions:
- Canonical write path root is now `users/{ownerUid}/...` for upload source files, assessment results, and export artifacts.
- Owner path validation remains server-authoritative and mandatory for all storage reads/writes/deletes via `assertOwnerScopedStoragePath(...)`.
- A dual-layout compatibility layer in `owner-scope.ts` resolves path metadata and owner-scope checks for both `legacy-v1` and `unified-v2` layouts.
- Storage RLS policy migration rebuilds `zootopia_private_owner_*` policies to support canonical + legacy object names for authenticated-role compatibility.

Files Changed:
- apps/web/lib/server/owner-scope.ts
- apps/web/lib/server/document-runtime.ts
- apps/web/lib/server/assessment-artifact-storage.ts
- apps/web/lib/server/assessment-records.ts
- apps/web/lib/server/repository.ts
- apps/web/lib/server/storage-retention-config.ts
- apps/web/lib/server/supabase-blob-storage.ts
- apps/web/app/api/uploads/route.ts
- apps/web/app/api/admin/storage/cleanup/route.ts
- apps/web/app/(protected)/admin/users/[uid]/page.tsx
- apps/web/app/(protected)/admin/storage/page.tsx
- packages/shared-types/src/document.ts
- packages/shared-types/src/assessment.ts
- supabase/migrations/20260414103000_storage_owner_policies_unified_user_root.sql
- zootopia-club-next-ledger.txt

Behavioral Changes:
- New write layout:
  - Upload binaries: `users/{ownerUid}/documents/{documentId}/{fileName}`
  - Assessment canonical result: `users/{ownerUid}/assessment-results/{generationId}/result.json`
  - Assessment exports: `users/{ownerUid}/assessment-exports/{generationId}/{artifactKey}/{variant}.{ext}`
- Legacy read/delete compatibility preserved through owner-scope metadata inference and assertions.
- Document and assessment artifacts now persist optional storage metadata fields:
  - `storageDataClass`
  - `storageOwnerUid`
  - `storageLayoutVersion`
- Assessment artifact normalization now drops malformed/cross-owner storage entries instead of trusting stored artifact paths blindly.
- Admin per-user namespace summaries and cleanup now query/delete both canonical and legacy prefixes.
- Admin global storage cleanup now includes canonical root (`users/*`) in addition to legacy namespace roots.
- Retention documentation and namespace-to-scope resolver updated to understand canonical and legacy path forms.

Verification:
- web-lint-post-polish: pass
- web-typecheck-post-polish: pass
- web-build-post-polish: pass
- npm run supabase:migrations:validate: pass (`Validated 9 migration file(s).`)

Risks / Notes:
- Global storage cleanup remains intentionally destructive; adding canonical `users/*` root broadens cleanup coverage for user-owned objects by design.
- Existing historical objects are not auto-migrated in-place; compatibility is handled at runtime through dual-layout scope checks.
- Workspace still reports pre-existing CSS language-service diagnostics in `apps/web/app/globals.css` for Tailwind custom at-rules (non-blocking).

---

2026-04-13 — Sidebar Branding + Settings Density Cleanup (UI-Only)
===================================================================
Type: UI cleanup / layout density optimization / fallback message polish
Scope: protected home fallback notice visibility, sidebar brand presentation, settings page/form spacing and status UX

Summary:
- Completed mandatory full-ledger read and repo exploration before edits.
- Removed the user-facing fallback warning sentence from the protected home page while preserving fail-soft data loading and warning logs.
- Simplified protected sidebar branding by removing the extra tagline row and rebalancing logo/title sizing for cleaner single-line alignment.
- Removed slash separator clutter from the protected top-shell app title chip.
- Replaced oversized settings summary cards with a compact preferences strip.
- Removed large profile-completion summary blocks from the settings form header and introduced compact inline saved indicators per field.
- Tightened settings spacing/padding to reduce empty space while preserving existing save flows, validation, and redirects.

Files Changed:
- apps/web/app/(protected)/(completed)/page.tsx
- apps/web/components/layout/shell-nav.tsx
- apps/web/components/layout/protected-shell.tsx
- apps/web/app/(protected)/settings/page.tsx
- apps/web/components/settings/profile-settings-form.tsx
- zootopia-club-next-ledger.txt

Behavioral Changes:
- Home page no longer renders the sentence "Some workspace datasets are temporarily unavailable. Showing fallback cards and empty states." to end users.
- Sidebar brand area now shows a cleaner mark + app title lockup with reduced visual clutter and better width fit.
- Settings page no longer renders large profile/runtime summary card grid.
- Profile settings form now shows compact saved-state check markers beside full name, university code, phone, gender, and nationality fields when values match last persisted state.

Verification:
- `npm run lint --workspace @zootopia/web`: pass (`LINT_EXIT_CODE=0`)
- `npm run typecheck --workspace @zootopia/web`: pass (`TYPECHECK_EXIT_CODE=0`)
- `npm run build --workspace @zootopia/web`: pass (`BUILD_EXIT_CODE=0`)
- Source scan: no remaining matches for the removed fallback sentence in `apps/web`

Risks / Notes:
- This pass intentionally avoids auth, security, API, and business-logic changes.
- Inline saved indicators are presentation-only and derive from last successful persisted form snapshot plus refreshed server state.

---

2026-04-13 — Theme Switch Performance + Protected Light Surface Polish
=======================================================================
Type: UI performance polish / theme-switch rendering stability / protected-workspace visual refinement
Scope: theme toggle client behavior, global theme tokens/transitions/scrollbars, protected sidebar/header surfaces, protected background layering

Summary:
- Completed full-ledger read and source exploration before edits (including theme pipeline files and protected shell/background ownership).
- Removed costly route refresh from theme toggle and switched to immediate DOM theme application (`data-theme` + `.dark` class + cookie write) to reduce visible switching lag.
- Added a short theme-switch transition suppression guard to prevent full-page transition cascades during manual theme flips.
- Integrated protected light-mode background image ownership (`/my-app-background-light.png`) with a subtle readability overlay while preserving existing dark/system-dark image ownership.
- Updated protected sidebar and top header surfaces/icons/borders to use theme tokens for cleaner light-mode translucency without redesigning architecture.
- Converted custom scrollbars to token-driven, theme-aware styling for both WebKit and Firefox engines.

Files Changed:
- apps/web/components/preferences/theme-toggle.tsx
- apps/web/app/globals.css
- apps/web/components/layout/shell-nav.tsx
- apps/web/components/layout/protected-shell.tsx
- zootopia-club-next-ledger.txt

Root Cause (code-verified):
- Theme switching previously triggered `router.refresh()` from the toggle, forcing a server roundtrip/re-render cycle for a purely visual preference change.
- Theme DOM application only set `data-theme` client-side but relied on refresh for `.dark` class consistency, causing perceived lag/inconsistent style activation windows.
- A broad global `transition: all` rule amplified repaint/reflow work across many interactive surfaces during token swaps.

Behavioral Changes:
- Theme toggle now:
  - writes cookie
  - applies `data-theme` immediately
  - synchronizes `.dark` class immediately
  - updates `color-scheme` immediately
  - skips `router.refresh()` for normal theme toggles
  - listens for system preference changes when current mode is `system`
- Added `.theme-switching` guard in global CSS to disable transitions briefly during manual theme flip.
- Narrowed broad interactive transition declaration from `all` to targeted properties (color/background/border/shadow/opacity/transform).
- Added new scrollbar tokens for light and dark variants and wired `.global-scrollbar` + `.side-scrollbar` to tokenized colors.
- Protected workspace background:
  - light default now uses `/my-app-background-light.png`
  - light overlay/base/grid tuned for readability and softer premium translucency
  - dark/system-dark explicitly retain `/my-app-background.png` with prior dark treatment
  - login/admin-login background ownership remains untouched (still isolated to auth media classes)
- Protected sidebar/header visual polish:
  - replaced hardcoded white/zinc shell accents with theme-tokenized foreground/border/background classes
  - preserved structure/routes/actions while improving icon/text/border adaptation in light mode

Verification:
- web-lint-post-polish: pass
- web-typecheck-post-polish: pass
- web-build-post-polish: pass

Risks / Notes:
- This pass intentionally avoids auth/business-logic/storage/API ownership changes and keeps modifications UI/theme-layer only.
- Existing workspace CSS language-service warnings for Tailwind custom at-rules (`@custom-variant`, `@theme`, `@apply`) remain pre-existing/non-blocking.
- Theme switch suppression is intentionally brief (220ms) and scoped to manual toggle events to avoid persistent animation disablement.


---

2026-04-13 — Assessment 502 Production Classification Fix + Lifecycle Audit Hardening
========================================================================================
Type: Backend reliability fix / provider error classification correction / assessment attempt lifecycle observability
Scope: assessment API route execution envelope, provider HTTP classifier, assessment default model ordering

Summary:
- Completed mandatory full-ledger read, subagent-first exploration, and direct source-chain validation before edits.
- Reproduced the reported production-class failure on `POST /api/assessment` with a live authenticated call path.
- Confirmed backend root cause was provider-classification drift: Qwen/DashScope returned HTTP 400 with `code=arrearage`, but server fallback classification mapped it to generic `ASSESSMENT_PROVIDER_EXECUTION_FAILED` (502).
- Applied a surgical backend fix so arrearage/billing-exhaustion signals are treated as quota exhaustion (`ASSESSMENT_PROVIDER_RATE_LIMITED`, 429) instead of gateway-style 502.
- Shifted default Assessment model order to Gemini lane first so default generation path no longer depends on a degraded Qwen billing state.
- Added durable assessment attempt lifecycle audit events (`started`, `failed`, `finalization-failed`, existing `generated`) with best-effort semantics that cannot break generation/credit transactions.
- Added route-segment execution budget export (`maxDuration`) to reduce platform timeout risk during provider calls.

Root Cause (code-verified):
- In `apps/web/lib/server/ai/execution.ts`, `buildProviderExecutionError(...)` did not classify DashScope `arrearage` responses.
- Upstream `400 arrearage` therefore fell through to generic execution branch:
  - `code: ASSESSMENT_PROVIDER_EXECUTION_FAILED`
  - `status: 502`
- Because `qwen3.5-flash` was the first Assessment model in shared config, default request lanes repeatedly hit this degraded provider path.

Files Changed:
- apps/web/lib/server/ai/execution.ts
- packages/shared-config/src/models.ts
- apps/web/app/api/assessment/route.ts
- zootopia-club-next-ledger.txt

Behavioral Changes:
- Provider error classification:
  - DashScope/Qwen arrearage and billing-exhaustion signatures now map to `ASSESSMENT_PROVIDER_RATE_LIMITED` (429).
  - Generic 502 execution fallback is no longer used for this known provider quota state.
- Assessment default model order:
  - Default Assessment lane now resolves to Gemini first (Qwen remains available/selectable).
- Assessment route lifecycle persistence:
  - Added best-effort `appendAdminLog(...)` events for:
    - `assessment-generation-started`
    - `assessment-generation-failed`
    - `assessment-finalization-failed`
    - Existing `assessment-generated` retained
  - Logging failures are now explicitly non-fatal and cannot flip successful writes into false finalization failures.
- Route runtime envelope:
  - Added `export const maxDuration = 120` on `/api/assessment` route segment.

Verification:
- Runtime reproduction (authenticated admin path):
  - Pre-fix reproduction: `POST /api/assessment` => 502 `ASSESSMENT_PROVIDER_EXECUTION_FAILED` with upstream `{ status: 400, code: arrearage }` in server diagnostics.
  - Post-fix Qwen forced lane (`modelId=qwen3.5-flash`): `POST /api/assessment` => 429 `ASSESSMENT_PROVIDER_RATE_LIMITED` (no 502).
  - Post-fix default lane: `POST /api/assessment` no longer returns 502; now returns classified Google runtime auth failure in this environment (`ASSESSMENT_PROVIDER_AUTH_FAILED`, 503).
- Build/quality checks:
  - web-lint-post-polish: pass
  - web-typecheck-post-polish: pass
  - web-build-post-polish: pass

Risks / Notes:
- This fix eliminates the misleading 502 class for known provider arrearage but does not override upstream provider account state (billing/quota/auth must still be healthy for successful generation).
- Default-lane switch to Gemini is intentional resilience behavior to avoid outage-by-default when Qwen billing is degraded.
- Credit reserve/commit semantics and owner-scope isolation were preserved; no client-side authority expansion was introduced.

---

2026-04-13 — Upload Page Premium Glass Polish + Veil Tuning (UI-Only)
========================================================================
Type: UI polish / Upload route visual refinement / protected background translucency tuning
Scope: `/upload` hero depth stack, `UploadWorkspace` intake surfaces, protected workspace overlay visibility

Summary:
- Completed mandatory full-ledger read, Next.js CSS guidance review, route-chain analysis, and an explorer subagent repo survey before editing.
- Removed the warm brown feel from the `Document intake` header/container by replacing token-driven warm surfaces with a cooler dark glass treatment scoped to the Upload workspace.
- Increased translucency across the Upload hero shell, intake card, dropzone, status panels, and quick-action cards so the protected workspace artwork reads through more clearly.
- Added restrained perspective layering behind the Upload hero shell to create deeper stacking and elevation without changing layout structure or workflow.
- Lightened the shared protected workspace image/overlay veil slightly so the page background is more visible while keeping text contrast controlled.

Files Changed:
- apps/web/app/(protected)/(completed)/upload/page.tsx
- apps/web/components/upload/upload-workspace.tsx
- apps/web/app/globals.css
- zootopia-club-next-ledger.txt

Behavioral Changes:
- Upload route hero shell now uses:
  - cooler dark-glass background treatment
  - subtle cyan/teal radial lighting
  - layered perspective plates gated to larger breakpoints (`sm`/`md`) to avoid mobile clutter
- `UploadWorkspace` now uses page-scoped Upload glass presets for:
  - outer intake card
  - `Document intake` header surface
  - dropzone shell and floating document mini-cards
  - uploading/recent-upload panels
  - active-document continuation card
- Infographic CTA inside the active-document card now uses a neutral glass base with amber hover accents instead of a heavier amber fill, reducing warm patchiness.
- Protected workspace background tuning:
  - light image opacity increased from `0.30` → `0.34`
  - dark/system-dark image opacity increased from `0.32` → `0.38`
  - dark/system-dark veil gradient density reduced (`0.14/0.34/0.66/0.88` → `0.10/0.28/0.54/0.78`)
  - dark/system-dark grid opacity reduced from `0.12` → `0.10`

Verification:
- `npm run lint --workspace @zootopia/web`: pass
- `npm run typecheck --workspace @zootopia/web`: pass
- `npm run build --workspace @zootopia/web`: pass
- Production-style local server:
  - built app served successfully on `http://localhost:3100`
  - `GET /` returned `200`
- Protected-route browser verification:
  - attempted via Playwright CLI against `/upload`
  - actual result: redirect to `/login`
  - code-verified blocker: available Playwright session `authcheck` was not authenticated for the current environment, and the repo no longer exposes a usable auth bootstrap endpoint (`/api/auth/bootstrap` and `/api/auth/admin/bootstrap` both return deprecation `410`)
- Responsive verification status:
  - full live authenticated browser verification of `/upload` was not completed because of the auth redirect above
  - code-level responsive safeguards were validated in source:
    - hero perspective plates are hidden below `sm`/`md`
    - Upload shell and workspace preserve `min-w-0`, `max-w-3xl`, and existing grid breakpoints
    - no new fixed-width horizontal layout was introduced
    - new depth layers remain clipped inside existing overflow containers to avoid page-level spillover

Risks / Notes:
- This pass intentionally avoided auth, storage, routing, upload API, validation, and document-processing logic.
- The protected workspace veil change is global across authenticated pages, but it was kept intentionally small to avoid broad readability regressions.
- The Upload page still needs a true authenticated live browser pass in this environment if a reusable session or test credentials become available.

---

2026-04-13 — Backend-Authoritative Tool Default Model Env Resolver (Assessment + Infographic)
===============================================================================================
Type: Backend configuration hardening / tool-scoped model-default alignment / frontend-server consistency
Scope: assessment + infographic model defaults, route validation, server page initial state, env template

Summary:
- Added a single server-only default-model resolver for the real current tool scopes only: `assessment` and `infographic`.
- Wired both backend routes and both server-rendered pages to the same resolver so frontend initial selection now mirrors the backend-authoritative default without duplicating hardcoded model ids.
- Preserved provider-level runtime model overrides as a separate concern; this pass only changes tool-default selection.
- Closed an infographic safety gap where unsupported infographic model ids could previously fall through to the generic catalog helper and land on the assessment default.

Files Changed:
- apps/web/lib/server/ai/default-models.ts
- apps/web/app/api/assessment/route.ts
- apps/web/app/api/infographic/route.ts
- apps/web/app/(protected)/(completed)/assessment/page.tsx
- apps/web/app/(protected)/(completed)/infographic/page.tsx
- apps/web/components/assessment/assessment-studio.tsx
- apps/web/components/infographic/infographic-studio.tsx
- apps/web/lib/server/ai/execution.ts
- .env.example
- zootopia-club-next-ledger.txt

Env Keys Added:
- `ZOOTOPIA_DEFAULT_MODEL_ASSESSMENT`
- `ZOOTOPIA_DEFAULT_MODEL_INFOGRAPHIC`

Backend / Frontend Alignment Approach:
- Central resolver:
  - `apps/web/lib/server/ai/default-models.ts`
  - strict mapping:
    - assessment -> `ZOOTOPIA_DEFAULT_MODEL_ASSESSMENT`
    - infographic -> `ZOOTOPIA_DEFAULT_MODEL_INFOGRAPHIC`
- Backend remains source of truth:
  - assessment API now takes its `defaultModelId` from the central resolver before shared request validation
  - infographic API now takes its default from the same resolver, canonicalizes the submitted id, and rejects unsupported ids with `INFOGRAPHIC_MODEL_UNSUPPORTED`
  - infographic execution now re-checks model scope and falls back only to the infographic default, never the assessment default
- Frontend alignment:
  - assessment and infographic server pages resolve the default model on the server and pass `defaultModelId` into the client studios
  - client initial request state now seeds `modelId` from that server prop instead of `models[0]` / hardcoded literals

Fallback Behavior:
- Assessment fallback when env is missing/invalid/unsupported:
  - `gemini-3.1-flash-lite-preview`
- Infographic fallback when env is missing/invalid/unsupported or cross-tool:
  - `google-balanced`
- Cross-tool isolation:
  - assessment defaults cannot leak into infographic
  - infographic defaults cannot leak into assessment

Verification:
- Code-path verification:
  - assessment route still uses shared assessment validation with tool-scoped normalization/support checks, but now sources its default from the central resolver
  - infographic route now rejects unsupported model ids before execution
  - infographic execution has a second tool-scoped guard to prevent generic fallback into assessment defaults
- Resolver behavior probe (using `node --experimental-transform-types` against `packages/shared-config/src/models.ts` to mirror the resolver mapping logic):
  - no env values -> `{"assessment":"gemini-3.1-flash-lite-preview","infographic":"google-balanced"}`
  - valid env values (`assessment=gemini-2.5-pro`, `infographic=qwen-balanced`) ->
    `{"assessment":"gemini-2.5-pro","infographic":"qwen-balanced"}`
  - invalid / cross-tool env values (`assessment=not-a-model`, `infographic=gemini-2.5-pro`) ->
    `{"assessment":"gemini-3.1-flash-lite-preview","infographic":"google-balanced"}`
- Build/quality checks:
  - `npm run lint --workspace @zootopia/web`: pass
  - `npm run typecheck --workspace @zootopia/web`: pass
  - `npm run build --workspace @zootopia/web`: pass

Risks / Notes:
- This pass intentionally avoids auth, routing ownership, credit logic, storage behavior, and provider runtime env semantics.
- Direct standalone Node import of the new server resolver is blocked outside Next runtime by the `server-only` marker, so the env-resolution probe validated the same mapping logic against the shared model helpers instead of importing the server file directly.
- Existing unrelated working-tree changes from earlier tasks were left untouched.

---

2026-04-13 — Upload Page Light-Mode Airiness Pass + Clutter Removal (UI-Only)
==============================================================================
Type: UI polish / Upload route light-mode refinement / protected background visibility tuning
Scope: `/upload` hero shell, `UploadWorkspace` light surfaces, light-mode protected workspace veil

Summary:
- Applied a focused light-mode polish pass to the Upload page so the laboratory background reads through more clearly and the main intake surface feels more free-standing.
- Removed the page-level stacked decorative ghost cards behind the main Upload card and replaced them with a lighter ambient field that keeps depth without clutter.
- Corrected the light-mode card, dropzone, and CTA color treatment so Upload now aligns with the app’s existing premium light palette instead of carrying dark-heavy surfaces into light mode.
- Preserved the existing Upload workflow, auth boundary, storage behavior, and upload logic.

Files Changed:
- apps/web/app/(protected)/(completed)/upload/page.tsx
- apps/web/components/upload/upload-workspace.tsx
- apps/web/app/globals.css
- zootopia-club-next-ledger.txt

Visual Clutter Removed:
- Deleted the two rotated decorative slabs that previously sat behind the main Upload hero shell.
- Removed the stacked mini-document cards inside the dropzone and replaced them with a single centered upload glyph so the intake action reads as one clean focal surface.
- Kept all decorative depth layers clipped and non-interactive so smaller screens do not inherit overflow clutter.

Translucency / Depth Improvements:
- Upload hero shell now uses a lighter, theme-aware glass wrapper in light mode with restrained ambient highlights instead of a darker boxed-in container.
- Main Upload workspace shell, header, status panels, and active-document card now use brighter translucent glass fills, softer inner highlights, and lighter shadows in light mode.
- Dropzone now feels more free-standing through:
  - increased breathing room and responsive padding
  - softer edge contour lines
  - a subtle hover lift with cleaner architectural highlights
- Protected workspace light background tuning:
  - light background artwork opacity increased from `0.34` → `0.38`
  - light overlay veil density reduced (`0.10/0.20/0.44/0.64` → `0.08/0.16/0.36/0.56`)
  - light overlay blur reduced from `0.9px` → `0.72px`
  - light grid opacity reduced from `0.14` → `0.11`

Light-Mode Upload Color Corrections:
- `Document intake` header now uses a bright neutral-laboratory glass tint in light mode instead of a dark imported treatment.
- Quick-action cards on `/upload` now use light glass surfaces with dark readable text in light mode, while keeping the existing dark styling intact.
- Pending upload / recent upload accents were corrected to use stronger `accent-strong` treatment so icons, progress bars, and action buttons keep adequate contrast in light mode.
- Active-document continuation card now uses a pale glass + emerald/sky tint in light mode instead of a heavier dark mass.

Responsive Spacing Improvements:
- Upload hero wrapper padding was reduced slightly at the page shell level so the main workspace feels less trapped inside nested layers.
- `UploadWorkspace` body spacing shifted from a denser `space-y-8` / `p-6` pattern to a lighter responsive rhythm with smaller base padding and preserved larger-screen breathing room.
- Dropzone now uses responsive `min-height` and `px/py` values so mobile keeps enough room without forcing desktop into a cramped look.
- Decorative contour lines remain inside the dropzone bounds and do not create horizontal overflow.

Verification:
- `npm run lint --workspace @zootopia/web`: pass
- `npm run typecheck --workspace @zootopia/web`: pass
- `npm run build --workspace @zootopia/web`: pass
- Browser reachability probe:
  - launched a local production-style server at `http://127.0.0.1:3101`
  - checked `/upload` at mobile (`390x844`), tablet (`834x1194`), and desktop (`1440x1200`) using a local `puppeteer-core` script
  - all three viewports redirected to `/login`, confirming the protected-route gate but blocking a live authenticated Upload UI visual pass
- Responsive verification status:
  - live authenticated `/upload` verification remains pending because no reusable authenticated browser session was available in this environment
  - code-level responsive checks completed:
    - no new fixed-width layout was introduced
    - hero clutter layers were removed rather than added
    - dropzone uses responsive min-height and padding
    - Upload shell remains constrained to existing `max-w-3xl` workspace bounds

Risks / Notes:
- This pass intentionally avoided auth, routing, backend, storage, validation, and upload business logic.
- The protected workspace veil adjustment is shared across authenticated pages, but it was kept narrow and light-mode-only to minimize cross-route visual drift.
- The prior dark-mode Upload premium glass work remains intact; this pass specifically corrects light mode without redesigning the feature.

---

2026-04-13 — Auth Responsive Header Cleanup + Default Theme Env Fallback
=======================================================================
Type: Auth surface responsive polish / visual overlap cleanup / theme bootstrap default hardening
Scope: user login, admin login, confirm-email, public auth shell controls and branding, auth overlay tuning, env default theme fallback

Summary:
- Completed mandatory full-ledger read first, then ran a dedicated Explore subagent and direct source verification before edits.
- Applied a focused auth-only responsive cleanup pass with no auth/business/security flow rewrites.
- Replaced small-screen auth toggle groups with compact icon controls while preserving richer controls on larger breakpoints.
- Rebalanced login branding placement and shared auth lockup sizing to reduce crowding near top controls.
- Lightened auth overlays slightly so background media remains more visible without sacrificing card/input readability.
- Added env-backed initial theme fallback key `ZOOTOPIA_DEFAULT_THEME_MODE` with strict allowed values (`dark` | `light`) and fail-closed fallback to dark.

Files Changed:
- apps/web/components/auth/auth-top-controls.tsx
- apps/web/components/preferences/locale-toggle.tsx
- apps/web/app/(public)/login/page.tsx
- apps/web/app/(public)/admin/login/page.tsx
- apps/web/app/(public)/confirm-email/page.tsx
- apps/web/components/auth/public-auth-shell.tsx
- apps/web/app/globals.css
- apps/web/lib/preferences.ts
- apps/web/lib/server/request-context.ts
- .env.example
- zootopia-club-next-ledger.txt

Behavioral Changes:
- Small-screen auth controls:
  - User login, admin login, and confirm-email now use one icon button per control on mobile (`< md`).
  - Theme icon button is state-aware (light/dark/system icon changes with current selection).
  - Locale icon button is state-aware (icon changes with current locale selection).
  - On `md` and above, richer compact grouped toggles remain in place.
- University/faculty branding cleanup:
  - Student login faculty badge moved tighter to corner placement and reduced in visual weight to avoid crowding.
  - Shared auth lockup (`ZootopiaLockup`) in admin/confirm shell now uses responsive auth-specific sizing to reduce header pressure.
- Overlay/background balance:
  - Login page veil reduced from `bg-background/60` to `bg-background/52`.
  - Shared auth media panel and gradient overlays were tuned to slightly lower opacity in light and dark/system-dark variants.
- Confirm-email consistency:
  - Confirm-email now uses the same shared responsive control component and behavior as user/admin login surfaces.
- Env-backed default theme behavior:
  - Added `ZOOTOPIA_DEFAULT_THEME_MODE=dark` to `.env.example` with allowed-values guidance.
  - `getRequestThemeMode()` now resolves request fallback from env via strict dark/light resolver.
  - Cookie-selected user preference behavior remains unchanged; env value is only used when cookie is missing/invalid.

Verification:
- `shell: web-lint-post-polish`: pass
- `shell: web-typecheck-post-polish`: fail (pre-existing unrelated errors)
  - `apps/web/components/assessment/assessment-studio.tsx` type errors
  - `packages/shared-utils/src/validation.ts` type error
- `shell: web-build-post-polish`: fail (blocked by same pre-existing type errors above)
- IDE diagnostics on edited auth/theme files in this pass: no errors.

Risks / Notes:
- The workspace contains unrelated pre-existing changes and type failures outside auth surfaces; this pass did not alter those files.
- Auth/business/security logic ownership remains unchanged; this is a UI/layout/bootstrap-default pass only.

---

2026-04-13 — Assessment Active Type Reduction Finalization + Build Health Restore
===============================================================================
Type: Assessment contract hardening / frontend-to-backend canonical flow enforcement / compile-health restoration
Scope: temporary active question-type catalog, orchestration alignment, normalization/persistence metadata readiness, validation typing fixes

Summary:
- Completed mandatory full-ledger read first, then ran a dedicated Explore subagent and direct source verification before edits.
- Finalized the temporary assessment question-type reduction to the active canonical 8-type set across UI selection, request validation, prompt orchestration, provider normalization, and persistence/render metadata contracts.
- Preserved architecture for future dynamic viewer/export behavior by keeping canonical `type` + per-question `rendering` metadata in saved results.
- Fixed the previously failing typecheck/build blockers in assessment/shared-utils typing boundaries so the workspace verification gates pass again.

Active Canonical Types (temporary generation catalog):
- `mcq`
- `true_false`
- `definition`
- `terminology`
- `short_answer`
- `fill_blanks`
- `comparison`
- `scientific_term`

Temporarily Removed From Frontend Selection UI:
- `essay`
- `matching`
- `multiple_response`
- `labeling`
- `classification`
- `sequencing`
- `process_mechanism`
- `cause_effect`
- `distinguish_between`
- `identify_structure`
- `identify_compound`

Canonical Flow Enforcement (verified):
- Frontend selector uses `ASSESSMENT_ACTIVE_QUESTION_TYPES` and request sanitization to prevent stale UI leakage.
- Request validation rejects unsupported types and distribution mismatches with explicit field errors.
- Prompt orchestration includes selected canonical type ids and type-distribution directives in the model prompt.
- Execution normalization maps provider wording/synonyms into canonical type ids (including `scientific_term` aliases) before persistence.
- Saved question payload keeps stable `type` plus `rendering` metadata for dynamic type-aware rendering/export readiness.

Files Changed (this finalization pass):
- apps/web/components/assessment/assessment-studio.tsx
  - fixed question-type sanitizer typing to keep `AssessmentQuestionType[]` contract (prevents `string[]` widening).
- packages/shared-utils/src/validation.ts
  - fixed active-type includes narrowing to `AssessmentActiveQuestionType` so validation remains strict and type-safe.
- zootopia-club-next-ledger.txt
  - added this finalization record and updated current state headline.

Verified Existing Contract Files (no direct edit in this pass):
- apps/web/lib/server/ai/prompt-orchestrator.ts
- apps/web/lib/server/ai/execution.ts
- apps/web/lib/server/assessment-records.ts
- apps/web/lib/server/assessment-preview.ts
- packages/shared-types/src/assessment.ts
- apps/web/messages/en.json
- apps/web/messages/ar.json

Verification:
- `shell: web-lint-post-polish`: pass
- `shell: web-typecheck-post-polish`: pass
- `shell: web-build-post-polish`: pass

Risks / Notes:
- Legacy question-type rendering branches remain in viewer compatibility layers for historical/backward-readable records, while new generation/validation stays locked to the active 8-type set.
- Auth, credits, ownership, and admin boundary logic were not modified in this pass.

---

2026-04-13 — Metadata Persistence Audit Hardening (Uploads + Assessment Artifacts)
===============================================================================
Type: Persistence-contract hardening / queryability upgrade / backward-compatible metadata indexing
Scope: upload document metadata durability, assessment artifact metadata query surfaces, migration/index coverage, owner-isolated cleanup

Summary:
- Completed mandatory full-ledger read, subagent-first exploration, and direct source/migration verification before edits.
- Confirmed the active runtime persists assessment artifact metadata inside generation `artifacts` maps (durable but weak for global querying by owner/kind/expiry).
- Added a dedicated `assessmentArtifactMetadata` mirror persistence path so every saved generation write now keeps independently queryable artifact metadata rows.
- Kept backward compatibility by preserving existing generation `artifacts` payloads and adding mirror persistence only.
- Added upload metadata hardening so new document records persist normalized `fileExtension` and content digest `contentSha256`.

Files Changed:
- apps/web/lib/server/repository.ts
- apps/web/lib/server/document-runtime.ts
- packages/shared-types/src/document.ts
- packages/shared-types/src/admin.ts
- supabase/migrations/20260414153000_assessment_artifact_metadata_index_and_document_extension.sql
- zootopia-club-next-ledger.txt

Behavioral Changes:
- Assessment artifact metadata mirroring:
  - New collection name: `assessmentArtifactMetadata` (stored in `zc_entities`)
  - Write paths now sync mirror rows during:
    - `saveAssessmentGeneration(...)`
    - `saveAssessmentGenerationWithCreditCommit(...)` (same DB transaction path)
  - Expired-generation cleanup now removes both:
    - generation record
    - mirrored artifact metadata rows for that generation
  - Admin user deletion now removes mirrored artifact metadata rows and reports count in deletion summary.
- Upload metadata durability:
  - New `DocumentRecord` optional fields: `fileExtension`, `contentSha256`
  - New uploads persist both fields at creation time.
  - Document normalization backfills/sanitizes extension and digest values for read consistency.

Migration Added:
- `20260414153000_assessment_artifact_metadata_index_and_document_extension.sql`
  - Adds indexes for `assessmentArtifactMetadata` owner/generation, owner/kind/status, and expiresAt query lanes.
  - Backfills `assessmentArtifactMetadata` rows from existing `assessmentGenerations.body.artifacts` entries.
  - Backfills missing `documents.body.fileExtension` (derivable from `fileName`) and adds a file-extension index.

Owner Isolation Guarantees (verified in code paths):
- Mirror rows store canonical `ownerUid` from generation ownership.
- User-facing read/export routes remain owner-checked as before.
- Storage object reads/writes/deletes remain owner-scoped via `assertOwnerScopedStoragePath(...)`.
- Admin delete flow still freezes/revokes auth first, then performs owner-scoped data cleanup (including new metadata rows).

Verification:
- File diagnostics (`get_errors`) on edited files: no errors.
- `shell: web-lint-post-polish`: pass
- `shell: web-typecheck-post-polish`: pass
- `shell: web-build-post-polish`: pass
- `npm run supabase:check`: pass
  - migration ordering validated
  - migration list includes new `20260414153000...` file

Risks / Notes:
- Mirror indexing intentionally duplicates artifact metadata; generation `artifacts` remains authoritative for backward-compatible reads.
- No auth/session/provider/runtime ownership models were changed in this pass.

---

2026-04-13 — Assessment Setup Model Selector Compact Right-Rail Refinement
==========================================================================
Type: UI layout refinement / responsive compactness polish
Scope: assessment setup header/model control placement, shared assessment selector sizing behavior

Summary:
- Completed mandatory full-ledger read first, then ran an Explore subagent and direct source analysis before edits.
- Moved the Assessment model selector from the mid-form field grid into a dedicated setup-header control lane so it sits in the upper-right area of the main setup card.
- Kept all existing model selection logic, state handling, and validation wiring unchanged; this pass is presentation/layout only.
- Added scoped responsive styling so the selector is compact and horizontal on tablet/desktop, while preserving clean vertical stacking on mobile.

Files Changed:
- apps/web/components/assessment/assessment-studio.tsx
- apps/web/app/globals.css
- zootopia-club-next-ledger.txt

Behavioral Changes:
- Setup header now uses a dedicated `assessment-setup-top-row` layout container.
- Model selector now renders inside `assessment-model-control` in the header-right lane instead of the form grid.
- Model meta chips now render directly under the moved selector within the same compact control area.
- Remaining setup field grid now focuses on question count, difficulty, and language (`xl:grid-cols-3`), reducing perceived vertical heaviness.
- Shared CSS now applies compact trigger/icon sizing for the header model control at `min-width: 768px` and keeps full-width stack behavior on mobile.
- Added dark and system-dark theme variants for the new compact model control surface so premium styling remains consistent.

Verification:
- `shell: web-lint-post-polish`: pass
- `shell: web-typecheck-post-polish`: pass
- `shell: web-build-post-polish`: pass

Risks / Notes:
- This pass intentionally avoids backend/model catalog/auth/session/credit logic and only adjusts Assessment setup presentation.
- A transient JSX merge corruption occurred during the first refactor attempt in `assessment-studio.tsx`; it was repaired immediately, and all gates passed afterward.

---

2026-04-13 — Upload Single-Shell Translucent Container Cleanup (UI-Only)
=========================================================================
Type: UI visual cleanup / layered backdrop simplification / theme-aware translucency tuning
Scope: upload hero wrapper backdrop ownership, upload workspace shell backdrop layering

Summary:
- Completed mandatory full-ledger read first, then ran an Explore subagent and direct source analysis before edits.
- Removed the extra Upload page backdrop overlays that were creating a stacked multi-panel effect behind the main Document intake surface.
- Kept UploadWorkspace as the single translucent container and tuned its glass opacity/blur to reveal the lab background slightly while preserving readability.
- Preserved all upload/runtime behavior: no changes to routes, auth, storage, APIs, or business logic.

Files Changed:
- apps/web/app/(protected)/(completed)/upload/page.tsx
- apps/web/components/upload/upload-workspace.tsx
- zootopia-club-next-ledger.txt

Behavioral Changes:
- Upload page hero wrapper now stays visually neutral (layout wrapper only) instead of rendering extra decorative ambient overlays behind intake.
- Removed the page-level `aria-hidden` ambient radial/noise overlay block behind the Upload intake area.
- Removed UploadWorkspace shell-level decorative glow overlays so the intake stack no longer reads as layered ghost cards.
- Retained one main premium translucent shell (`uploadWorkspaceShellClassName`) and slightly rebalanced light/dark translucency + blur to keep background visibility subtle and text contrast strong.
- Preserved responsive structure (`min-w-0`, existing spacing and breakpoints) with no new wrappers or layout ownership changes.

Verification:
- `shell: web-lint-post-polish`: pass
- `shell: web-typecheck-post-polish`: pass
- `shell: web-build-post-polish`: pass

Risks / Notes:
- This cleanup intentionally avoids shared protected background ownership in `globals.css` / `ProtectedWorkspaceBackground`; those layers remain global across all protected routes.
- Dropzone and active-document internal visual accents remain intact because they are inside the single intake container and do not recreate the removed stacked backdrop slabs.

---

2026-04-13 — Assessment Credit Hardening (Renewal Window + Fresh-State Sync)
=============================================================================
Type: Credit lifecycle hardening / config flexibility / UI-state synchronization
Scope: assessment credit renewal-window resolver, credit summary cache policy, protected-shell refresh cadence, localized exhausted-credit messaging

Summary:
- Completed mandatory full-ledger read and source-first verification of reserve/release/commit credit lifecycle before edits.
- Confirmed success-only consumption semantics already existed in route/repository flow:
  reserve before generation, release on execution failure/finalization failure, commit on durable save only.
- Implemented an env-configurable UTC credit renewal interval with strict guardrails and a safe 24-hour default.
- Hardened credit-state freshness so protected-shell balances update more reliably after admin-side changes.
- Updated exhausted-credit copy to premium-aware wording (daily + extra credits) and localized reset-window language.

Files Changed:
- apps/web/lib/server/assessment-daily-credits.ts
- apps/web/app/api/assessment/credits/route.ts
- apps/web/components/layout/protected-shell.tsx
- apps/web/components/assessment/assessment-studio.tsx
- apps/web/lib/server/repository.ts
- apps/web/messages/en.json
- apps/web/messages/ar.json
- .env.example
- zootopia-club-next-ledger.txt

Behavioral Changes:
- Renewal window config:
  - Added `ZOOTOPIA_ASSESSMENT_CREDIT_RENEWAL_WINDOW_HOURS` (1..168, default 24)
  - 24-hour mode preserves existing daily key format (`YYYY-MM-DD`) for backward compatibility
  - Non-24h windows use deterministic UTC key format (`utc-{hours}h-{windowStartMs}`)
  - Reset timestamp parsing now supports both legacy daily keys and new interval keys
- Freshness hardening:
  - `GET /api/assessment/credits` now uses explicit no-store headers via `applyNoStore(...)`
  - Protected shell balance refresh now runs on:
    - `zootopia:assessment-credit-refresh` event
    - browser focus
    - document visibility return
    - 60-second periodic sync
  - Assessment Studio now dispatches credit-refresh for `ASSESSMENT_ACCESS_DISABLED`
    and `ASSESSMENT_FINALIZATION_FAILED` responses in addition to exhausted-credit responses
- Messaging:
  - Exhausted-credit EN/AR text now states daily + extra credits are depleted
  - Renewal copy now references the next reset window (not hardcoded "tomorrow")
  - Backend exhausted-credit error message updated to match reset-window semantics

Verification:
- `shell: web-lint-post-polish`: pass
- `shell: web-typecheck-post-polish`: pass
- `shell: web-build-post-polish`: pass

Risks / Notes:
- UI labels still use existing "daily" terminology for continuity; behavior remains identical in the default 24h configuration.
- No auth boundary, route ownership, or credit commit authority was moved client-side.
- Existing unrelated working-tree changes were left untouched.

---

2026-04-13 — Admin User Credit Mutation Controls + Durable Per-User Audit Ledger
===============================================================================
Type: Admin governance hardening / per-user credit mutation auditability / server-action management UI
Scope: admin user detail credit controls, repository mutation persistence, admin credit state contracts, deletion summary telemetry

Summary:
- Completed analysis-first implementation for server-authoritative credit mutation controls directly on `/admin/users/[uid]`.
- Added durable per-user mutation-history persistence with before/after balance snapshots for every admin credit mutation.
- Added admin UI ledger rendering so operators can inspect recent credit mutations without querying backend logs.
- Preserved backend authority: all mutations flow through repository APIs; browser remains display + form submission only.

Files Changed:
- packages/shared-types/src/admin.ts
- apps/web/lib/server/repository.ts
- apps/web/app/(protected)/admin/users/[uid]/page.tsx
- apps/web/app/api/admin/users/[uid]/credits/route.ts
- apps/web/app/api/admin/users/[uid]/route.ts
- zootopia-club-next-ledger.txt

Behavioral Changes:
- Shared contracts:
  - `AdminAssessmentCreditState` now includes `history`.
  - Added `AdminAssessmentCreditMutationRecord` and `AdminAssessmentCreditMutationBalanceSnapshot`.
  - Deletion summary now tracks `deletedCreditMutationHistory`.
- Repository credit mutation flow:
  - New collection key: `assessmentCreditMutationHistory`.
  - Each mutation now computes and stores before/after snapshots in the same authoritative mutation path.
  - Mutation execution is consolidated through `applyAssessmentCreditMutationToState(...)` for both Postgres and memory fallback lanes.
  - `getAdminAssessmentCreditStateForUser(...)` now returns latest mutation history (bounded list).
- Admin user detail UI:
  - Added server-action forms for access toggle, daily override set/clear, manual add/subtract/set, grant create, and grant revoke.
  - Added success/error feedback mapping for mutation outcomes.
  - Added Credit Grants and Credit Mutation History panels with readable before/after balance context.
- Admin APIs:
  - Credits PATCH logging metadata now includes `reason` and `note`.
  - User deletion API failure payload now includes `deletion.deletedCreditMutationHistory`.

Verification:
- `shell: web-lint-post-polish`: pass
- `shell: web-typecheck-post-polish`: pass
- `shell: web-build-post-polish`: pass
- File diagnostics on edited admin files: no errors.

Risks / Notes:
- Mutation history intentionally returns a bounded recent list in admin state responses for UI stability.
- Admin self-mutation guard remains enforced (`ASSESSMENT_CREDIT_SELF_MUTATION_FORBIDDEN`).
- This pass intentionally avoids widening any client authority; all credit writes remain backend-owned.

---

2026-04-13 — Compact Role/Gender Identity Avatars (Header + Settings)
======================================================================
Type: UI identity hardening / avatar policy unification / compact layout-preserving polish
Scope: protected shell header identity badge, settings hero identity chip, shared avatar resolver

Summary:
- Implemented one shared avatar-resolution policy for authenticated identity surfaces.
- Enforced deterministic precedence: `admin` role asset first, then managed `gender` asset, then initial fallback for missing/unknown gender.
- Kept both surfaces compact so the protected shell and settings hero maintain their existing spacing rhythm.
- Restricted role/gender avatars to local public assets to avoid remote image dependency drift.

Files Changed:
- apps/web/lib/avatar.ts
- apps/web/components/layout/protected-shell.tsx
- apps/web/app/(protected)/settings/page.tsx
- zootopia-club-next-ledger.txt

Behavioral Changes:
- Added shared helper policy in `apps/web/lib/avatar.ts`:
  - `admin` -> `/admin-avatar.png`
  - `female` -> `/female-avatar.png`
  - `male` -> `/male-avatar.png`
  - missing/unknown `gender` -> `null` (forces compact initial fallback)
- Header user badge (`ProtectedShell`) now uses role/gender local asset rendering with the same compact `h-7 w-7` footprint and existing verification glyph.
- Settings hero now includes a compact identity chip that mirrors the same avatar policy and fallback behavior, with a truncated display-name label to protect responsive density.

Verification:
- `shell: web-lint-post-polish`: pass
- `shell: web-typecheck-post-polish`: pass
- `shell: web-build-post-polish`: pass

Risks / Notes:
- Unknown or unset gender intentionally falls back to initials (not a gendered avatar) to avoid incorrect assignment.
- This pass intentionally avoided auth/session/repository changes; behavior is presentation-layer only.
- Existing unrelated working-tree changes were left untouched.

---

2026-04-13 — Locale Default Bootstrap + Home/Assessment/Auth Visual Stability Polish
====================================================================================
Type: Preference bootstrap hardening / layout polish / translucency and stacking refinement
Scope: request locale bootstrap, env template, home hero/stat composition, assessment surface translucency, public auth overlap/overlay tuning

Summary:
- Completed mandatory ledger-first analysis and source tracing before edits.
- Added strict env-driven first-load locale fallback with cookie-first precedence.
- Moved Home stat counters from the hero header into a lower snapshot section to keep the title lane uncluttered.
- Slightly increased Assessment container translucency while keeping field/control contrast intact.
- Applied targeted public-auth visual stability guards for overlap, stacking, overlay density, and heavy-shadow balance.

Files Changed:
- apps/web/lib/preferences.ts
- apps/web/lib/server/request-context.ts
- .env.example
- apps/web/app/(protected)/(completed)/page.tsx
- apps/web/app/(protected)/(completed)/assessment/page.tsx
- apps/web/app/(public)/login/page.tsx
- apps/web/components/auth/auth-top-controls.tsx
- apps/web/components/auth/login-panel.tsx
- apps/web/components/auth/admin-login-panel.tsx
- apps/web/components/auth/confirm-email-panel.tsx
- apps/web/app/globals.css
- zootopia-club-next-ledger.txt

Behavioral Changes:
- Locale bootstrap:
  - Added `resolveInitialLocale(...)` and strict `en|ar` validation with fail-closed `en` fallback.
  - `getRequestLocale()` now keeps locale-cookie authority and uses `ZOOTOPIA_DEFAULT_LANGUAGE` only when cookie is missing/invalid.
  - Added `ZOOTOPIA_DEFAULT_LANGUAGE=en` guidance to `.env.example`.
- Home composition:
  - Extracted stat card data into a shared `homeStats` map.
  - Removed top-right stat cards from the hero header.
  - Added a dedicated lower "Workspace Snapshot" stats section with responsive 1/2/3-column behavior.
- Assessment translucency:
  - Increased transparency in assessment hero gradient backgrounds (light/dark).
  - Increased transparency in `.assessment-premium-panel` (light/dark/system-dark) without changing field focus/readability behavior.
- Public auth stability polish:
  - Login page now reserves top-rail width against the decorative badge and adds a dedicated top offset so absolute controls do not overlap centered content.
  - `AuthTopControls` now wraps when width is constrained.
  - Auth shell header and media panel now enforce clearer stacking (`z-index` guard + isolated media context).
  - Media overlays were slightly softened and made pointer-event transparent.
  - Login/admin/confirm cards now use isolated card contexts with slightly lighter shadow density.

Verification:
- `shell: web-lint-post-polish`: pass
- `shell: web-typecheck-post-polish`: pass
- `shell: web-build-post-polish`: pass
- Edited-file diagnostics: no errors.

Risks / Notes:
- No auth/session/provider authority changes were introduced; this pass is preference bootstrap + presentation-layer only.
- Locale cookies still remain the primary persisted preference source.
- Existing unrelated working-tree changes were left untouched.

---

2026-04-14 — Assessment Finalization Durability Hardening (Artifact Degradation + Transaction Await Safety)
===========================================================================================================
Type: Backend reliability hardening / finalization durability / transaction determinism
Scope: assessment create route finalization path, repository transaction-backed assessment/idempotency/credit helpers

Summary:
- Completed ledger-first and source-first analysis for `/api/assessment` finalization failures that surface as `ASSESSMENT_FINALIZATION_FAILED`.
- Hardened the finalization boundary so canonical-result artifact upload failures no longer discard a successful model generation.
- Removed non-awaited transaction mutations in assessment-critical repository paths so commit-time failures surface deterministically.
- Serialized artifact-metadata mirror writes/deletes when running inside a transaction to avoid floating mutation promises during one commit scope.

Root Cause (code-verified):
- `/api/assessment` grouped canonical artifact persistence and durable generation+credit commit in one finalization `try` block. Any transient storage upload failure (before durable save) triggered the same 500 finalization response path.
- Assessment/idempotency/credit transaction paths contained multiple fire-and-forget `transaction.set(...)` / `transaction.delete(...)` calls plus transaction-scoped metadata sync fan-out via `Promise.all(...)`, reducing deterministic error propagation during finalization.

Files Changed:
- apps/web/app/api/assessment/route.ts
- apps/web/lib/server/repository.ts
- zootopia-club-next-ledger.txt

Behavioral Changes:
- Assessment create route (`/api/assessment`):
  - Canonical result artifact persistence is now best-effort.
  - If artifact upload fails, finalization continues with durable DB save + credit commit.
  - Success log metadata now records:
    - `canonicalResultArtifactPersisted`
    - `artifactPersistenceDegraded`
- Repository transaction safety:
  - Added explicit `await` to transaction mutations in assessment/idempotency credit lifecycle paths:
    - `beginAssessmentGenerationIdempotency(...)`
    - `completeAssessmentGenerationIdempotency(...)`
    - `clearAssessmentGenerationIdempotencyLock(...)`
    - `reserveAssessmentDailyCreditAttempt(...)`
    - `releaseAssessmentDailyCreditReservation(...)`
    - `saveAssessmentGenerationWithCreditCommit(...)`
  - Transaction-scoped artifact metadata sync/delete now executes sequential writes when a transaction object is provided.

Verification:
- `shell: web-lint-post-polish`: pass
- `shell: web-typecheck-post-polish`: pass
- `shell: web-build-post-polish`: pass
- File diagnostics on edited files: no errors
- Attempted runtime QA command:
  - `shell: assessment-actions-auth-qa`: failed in local workspace because `.tmp/assessment-actions-auth-qa.mjs` imports `firebase-admin` which is not installed in this environment (`ERR_MODULE_NOT_FOUND`).

Risks / Notes:
- Canonical-result artifact cache can now be absent when storage upload fails, but durable generation records remain saved and export routes can still regenerate output from persisted generation payloads.
- Auth boundaries, owner-scope enforcement, and reserve/release/commit credit authority remain backend-owned.

---

2026-04-14 — Protected Workspace Translucency Normalization + Exhausted-Credit Escalation Copy (EN/AR)
=======================================================================================================
Type: UI polish / localization refinement / protected-surface consistency
Scope: protected page wrappers and shared surface translucency, exhausted-credit user messaging in active locale

Summary:
- Completed mandatory ledger-first and source-first analysis with an Explore subagent before edits.
- Applied a subtle protected-surface translucency pass across shared background/surface rules and page-level wrappers so the workspace backdrop breathes through more consistently.
- Preserved readability by keeping input, selector, and action controls relatively solid while reducing wrapper/card opacity only.
- Added localized exhausted-credit support/escalation wording in English and Arabic, with a secondary supportive line shown only in the exhausted-credit state.
- Preserved auth/credit/admin enforcement logic; this pass is visual and localized copy only.

Files Changed:
- apps/web/app/globals.css
- apps/web/app/(protected)/(completed)/page.tsx
- apps/web/app/(protected)/(completed)/assessment/page.tsx
- apps/web/app/(protected)/(completed)/infographic/page.tsx
- apps/web/app/(protected)/(completed)/upload/page.tsx
- apps/web/app/(protected)/(completed)/history/page.tsx
- apps/web/app/(protected)/(completed)/assessment/preview/[id]/page.tsx
- apps/web/app/(protected)/(completed)/assessment/results/[id]/page.tsx
- apps/web/components/upload/upload-workspace.tsx
- apps/web/components/infographic/infographic-studio.tsx
- apps/web/app/(protected)/settings/page.tsx
- apps/web/components/settings/profile-settings-form.tsx
- apps/web/components/assessment/assessment-studio.tsx
- apps/web/messages/en.json
- apps/web/messages/ar.json
- zootopia-club-next-ledger.txt

Behavioral Changes:
- Shared protected background and surfaces:
  - Increased protected background image visibility (`0.38 -> 0.42`) in light/dark/system variants.
  - Reduced protected overlay veil density in light/dark/system variants.
  - Updated `.surface-strong` to a slightly more translucent glass treatment with restrained blur.
  - Reduced `assessment-premium-panel` alpha values in light/dark/system variants.
- Protected pages:
  - Home, Assessment, Infographic, Settings, Upload, and History wrappers/cards now use slightly lower fill opacity for a calmer unified glass language.
  - Assessment preview/result unavailable fallback cards now match the lighter protected surface tone.
- Exhausted-credit messaging:
  - EN/AR `assessmentDailyCreditsExhaustedBody` now includes escalation guidance.
  - Added EN/AR key: `assessmentDailyCreditsExhaustedSupportNote`.
  - Assessment Studio credits panel now renders the support note as a secondary line only when credits are exhausted.

Verification:
- `shell: web-lint-post-polish`: pass
- `shell: web-typecheck-post-polish`: pass
- `shell: web-build-post-polish`: pass
- Source-level localization verification:
  - EN/AR exhausted-credit body + support-note keys confirmed in message files.
  - Exhausted-only secondary-line rendering confirmed in Assessment Studio source.
- Source-level translucency verification:
  - Updated wrapper/surface classes confirmed across Home, Assessment, Infographic, Settings, Upload, History, and shared globals.

Risks / Notes:
- Shared global protected-surface tuning (`globals.css`) affects all protected routes, including admin surfaces that depend on the same background/surface system.
- This pass intentionally avoids making form fields more transparent; wrappers were tuned while field/control readability remains prioritized.

---

2026-04-14 — Home Mobile Spacing Tightening + Subtle Fold-Discovery Cue (UI-Only)
==============================================================================
Type: UI polish / responsive spacing refinement / discoverability cue enhancement
Scope: protected Home page spacing rhythm on mobile and scroll-discovery hint presentation

Summary:
- Completed mandatory ledger-first and source-first analysis with an Explore subagent before edits.
- Applied a surgical Home-only spacing pass that tightens small-screen padding/gaps by one step while preserving existing desktop/tablet spacing and route behavior.
- Refined the existing scroll hint into a cleaner fold-discovery cue with gentler motion, mobile-centered placement, and reduced-motion support.
- Preserved auth, repository, API, credit, and database logic ownership; this pass is presentation-only.

Files Changed:
- apps/web/app/(protected)/(completed)/page.tsx
- zootopia-club-next-ledger.txt

Behavioral Changes:
- Home mobile spacing:
  - top-level section rhythm changed from `space-y-6` to `space-y-5` on mobile (`md` unchanged)
  - key hero/content paddings reduced by one step (`6 -> 5`) on mobile only
  - recent-activity cards and upload CTA now use slightly tighter mobile padding while preserving `md:p-8`
- Fold-discovery cue:
  - status row now stacks on mobile and keeps previous row behavior on `md+`
  - scroll cue is centered on mobile for clearer near-fold discoverability
  - cue now uses a gentler bounce cadence (`1.8s`) with `motion-reduce:animate-none`
  - cue includes localized accessibility labeling for EN/AR screen-reader context

Verification:
- `shell: web-lint-post-polish`: pass
- `shell: web-typecheck-post-polish`: pass
- `shell: web-build-post-polish`: pass
- Edited-file diagnostics: no errors.

Risks / Notes:
- This pass intentionally avoids shared/global CSS token rewrites to keep changes isolated to Home route composition classes.
- Visual behavior is now slightly denser on very small screens by design; desktop spacing and content ownership remain unchanged.

---

2026-04-14 — Journey Modal Safety Hardening + Journey Light-Mode Visual Coherence (UI-Only)
=============================================================================================
Type: UI/UX hardening / modal safety / light-mode readability polish
Scope: Home roadmap preview modal opened by Journey CTA + Journey public route light-mode surfaces

Summary:
- Completed mandatory ledger-first and source-first analysis with an Explore subagent before edits.
- Traced the user-reported image preview path and confirmed it is rendered by `PlatformStoryCta` on Home (not inside `/journey` route ownership).
- Hardened the roadmap image modal container to stay centered, viewport-contained, and scroll-safe with a clear always-visible close control.
- Polished Journey route light-mode text/surface classes to remove low-contrast dark-biased styling while preserving dark mode via explicit `dark:` variants.
- Preserved auth, business, API, and database logic; this pass is visual and interaction safety only.

Root Cause (code-verified):
- Home roadmap preview modal used dark-leaning hardcoded surfaces/controls (`bg-slate-950/90`, white-on-dark close/action styles), which produced weak readability and mismatched look in light mode.
- Modal composition relied on absolute close placement + fixed dark shell accents without theme-aware defaults, increasing risk of overlap/collision on smaller viewports.
- Journey public route used many hardcoded dark-tone text/surface classes (`text-white`, `text-emerald-100`, `bg-slate-950/...`) that degraded daytime contrast coherence.

Files Changed:
- apps/web/components/home/platform-story-cta.tsx
- apps/web/app/globals.css
- apps/web/app/(site)/journey/page.tsx
- zootopia-club-next-ledger.txt

Behavioral Changes:
- Roadmap preview modal hardening (Home CTA path):
  - modal wrapper now centers on all breakpoints with viewport edge padding
  - panel now enforces viewport-safe max-height and internal scroll lane
  - close button now has a dedicated class with logical corner anchoring (`inset-inline-end`) for RTL/LTR-safe placement
  - image shell keeps responsive max-height containment and no uncontrolled overflow
  - Journey action button inside modal now uses theme-aware CSS variant styling (light + dark/system-dark)
- Modal visual polish:
  - default (light) backdrop/shell/image-surface styles now align with warm daytime palette
  - dark/system-dark overrides preserve existing premium dark visual language
  - panel isolation + scroll containment reduces layered collisions and clipping risk
- Journey route light-mode polish:
  - replaced dark-biased text/surface classes with theme-aware foreground/background classes plus explicit `dark:` fallbacks
  - harmonized hero blocks, milestone cards, chips, callouts, and CTA side panel for coherent daytime readability
  - retained dark-mode visual intent with scoped `dark:` classes

Verification:
- `shell: web-lint-post-polish`: pass
- `shell: web-typecheck-post-polish`: pass
- `shell: web-build-post-polish`: pass
- Runtime QA environment:
  - standalone production server run on `http://127.0.0.1:3020` via `node apps/web/.next/standalone/server.js`
  - health probe passed: `/api/health` returned `ok=true`
- Automated viewport QA report:
  - local temporary Puppeteer report generated during this pass (Journey light mode + Home modal access checks)
  - Journey route (`/journey`) validated across narrow-mobile/tablet/laptop/desktop with no horizontal overflow
  - Home modal live-open verification was auth-gated in this environment (`/` redirected to `/login` in all tested viewports), so modal open/close geometry was validated by source-level interaction path and layout constraints on edited code

Risks / Notes:
- Protected Home modal runtime interaction remains dependent on authenticated session availability for full in-browser E2E confirmation.
- This pass intentionally avoids introducing new dialog libraries and keeps bundle/runtime impact minimal by hardening existing modal primitives.

---

2026-04-14 — Upload Hero Single-Surface Depth Refactor (UI-Only)
=================================================================
Type: UI/UX refinement / visual hierarchy simplification / responsive depth polish
Scope: `/upload` hero shell ownership and intake surface layering

Summary:
- Completed mandatory ledger-first, docs check, subagent-first exploration, and direct source verification before edits.
- Refactored `/upload` hero to present one authoritative premium surface by removing the redundant route-level title pill and flattening the nested header/body shell split.
- Added a restrained upper-left directional depth reaction on the main upload shell hover to simulate pushing deeper into the room without heavy motion.
- Softened inner dropzone and active-document decorative glow intensity so the page reads as one cohesive container rather than stacked slabs.
- Preserved upload workflow, auth gates, API contracts, storage, and business logic.

Root Cause (code-verified):
- Upload route rendered a decorative title capsule above the intake shell while `UploadWorkspace` rendered separate high-contrast header/body tint blocks plus strong nested glows.
- Combined layers produced a card-within-card impression that reduced clarity of the single primary action surface.

Files Changed:
- apps/web/components/upload/upload-workspace.tsx
- apps/web/app/(protected)/(completed)/upload/page.tsx
- zootopia-club-next-ledger.txt

Behavioral Changes:
- Upload hero title pill was removed from the route page so `UploadWorkspace` remains the single focal surface.
- Main upload shell now includes a subtle upper-left depth illusion:
  - micro hover shift (`translate-x`/`translate-y`)
  - directional inset + outer shadow blend
  - restrained motion with `motion-reduce` safeguards
- Header/body backgrounds inside `UploadWorkspace` were unified into one shell-owned surface (`z-10` content over a shared shell depth layer).
- Dropzone and active-document accent glows were reduced to keep visual hierarchy cleaner while preserving affordance and readability.
- All decorative depth layers remain non-interactive (`pointer-events-none`) and behind actionable content.

Verification:
- `shell: web-lint-post-polish`: pass
- `shell: web-typecheck-post-polish`: pass
- `shell: web-build-post-polish`: pass
- Edited-file diagnostics:
  - apps/web/components/upload/upload-workspace.tsx: no errors
  - apps/web/app/(protected)/(completed)/upload/page.tsx: no errors

Risks / Notes:
- This pass intentionally avoids shared protected background/global layout ownership and keeps changes confined to `/upload` route UI composition.
- Live authenticated viewport QA was not executed in this pass; responsive safety is preserved by existing `max-w-3xl`, `min-w-0`, and prior breakpoint guards.

---

2026-04-14 — Assessment Model-ID Refresh + Gemini 2.5 Flash-Lite Default
==========================================================================
Type: Model catalog correction / default-model alignment / legacy compatibility hardening
Scope: assessment model catalog, assessment runtime canonicalization, env template default

Summary:
- Completed mandatory ledger-first analysis and source validation before edits.
- Corrected the assessment model catalog so `gemini-2.5-flash-lite` is a real active assessment lane instead of a legacy-only id.
- Set `gemini-2.5-flash-lite` as the built-in assessment default while keeping `gemini-3.1-flash-lite-preview` active.
- Preserved backward compatibility by canonicalizing legacy `gemini-2.5-flash` requests into `gemini-2.5-flash-lite`.

Root Cause (code-verified):
- Shared catalog marked `gemini-2.5-flash-lite` as legacy (`toolScopes: []`) and aliased it to `gemini-3.1-flash-lite-preview`.
- Assessment runtime canonicalization in `execution.ts` mirrored this aliasing, preventing true lane selection for `gemini-2.5-flash-lite`.

Files Changed:
- packages/shared-config/src/models.ts
- apps/web/lib/server/ai/execution.ts
- .env.example
- zootopia-club-next-ledger.txt

Behavioral Changes:
- `packages/shared-config/src/models.ts`:
  - assessment default order now starts with `gemini-2.5-flash-lite`
  - `gemini-2.5-flash-lite` promoted to active assessment lane
  - `gemini-2.5-flash` downgraded to legacy metadata-only id (`toolScopes: []`)
  - legacy alias map now rewrites `gemini-2.5-flash` -> `gemini-2.5-flash-lite`
- `apps/web/lib/server/ai/execution.ts`:
  - runtime canonicalization now rewrites `gemini-2.5-flash` -> `gemini-2.5-flash-lite`
  - provider dispatch now includes `gemini-2.5-flash-lite` as a live Google execution lane
- `.env.example`:
  - `ZOOTOPIA_DEFAULT_MODEL_ASSESSMENT` updated to `gemini-2.5-flash-lite`

Verification:
- Runtime catalog probe:
  - `default=gemini-2.5-flash-lite`
  - `active-25-lite=gemini-2.5-flash-lite`
  - `active-31-lite=gemini-3.1-flash-lite-preview`
  - `legacy-25-flash->gemini-2.5-flash-lite`
- `shell: web-lint-post-polish`: pass (2 warnings, 0 errors; existing warnings in `apps/web/components/upload/upload-workspace.tsx`)
- `shell: web-typecheck-post-polish`: pass
- `shell: web-build-post-polish`: pass

Risks / Notes:
- This pass intentionally avoids changing infographic defaults or provider env override contracts (`GOOGLE_AI_MODEL`, `GOOGLE_AI_ADVANCED_MODEL`, `QWEN_MODEL`).
- Legacy record readability remains intact via preserved legacy descriptors and canonicalization guards.

---

2026-04-14 — Assessment PDF Body Content Recovery (Pro + Fast Lanes)
====================================================================
Type: Export pipeline bug fix / Vercel serverless compatibility hardening / artifact cache resilience
Scope: assessment PDF export lanes, shared print renderer diagnostics, Vercel Pro browser launch path

Summary:
- Completed mandatory ledger-first review, codebase-first tracing, Next.js docs verification, and subagent exploration before edits.
- Verified from production-backed assessment data that the stored generation record, canonical result artifact, and cached print HTML still contained the real assessment questions and answers.
- Isolated the content-loss symptom to export rendering/caching rather than preview/result data loading or owner-scope retrieval.
- Fixed the Pro Vercel browser lane by removing the `@sparticuz/chromium.font()` call that is associated with upstream serverless PDF/text disappearance.
- Added shared render diagnostics plus Fast-lane cache guards so shell-only HTML artifacts are not silently reused or persisted.
- Bumped the shared print layout version so both Pro and Fast lanes regenerate stale export artifacts instead of continuing to serve previously bad output.

Root Cause (code-verified):
- Upstream assessment content was healthy:
  - live generation record still had normalized `questions`
  - canonical result artifact still had full body content
  - cached `export-print-html` artifact still contained question titles and answer cards
- The Pro lane still used `chromium.font()` inside the Linux/Vercel browser path. Upstream `@sparticuz/chromium` maintainers later removed that API after reports that invoking it could make PDF/screenshot text disappear in serverless rendering.
- Once a malformed export artifact existed, the export lanes had no body-marker validation before reuse. That meant stale bad artifacts could survive even though the live generation record still had the full assessment body.
- No auth/session/owner-scope bug was found. `buildAssessmentExportRouteContext()` still loads owner-scoped generations directly and `buildAssessmentPreview()` still derives preview/export content from the normalized generation record.

Shared Failure Point Between Pro and Fast:
- Shared print/export caching trusted prior artifacts too early and did not record whether the rendered export surface actually contained question/answer body markers.
- Pro additionally had a Vercel/serverless Chromium regression vector via `chromium.font()`.
- Fast did not show a code-verified upstream data-loss bug in the renderer source, so the durable fix there is:
  - invalidate stale cached HTML
  - log render diagnostics
  - refuse to reuse/persist shell-only HTML when live question data exists

Files Changed:
- apps/web/lib/server/assessment-pdf-download.ts
- apps/web/lib/server/assessment-print-renderer.ts
- apps/web/lib/server/assessment-pro-pdf-export.ts
- apps/web/lib/server/assessment-fast-pdf-export.ts
- apps/web/app/api/assessment/export/pdf/fast/[id]/route.ts
- zootopia-club-next-ledger.txt

Behavioral Changes:
- `assessment-pdf-download.ts`
  - removed serverless `chromium.font()` usage from the Pro Vercel lane
  - preserved local Chrome/Edge override behavior for development
  - preserved packaged Chromium usage for Linux serverless runtimes
- `assessment-print-renderer.ts`
  - bumped `ASSESSMENT_PRINT_LAYOUT_VERSION` to `2026-04-14-pdf-content-guard-v24`
  - added shared `buildAssessmentPrintRenderDiagnostics()` helper
  - diagnostics now report:
    - content source
    - question count
    - question page count
    - section count
    - metadata/composition badge counts
    - HTML question-title / answer-card marker counts when HTML is available
- `assessment-pro-pdf-export.ts`
  - logs structured render diagnostics before PDF capture
  - classifies render-shape failures separately from HTML render and PDF buffer failures
  - regenerates under the new Pro layout version so stale malformed PDF artifacts are bypassed
- `assessment-fast-pdf-export.ts`
  - now logs structured lane-specific diagnostics
  - wraps artifact load / QR build / HTML render / artifact store / generation save / audit-log failures
  - validates cached/generated HTML body markers before reuse/persist
  - regenerates under the new Fast layout version so stale HTML artifacts are bypassed
- `apps/web/app/api/assessment/export/pdf/fast/[id]/route.ts`
  - now emits structured failure details instead of raw error dumping

Vercel-Specific Fix:
- The Pro serverless PDF lane no longer calls `chromium.font()` before launching packaged Chromium.
- This keeps the Vercel/Linux browser path aligned with the current upstream guidance trend away from that API after serverless text-disappearance reports.
- Existing Chromium binary tracing remains intact; the built Pro route trace still includes:
  - `al2023.tar.br`
  - `chromium.br`
  - `fonts.tar.br`
  - `swiftshader.tar.br`

Verification:
- `npm run lint`: pass
  - 2 pre-existing warnings remain in `apps/web/components/upload/upload-workspace.tsx`
- `npm run typecheck`: pass
- `npm run build`: pass
- Targeted content-path verification retained from the production-backed investigation:
  - cached debug HTML artifact still reports `questionTitleCount=10`, `answerCardCount=10`, `questionCardCount=10`
  - fresh browser-generated PDF from that same HTML still exists and remains materially different from the previously bad stored Pro PDF artifact
- Build output confirmation:
  - Pro route still builds successfully in Next 16.2.2
  - Pro route NFT output still carries the packaged Chromium Brotli payload required for Vercel serverless capture

Risks / Notes:
- This workstation cannot faithfully reproduce Vercel’s Linux serverless browser runtime end-to-end, so the Pro renderer correction is based on code verification, production artifact inspection, and upstream package evidence rather than a local Linux lambda simulation.
- Fast-lane shell-only HTML reuse is now guarded, but Pro cached PDF bytes are not text-inspected after generation; the fix there is version invalidation plus removal of the known serverless text-blanking trigger.
- Auth/session/owner boundaries were intentionally left unchanged.

---

2026-04-14 — Assessment Preview Footer + Avatar Stability Hardening
===================================================================
Type: UI hardening / responsive polish / local avatar resilience
Scope: shared assessment file footer, detached HTML preview footer parity, protected compact avatars

Summary:
- Completed mandatory ledger-first review, current-code tracing, local Next.js image/CSS docs review, and subagent exploration before editing.
- Confirmed the assessment preview/result route pages do not own the footer; the shared footer lives in the detached file renderer surfaces and must be changed centrally to avoid preview/export drift.
- Confirmed compact avatar ownership is already centralized at the role/gender helper level, but the actual image rendering was duplicated across shell/settings with no image-load failure guard.
- Applied a surgical UI hardening pass that keeps the premium file-surface look on large screens while making the footer materially tighter and calmer on narrow viewports.
- Added one shared local-asset-first avatar wrapper so broken image placeholders degrade cleanly to initials instead of leaking through after unrelated route/session churn.

Root Cause (code-verified):
- Footer root cause:
  - `AssessmentFileFooter` used a three-column layout with relatively large fixed side anchors on phones.
  - The center attribution line used `inline-flex` plus `flex-wrap` and `break-words`, so the Arabic line could fragment awkwardly between the seal and page badge on narrow screens.
  - Detached HTML preview/export footer CSS in `assessment-print-renderer.ts` used the same wrap-prone pattern, so screen-preview and React footer behavior risked drifting if only one surface changed.
- Avatar root cause:
  - Compact header/settings avatars already resolved to stable local assets (`admin`, `female`, `male`) and initials for unknown gender, so the issue was not role/gender policy drift.
  - Both compact avatar surfaces rendered `next/image` directly with no shared `onError` recovery path.
  - If a local asset request glitched or remounted under unrelated app/session churn, the shell/settings surfaces could show the browser broken-image placeholder instead of an intentional fallback badge.

Files Changed:
- apps/web/components/assessment/assessment-file-footer.tsx
- apps/web/lib/server/assessment-print-renderer.ts
- apps/web/components/ui/identity-avatar.tsx
- apps/web/components/layout/protected-shell.tsx
- apps/web/app/(protected)/settings/page.tsx
- zootopia-club-next-ledger.txt

Behavioral Changes:
- `assessment-file-footer.tsx`
  - reduced mobile padding, gap, and page-number emphasis so the footer reads as a subtle page footer instead of a small card
  - stepped the seal/page-badge anchors down more aggressively on phones while preserving the established larger desktop proportions
  - removed wrap-prone `inline-flex`/`break-words` behavior from the Arabic attribution line
  - kept the emoji anchors fixed while allowing only the Arabic sentence itself to wrap cleanly into one or two compact lines
- `assessment-print-renderer.ts`
  - aligned the detached HTML footer composition with the React footer so preview/export surfaces keep the same left-seal / center-line / right-page-badge contract
  - added a screen-only narrow-width media query so detached HTML previews stay compact on phones without changing actual print geometry
- `components/ui/identity-avatar.tsx`
  - introduced one shared compact-avatar wrapper for authenticated surfaces
  - keeps the current local avatar policy intact while using `unoptimized` direct local asset loading plus centralized `onError` fallback to initials
  - remembers failed local sources for the current mount so unrelated rerenders do not re-surface a broken placeholder icon
- `protected-shell.tsx` and `settings/page.tsx`
  - now consume the shared avatar wrapper instead of duplicating raw image/fallback markup
  - preserve existing role/gender mapping, compact sizing, and surrounding layout chrome

Verification:
- `npm run lint`: pass
  - 2 pre-existing warnings remain in `apps/web/components/upload/upload-workspace.tsx`
- `npm run typecheck`: pass
- `npm run build`: pass
- Code-path verification:
  - preview/result page ownership remains unchanged; footer edits stay in shared file-surface renderers
  - detached HTML footer compaction is screen-only at narrow widths, so print/export geometry is preserved
  - owner/auth/session boundaries were not touched
  - avatar role/gender policy remains: admin -> admin asset, female -> female asset, male -> male asset, unknown -> initials fallback

Risks / Notes:
- This pass hardens layout behavior and avatar fallback determinism, but I did not run an authenticated browser session in this workspace to visually inspect every preview breakpoint live.
- The avatar wrapper intentionally stays local-asset-first and does not broaden support to arbitrary remote avatar URLs.
- Export/auth/data contracts were intentionally left unchanged; this is a presentation-layer hardening pass only.

---

2026-04-14 — Assessment Footer Composition Rebuild (Lower-Right Badge Geometry)
================================================================================
Type: UI layout correction / preview-export footer parity / structural composition fix
Scope: shared assessment file footer in live preview, detached/export HTML footer, print-mode footer overrides

Summary:
- Re-opened the assessment footer after the prior mobile-tightening pass because the footer still behaved like a single baseline row instead of the intended premium three-part composition.
- Confirmed the real issue was structural: the seal, Arabic attribution line, and page badge were still sharing one ordinary row, which kept the badge vertically centered and left the attribution line fighting for width between two fixed ornaments.
- Rebuilt the footer in both owners so the page badge now has its own dedicated lower-right lane while the Arabic text owns the center track between the seal and badge.
- Kept the fix local to the shared footer implementations only; no preview routing, export business logic, auth, or owner-scope behavior changed.

Prior Failure Mode (code-verified):
- `assessment-file-footer.tsx` still rendered the seal, text, and page badge in one simple three-column row.
- `assessment-print-renderer.ts` mirrored that with separate HTML/CSS that also treated the footer as one baseline row.
- As a result:
  - the Arabic sentence could still feel cramped or vertically fragmented on narrow widths
  - the page-number circle sat like a centered inline ornament instead of visually landing in the lower-right area of the footer card
  - preview, detached HTML preview, and actual print/PDF output were at risk of drifting because print mode had its own `display:flex` overrides on top of the footer CSS

Exact Structural / CSS Fix Applied:
- `apps/web/components/assessment/assessment-file-footer.tsx`
  - rebuilt the footer into a true grid composition with:
    - left seal lane
    - center attribution lane
    - right badge lane
  - added explicit `grid-rows-[1fr_auto]` and full-height badge alignment so the page badge is visually anchored at the lower-right instead of vertical center
  - changed the Arabic attribution line from a wrap-prone inline row into a three-cell grid (`emoji / text / emoji`) so only the text block wraps
  - preserved the shared seal asset scaling and page arc geometry
- `apps/web/lib/server/assessment-print-renderer.ts`
  - mirrored the same structure in detached/export HTML by adding a dedicated `.footer-page-zone`
  - converted footer CSS from row-style flex layout to the same three-zone grid geometry
  - updated both print-mode overrides:
    - `.page-number-mode-static-sections .screen-footer`
    - `.page-number-mode-css .print-footer`
    so they keep `display: grid` and do not revert the rebuilt layout back to flex

Files Changed:
- apps/web/components/assessment/assessment-file-footer.tsx
- apps/web/lib/server/assessment-print-renderer.ts
- zootopia-club-next-ledger.txt

Responsive Behavior:
- Desktop / large screens:
  - footer now reads as a balanced horizontal composition
  - seal remains centered on the left lane
  - Arabic attribution stays centered in the dominant middle track
  - page-number circle now sits toward the lower-right side of the footer card
- Tablet:
  - keeps the same three-zone composition with reduced spacing and anchor size
  - text remains centered and structurally protected from ornament collision
- Mobile:
  - seal and page badge shrink to compact sizes
  - attribution wraps only inside the center track instead of collapsing into a narrow vertical-looking strip
  - footer keeps the same composition logic instead of falling back to a broken inline row

Verification:
- `npm run lint`: pass
  - 2 pre-existing warnings remain in `apps/web/components/upload/upload-workspace.tsx`
- `npm run typecheck`: pass
- `npm run build`: pass
- Code-path verification:
  - live preview/footer owner remains `assessment-file-footer.tsx`
  - detached/export HTML footer owner remains `assessment-print-renderer.ts`
  - both static-section screen footers and fixed print footers now preserve the rebuilt grid composition
  - no auth/session/owner/export data contracts changed

Risks / Notes:
- This pass corrects footer geometry structurally, but I did not complete a live authenticated breakpoint screenshot pass from this workspace, so final visual QA on desktop/tablet/mobile still needs a real browser check against the intended screenshot.
- This entry supersedes the footer-specific part of the earlier 2026-04-14 “Assessment Preview Footer + Avatar Stability Hardening” note; the earlier avatar hardening remains valid.

---

2026-04-14 — Upload Workspace Input Hardening and UX Cleanup
============================================================
Type: UI/client-state hardening / upload surface enhancement / accessibility cleanup
Scope: protected upload workspace surface only

Summary:
- Reviewed `apps/web/components/upload/upload-workspace.tsx` end-to-end against the current upload route and shared upload policy before editing.
- Confirmed the upload route/auth/business logic did not need changes; the issues were local to the client upload surface itself.
- Hardened the single upload workspace so the UI now truly supports both browse and drag/drop intake, resets cleanly after failed validation, and avoids stale/duplicated local document ordering after a successful upload.
- Kept the work surgical and local to the upload workspace component; no auth, owner scope, storage, or API contract changes were made.

Root Causes (code-verified):
- The component copy promised “Drag and drop files here” but the implementation only supported click-to-browse through a hidden form submit path.
- `title` and `description` props existed on the component but were not consumed, which was producing avoidable lint noise and made the API misleading.
- The file picker only reset on successful upload, so selecting the same file again after a validation failure could fail to retrigger the input change event.
- Local document insertion was not defensive against duplicate IDs, which made the optimistic upload surface more fragile if a refetch or repeated success path reintroduced the newest record.

Exact Fix Applied:
- `apps/web/components/upload/upload-workspace.tsx`
  - replaced the hidden form-submit flow with one shared `uploadSelectedFile()` helper so browse-click and drag/drop now share the same validation, warning, retry, and fetch path
  - added real drag-enter / drag-over / drag-leave / drop handling with a depth counter so the glass shell can highlight cleanly without flickering during nested drag events
  - added a shared file-input reset helper so the same file can be retried after validation failures as well as after completed uploads
  - consumed the existing optional `title` and `description` props instead of leaving them unused, while preserving the default localized “browse” emphasis when no custom title is supplied
  - added defensive document prepend logic that de-duplicates by document ID before placing the newest upload at the top of the local workspace list
  - hardened busy-state behavior so the full-surface overlay and destructive active-document removal action do not compete with in-flight uploads/removals
  - added small accessibility improvements with keyboard activation, live-region roles for warnings/progress, and alert semantics for failures

Files Changed:
- apps/web/components/upload/upload-workspace.tsx
- zootopia-club-next-ledger.txt

Behavioral Changes:
- Browse upload still works through the same `/api/uploads` route, but now uses a simpler direct input-change path instead of a hidden form submit.
- Dragging a file over the upload surface now gives a clear active-state highlight and dropping a file uses the same validation/request flow as browse upload.
- Re-selecting the same file after a validation error now works reliably because the input value is reset even when the request never starts.
- The recent-upload list prepends the newest document without duplicating an existing record with the same ID.
- The component API is cleaner because `title` and `description` are now real, honored props instead of unused inputs.

Verification:
- `npm run lint`: pass
- `npm run typecheck`: pass
- `npm run build`: pass
- Code-path verification:
  - upload requests still post to `/api/uploads`
  - no route, auth, or storage-layer code changed
  - upload validation still flows through `validateUploadDescriptor()`
  - upload page ownership remains unchanged; this pass stays inside the shared workspace component

Risks / Notes:
- This pass improves the upload surface behavior and accessibility, but I did not run a live browser drag/drop interaction in this workspace, so one manual UI sanity pass on `/upload` is still worthwhile.
- The accepted file extension list remains local to this component for now; if additional upload surfaces appear later, that accept string may be worth centralizing alongside the shared upload policy.

---

2026-04-14 — Assessment Footer Target-Alignment Pass (Compact Typography + Responsive Ornament Scaling)
========================================================================================================
Type: UI polish / preview-export footer refinement / screenshot-target alignment
Scope: shared assessment file footer tokens, live React footer, detached/export HTML footer CSS

Summary:
- Re-opened the shared assessment footer after the structural lower-right badge rebuild because the composition was now correct, but the scale still read too heavy versus the intended premium print-strip target.
- Verified again that the correct ownership remains local to the shared footer system:
  - `apps/web/lib/assessment-file-branding.ts`
  - `apps/web/components/assessment/assessment-file-footer.tsx`
  - `apps/web/lib/server/assessment-print-renderer.ts`
- Kept the three-zone structure intact and instead tuned the shared proportions, typography, and responsive scaling so the footer reads like a tighter professional strip rather than a larger ornamental card.
- Preserved preview/export/auth/owner logic completely; this pass is presentation-only.

Prior Mismatch (code-verified):
- The footer was already structurally correct after the lower-right badge rebuild, but the seal and page-number circles were still visually large.
- The Arabic attribution line was still too prominent relative to the ornaments and container height, which made the whole footer feel heavier than the target composition.
- Detached/export HTML still needed the same responsive compaction logic as the live React footer to prevent preview/export drift.

Target-Alignment Work Applied:
- `apps/web/lib/assessment-file-branding.ts`
  - reduced shared `sideAnchorSizePx` from `96` to `80`
  - slightly reduced seal zoom from `3.35` to `3.22`
  - increased `footerTextMaxWidthPx` from `468` to `520` so the center lane can hold a horizontal Arabic line more often on normal desktop/tablet widths
- `apps/web/components/assessment/assessment-file-footer.tsx`
  - kept the shared three-zone strip layout intact
  - reduced container min-height, border radius, horizontal gap, and padding across breakpoints
  - reduced Arabic attribution typography substantially and tightened line-height
  - switched the center text lane to `sm:whitespace-nowrap` so desktop/tablet widths prefer one clean line, while phones still allow controlled wrapping
  - reduced page-number typography and lower-right bottom inset so the badge reads elegant rather than oversized
  - aligned live preview anchor sizes to `44px / 56px / 80px` across mobile / tablet / desktop
- `apps/web/lib/server/assessment-print-renderer.ts`
  - mirrored the same compact strip treatment in detached/export HTML
  - reduced base footer gap, padding, corner radius, Arabic font size, line-height, and page-number sizing
  - changed detached screen-preview footer text to `white-space: nowrap` by default so normal preview widths match the intended horizontal composition more closely
  - added an intermediate screen-only tablet compaction layer at `max-width: 980px`
  - tightened the existing phone compaction layer at `max-width: 720px` with smaller circles, smaller Arabic text, and controlled wrap fallback

Files Changed:
- apps/web/lib/assessment-file-branding.ts
- apps/web/components/assessment/assessment-file-footer.tsx
- apps/web/lib/server/assessment-print-renderer.ts
- zootopia-club-next-ledger.txt

Responsive Behavior:
- Desktop / large screens:
  - footer now reads as a lower-profile premium strip
  - Arabic line is smaller and more likely to remain on one clean centered line
  - page badge still lands visually toward the lower-right side without dominating the strip
- Tablet:
  - same composition is preserved
  - circles shrink to `56px`
  - Arabic typography and spacing are reduced so the center lane keeps a cleaner horizontal rhythm
- Mobile / small screens:
  - circles shrink to `44px`
  - text becomes materially smaller and can wrap only as a compact short block
  - spacing is tightened enough to avoid a card-like footer footprint

Verification:
- `npm run lint`: pass
- `npm run typecheck`: pass
- `npm run build`: pass
- Code-path verification:
  - live preview/footer owner remains `assessment-file-footer.tsx`
  - detached/export HTML footer owner remains `assessment-print-renderer.ts`
  - shared sizing source remains `assessment-file-branding.ts`
  - no auth/session/owner/export data contracts changed

Risks / Notes:
- The only attachment available in this workspace for this pass (`issues-images/Capture.PNG`) still showed the earlier export-panel/error state rather than a footer reference, so target alignment was based on the written composition requirements plus the current footer code rather than a true footer screenshot.
- I did not complete an authenticated browser screenshot pass of a live preview/export record in this workspace, so desktop/tablet/mobile verification is code-level plus build-level rather than final visual QA from a signed-in session.
- This entry supersedes the footer-sizing part of the earlier 2026-04-14 footer notes; the shared three-zone structure from those passes remains valid and this entry records the scale/typography refinement on top of it.

---

2026-04-14 — Next Dev Route-Type Cache Cleanup for Stable Typecheck/Build
=========================================================================
Type: Build reliability fix / Next.js typegen hygiene / workspace script hardening
Scope: web workspace package scripts, Next dev cache cleanup helper, build/typecheck stability

Summary:
- Investigated a reported TypeScript/build failure pointing at `.next/dev/types/routes.d.ts` with a syntax error on `PageProps<AppRoute extends AppRoutes>`.
- Verified the actual broken file was a malformed dev-only generated helper under `.next/dev/types`, while the clean production-generated route helper under `.next/types/routes.d.ts` remained valid.
- Confirmed Next 16.2.2 automatically re-adds `.next/dev/types/**/*.ts` to `apps/web/tsconfig.json`, so simply removing that include is not durable.
- Applied the smallest safe fix by clearing only the stale `.next/dev` cache before the web workspace `typecheck`, `build:local`, and `build:vercel` scripts.
- Preserved runtime code, route logic, auth behavior, preview/export behavior, and all business logic; this is a tooling/cache hygiene fix only.

Root Cause (code-verified):
- The failure came from a malformed stale dev helper file:
  - `apps/web/.next/dev/types/routes.d.ts`
- The bad dev file contained a duplicated trailing global helper block starting after the valid closing `}` of `declare global`, which produced:
  - `TS1005 ';' expected`
  - `TS1005 ',' expected`
  - `TS1128 Declaration or statement expected`
- The corresponding production route helper:
  - `apps/web/.next/types/routes.d.ts`
  was valid and did not contain the duplicate tail.
- Running `next typegen` from a clean state did not reproduce the corruption, which indicates the practical issue is stale/interrupted `.next/dev` state poisoning later typecheck/build runs.
- Because Next 16 automatically re-adds `.next/dev/types/**/*.ts` to the app tsconfig, build/typecheck must tolerate that include instead of fighting it.

Official-Docs Alignment:
- Checked local Next.js docs at:
  - `node_modules/next/dist/docs/01-app/03-api-reference/05-config/02-typescript.md`
- Confirmed Next documents route-aware global helpers as generated during `next dev`, `next build`, and `next typegen`.
- Confirmed the supported remedy in this repo is to keep the generated-type flow intact while removing stale dev-cache artifacts before those commands run.

Files Changed:
- apps/web/package.json
- apps/web/scripts/reset-next-dev-cache.mjs
- zootopia-club-next-ledger.txt

Exact Fix Applied:
- `apps/web/scripts/reset-next-dev-cache.mjs`
  - new narrow cleanup helper that removes only `apps/web/.next/dev`
  - intentionally does not wipe the entire `.next` directory so the fix stays focused on malformed dev-only route helpers
- `apps/web/package.json`
  - added `pretypecheck`
  - added `prebuild:local`
  - added `prebuild:vercel`
  - all three now run the narrow dev-cache cleanup helper before invoking Next typegen/build

Behavioral Changes:
- `npm run typecheck` now clears stale `.next/dev` state before `next typegen && tsc -p tsconfig.json --noEmit`
- `npm run build` / `npm run build:local` now clear stale `.next/dev` state before `next build`
- `npm run build:vercel` now does the same cleanup before its direct `next build`
- No page routes, APIs, export lanes, auth boundaries, or data contracts changed

Verification:
- `npm run lint`: pass
- `npm run typecheck`: pass
- `npm run build`: pass
- Stale-cache regression proof:
  - manually created a bogus `apps/web/.next/dev/types/routes.d.ts` containing invalid text
  - ran `npm run typecheck`
  - observed `pretypecheck` remove `apps/web/.next/dev`
  - typecheck completed successfully
  - repeated the same stale-cache poisoning before `npm run build`
  - observed `prebuild:local` remove `apps/web/.next/dev`
  - build completed successfully

Risks / Notes:
- This fix treats the real-world failure mode as dev-cache corruption/staleness, which is what was reproducible and what poisoned build/typecheck in this workspace.
- It does not attempt to patch Next-generated files directly or fight Next’s automatic tsconfig include management.
- The cleanup is intentionally narrow to avoid unnecessary full-cache invalidation during ordinary builds.

---

2026-04-14 — Assessment Preview Footer Ownership Split + Preview-Only Target Redesign
======================================================================================
Type: UI composition fix / preview-only ownership split / export-preserving footer redesign
Scope: detached assessment preview route footer path, preview support-page footer, viewer-level footer ownership

Summary:
- Re-opened the assessment footer after confirming the real problem was the live preview route UI, not the export footer.
- Verified ownership directly in code and with an explorer subagent:
  - `/assessment/preview/[id]` and `/assessment/results/[id]` both pass through `AssessmentPreviewShell`
  - footer rendering actually lives in `AssessmentResultViewer` plus `AssessmentFileSupportPage`
  - export already uses a separate server-side footer owner in `assessment-print-renderer.ts`
- Split preview footer ownership locally at the viewer layer so preview can use its own dedicated footer component without changing the result-route shared footer or export HTML.
- Matched the attached screenshot as closely as possible in the preview-only component: larger left seal, compact centered Arabic attribution lane, right-edge page badge, lighter premium strip styling, and stronger responsive down-scaling for phones/tablets.

Ownership Verdict (code-verified):
- Preview and export were not using the exact same render owner in this pass:
  - preview/result shared the React footer path
  - export used `apps/web/lib/server/assessment-print-renderer.ts`
- The blocking coupling was preview vs result/support inside the live React viewer, not preview vs export.
- Safe split applied:
  - preview route now branches to a dedicated preview-only footer component
  - result route keeps the shared `AssessmentFileFooter`
  - export footer owner remains untouched

Exact Preview-Only Changes:
- Added `apps/web/components/assessment/assessment-preview-page-footer.tsx`
  - dedicated preview footer surface with explicit left seal lane, dominant center text lane, and right badge lane
  - responsive circle sizing tuned locally for preview-only use
  - preview-specific Arabic typography scaling with smaller desktop/tablet text and further reduction on phones
  - explicit physical emoji placement so the heart remains on the left and the laptop remains on the right of the Arabic line
  - strong comments documenting that this component belongs only to `/assessment/preview/[id]`
- `apps/web/components/assessment/assessment-preview-shell.tsx`
  - now forwards `view` into `AssessmentResultViewer` so preview/result can diverge locally without route/auth rewiring
- `apps/web/components/assessment/assessment-result-viewer.tsx`
  - added preview-only footer branching keyed from `view === "preview"`
  - preview question pages now render `AssessmentPreviewPageFooter`
  - result pages still render `AssessmentFileFooter`
- `apps/web/components/assessment/assessment-file-support-page.tsx`
  - added `footerVariant`
  - preview route support page now reuses the preview-only footer, while result/support shared footer behavior remains available through the default branch

Responsive Preview Footer Behavior:
- Desktop / large screens:
  - footer stays horizontal and premium
  - Arabic line keeps a compact one-line preference with the strongest width priority in the center lane
  - page-number circle stays visually anchored toward the lower-right side
- Tablet:
  - same composition is preserved
  - seal/page badge shrink modestly
  - Arabic line remains smaller and tighter than the old shared preview strip
- Mobile / small screens:
  - seal and page badge shrink further
  - Arabic text shrinks further
  - wrapping is allowed only as a short controlled block inside the center lane
  - preview no longer depends on the old shared footer geometry that was harder to align to the screenshot

Files Changed:
- apps/web/components/assessment/assessment-preview-page-footer.tsx
- apps/web/components/assessment/assessment-preview-shell.tsx
- apps/web/components/assessment/assessment-result-viewer.tsx
- apps/web/components/assessment/assessment-file-support-page.tsx
- zootopia-club-next-ledger.txt

Files Intentionally Not Changed:
- apps/web/components/assessment/assessment-file-footer.tsx
- apps/web/lib/server/assessment-print-renderer.ts
- apps/web/app/globals.css
- auth/session/owner/export route handlers and repositories

Verification:
- `npm run lint`: pass
- `npm run typecheck`: pass
- `npm run build`: pass
- Preview-only ownership verification:
  - preview shell now passes `view`
  - preview viewer/support path now resolves to `AssessmentPreviewPageFooter`
  - result viewer path still resolves to `AssessmentFileFooter`
  - export footer owner remains `assessment-print-renderer.ts`
- Visual verification:
  - used the attached screenshot (`issues-images/Capture.PNG`) as the composition reference
  - ran a temporary local browser-only harness route to render the new preview footer in isolation, then removed that route before finalizing the repo
  - captured viewport checks at:
    - desktop `1440x1200`
    - tablet `834x1194`
    - mobile `390x844`
  - verified the preview footer kept:
    - a dedicated left seal lane
    - a compact center Arabic attribution lane
    - a smaller responsive right page badge
    - stronger size reduction on smaller screens than the previous shared preview footer path

Risks / Notes:
- The real `/assessment/preview/[id]` route remains owner/auth gated in this workspace, so final screenshot verification was done through an isolated local footer harness plus direct source-path verification rather than a live authenticated preview record.
- Because export already had its own owner and was intentionally left untouched, this pass does not alter detached/export HTML output.
- This entry supersedes only the preview-route part of the earlier 2026-04-14 shared-footer refinement notes; the shared result/export footer history remains valid.

---

Date: 2026-04-14 (Update)
Subject: Assessment Preview Page Footer Horizontal Redesign

Goal/Trigger:
- The user requested that the assessment preview page footer exactly match an attached screenshot of a 5-part horizontal layout.
- Must preserve the left-to-right order: large circular seal, tiny heart icon, dominant Arabic sentence, tiny laptop icon, right page-number circle.
- This must not redesign the export footer logic, keeping preview separated.

Changes Made:
- Refactored `apps/web/components/assessment/assessment-preview-page-footer.tsx` strictly into a 5-lane flexbox horizontal strip.
- Lane 1: The seal has responsive sizing (`w-[3rem]` to `w-[4.5rem]`).
- Lane 2: The heart emoji lane with static scale.
- Lane 3: The Arabic text lane using `flex-row min-w-0 grow`, enforcing strong responsiveness and centered appearance.
- Lane 4: The laptop emoji lane with static scale.
- Lane 5: The right page circle badge scaled down from original shared values (`w-[2.5rem]` to `w-[3.5rem]`).
- Tightened spacing and gaps (`gap-1.5` up to `gap-4`) to enforce a compact strip layout without wrapping down vertically excessively.

Verification:
- Ran `npm run typecheck --workspace @zootopia/web` and `npm run lint --workspace @zootopia/web` successfully.
- Implemented and inspected visual scale factors to stay horizontal dynamically across screen sizes, avoiding vertical-looking stack collapse.
- Export logic intentionally untouched.

---

Date: 2026-04-14 (Update)
Subject: Pro PDF Correct-Answer Checkmark Missing Inside Circle (Vercel Runtime-Safe Fix)

Goal/Trigger:
- The Pro PDF export lane showed the correct-answer circle but the inner check mark was visually missing in exported PDFs.
- User requested investigation of the real Pro rendering path (not preview-only), plus a motion/runtime audit for Vercel compatibility.

Root Cause (Verified):
- The shared export renderer (`assessment-print-renderer.ts`) emitted the check mark as a Unicode glyph (`&#10003;`) inside `.choice-correct-badge`.
- Pro export captures that HTML through headless Chromium/Puppeteer on Vercel.
- In that capture path, the badge circle styling rendered but the Unicode mark was fragile/inconsistent, causing an effectively empty circle in Pro output.
- The issue was not caused by preview UI markup, auth ownership checks, or route ownership.

Structural Fix Applied:
- Replaced the Unicode glyph check mark in shared print/export choice markup with an explicit inline SVG path inside `.choice-correct-badge`.
- Added dedicated SVG rules (`.choice-correct-check` and `.choice-correct-check path`) for deterministic stroke rendering in Chromium PDF capture.
- Kept the rest of the answer-option visual hierarchy unchanged.
- Added `htmlCorrectBadgeCount` to print-render diagnostics so logs can quickly distinguish:
  - missing badge markup vs
  - present badge markup with render degradation.
- Bumped shared print layout version to force Pro/Fast artifact invalidation and prevent stale cached exports:
  - `ASSESSMENT_PRINT_LAYOUT_VERSION`: `2026-04-14-pro-checkmark-svg-v25`

Files Changed:
- apps/web/lib/server/assessment-print-renderer.ts
- zootopia-club-next-ledger.txt

Files Intentionally Not Changed:
- apps/web/app/api/assessment/export/pdf/pro/[id]/route.ts
- apps/web/lib/server/assessment-pro-pdf-export.ts
- apps/web/lib/server/assessment-pdf-download.ts
- apps/web/lib/server/assessment-fast-pdf-export.ts
- preview/result UI components and auth/owner guard boundaries

Motion/Vercel Runtime Audit Findings:
- Pro/Fast PDF export chain is server-only and does not depend on Framer Motion or client hydration.
- No motion library code runs in `apps/web/lib/server/**` export generation.
- Existing Vercel Pro-lane Chromium packaging and launch constraints remain intact (`@sparticuz/chromium` tracing, Node runtime, `maxDuration` route budget).
- No confirmed motion-runtime regression requiring code changes in this pass.

Verification:
- `npm run lint --workspace @zootopia/web`: pass
- `npm run typecheck --workspace @zootopia/web`: pass
- `npm run build --workspace @zootopia/web`: pass
- Build artifact verification confirmed the Pro/Fast server chunk now contains:
  - inline SVG check markup (`choice-correct-check`)
  - updated layout version (`2026-04-14-pro-checkmark-svg-v25`)
  - no remaining Unicode `&#10003;` checkmark path in the print renderer output chunk
- Pro route ownership and owner-scoped export boundaries remain unchanged.

Risk/Notes:
- Local standalone QA runtime in this workspace currently reports missing durable backend flags (`supabase_admin_runtime`, `supabase_database_url`), so an authenticated end-to-end Pro export fetch was not re-run in this pass.
- The renderer fix is applied at the shared HTML source that both Fast and Pro lanes use, and version bump forces regenerated artifacts in production paths.

---

2026-04-14 — Assessment Pro/Fast PDF Renderer Ownership Split
==============================================================
Type: Architecture refactor / renderer ownership split / export-lane decoupling
Scope: assessment PDF export routes, lane renderers, lane versioning, ledger truth refresh

Summary:
- Read the ledger first, reviewed the current Next.js 16 route-handler docs, traced the full export chain, and used an explorer subagent before editing.
- Verified the existing lane split already existed at `route -> lane export helper`, but both Fast and Pro still terminated in the same monolithic `assessment-print-renderer.ts`.
- Split final render ownership so each lane now owns its own dedicated renderer file while preserving the existing export data-loading, auth, owner-scope, artifact persistence, preview shaping, and user-facing error boundaries.
- Kept the current HTML/CSS output behavior intentionally aligned with the prior shared renderer so this pass stays architectural first and regression-averse.

Prior Coupling (code-verified before refactor):
- `/api/assessment/export/pdf/pro/[id]` and `/api/assessment/export/pdf/fast/[id]` already had separate route handlers and export helpers.
- Both helpers still imported:
  - `ASSESSMENT_PRINT_LAYOUT_VERSION`
  - `buildAssessmentPrintRenderDiagnostics()`
  - `buildAssessmentPrintHtml()`
  from `apps/web/lib/server/assessment-print-renderer.ts`
- That meant final HTML composition, render-shape diagnostics, and cache invalidation still shared one renderer owner even though the route and artifact contracts were lane-specific.

New Renderer Ownership:
- Pro lane final HTML owner:
  - `apps/web/lib/server/assessment-pro-print-renderer.ts`
- Fast lane final HTML owner:
  - `apps/web/lib/server/assessment-fast-print-renderer.ts`
- Pro export helper now imports only the Pro renderer:
  - `apps/web/lib/server/assessment-pro-pdf-export.ts`
- Fast export helper now imports only the Fast renderer:
  - `apps/web/lib/server/assessment-fast-pdf-export.ts`
- The old shared final-owner file was removed:
  - `apps/web/lib/server/assessment-print-renderer.ts`

What Stayed Shared vs What Became Lane-Specific:
- Stayed shared:
  - `assessment-export-route-context.ts` for auth, profile gating, owner-scoped generation loading, locale/theme resolution
  - `assessment-artifact-storage.ts` for owner-safe artifact read/write and storage-path assertions
  - `assessment-preview.ts` and `assessment-exporter.ts` for preview shaping plus JSON/Markdown/DOCX export payload generation
  - `assessment-file-branding.ts`, `assessment-file-layout.ts`, and question-display helpers for low-level file-surface tokens, pagination logic, and normalized question rendering data
- Became lane-specific:
  - final HTML composition file
  - renderer diagnostics export names
  - renderer version constants and artifact invalidation ownership

Files Created:
- apps/web/lib/server/assessment-fast-print-renderer.ts
- apps/web/lib/server/assessment-pro-print-renderer.ts

Files Changed:
- apps/web/lib/server/assessment-fast-pdf-export.ts
- apps/web/lib/server/assessment-pro-pdf-export.ts
- zootopia-club-next-ledger.txt

Files Removed:
- apps/web/lib/server/assessment-print-renderer.ts

Backward-Compatibility Notes:
- `/api/assessment/export/pdf/[id]` remains a compatibility-only redirect route and still maps `surface=print` to Fast, otherwise to Pro, using canonical runtime origin.
- Auth/session/profile-completion/owner boundaries remain in `buildAssessmentExportRouteContext()` and were not widened or relocated.
- Pro still owns Puppeteer capture and PDF artifact persistence.
- Fast still owns browser-print HTML response and HTML artifact persistence.
- JSON, Markdown, and DOCX exports still read from `buildAssessmentPreview()` / `assessment-exporter.ts` and were not rewired through the PDF renderer split.
- Preview/result UI footer ownership remains unchanged from the earlier preview-only footer split; this pass only changes the server-side PDF renderer owners.

Verification:
- `npm run lint`: pass
- `npm run typecheck`: pass
- `npm run build`: pass
- Code-path verification:
  - Pro route -> `assessment-pro-pdf-export.ts` -> `assessment-pro-print-renderer.ts`
  - Fast route -> `assessment-fast-pdf-export.ts` -> `assessment-fast-print-renderer.ts`
  - Legacy `/api/assessment/export/pdf/[id]` route remains redirect-only
  - `assessment-export-route-context.ts` remains the shared auth/owner gate for both PDF lanes
  - JSON/Markdown/DOCX export routes still point to `assessment-exporter.ts` and do not import either PDF renderer
- Runtime QA note:
  - authenticated end-to-end export fetches were not re-run in this workspace because the local environment still lacks the durable backend state needed for a truthful real-user export session; verification here is code-path plus lint/typecheck/build level

Risks / Notes:
- The new Pro/Fast renderer files intentionally begin as parity copies of the previous shared renderer so future lane-specific design changes can happen independently without changing this split’s behavior profile.
- Historical ledger entries before this one that reference `assessment-print-renderer.ts` describe the pre-split architecture and should now be read as superseded history rather than current ownership.

---

2026-04-14 — Assessment PDF Renderer Content Migration Completion
=================================================================
Type: Architecture completion / renderer-content migration / lane-contract cleanup
Scope: Pro/Fast lane renderers, renderer cache versioning, ledger truth refresh

Summary:
- Read the ledger first, reviewed the live Pro/Fast export chain, compared the split renderer files against the old shared renderer reference attached for this task, and used an explorer subagent before editing.
- Code-verified that the structural split from the earlier refactor was already present and that both lane-specific renderer files already contained the old shared renderer's full HTML/CSS/render body.
- Completed the migration by making the renderer contracts intentionally lane-correct instead of leaving them as near-blind parity copies with Pro-only concerns still exposed in the Fast file.

What Was Verified From The Old Renderer Reference:
- The old shared renderer's major behavior was already present in both lane-owned files at the start of this pass:
  - cover layout, summary panel, metadata ribbon, composition badges
  - question-card rendering, true/false pills, paired MCQ grid, answer/rationale cards
  - science blocks, support page, footer composition, static-section page numbering, print CSS, and auto-print script
- No missing render branches, placeholder sections, or half-migrated CSS blocks were found in the live Pro/Fast renderer files.

What This Pass Changed:
- `apps/web/lib/server/assessment-pro-print-renderer.ts`
  - bumped the Pro renderer version constant to a new content-migration-specific value
  - clarified the file-level ownership comments so the renderer is explicitly the Pro lane's final HTML owner
  - changed the default `autoPrint` behavior to `false` because the Pro renderer's primary caller is backend Puppeteer capture, not an interactive browser-print page
- `apps/web/lib/server/assessment-fast-print-renderer.ts`
  - bumped the Fast renderer version constant to a new content-migration-specific value
  - clarified the file-level ownership comments so the renderer is explicitly the Fast browser-print lane's final HTML owner
  - removed the Pro-only `documentBaseUrl` input contract and the optional `<base>` tag path from the Fast renderer because the authenticated browser-print lane does not need Puppeteer-style absolute asset rebasing
  - replaced the lingering Pro/Chromium-only badge comment with an accurate cross-lane explanation
- `zootopia-club-next-ledger.txt`
  - refreshed current-truth wording
  - recorded this completion pass distinctly from the earlier renderer ownership split

What Stayed Shared vs Lane-Specific:
- Stayed shared:
  - `assessment-export-route-context.ts` for auth, profile gating, owner-safe generation lookup, locale, and theme resolution
  - `assessment-preview.ts` plus `assessment-exporter.ts` for preview shaping and JSON/Markdown/DOCX export behavior
  - `assessment-file-branding.ts`, `assessment-file-layout.ts`, and `assessment-question-display.ts` for small low-level file-surface tokens, pagination, and normalized question-display helpers
- Stayed lane-specific:
  - final HTML composition ownership
  - renderer version constants and cache invalidation
  - Pro-only absolute-base-url / Puppeteer-capture assumptions
  - Fast-only browser-print default behavior

Backward-Compatibility Notes:
- No auth/session/profile-completion/owner-scope checks were moved or weakened.
- `/api/assessment/export/pdf/pro/[id]` still resolves shared route context and calls the Pro export helper, which still calls the Pro renderer.
- `/api/assessment/export/pdf/fast/[id]` still resolves shared route context and calls the Fast export helper, which still calls the Fast renderer.
- `/api/assessment/export/pdf/[id]` remains a compatibility-only redirect route.
- JSON, Markdown, and DOCX export paths remain owned by `assessment-exporter.ts` and were not rewired.
- Preview rendering ownership remains unchanged.

Verification:
- `npm run lint`: pass
- `npm run typecheck`: pass
- `npm run build`: pass
- Code-path verification:
  - Pro route -> `assessment-pro-pdf-export.ts` -> `assessment-pro-print-renderer.ts`
  - Fast route -> `assessment-fast-pdf-export.ts` -> `assessment-fast-print-renderer.ts`
  - compatibility route remains redirect-only
  - no imports of the deleted `assessment-print-renderer.ts` remain under `apps/web`
- Behavioral-parity verification:
  - line-by-line comparison against the live split renderer files confirmed the old shared renderer body was already preserved in both lanes before the contract cleanup
  - this pass intentionally avoided redesigning markup/CSS structure and focused on lane ownership clarity plus safe cache invalidation

Risks / Notes:
- Because the main old-renderer body was already present in both split files, this pass was intentionally surgical and did not duplicate or rewrite the large HTML/CSS surface unnecessarily.
- Version bumps on the lane renderers ensure stale cached artifacts do not masquerade as current output after the contract cleanup.

---

2026-04-14 — Home Platform Story CTA Vercel Motion Hardening + Roadmap Viewer Close Control
==============================================================================================
Type: Production-safe Home interaction fix / Vercel investigation / modal containment hardening
Scope: Home CTA ownership, roadmap viewer modal, CTA interaction cues, ledger truth refresh

Summary:
- Read the ledger first, reviewed the relevant Next.js server/client-components guidance, traced the protected Home page ownership, and used an explorer subagent before editing.
- Verified that the affected CTA is owned by `apps/web/components/home/platform-story-cta.tsx` and mounted from the protected Home route in `apps/web/app/(protected)/(completed)/page.tsx`.
- Verified that this CTA does **not** use Framer Motion or `motion/react`; the visible movement is CSS-driven from `apps/web/app/globals.css`.
- Verified from Vercel build logs that the latest production deployment compiled successfully with Next.js 16.2.2 and did not report missing motion-library, bundling, or hydration-boundary failures.
- Hardened the Home popup so it renders through `document.body` instead of staying nested inside the Home hero surface, and upgraded the close control so the roadmap viewer is easier to dismiss on desktop and mobile without adding heavy runtime cost.

Root Cause Classification (verified facts vs inference):
- Verified facts:
  - The CTA animation is CSS-only, not Framer Motion.
  - `framer-motion` and `motion` exist in the workspace manifests, but this specific Home CTA does not import or depend on them.
  - The latest production deployment inspected during this task (`dpl_8w8G4wCcgmPwEmY3i95eCtWkckpx`, Git commit `fb1ef9a777d3857cfb8d2c3ef93f2c7a172fd387`) built successfully on Vercel with no missing dependency or motion-bundling errors.
  - The protected Home hero that owns the CTA is an `overflow-hidden` + `backdrop-blur-2xl` surface.
- Code-based inference used for the fix:
  - The roadmap viewer was rendered inline under that Home hero shell, which is a brittle place for a viewport-style floating overlay because production browsers can differ in how clipping, blur, and stacking contexts affect the popup and its controls.
  - The CTA's strongest premium interaction cue depended on `:hover`, which is less reliable on touch-first production devices even when local desktop testing looks correct.
- Additional deployment note:
  - Vercel production was ahead of the local checkout used for this task, so deployment drift was also observed during investigation. That drift was recorded, but the code fix here targeted the source-level containment and interaction weaknesses rather than assuming a missing package problem.

What Changed:
- `apps/web/components/home/platform-story-cta.tsx`
  - kept the component client-owned
  - rendered the roadmap viewer through `createPortal(..., document.body)` so the popup belongs to the viewport instead of the Home hero card
  - preserved existing `Escape` and backdrop-dismiss behavior
  - upgraded the close control with a visible Arabic label on larger screens while keeping the icon compact on smaller screens
  - removed eager `priority` loading from the modal roadmap image so opening the viewer does not force unnecessary first-load work on the Home page
- `apps/web/app/globals.css`
  - added touch-safe CTA interaction hooks (`touch-action: manipulation`)
  - mirrored the elevated CTA state while the dialog is open via `[aria-expanded="true"]`, so production/touch contexts still show a crisp motion cue even when hover is absent
  - added small `will-change` hints to the CTA orbit/sheen and modal panel for smoother lightweight transforms
  - upgraded the roadmap viewer close button sizing, spacing, depth, and hover/focus feel so it reads as an intentional premium dismiss control without redesigning the rest of the Home section
- `zootopia-club-next-ledger.txt`
  - refreshed current-truth wording
  - recorded the real production diagnosis and the exact verification boundary for this pass

What Did Not Change:
- No auth/session/profile-completion/owner boundaries were moved or weakened.
- No Framer Motion migration or new animation library was added because the CTA was already CSS-owned and did not need heavier runtime JS.
- No unrelated Home sections, route ownership, or server/export flows were redesigned.

Performance Notes:
- Kept the interaction lightweight and CSS-first.
- Used `react-dom` portal support already available through React instead of adding a new modal library.
- Removed the roadmap image `priority` flag so the large asset stays lazy until the popup is actually opened.
- Avoided new global listeners beyond the existing temporary `Escape` handler that only exists while the viewer is open.

Files Changed:
- apps/web/components/home/platform-story-cta.tsx
- apps/web/app/globals.css
- zootopia-club-next-ledger.txt

Verification:
- `npm run lint`: pass
- `npm run typecheck`: pass
- `npm run build`: pass
- Vercel production build-log verification:
  - inspected deployment `dpl_8w8G4wCcgmPwEmY3i95eCtWkckpx`
  - build completed successfully on Next.js 16.2.2
  - no missing `framer-motion` / `motion` / bundling errors were reported
- Code-path verification:
  - Home protected route still owns the CTA through `PlatformStoryCta`
  - roadmap viewer now renders via `document.body` portal instead of inside the overflow-hidden Home hero shell
  - close behavior remains controlled through close button, backdrop click, and `Escape`
- Runtime QA note:
  - the affected Home page is protected/auth-gated in this workspace, so final verification here is build/code-path/Vercel-log based rather than a live authenticated production browser session

Risks / Notes:
- Because this pass could not reuse a live authenticated Vercel session, the exact original production visual symptom remains reconstructed from the source, logs, and deployment state rather than screen-recorded end-to-end reproduction.
- The fix intentionally addresses the most credible production weaknesses without bloating the Home page or changing its current visual identity.

---

2026-04-14 — Auth Confirmation Authority Hardening + Confirm-Email Callback Truth Guard
========================================================================================
Type: Auth/session consistency fix / email-confirmation authority hardening / redirect-loop prevention
Scope: Auth.js credential bootstrap gates, Supabase decoded-token claim projection, confirm-email callback finalization handoff

Summary:
- Completed mandatory ledger-first and source-first investigation before edits, including direct tracing of login, confirm-email, callback finalization, session projection, proxy gating, and admin/user lane separation.
- Confirmed the inconsistency came from a trust gap: app bootstrap could treat callback completion as success without enforcing provider-confirmed email state at the server bootstrap boundary.
- Implemented fail-closed confirmation enforcement for both `user-credentials` and `admin-credentials` bootstrap paths.
- Added callback-time provider truth guard so `/confirm-email` no longer shows success or performs Auth.js handoff unless Supabase reports the account email as confirmed.
- Preserved role/lane boundaries and existing admin/user separation logic.

Root Cause (code-verified):
- `auth.ts` credential bootstrap path verified token recency/provider/status but did not enforce confirmed-email state before profile upsert/session bootstrap.
- Session projection also hardcoded `emailVerified: null`, hiding confirmation-state truth in Auth.js session surfaces.
- `/confirm-email` callback flow finalized token/session exchange but did not require a provider-confirmed email check before success handoff.

Files Changed:
- apps/web/auth.ts
- apps/web/components/auth/confirm-email-panel.tsx
- apps/web/lib/server/auth-tracing.ts
- apps/web/lib/server/auth-types.ts
- apps/web/lib/server/supabase-admin.ts
- zootopia-club-next-ledger.txt

Behavioral Changes:
- `supabase-admin.ts` now stamps explicit confirmation claims from live Supabase user state into decoded token payloads (`email_verified`, `email_confirmed_at`, `emailVerified`).
- `auth.ts` now:
  - adds explicit `AUTH_STAGE_EMAIL_CONFIRMATION` tracing
  - fails closed with `AUTH_EMAIL_NOT_CONFIRMED` for user/admin credential bootstrap when provider state is unconfirmed
  - propagates confirmation truth into JWT/session projection instead of forcing `emailVerified: null`
- `confirm-email-panel.tsx` now:
  - validates callback-produced token against `supabase.auth.getUser(idToken)` confirmation state before Auth.js bootstrap
  - maps `AUTH_EMAIL_NOT_CONFIRMED` to actionable invalid-link/refresh-confirmation UX messaging

Verification:
- `shell: web-lint-post-polish`: pass
- `shell: web-typecheck-post-polish`: pass
- `npm run lint --workspace @zootopia/web`: pass
- `npm run typecheck --workspace @zootopia/web`: pass
- `npm run build --workspace @zootopia/web`: pass

Risks / Notes:
- A stale concurrent Next build process was detected once during verification and was cleared before the final successful build run.
- This pass intentionally does not alter proxy role gating ownership or profile-completion ownership; those remain in their existing server-authoritative boundaries.
- No admin privilege widening or client-side secret exposure was introduced.

---

2026-04-15 — Health/Performance Audit Pass + Credit Refresh Coalescing
========================================================================
Type: Performance hardening / cache intent clarity / server-boundary-safe optimization
Scope: protected-shell credit polling path, admin mutable API cache declarations, ledger truth refresh

Summary:
- Completed full ledger-first and source-first audit workflow, then ran an Explore subagent for repository-wide health/performance findings before editing.
- Validated findings directly in current source and applied only evidence-backed, low-risk fixes.
- Reduced duplicate owner credit refresh requests in the protected shell by coalescing concurrent triggers into a single in-flight fetch and by skipping interval refreshes while tabs are hidden.
- Hardened mutable admin API cache intent by explicitly marking admin overview/user-list routes as dynamic with no revalidation.
- Preserved auth/session/admin/owner boundaries; no backend authority moved to the client.

Files Changed:
- apps/web/components/layout/protected-shell.tsx
- apps/web/app/api/admin/overview/route.ts
- apps/web/app/api/admin/users/route.ts
- zootopia-club-next-ledger.txt

Behavioral Changes:
- Protected shell credit refresh:
  - added in-flight request coalescing so simultaneous focus/visibility/event/interval triggers share one `/api/assessment/credits` request
  - added summary equality guard to avoid unnecessary state updates/rerenders when credit payload values are unchanged
  - interval refresh now skips background polling when `document.visibilityState !== "visible"`
- Admin route cache guard clarity:
  - `/api/admin/overview` now exports `dynamic = "force-dynamic"` and `revalidate = 0`
  - `/api/admin/users` now exports `dynamic = "force-dynamic"` and `revalidate = 0`
  - existing `applyNoStore(...)` response headers remain in place

Verification:
- `shell: web-lint-post-polish`: pass
- `shell: web-typecheck-post-polish`: pass
- `shell: web-build-post-polish`: pass

Risks / Notes:
- This pass intentionally avoided broad repository or storage cleanup refactors to keep changes surgical and boundary-safe.
- Existing stale editor diagnostics referencing historical renderer paths were not treated as build blockers in this pass because web typecheck/build succeeded against current source.

---

2026-04-14 — User Password Policy + Forgot/Reset + Settings Password Change Hardening
========================================================================================
Type: Auth hardening / password lifecycle expansion / user-lane security boundary enforcement
Scope: Public auth pages, user password APIs, settings security UI, shared route/policy contracts, migration persistence

Summary:
- Implemented a full regular-user password lifecycle with strict admin-lane separation: forgot-password request, recovery validation/completion, and in-account password change.
- Added `/forgot-password` and `/reset-password` public pages with secure callback finalization and explicit policy-safe password update UX.
- Added `/api/auth/password/forgot`, `/api/auth/password/recovery`, and `/api/auth/password/change` server routes with stable API error contracts and fail-closed admin blocking.
- Added password security metadata persistence for reset/change events without storing raw passwords or password hashes.
- Enforced shared regular-user password policy at sign-up (`login-panel.tsx`), recovery reset, and settings password-change surfaces.

Files Changed:
- packages/shared-config/src/routes.ts
- packages/shared-utils/src/validation.ts
- apps/web/lib/password-policy.ts
- apps/web/lib/server/password-security-events.ts
- supabase/migrations/20260414230000_user_password_security_events.sql
- apps/web/app/api/auth/password/forgot/route.ts
- apps/web/app/api/auth/password/recovery/route.ts
- apps/web/app/api/auth/password/change/route.ts
- apps/web/components/auth/forgot-password-panel.tsx
- apps/web/components/auth/reset-password-panel.tsx
- apps/web/app/(public)/forgot-password/page.tsx
- apps/web/app/(public)/reset-password/page.tsx
- apps/web/components/auth/login-panel.tsx
- apps/web/components/settings/password-change-panel.tsx
- apps/web/app/(protected)/settings/page.tsx
- apps/web/proxy.ts
- zootopia-club-next-ledger.txt

Behavioral Changes:
- User sign-up now blocks weak passwords before provider submission and surfaces localized policy guidance.
- Login surface now links to forgot-password and pre-fills email into the recovery request flow.
- Recovery route now validates token identity server-side, blocks admin identities, and persists completion metadata.
- Settings now exposes an in-account password change panel for non-admin users and enforces immediate re-authentication handoff after success.
- Proxy matcher now treats forgot/reset/confirm/login as auth-entry routes and redirects already-authenticated sessions away from them.

Verification:
- `shell: web-lint-post-polish`: pass
- `shell: web-typecheck-post-polish`: pass
- `shell: web-build-post-polish`: pass

Risks / Notes:
- Existing unrelated diagnostics in `apps/web/lib/server/assessment-print-renderer.ts` were pre-existing and not modified in this pass.
- Recovery/security event persistence is intentionally best-effort safe; completion route returns explicit `PASSWORD_RECOVERY_COMPLETE_FAILED` if metadata write fails.

---

2026-04-15 — Assessment Type-Oriented Rendering + Grouped Export Safety Completion
===================================================================================
Type: Assessment normalization hardening / grouped preview-export rendering / modular type-ownership completion
Scope: normalized question metadata, grouped preview/result rendering, DOCX/Fast/Pro export grouping, page-budget safety, ledger truth refresh

Summary:
- Read the ledger first, reviewed the attached implementation guide as the source of truth, verified the live assessment request/prompt/normalization path in source, and used an explorer subagent before editing.
- Confirmed the existing request pipeline already sends user-selected question types plus distribution to the backend and prompt orchestrator, so this pass stayed surgical and did not rewire auth, admin boundaries, owner-scope, or assessment generation ownership.
- Completed the missing type-oriented rendering/export layer by making normalized preview data section-aware, adding modular renderer ownership per active type, and grouping preview/DOCX/Fast/Pro outputs by selected type only.
- Preserved tolerance for imperfect model output: extra/unselected types remain ignored in final grouped display/export, and missing fields now degrade to safe blanks/placeholders instead of breaking viewer/export surfaces.

What Was Already Verified As Current Before Editing:
- UI request payload already sends canonical selected question types plus `questionTypeDistribution`.
- Prompt orchestration already injects selected types and distribution into the model prompt.
- Provider normalization already canonicalizes supported active types and skips explicit unsupported/unselected returned types instead of failing the whole generation.
- `buildAssessmentPreview()` was already the correct server-authoritative seam for final selected-type filtering and grouped section generation.

Files Changed:
- `packages/shared-types/src/assessment.ts`
- `apps/web/lib/assessment-question-display.ts`
- `apps/web/lib/assessment-preview-model.ts`
- `apps/web/lib/assessment-render-copy.ts`
- `apps/web/lib/assessment-file-layout.ts`
- `apps/web/lib/server/assessment-preview.ts`
- `apps/web/components/assessment/assessment-result-viewer.tsx`
- `apps/web/components/assessment/assessment-grouped-view.tsx`
- `apps/web/components/assessment/question-renderer-switch.tsx`
- `apps/web/components/assessment/question-section.tsx`
- `apps/web/components/assessment/renderers/type-renderers.tsx`
- `apps/web/components/assessment/renderers/rendering-primitives.tsx`
- `apps/web/components/assessment/renderers/render-mcq.tsx`
- `apps/web/components/assessment/renderers/render-true-false.tsx`
- `apps/web/components/assessment/renderers/render-definition.tsx`
- `apps/web/components/assessment/renderers/render-terminology.tsx`
- `apps/web/components/assessment/renderers/render-short-answer.tsx`
- `apps/web/components/assessment/renderers/render-fill-blanks.tsx`
- `apps/web/components/assessment/renderers/render-comparison.tsx`
- `apps/web/components/assessment/renderers/render-scientific-term.tsx`
- `apps/web/lib/server/assessment-exporter.ts`
- `apps/web/lib/server/assessment-fast-print-renderer.ts`
- `apps/web/lib/server/assessment-pro-print-renderer.ts`
- `zootopia-club-next-ledger.txt`

Behavioral Changes:
- Normalized metadata:
  - `AssessmentQuestionRenderMetadata` now carries explicit `answerMode`, `renderVariant`, and `exportVariant` fields so rendering/export surfaces can branch by normalized backend-authored intent instead of reparsing raw text ad hoc.
  - Preview question items now carry section metadata (`sectionKey`, `sectionTitle`, `sectionHeading`, `sectionOrder`, `displayOrder`, `startsSection`) so grouped rendering/export stays deterministic and selection-order-safe.
  - Legacy or older persisted rendering payloads are re-hardened during preview shaping by merging fresh derived rendering metadata without changing the stored canonical question text/answer text.
- Grouped preview/result rendering:
  - Added dedicated grouped-view ownership:
    - `assessment-grouped-view.tsx`
    - `question-section.tsx`
    - `question-renderer-switch.tsx`
  - Added dedicated active-type renderer ownership:
    - `render-mcq.tsx`
    - `render-true-false.tsx`
    - `render-definition.tsx`
    - `render-terminology.tsx`
    - `render-short-answer.tsx`
    - `render-fill-blanks.tsx`
    - `render-comparison.tsx`
    - `render-scientific-term.tsx`
  - Preview/result pages now render section headings only where the normalized section actually starts, which allows a section to continue onto a later page without duplicating headings on every continuation page.
  - Missing prompt/answer/tag/supplemental data now falls back to stable blank-safe UI instead of crashing the page shell.
- Grouped export behavior:
  - DOCX export now iterates `preview.questionSections`, emits section headings, and uses type-aware answer labels from normalized question metadata.
  - Fast and Pro HTML/PDF lanes now inject normalized section headings before the first question of each grouped type and keep heading-plus-first-card ownership inside a page-safe wrapper.
  - Shared page-slot complexity budgeting now counts section-heading cost so the grouped export surface still protects against footer collisions and overflow regressions.

Shared vs Isolated Ownership After This Pass:
- Still shared / server-authoritative:
  - request validation
  - prompt orchestration
  - provider normalization and selected-type filtering
  - preview shaping in `apps/web/lib/server/assessment-preview.ts`
  - page-budget math in `apps/web/lib/assessment-file-layout.ts`
- Newly isolated by question type:
  - active question-type preview/result card behavior under
    `apps/web/components/assessment/renderers/*`
- Still lane-isolated:
  - Fast PDF/browser-print HTML in
    `apps/web/lib/server/assessment-fast-print-renderer.ts`
  - Pro PDF capture HTML in
    `apps/web/lib/server/assessment-pro-print-renderer.ts`
- Intentionally unchanged:
  - auth and owner-scope guards
  - admin separation
  - assessment generation route ownership
  - preview/result route ownership
  - export route context/auth gating

Verification:
- `npm run lint:web`: pass
- `npm run typecheck:web`: pass
- `npm run build:web`: pass

Verification Notes:
- Initial sandboxed `typecheck`/`build` attempts hit Windows `EPERM` writes under `apps/web/.next` (`routes.d.ts`, `trace`) rather than application-type errors.
- Re-running the verification commands with elevated filesystem permissions resolved the environment-level file-lock issue without additional code changes.
- Elevated `typecheck` completed cleanly with `✓ Types generated successfully`.
- Elevated `build` completed successfully on Next.js `16.2.2`; optimized production build, TypeScript pass, page-data collection, and route generation all finished successfully.

Risks / Notes:
- This pass intentionally did not widen the active generation catalog beyond the current canonical 8 active types; legacy types still remain supported at the normalization/render-metadata level for backward compatibility but are not reintroduced to the selection UI here.
- Existing unrelated worktree changes outside this implementation area were left intact and not reverted.
- The grouped export layout now depends on normalized section metadata staying authoritative; future agents should extend that server-owned metadata path first before changing viewer/export grouping behavior.

---

2026-04-15 — Assessment Type-Oriented System Strict Verification Audit
========================================================================
Type: Source-traced verification / AI-contract audit / Supabase persistence audit
Scope: assessment request payload, prompt orchestration, provider JSON contract, normalized question model, grouped preview/export path, active persistence model

Summary:
- Performed a strict verification-only pass after reading the ledger and re-reading `Guide-Files/ASSESSMENT_TYPE_ORIENTED_RENDERING_AND_EXPORT_SYSTEM_GUIDE.md` as the source of truth.
- Re-traced the live path from Assessment Studio submission through backend validation, prompt orchestration, provider execution, normalization, persistence, preview shaping, and DOCX/Fast/Pro export consumption.
- No code changes were made in this pass; this entry records verified truth plus the remaining contract/persistence gaps.

Verified Correct:
- Selected canonical question types and `questionTypeDistribution` are sent from the client payload, validated on the backend, injected into the assessment prompt, and used to shape fallback generation sequence.
- Prompt orchestration explicitly asks for valid JSON only, predictable question keys, canonical `type`, canonical `difficulty`, and type-aware `structuredData`; provider requests also force JSON-mode responses (`responseMimeType: "application/json"` for Google and `response_format: { type: "json_object" }` for Qwen).
- Provider output is normalized into structured typed question records with `type`, `difficulty`, `tags`, `structuredData`, and lightweight render/export metadata (`answerMode`, `renderVariant`, `exportVariant`).
- Final preview rendering is grouped by selected type only through `buildAssessmentPreview()` -> `buildQuestionSections()` -> `preview.questionSections` / `preview.questions`.
- Extra or unselected provider-returned question types are skipped during normalization and therefore do not reach final grouped preview/export output.
- Missing prompt/answer/rationale/tag/structured science fields degrade safely through normalization plus renderer-safe fallback helpers instead of crashing preview/export.
- DOCX, Fast PDF, and Pro PDF all consume the normalized grouped preview path rather than raw provider text.
- Shared page-budget logic counts grouped headings and card complexity, and both Fast/Pro HTML lanes keep the section heading attached to the first grouped question block to reduce footer-collision risk.

Partially Correct / Still Weaker Than The Guide:
- The AI result contract is structured and usable, but it is not yet as rich as the guide’s ideal professional record model:
  - no persisted `rawModelResult`
  - no separate persisted `normalizedQuestions` envelope distinct from the generation snapshot
  - no persisted `renderModelVersion` / `normalizationVersion`
  - no persisted section/group ordering fields on stored questions
- The prompt contract is explicit JSON, but it does not ask the model for richer grouping/order/export metadata. Grouping remains derived server-side from selected-type order rather than being carried as first-class stored result metadata.
- The persisted question contract is solid for current rendering/export, but future analytics/filtering/auditing will still depend on querying nested JSON blobs plus preview-time derivation instead of a richer dedicated result schema.
- `short_answer` has a dedicated renderer file, but `question-renderer-switch.tsx` still reaches it through the default branch instead of an explicit `short_answer_card` case.

False Assumptions Ruled Out:
- It is false to assume raw provider/model output is stored in Supabase today. The raw provider payload is transient inside `apps/web/lib/server/ai/execution.ts` and is not persisted in the saved generation record or canonical-result artifact.
- It is false to assume section metadata is stored in the durable assessment record. `sectionKey`, `sectionHeading`, `sectionOrder`, `displayOrder`, and `startsSection` are generated in the preview model, not saved in the core persisted question list.
- It is false to assume the active Supabase persistence shape is a dedicated typed `assessment_generations` relational contract. The live repository uses the generic `zc_entities` JSONB document-store path (`collection("assessmentGenerations")`) for assessment generation persistence.

Supabase Storage Reality:
- Active persistence is backend-authoritative and owner-scoped, but it is still a JSON snapshot model:
  - durable generation record saved via repository normalization into `zc_entities`
  - optional canonical-result JSON artifact saved to private object storage
  - export artifacts mirrored into artifact metadata records for lookup/caching
- This is maintainable for the current product flow, but it is weaker than a richer, classification-first assessment result schema if future grouped analytics or structured querying become a core product requirement.

Verification:
- `npm run lint`: pass
- `npm run typecheck`: pass
- `npm run build`: pass

Risks / Notes:
- Current architecture is strong on runtime normalization, grouped rendering, and export safety.

---

2026-04-15 — Assessment Prompt Admin Entitlement System (Durable Grant/Revocation)
===================================================================================
Type: Authorization hardening / admin governance / prompt-feature access control
Scope: assessment prompt lock runtime, unlock route gate, credit-account contract, admin user visibility/actions

Summary:
- Upgraded prompt access from session-cookie-only behavior into a durable
  admin-managed entitlement model per user.
- Added persistent prompt entitlement state on the existing assessment credit
  account contract (`assessmentPromptEntitlement: enabled|disabled`) and kept
  defaults fail-closed (`disabled`) for normal users.
- Preserved admin bypass semantics: admin users remain always unlocked.
- Preserved existing unlock-password/env-password mechanism as a second gate for
  entitled users (no account-password flow coupling introduced).
- Added admin controls to grant/revoke prompt entitlement on
  `/admin/users/[uid]` and added user-list visibility badges on `/admin/users`
  so admins can quickly see who is enabled.

What Changed:
- Shared contract:
  - `packages/shared-types/src/assessment.ts`
  - Added `AssessmentPromptEntitlement` type and
    `assessmentPromptEntitlement` on `AssessmentCreditAccountRecord`.
- Repository/persistence:
  - `apps/web/lib/server/repository.ts`
  - Normalizes prompt entitlement with safe default (`disabled`).
  - Added helpers:
    - `getAssessmentPromptEntitlementForUser(...)`
    - `setAssessmentPromptEntitlementForUser(...)`
    - `listUserUidsWithAssessmentPromptEntitlementEnabled(...)`
- Prompt access enforcement:
  - `apps/web/lib/server/assessment-prompt-lock.ts`
  - Entitlement check is now the first gate for non-admin users.
  - Revoked users remain locked even if a stale unlock cookie exists.
- Unlock route hardening:
  - `apps/web/app/api/assessment/prompt-unlock/route.ts`
  - Non-entitled users are denied unlock attempts and stale unlock cookies are cleared.
- Admin UI:
  - `apps/web/app/(protected)/admin/users/[uid]/page.tsx`
    - Added prompt entitlement status card + toggle action.
    - Added server-side mutation helper + audit logging.
  - `apps/web/app/(protected)/admin/users/page.tsx`
    - Loads enabled prompt-entitlement uid set for visibility.
  - `apps/web/components/admin/users-table.tsx`
    - Renders per-user prompt access enabled/locked badges.

Password/Auth Isolation Verification:
- No changes were made to:
  - `/api/auth/password/forgot`
  - `/api/auth/password/recovery`
  - `/api/auth/password/change`
  - shared password policy validation
  - login/session bootstrap behavior
- Prompt entitlement logic is isolated from account-password lifecycle logic.

Migration Decision:
- No SQL migration was required for this pass.
- Rationale: active repository persistence for this domain uses the
  `zc_entities` document-store contract (`assessmentCreditAccounts` collection),
  so adding one account field is a backward-compatible, durable server-side
  contract extension without relational schema coupling.

Verification:
- `shell: web-lint-post-polish`: pass
- `shell: web-typecheck-post-polish`: pass
- `shell: web-build-post-polish`: pass

Risks / Notes:
- Existing users default to `assessmentPromptEntitlement=disabled` unless admins
  explicitly enable the feature for their account.
- Entitled non-admin users still rely on the existing unlock-password session
  mechanism when env lock mode is enabled; this is intentional to preserve the
  prior operator flow while adding durable admin control.
- Current architecture is weaker on long-term raw-vs-normalized auditability and professional persistence richness because the saved assessment record remains one normalized generation snapshot instead of a richer versioned result envelope.
- Future agents should treat the preview model as the current grouping authority, but should extend the persisted assessment contract first if analytics, auditing, or advanced filtering become first-class requirements.

---

2026-04-15 — Assessment Phase 2 Contract Hardening (Raw Model Persistence + Versioned Normalized Result)
=========================================================================================================
Type: Backend contract hardening / persistence-model enrichment / prompt-contract strengthening / Supabase index enhancement
Scope: shared assessment types, assessment prompt orchestration, assessment execution persistence, assessment record normalization, Supabase `zc_entities` assessment-generation indexes

Summary:
- Completed the requested Phase 2 hardening pass without rewriting the working preview/export/auth/owner/admin flow.
- Persisted a new `rawModelResult` alongside each generated assessment so the saved record now retains the parsed model-returned JSON and raw JSON text for debugging, auditability, and future re-normalization.
- Added a richer versioned `normalizedResult` envelope that stores backend-authored grouped/orderable/classification-ready assessment metadata while preserving the existing top-level `questions` and `meta` compatibility contract.
- Strengthened the AI prompt JSON contract to request a professional structured payload with explicit stable keys (`contractVersion`, `selectedQuestionTypes`, `requestedDistribution`, `displayOrder`, `sectionKey`, `sectionTitle`, `answerMetadata`) while keeping renderer/export ownership server-side.
- Kept Supabase persistence on the active `zc_entities` model and added a narrow migration for new assessment-generation result/version indexes instead of introducing a risky new table.

Files Changed:
- packages/shared-types/src/assessment.ts
- apps/web/lib/server/ai/prompt-orchestrator.ts
- apps/web/lib/server/ai/execution.ts
- apps/web/lib/server/assessment-records.ts
- supabase/migrations/20260415120000_assessment_generation_phase2_contract_indexes.sql
- zootopia-club-next-ledger.txt

What Was Added:
- Shared contract hardening:
  - added version constants:
    - `ASSESSMENT_PROMPT_CONTRACT_VERSION`
    - `ASSESSMENT_NORMALIZATION_VERSION`
    - `ASSESSMENT_RENDER_MODEL_VERSION`
  - added `AssessmentRawModelResult`
  - added `AssessmentNormalizedResult`
  - added `AssessmentNormalizedQuestion`
  - added normalized section/group/order/classification/source metadata interfaces
  - extended `AssessmentGeneration` with optional:
    - `rawModelResult`
    - `normalizedResult`
- Prompt contract hardening:
  - prompt now explicitly asks the model for versioned JSON with exact predictable top-level keys
  - prompt now requests:
    - echoed `selectedQuestionTypes`
    - echoed `requestedDistribution`
    - per-question `displayOrder`
    - per-question `sectionKey`
    - per-question `sectionTitle`
    - optional `answerMetadata`
  - prompt explicitly forbids hallucinated UI-only renderer/export ids and keeps render/export authority on the backend
- Execution/persistence hardening:
  - provider execution now captures both:
    - parsed provider payload
    - raw JSON text returned by the model
  - `generateAssessment(...)` now persists:
    - `rawModelResult`
    - backend-authored `normalizedResult`
  - normalization now records ignored/unselected returned type keys/counts instead of discarding that fact completely
  - persisted top-level `questions` now come from `normalizedResult.normalizedQuestions`, so preview/export continue to read the stable legacy field while benefiting from the richer model
- Record readback hardening:
  - `normalizeAssessmentGenerationRecord(...)` now preserves and normalizes:
    - `rawModelResult`
    - `normalizedResult`
  - readback prefers persisted normalized questions when present
  - older rows are backfilled safely into the new normalized contract from legacy `questions`/`meta`/`request`
  - missing request question-type metadata now degrades more safely by inferring available types from saved questions before falling back to the default type
- Supabase schema/storage hardening:
  - stayed on the live `zc_entities` persistence model
  - added targeted partial indexes for `assessmentGenerations` on:
    - `rawModelResult.provider`
    - `normalizedResult.promptContractVersion`
    - `normalizedResult.normalizationVersion`
    - `normalizedResult.renderModelVersion`
    - `normalizedResult.selectedQuestionTypes` (GIN)

What Stayed Shared vs Isolated:
- Shared / preserved:
  - repository save/load ownership
  - owner-scope enforcement
  - admin/user separation
  - preview/result route ownership
  - DOCX export path
  - Fast PDF export path
  - Pro PDF export path
  - canonical-result artifact behavior
- Isolated to Phase 2:
  - richer assessment result contract
  - prompt JSON contract wording
  - execution-time raw-model capture
  - record normalization/backfill logic
  - additive Supabase indexing

Backward Compatibility Notes:
- Existing top-level `questions` and `meta` were not removed.
- Existing preview/export consumers continue to work because they still read `generation.questions`, which is now fed from the richer normalized model.
- Existing saved generations without `rawModelResult` or `normalizedResult` are normalized safely on read.
- No UI redesign was introduced.
- No auth, owner-scope, admin separation, or export-route authority moved to the client.
- No new dedicated relational assessment-results table was introduced in this pass.
- `supabase` CLI was not available in this workspace on 2026-04-15, so the migration file was added manually using the repo’s existing timestamped migration pattern.

Verification:
- `npm run lint`: pass
- `npm run typecheck`: pass
- `npm run build`: pass

Risks / Notes:
- `rawModelResult` now persists the parsed model JSON and raw JSON text, but it intentionally does not store full HTTP envelopes or duplicated document-context strings to avoid bloating both `zc_entities` rows and canonical-result artifacts.
- Grouped preview/export rendering code was intentionally left stable in Phase 2; the richer contract is threaded underneath the existing successful display/export path rather than forcing a UI rewrite.
- Future analytics/reporting work should prefer extending the new `normalizedResult` contract and its indexes before introducing any parallel assessment-result shape.

---

2026-04-15 — Multi-User Isolation / Session Truth / Admin Separation Audit
==========================================================================
Type: strict production-readiness audit / isolation verification / minimal auth hardening
Scope: Auth.js session truth, Supabase auth bootstrap, admin separation, owner-scope enforcement, storage ownership, credits, email confirmation, password reset/change, fallback behavior, concurrency-readiness

Summary:
- Performed a source-traced audit after reading the ledger and re-checking the live Next.js and Supabase auth/session paths before editing.
- Verified the real protected-route, admin-route, owner-scope, export, storage, and account-security code paths instead of relying on assumptions.
- Applied one minimal hardening change in `apps/web/lib/server/session.ts` so security-sensitive server routes now fail closed when live user lookup is unavailable instead of falling back to stale signed-cookie claims.

Fully Correct:
- User-to-user isolation is strongly enforced on the live document, assessment, infographic, and export paths:
  - uploads are created with `ownerUid` from server session only
  - read/delete paths use owner-bound helpers such as `getDocumentByIdForOwner`
  - assessment result/export/readback paths use `getAssessmentGenerationForOwner`
  - infographic readback uses `getInfographicGenerationForOwner`
  - storage reads/writes/deletes re-assert canonical owner-scoped paths through `assertOwnerScopedStoragePath`
- Admin separation is intentionally distinct from user auth:
  - separate admin bootstrap/provider
  - allowlist-gated identifier resolution
  - explicit admin-claim activation checks
  - user lane blocks allowlisted admin identities
  - admin role mutation is limited to allowlisted admin emails only
- Credits remain owner-bound and transaction-backed:
  - reservation/commit path is server-authoritative
  - idempotency is owner-scoped
  - admin credit mutation blocks self-mutation
  - save/commit path rejects owner mismatch
- Email confirmation flow is professional and fail-closed in the important places:
  - resend callbacks are pinned to server-derived origin
  - governance state is durable and transaction-backed in Postgres
  - provider rejection rolls back consumed attempts except cooldown
  - final confirmation success is checked against live Supabase user state before session bootstrap is treated as complete
- Password reset / password change flows are user-scoped and professional:
  - forgot-password returns privacy-preserving accepted responses
  - reset callback target is server-derived
  - recovery token is verified server-side before completion metadata is accepted
  - in-account password change requires current password re-auth and attempts session hardening with `signOut({ scope: "others" })`

Partially Correct:
- `apps/web/proxy.ts` uses signed-cookie session claims for redirect classification only.
  - This is acceptable as a UX routing layer because server routes/layouts still re-check live session truth.
  - It must not become the authority for admin, suspension, or owner-scope decisions.
- Raw repository lookup helpers such as `getDocumentById`, `getAssessmentGenerationById`, and `getInfographicGenerationById` are safe today because current callers wrap them with owner/admin checks.
  - They remain sharp edges for future misuse if a later route bypasses the owner-bound wrappers.
- Production durable persistence posture is strong:
  - repository memory fallback is already blocked when production durable persistence is required
  - missing auth/database runtime throws instead of silently fragmenting truth across instances
  - development/test can still use memory fallback intentionally

Isolation / Session / Stale-Truth Findings:
- Main real weak point before this pass:
  - `apps/web/lib/server/session.ts` previously fell back to signed session claims when `getUserByUid()` failed
  - that could preserve stale truth for `role`, `status`, `profileCompleted`, and admin/user routing decisions during backend lookup failure
  - affected `getAuthenticatedSessionUser()`, `getAdminSessionUser()`, `requireAdminUser()`, password change, exports/downloads, protected pages, and other session-gated routes
- Hardening applied:
  - `getVerifiedSessionContext()` now requires a live persisted/auth-backed user lookup
  - lookup failure now rejects the session for that request instead of trusting stale cookie claims
  - this keeps admin demotion, suspension, account deletion, and stale role/status truth from surviving lookup failure on sensitive server paths

Fallback / In-Memory Findings:
- `apps/web/lib/server/request-rate-limit.ts` is intentionally process-local `globalThis` state.
  - good for local friction
  - not a globally enforced production control across multiple replicas
  - should not be treated as a 3000-user security guarantee
- `sweepExpiredUploadedSources()` uses per-instance throttling via `globalThis`.
  - acceptable as best-effort cleanup
  - not a cross-instance coordination mechanism
- Verification resend governance is materially stronger than the lightweight auth rate-limit path because it is Postgres-backed and transactional.

Admin-Boundary Findings:
- Admin-only APIs and admin pages are consistently gated through `getAdminSessionUser()` / `requireAdminUser()`.
- Admin role/status mutations revoke refresh tokens and update server-side auth truth.
- Admin export stays server-authoritative, but workbook generation is operationally expensive because it fans out across user auth metadata plus per-user credit lookups.

Scalability Findings For 3000+ Concurrent Users:
- Architecture is broadly suitable in principle for isolated multi-user operation because ownership is server-derived and durable persistence is required in production.
- Real bottlenecks/operational risks still present:
  - every protected request depends on live `getUserByUid()` auth lookup, which improves security but adds Supabase Auth pressure under heavy load
  - admin users export is N+1 for credit-state enrichment and can degrade with large user counts
  - password-forgot/admin-resolver rate limiting is per-instance only
  - Postgres pool in `apps/web/lib/server/zootopia-postgres-adapter.ts` is intentionally capped at `max: 3` per instance; safe for Supabase pool limits, but it constrains per-instance burst throughput and assumes horizontal scaling plus PgBouncer transaction pooling
- No code evidence was found that would allow one normal user to read, mutate, or export another user's records through the live user routes reviewed in this pass.

Files Changed:
- `apps/web/lib/server/session.ts`
- `zootopia-club-next-ledger.txt`

Verification:
- `npm run lint`: pass
- `npm run typecheck`: pass
- `npm run build`: pass

Risks / Notes:
- This pass intentionally did not redesign proxy behavior, repository shape, or admin export architecture because the request was for surgical hardening without breaking working behavior.
- Future agents must not reintroduce signed-cookie fallback for security-sensitive server session truth.
- If stronger scale protections are needed later, prioritize:
  - globally coordinated rate limits
  - reduced live-auth read amplification on hot protected routes
  - batched/admin-export enrichment rather than per-user serial fan-out

---

2026-04-15 — Upload Flow Production Fix / Direct Storage Transport / Non-JSON Hardening
========================================================================================
Type: production bug fix / upload transport hardening / end-to-end flow verification
Scope: `/upload` page, `/api/uploads`, Storage signed uploads, document persistence, linked-document handoff, assessment client error handling

Summary:
- Investigated the real `/upload` failure after reading the ledger first and tracing the live upload/storage/session/result pipeline before editing.
- Confirmed the visible crash was real: `UploadWorkspace` was unconditionally calling `response.json()` and could crash on plain-text or malformed bodies.
- Confirmed the server route could emit non-JSON before its structured error handler because `request.formData()` was outside the route `try/catch`.
- Confirmed the deeper production mismatch: the app advertises 50MB uploads, but the old path still proxied the full file through `/api/uploads`, which is not production-safe for large uploads on Vercel-class function body limits.
- Reworked upload transport surgically so the browser now uploads directly to an owner-scoped signed Supabase Storage path, then the server finalizes the document record from that stored object.

Real Root Cause:
- Root cause 1:
  - `/upload` sent the whole file through the route handler as multipart form data.
  - Large uploads could fail before application JSON error handling, surfacing non-JSON responses like `Request Entity Too Large`.
  - The client then crashed trying to parse that body as JSON.
- Root cause 2:
  - The repo did not previously implement a supported browser-to-Supabase signed upload path for `/upload`.
  - The observed `No API key found in request` class is what Supabase returns when a Storage request is made without a publishable key header.
  - The new client path now uses the official browser Supabase client plus a server-issued signed upload token, so the upload request carries the proper Supabase client auth envelope instead of relying on ad hoc transport.

What Changed:
- `apps/web/app/api/uploads/route.ts`
  - Added JSON `prepare` and `complete` modes on `POST /api/uploads`.
  - `prepare`:
    - authenticates the current session
    - validates the file descriptor
    - generates a server-owned `documentId`
    - builds the canonical owner-scoped path `users/{ownerUid}/documents/{documentId}/{fileName}`
    - signs that exact Storage object path with the service-role client
  - `complete`:
    - re-authenticates the current session
    - recomputes the canonical owner-scoped path from session owner + prepared document id + file name
    - downloads the uploaded object from secure Storage
    - verifies byte size
    - creates the document metadata record from the authoritative stored object
    - persists the document through the existing repository path
  - Hardened multipart fallback so `request.formData()` parse failures now return structured JSON errors instead of raw framework responses.
  - Made upload/delete audit logging best-effort so logging degradation does not falsely turn a successful user action into a failed upload/delete.
- `apps/web/lib/server/supabase-blob-storage.ts`
  - Added a server helper to create signed upload URLs for the private bucket.
  - Exposed the bucket name through a helper so the browser path uses the real bucket without duplicating hidden constants.
- `apps/web/lib/server/document-runtime.ts`
  - Extended `createDocumentRecord()` so finalize mode can reuse a pre-uploaded owner-scoped Storage object instead of re-uploading the binary.
  - Re-asserts owner scope on the provided storage path before trusting it.
- `apps/web/components/upload/upload-workspace.tsx`
  - Browse and drag/drop still share the same file-entry helper.
  - The upload client now:
    - asks the server to prepare a signed direct upload
    - uploads with the official browser Supabase client via `uploadToSignedUrl`
    - asks the server to complete and persist the document
  - Added safe API response parsing so upload/delete never crash on non-JSON responses.
  - Keeps a fallback multipart path for runtimes where signed direct upload is unavailable.
- `apps/web/components/assessment/assessment-studio.tsx`
  - Hardened generation/readback fetch parsing so the post-upload assessment flow does not crash on malformed or non-JSON responses either.

Isolation / Ownership Verification:
- Upload ownership remains server-derived:
  - the browser never chooses `ownerUid`
  - the server still derives `ownerUid` from the authenticated session only
  - the signed upload path is generated server-side under the current owner namespace only
- Document persistence remains owner-bound:
  - final document save still uses the existing owner-scoped repository path
  - document read/delete/download paths still use owner-bound helpers such as `getDocumentByIdForOwner`
- Linked-document and result flow remain owner-bound:
  - assessment create still loads documents through `getDocumentByIdForOwner`
  - linked document resolution still uses the persisted owner-scoped document record
  - result save/export/readback still stay under owner-scoped generation helpers and owner-scoped Storage assertions
- No code path reviewed in this fix allows one user to read, mutate, or preview another user's uploaded document, linked document context, generated result, or export.

End-to-End Verification:
- Verified by code trace:
  - upload page -> `/api/uploads prepare` -> signed Storage path -> `/api/uploads complete`
  - saved document -> assessment linked-document resolution -> assessment generation persistence -> preview/result owner-scoped readback
- Verified live transport primitive:
  - created a real signed upload URL against the `zootopia-private` bucket
  - uploaded through the publishable-key browser client path
  - downloaded the object with the service-role client
  - content round-trip matched and cleanup succeeded
- Verified commands:
  - `npm run lint`: pass
  - `npm run typecheck`: pass
  - `npm run build`: pass

Files Changed:
- `apps/web/app/api/uploads/route.ts`
- `apps/web/components/upload/upload-workspace.tsx`
- `apps/web/components/assessment/assessment-studio.tsx`
- `apps/web/lib/server/document-runtime.ts`
- `apps/web/lib/server/supabase-blob-storage.ts`
- `zootopia-club-next-ledger.txt`

Risks / Notes:
- The multipart proxy path still exists as a compatibility fallback, but the production-safe path is now the signed direct upload flow.
- The finalize step still downloads the uploaded object server-side to extract markdown/context and persist the canonical document record; this preserves current behavior without redesigning the document pipeline.
- Full browser-authenticated UI clicking was not exercised end-to-end in a signed-in session during this pass, but the transport primitive, route/build integrity, and owner-scoped server code path were all verified directly.

---

2026-04-15 — Upload Integrity Follow-Up / Prepared Upload Lifecycle / Support Error Notes
=========================================================================================
Type: surgical integrity hardening / lifecycle cleanup / owner-scope verification / UI error refinement
Scope: signed upload prepare/finalize flow, upload cleanup sweep, logout cleanup, admin user deletion cleanup, manual delete response correctness, history truthfulness, shared operational support messaging

Summary:
- Performed a follow-up audit after the new signed direct-upload flow landed and traced the real live chain before editing:
  - `/api/uploads` prepare + complete flow
  - owner-scoped Storage path generation
  - document persistence + delete path
  - expiry/logout/admin cleanup paths
  - shared error UI surfaces
- Confirmed the biggest real gap was not cross-user scope, but lifecycle durability:
  - prepared direct uploads were writing into the final `users/{uid}/documents/{documentId}/{fileName}` namespace
  - no durable record existed for “prepared but never finalized” uploads
  - the existing sweep only knew how to delete persisted `documents` rows
- Confirmed user-facing owner scope remained strong throughout the reviewed upload/delete/download/finalize paths because all paths stay derived from authenticated session identity and are re-asserted by owner-scoped storage path guards.

Migration Decision:
- A new migration was genuinely needed in this pass.
- Reason:
  - this fix adds a new durable `uploadPreparations` lifecycle collection inside the active `zc_entities` persistence model
  - cleanup sweeps now query that collection by `expiresAt`
  - a targeted partial index was the minimal additive schema change needed to keep expiry sweeps maintainable and bounded
- Created:
  - `supabase/migrations/20260415183000_upload_preparation_lifecycle_expiry_index.sql`

What Changed:
- Durable prepared-upload lifecycle tracking:
  - added a minimal owner-scoped `uploadPreparations` record in the active repository layer
  - prepare mode now saves a lifecycle row with file identity, storage path, and expiry aligned to upload/session retention
  - complete mode now verifies the finalize request against that durable prepared row instead of trusting only client-echoed metadata
- Orphan cleanup correctness:
  - `sweepExpiredUploadedSources()` now cleans both:
    - persisted expired document rows
    - expired prepared-but-never-finalized upload rows
  - logout cleanup now clears both finalized document rows and prepared upload rows
  - admin user deletion now clears prepared upload rows/objects alongside document rows/objects
  - stale prepared rows left behind after successful finalize no longer risk deleting a live finalized file:
    - the sweep now checks whether a finalized document row already owns the same object path before deleting the binary
- Delete correctness:
  - manual delete still remains owner-scoped through `deleteDocumentForOwner(...)`
  - manual delete now also best-effort clears any stale lifecycle row for the same document id
  - the DELETE route no longer depends on a post-delete repository read to report success
  - it now builds the response from the pre-delete owner-scoped list, so a successful delete does not get reported back to the UI as a false failure because a later readback threw
- Replace / stale-link truthfulness:
  - superseded documents now record `sourceBinaryRetained: false` when their binary is retired during replacement
  - history download UI now respects that flag, so older rows no longer advertise a source download that the system has already retired
- Support/help messaging:
  - added a shared localized operational support note:
    - EN: contact the developer Elmahdy Abdallah if the platform hits an issue because it is still under development
    - AR: "في حال واجهت أي خطأ داخل المنصة، يُرجى التواصل مع المطور المهدي عبدالله. المنصة ما زالت قيد التطوير ونعمل على تحسينها باستمرار."
  - integrated subtly into meaningful operational error surfaces only, not trivial field validation
  - reused the existing expandable support-details UI pattern instead of duplicating one-off alert blocks

Files Changed:
- `apps/web/app/api/uploads/route.ts`
- `apps/web/app/api/auth/logout/route.ts`
- `apps/web/app/(protected)/(completed)/history/page.tsx`
- `apps/web/app/global-error.tsx`
- `apps/web/app/globals.css`
- `apps/web/components/upload/upload-workspace.tsx`
- `apps/web/components/assessment/assessment-studio.tsx`
- `apps/web/components/assessment/assessment-export-actions.tsx`
- `apps/web/components/infographic/infographic-studio.tsx`
- `apps/web/components/auth/auth-feedback.ts`
- `apps/web/components/auth/auth-status.tsx`
- `apps/web/components/auth/forgot-password-panel.tsx`
- `apps/web/components/auth/reset-password-panel.tsx`
- `apps/web/lib/server/document-runtime.ts`
- `apps/web/lib/server/repository.ts`
- `apps/web/lib/operational-support.ts`
- `apps/web/messages/en.json`
- `apps/web/messages/ar.json`
- `packages/shared-types/src/document.ts`
- `supabase/migrations/20260415183000_upload_preparation_lifecycle_expiry_index.sql`
- `zootopia-club-next-ledger.txt`

Verification:
- Verified by code trace:
  - prepare now persists a lifecycle row before returning the signed upload contract
  - finalize now requires the matching owner-scoped prepared row
  - expired-upload maintenance now covers unfinished prepared uploads
  - logout cleanup now clears prepared rows + finalized document rows
  - admin user deletion now clears prepared upload rows/objects too
  - manual delete remains owner-scoped and now avoids false “delete failed” responses caused by post-delete readback failure
- Verified owner-scope:
  - upload prepare/finalize/delete/download still derive ownership from authenticated session context
  - Storage path use remains guarded by owner-scoped path assertions
  - no new cross-user delete path was introduced
- Verified commands:
  - `npm run lint`: pass
  - `npm run typecheck`: pass
  - `npm run build`: pass

Risks / Notes:
- This pass intentionally did NOT redesign the upload architecture into a temp-path-then-move model.
- The chosen fix stays surgical:
  - keep the current signed direct upload contract
  - add durable pre-finalize lifecycle tracking around it
  - add the minimum index needed for expiry queries
- Browser-authenticated clicking for upload/delete/reupload was not replayed end-to-end in a signed-in UI session during this pass.
  - Manual delete / replace / cleanup verification in this entry is based on direct repository + route trace plus full lint/typecheck/build success.

---

2026-04-15 — Vercel Analytics + Speed Insights Global Integration (Root Layout)
===============================================================================
Type: Observability integration / global instrumentation wiring / minimal production-safe change
Scope: web workspace dependencies, true App Router root layout, telemetry mount uniqueness verification

Summary:
- Read this ledger first, then ran an Explore subagent and direct source verification before editing.
- Confirmed the app uses Next.js App Router and that the true global root layout owner is:
  - `apps/web/app/layout.tsx`
- Confirmed there were no prior source mounts/imports for Vercel Analytics or Speed Insights.
- Installed latest stable packages into `@zootopia/web`:
  - `@vercel/analytics@^2.0.1`
  - `@vercel/speed-insights@^2.0.0`
- Mounted both integrations exactly once in the global root layout body, after the main children wrapper:
  - `Analytics` from `@vercel/analytics/next`
  - `SpeedInsights` from `@vercel/speed-insights/next`

Mount Ownership and Visibility Contract:
- Global mount location:
  - `apps/web/app/layout.tsx`
- Placement:
  - inside `<body>`, after the `children` container and before closing `</body>`
- Coverage:
  - public site routes, auth routes, protected user routes, and admin routes are all covered because they all flow through the root layout
- Visibility:
  - no visible analytics widgets, banners, debug panes, or user-facing telemetry UI were added
  - integration is script-based and reports to Vercel dashboards

Files Changed:
- `apps/web/app/layout.tsx`
- `apps/web/package.json`
- `apps/web/package-lock.json`
- `package-lock.json`
- `zootopia-club-next-ledger.txt`

Verification:
- `shell: web-lint-post-polish`: pass
- `shell: web-typecheck-post-polish`: pass
- `shell: web-build-post-polish`: pass
- Duplicate mount verification:
  - source search across `apps/web/app/**`, `apps/web/components/**`, and `apps/web/lib/**` shows only one mount pair in `apps/web/app/layout.tsx`

Operational Notes:
- App-local lockfile regeneration required `--legacy-peer-deps` in this workspace because of pre-existing optional peer-resolution tension (`next-auth` / `nodemailer`), but runtime dependencies were not changed beyond the two new Vercel packages.
- Vercel dashboard enablement and redeploy are still required for data collection in production.

---

2026-04-15 — Assessment Summary Pipeline Hardening (Canonical Source Contract)
===============================================================================
Type: Surgical backend summary-contract hardening / prompt quality guard / cross-surface consistency
Scope: assessment prompt orchestration, assessment generation execution, canonical record normalization

Summary:
- Traced the full assessment summary flow end-to-end before editing:
  - prompt contract authoring
  - provider payload extraction
  - canonical generation persistence
  - record normalization readback used by preview/result/export surfaces
- Applied a minimal source-level fix so preview and all export lanes continue to consume one canonical `meta.summary`, but that field is now quality-validated and compacted server-side before persistence/use.
- Upgraded prompt guidance so providers are explicitly asked for a meaningful lecture/document summary, not generic placeholder text, with compact length targets suitable for small surfaces.

What Changed:
- Prompt contract strengthening:
  - Added explicit summary rules in assessment prompt orchestration requiring:
    - meaningful context-grounded lecture/document summary
    - compact 3–5 sentence target sized for small cards (roughly 5–8 visual lines)
- Provider-level execution alignment:
  - Updated the Qwen system prompt to explicitly require a meaningful compact summary in addition to existing JSON and emoji preservation rules.
- Canonical server summary seam:
  - Added centralized summary normalization + quality gating in `assessment-records`:
    - normalize noisy prefixes (for example `Summary:`)
    - reject weak placeholders / malformed JSON-leak summaries
    - require minimum content density (length + word diversity heuristics)
    - clamp overlong summaries to compact card-safe size with sentence-boundary preference
  - Added `buildCanonicalAssessmentSummary(...)` as the single server-owned summary resolver.
- Unified call sites:
  - Generation path now persists `meta.summary` via `buildCanonicalAssessmentSummary(...)`.
  - Record normalization/readback path now also resolves `meta.summary` via the same canonical helper.
  - This keeps preview, result API, Markdown, DOCX, and both PDF lanes aligned without per-surface fallback drift.
- Professional fallback improvement:
  - Enhanced fallback summary text in both EN/AR to include clearer educational framing and mode-aware guidance while remaining compact.

Files Changed:
- `apps/web/lib/server/ai/prompt-orchestrator.ts`
- `apps/web/lib/server/ai/execution.ts`
- `apps/web/lib/server/assessment-records.ts`
- `zootopia-club-next-ledger.txt`

Verification:
- `shell: web-lint-post-polish`: pass
- `shell: web-typecheck-post-polish`: pass
- `shell: web-build-post-polish`: pass

Risks / Notes:
- Summary quality heuristics are intentionally conservative to prevent malformed placeholder summaries from leaking into user-facing preview/export surfaces.
- Extremely brief but high-quality provider summaries may now fallback to the server-authored summary if they do not meet minimum density thresholds.
- No auth, ownership, grouped-question rendering, or export route ownership behavior was modified in this pass.

---

2026-04-15 — Protected Header Credit UX Hardening (Compact Balance + Arabic Help Popover)
==========================================================================================
Type: Surgical protected-shell UX refinement / owner-scope verification / non-mutating support action
Scope: protected header credit chip rendering, compact adjacent help action, popover close behavior

Summary:
- Completed a trace-first verification pass before editing to confirm credit authority and scope boundaries stayed server-owned:
  - `/api/assessment/credits` remains session-owned and owner-scoped
  - credit reservation/commit/release authority remains in repository server logic
  - `/api/admin/users/[uid]/credits` remains admin-only and self-mutation-blocked
- Applied a minimal UI-only patch in the protected header so users see balance value compactly beside the wallet icon across practical breakpoints.
- Added an adjacent plus action that opens a polished, non-intrusive popover with Arabic urgent-credit guidance directing requests to developer/admin Elmahdy Abdallah.

What Changed:
- `apps/web/components/layout/protected-shell.tsx`
  - Kept existing credit fetch and refresh lifecycle unchanged.
  - Updated the header credit chip layout so balance value is no longer restricted to `xl` text stack visibility.
  - Added a small adjacent `+` action next to the credit chip.
  - Added a compact header popover (informational only) with Arabic support guidance for urgent/private credits.
  - Added close guards for outside pointer interaction and `Escape` key, plus an explicit close button.
  - Preserved backend authority by keeping the new action strictly read-only (no credit mutation calls from header UI).

Files Changed:
- `apps/web/components/layout/protected-shell.tsx`
- `zootopia-club-next-ledger.txt`

Verification:
- `shell: web-lint-post-polish`: pass
- `shell: web-typecheck-post-polish`: pass
- `shell: web-build-post-polish`: pass

Risks / Notes:
- The new header help surface intentionally remains informational and does not replace admin credit tooling.
- Arabic help copy is intentionally fixed to satisfy urgent-credit guidance requirements regardless of locale.
- No repository credit arithmetic, session authority, admin guard, or assessment mutation paths were changed.

---

2026-04-15 — Assessment Prompt Lock (Admin Exempt + Server-Authoritative Unlock)
=================================================================================
Type: Feature governance hardening / backend-authoritative entitlement enforcement / premium UX lock state
Scope: assessment prompt field access, unlock verification route, assessment create-route enforcement, env controls, assessment page/studio ownership

Summary:
- Implemented a professional lock system for the Assessment prompt feature with strict server authority.
- Admin users now remain permanently unlocked with no password step.
- Normal users now see a locked prompt feature and must unlock it through server-side password verification.
- Unlock grants are user-specific, session-scoped, and stored as signed HttpOnly cookie tokens bound to:
  - authenticated user uid
  - current unlock-password fingerprint
  - session TTL expiry
- Backend now rejects locked prompt usage even if a client is tampered.

What Changed:
- Added server-only prompt-lock runtime module:
  - `apps/web/lib/server/assessment-prompt-lock.ts`
  - owns env parsing, lock mode resolution, timing-safe password checks, token signing/verification, and user access-state resolution
- Added unlock API route:
  - `apps/web/app/api/assessment/prompt-unlock/route.ts`
  - server-authoritative password verification for normal users
  - admin bypass lane (always unlocked)
  - env-disabled fallback lane (auto-unlocked)
  - issues signed HttpOnly unlock cookie on success
- Hardened assessment create backend enforcement:
  - `apps/web/app/api/assessment/route.ts`
  - blocks non-admin prompt submissions when lock is active and unlock grant is missing/invalid
  - returns explicit `ASSESSMENT_PROMPT_LOCKED` response code (tamper-resistant backend guard)
- Wired server-authoritative access state into assessment page owner:
  - `apps/web/app/(protected)/(completed)/assessment/page.tsx`
  - passes initial prompt lock/unlock state to client studio
- Added locked/unlocked premium UX in assessment studio:
  - `apps/web/components/assessment/assessment-studio.tsx`
  - Arabic-only lock messaging
  - compact password input + unlock action
  - disabled prompt textarea while locked
  - polished success state with visible check icon + subtle reveal animation
  - Arabic celebratory confirmation copy after successful unlock
- Documented env controls:
  - `.env.example`
  - added `ZOOTOPIA_ASSESSMENT_PROMPT_LOCK_ENABLED`
  - added `ZOOTOPIA_ASSESSMENT_PROMPT_UNLOCK_PASSWORD`

Env Fallback Behavior (explicit):
- Lock active only when:
  - `ZOOTOPIA_ASSESSMENT_PROMPT_UNLOCK_PASSWORD` is non-empty
  - and `ZOOTOPIA_ASSESSMENT_PROMPT_LOCK_ENABLED` is not set to false
- Lock disabled when password is removed or toggle is false.
- In disabled mode, normal users are treated as unlocked automatically.

Persistence / Access Model Decision:
- Chosen model: session-scoped unlock grant.
- No DB migration required.
- Rationale:
  - minimal, surgical, and production-safe
  - user-isolated by signed uid-bound grant
  - automatic revocation when unlock password changes or expires

Files Changed:
- `apps/web/lib/server/assessment-prompt-lock.ts`
- `apps/web/app/api/assessment/prompt-unlock/route.ts`
- `apps/web/app/api/assessment/route.ts`
- `apps/web/app/(protected)/(completed)/assessment/page.tsx`
- `apps/web/components/assessment/assessment-studio.tsx`
- `.env.example`
- `zootopia-club-next-ledger.txt`

Verification:
- `shell: web-lint-post-polish`: pass
- `shell: web-typecheck-post-polish`: pass
- `shell: web-build-post-polish`: pass

Risks / Notes:
- Unlock is intentionally session-scoped; users may need to re-unlock after session expiry.
- If lock is enabled but signing prerequisites are unavailable, unlock route fails closed with configuration error.
- No auth/admin boundary, credit flow, upload flow, export lanes, or storage ownership rules were widened.

---

2026-04-15 — Assessment Model Selector Role Split (Admin Visible / User Hidden)
===============================================================================
Type: UI role-lane refinement / admin-only control visibility / non-admin premium replacement panel
Scope: assessment setup header model-control lane, non-admin request-state guard, assessment page role contract reuse

Summary:
- Completed ledger-first and source-first verification before edits, then traced the exact ownership chain:
  `assessment/page.tsx` -> `AssessmentStudio` -> header `assessment-model` selector block.
- Implemented the requested role split for model selection in Assessment setup:
  - admin users keep the full model selector and chips exactly as before
  - normal users no longer see the selector and instead see a premium Arabic guidance panel in the same right-lane slot
- Reused existing server-derived role truth already passed into the studio (`initialPromptAccess.isAdmin`) and kept backend authority unchanged.
- Added a non-admin client-state guard that normalizes `request.modelId` back to `defaultModelId` so stale client state cannot keep a non-default selection after role-bound UI hiding.

Files Changed:
- `apps/web/components/assessment/assessment-studio.tsx`
- `zootopia-club-next-ledger.txt`

Behavioral Changes:
- Admin path:
  - `AssessmentFieldSelect` model control remains visible/interactive.
  - Existing model metadata chips remain visible.
- Normal-user path:
  - Selector is hidden.
  - Premium Arabic message panel is shown in the same model-control area.
  - Current effective model label is shown read-only for transparency.
- Request-state safety:
  - when `promptAccess.isAdmin` is false, studio state now reverts `request.modelId` to the backend-provided default model and clears stale model field errors.

Backend/API Contract Notes:
- No API route changes were required.
- `POST /api/assessment` validation remains authoritative and unchanged.
- This pass is intentionally UI-lane gating only; backend auth/admin separation remains server-owned.

Verification:
- `shell: web-lint-post-polish`: pass
- `shell: web-typecheck-post-polish`: pass
- `shell: web-build-post-polish`: pass

Risks / Notes:
- Non-admin model hiding is a presentation/UX boundary and does not introduce new backend role-based model restriction logic.
- Existing admin/user session and route ownership contracts were preserved without widening client authority.

---

2026-04-15 — Global Maintenance Mode (Env Toggle, Proxy Guard, Admin Bypass)
===============================================================================
Type: Runtime guard hardening / route-gate enforcement / premium maintenance UX
Scope: env contract, shared routes, proxy matcher coverage, public maintenance page, locale messages

Summary:
- Added server-shared maintenance flag helper:
  - `apps/web/lib/maintenance-mode.ts`
  - centralizes `ZOOTOPIA_MAINTENANCE_MODE_ENABLED` parsing with explicit fail-closed defaults
- Added canonical maintenance route constant:
  - `packages/shared-config/src/routes.ts`
  - `APP_ROUTES.maintenance = "/maintenance"`
- Added public maintenance page owner:
  - `apps/web/app/(public)/maintenance/page.tsx`
  - Arabic-first premium layout using existing auth shell controls
  - admin-session bypass path redirects into normal admin flow
  - maintenance-off safety path redirects into normal auth flow
- Hardened request-time routing enforcement in `apps/web/proxy.ts`:
  - expanded matcher coverage to include selected site pages plus `/maintenance`
  - redirects non-admin traffic on auth/public/site/protected lanes to `/maintenance`
  - preserves `/admin/login` reachability so admins can still authenticate during maintenance
  - preserves authenticated-admin bypass from maintenance redirects
- Added i18n maintenance copy:
  - `apps/web/messages/ar.json`
  - `apps/web/messages/en.json`
- Documented env contract in `.env.example`.

Files Changed:
- `apps/web/lib/maintenance-mode.ts`
- `packages/shared-config/src/routes.ts`
- `apps/web/proxy.ts`
- `apps/web/app/(public)/maintenance/page.tsx`
- `apps/web/messages/ar.json`
- `apps/web/messages/en.json`
- `.env.example`
- `zootopia-club-next-ledger.txt`

Verification:
- `shell: web-lint-post-polish`: pass
- `shell: web-typecheck-post-polish`: pass
- `shell: web-build-post-polish`: pass
- Standalone runtime probes (manual redirect-follow disabled):
  - Maintenance OFF (`ZOOTOPIA_MAINTENANCE_MODE_ENABLED=false`):
    - `/` -> `307 /login`
    - `/login` -> `200`
    - `/admin/login` -> `200`
    - `/upload` -> `307 /login`
    - `/about` -> `200`
    - `/maintenance` -> `307 /login`
  - Maintenance ON (`ZOOTOPIA_MAINTENANCE_MODE_ENABLED=true`):
    - `/` -> `307 /maintenance`
    - `/login` -> `307 /maintenance`
    - `/forgot-password` -> `307 /maintenance`
    - `/confirm-email` -> `307 /maintenance`
    - `/about` -> `307 /maintenance`
    - `/upload` -> `307 /maintenance`
    - `/maintenance` -> `200`
    - `/admin` -> `307 /admin/login`
    - `/admin/login` -> `200`

Risks / Notes:
- Proxy-level maintenance gating applies only to explicitly matched paths; future public pages should be added to maintenance matcher lists when they must be gated.
- Internal secret-gated upload cleanup maintenance route remains operationally separate from user-facing maintenance mode.

---

2026-04-15 — Assessment Non-Admin Model Panel Copy / Compact Arabic Refinement
===============================================================================
Type: UI-only refinement / compact typography tuning / non-admin lane polishing
Scope: assessment setup header non-admin model-replacement panel

Summary:
- Replaced the previous non-admin informational Arabic block in the Assessment
  model-replacement panel with the required rights ownership line:
  "جميع حقوق المنصة والتصاميم والافكار محفوظة للمطور ابن عبدالله © 2026".
- Added a compact secondary donation/support hint line:
  "تبرع لتطوير أنظمة الذكاء الاصطناعي".
- Updated panel icon treatment to use small expressive icons and tighter
  typography/spacing so the block stays elegant and horizontally efficient.

What Changed:
- `apps/web/components/assessment/assessment-studio.tsx`
  - Updated `ASSESSMENT_MODEL_VISIBILITY_COPY` to the new Arabic rights/support copy.
  - Refined only the non-admin (`promptAccess.isAdmin === false`) panel JSX to
    compact the text block and icon sizing.
  - Preserved admin branch (`AssessmentFieldSelect`) and all backend contracts.

Verification:
- `shell: web-lint-post-polish`: pass
- `shell: web-typecheck-post-polish`: pass
- `shell: web-build-post-polish`: pass

Risks / Notes:
- Change is intentionally local to non-admin presentation only; no server logic,
  role authority, model validation, or API behavior was altered.

---

2026-04-15 — Assessment Non-Admin Rights Panel Overflow Containment Refinement
===============================================================================
Type: UI-only containment hardening / responsive typography tuning / panel layout polish
Scope: assessment setup header non-admin model-replacement panel

Summary:
- Refined the non-admin rights panel to guarantee the Arabic rights line remains
  inside the panel container across small and large screens.
- Reduced visual scale and tightened line-height/gap/icon sizing to keep the
  block compact and balanced.
- Removed no-wrap/truncate pressure in the rights/support text path and switched
  to wrap-safe text behavior.

What Changed:
- `apps/web/components/assessment/assessment-studio.tsx`
  - Added panel-level overflow containment (`overflow-hidden`) for safety.
  - Tightened non-admin icon and spacing (`gap`, icon container size, icon size).
  - Updated rights-line typography to smaller responsive sizing with
    wrap-safe classes (`whitespace-normal`, `break-words`, `overflow-wrap:anywhere`).
  - Updated support-hint row to remain secondary and wrap safely instead of
    truncating text.
  - Kept admin model selector branch untouched.

Verification:
- `shell: web-lint-post-polish`: pass
- `shell: web-typecheck-post-polish`: pass
- `shell: web-build-post-polish`: pass

Risks / Notes:
- Change is scoped strictly to non-admin panel presentation.
- No backend logic, auth/role authority, model validation, or API routes changed.

---

NOTE: This ledger is a living document. Future agents should append new dated
entries to the Historical Change Log section and update relevant sections as
the project evolves. Do not delete genuine historical entries. Preserve
chronology. Mark outdated assumptions as superseded or legacy when appropriate.

================================================================================
END OF LEDGER
================================================================================
