================================================================================
ZOOTOPIA CLUB NEXT — PROJECT LEDGER
Authoritative Encyclopedia & Master Project Memory
================================================================================

Last Updated: 2026-04-19 (Supabase credit bridge audit + relational backfill hardening)
Repository: https://github.com/MahdyHQ/zootopia-club-next-no-firebase.git
Current Commit: 378c7a0063d7de2e3e75cf5d94be3409df64d46b
Framework: Next.js 16.2.2 (App Router) / React 19.2.4 / TypeScript 5
Runtime: Node.js >= 20.9.0

================================================================================
TABLE OF CONTENTS
================================================================================

  1.  Project Identity
  2.  Current Truth Snapshot
  3.  Project Purpose & Platform Direction
  4.  Architecture Style
  5.  Monorepo Structure
  6.  Dependency & Runtime Baseline
  7.  Active Deployment Model
  8.  Environment Strategy
  9.  Route Map
  10. Route Ownership Clarification
  11. Backend Runtime & Service Ownership
  12. Auth & Session Architecture
  13. Admin Authority Model
  14. Profile Completion & User Gating Model
  15. Upload / Document / Storage Flow
  16. Assessment Runtime / Result / Export Flow
  17. Infographic Flow
  18. Database / Migrations / Storage / Persistence Notes
  19. AI / Model / Provider Orchestration
  20. Redirect / Callback / Confirmation Behavior
  21. Email Verification & Resend Governance
  22. Security Assumptions & Trust Boundaries
  23. Important Folders & Files
  24. Sensitive & High-Risk Files
  25. Important Commands & Scripts
  26. Current Feature Set
  27. Deferred / Disabled / Legacy Areas
  28. Future Agent Guidance
  29. Historical Change Log

================================================================================
1. PROJECT IDENTITY
================================================================================

Name:           zootopia-club-next
Version:        0.1.0
Private:        Yes
Type:           Monorepo (npm workspaces)
Primary App:    @zootopia/web (Next.js 16.2.2)
Secondary App:  @zootopia/api (placeholder only — inactive)
Shared Packages:
  - @zootopia/shared-config   (constants, routes, env keys, feature flags)
  - @zootopia/shared-types    (TypeScript interfaces, DTOs, shared contracts)
  - @zootopia/shared-utils    (pure utilities: validation, cn, dates, RTL)

Git Remote:     origin https://github.com/MahdyHQ/zootopia-club-next-no-firebase.git
Branch:         main (primary)

Product Name:   Zootopia Club
Domain Purpose: AI-powered document analysis, assessment generation, and
                infographic creation platform with admin governance.

================================================================================
2. CURRENT TRUTH SNAPSHOT
================================================================================

As of 2026-04-19, the project is a Next.js 16.2.2 monorepo deployed on Vercel,
using Supabase as the unified backend provider (Postgres database, Auth, Storage).

Key verified facts:
- Firebase has been fully replaced by Supabase (no Firebase runtime dependencies)
- Auth.js (NextAuth v5) with JWT strategy manages sessions
- Supabase Auth provides identity (email/password only)
- Two credential providers: user-credentials and admin-credentials
- Admin access controlled by email allowlist (ZOOTOPIA_ADMIN_EMAILS)
- Auth bootstrap now fails closed when Supabase still reports email as unconfirmed
  (user + admin flows), and confirm-email callback success UI now requires
  provider-confirmed email state before Auth.js session handoff
- Regular-user password lifecycle now includes server-authoritative forgot-password,
  recovery reset, and in-account password-change flows with explicit admin-lane
  separation and password-security metadata persistence
- Public auth surfaces now include `/forgot-password` and `/reset-password`, and
  signup/reset/settings password entry points share one policy validator contract
- Public user sign-up now enters through same-origin `POST /api/auth/signup`
  instead of browser-direct `supabase.auth.signUp()`, so account creation,
  duplicate-account handling, and confirm-email redirect ownership stay
  backend-shaped without moving Auth.js trust out of the server
- Public user sign-in now performs same-origin admission preflight through
  `POST /api/auth/login/admission` before browser `signInWithPassword()`,
  using persisted cross-instance account/IP windows and admission-window-aware
  retry guidance when the auth edge is being shaped under pressure
- Login admission now short-circuits allowlisted admin emails plus
  `ZOOTOPIA_ACTIVE_NORMAL_USER_EXEMPT_EMAILS` identities before public
  auth-admission shaping, so exempt/admin users no longer depend on the
  admission-store path to bypass active normal-user capacity governance
- `AUTH_ACTIVE_USER_ADMISSION_UNAVAILABLE` now stays distinct through auth
  normalization and public login UI rendering, so temporary admission-state
  outages no longer show stale fixed-duration queue/pacing copy, and capacity-full
  fallback copy stays truthful even when a live count snapshot is unavailable
- Confirm-email success now clears the helper Supabase browser session and
  redirects back to `/login` or `/admin/login` with verified-email context;
  Auth.js bootstrap only happens after the user explicitly logs in again
- Duplicate sign-up attempts now normalize to `AUTH_ACCOUNT_ALREADY_EXISTS`
  with truthful guidance instead of surfacing as a generic upstream failure
- Profile completion gating blocks incomplete profiles from protected features
- AI providers: Google AI (Gemini) and DashScope (Qwen/Alibaba)
- Tool default models are now server-authoritative per real tool via
  `ZOOTOPIA_DEFAULT_MODEL_ASSESSMENT` and `ZOOTOPIA_DEFAULT_MODEL_INFOGRAPHIC`,
  with strict tool-scoped fallback validation
- Assessment Gemini model catalog now treats `gemini-2.5-flash-lite` and
  `gemini-3.1-flash-lite-preview` as active lanes, with
  `gemini-2.5-flash-lite` as the built-in default; legacy
  `gemini-2.5-flash` requests normalize to `gemini-2.5-flash-lite`
- Assessment export/result artifact metadata now mirrors into
  `assessmentArtifactMetadata` documents for owner/kind/status/expiry queries
- Upload records now persist normalized `fileExtension` and `contentSha256`
  metadata for durable reporting and retention tooling
- Document uploads stored with configurable retention (default 15 min)
- Assessment results stored with configurable retention (default 1440 min / 1 day)
- Export artifacts stored with configurable retention (default 15 min)
- Assessment credits limit AI generation with env-configurable UTC renewal windows
  (default 24h, daily behavior preserved)
- Admin credit mutations now persist durable per-user before/after history
  snapshots and are rendered on `/admin/users/[uid]`
- Admin credit mutations now publish the repository-returned post-commit credit
  summary for the exact target UID from both the admin detail-page server action
  flow and the admin PATCH route, without moving mutation authority out of the
  backend repository layer
- Admin user-detail same-page controls now use inline action-state feedback
  with scroll-preserving `router.refresh()` instead of same-page redirect/
  query-param remount flashes for the credit workspace, credit access toggle,
  prompt entitlement toggle, credit-grant revoke, role/status toggles, and
  per-user storage cleanup; the user-delete control intentionally keeps its
  redirect-to-list behavior by design
- Admin credit mutation follow-up work (`appendAdminLog` and live publish) is
  now explicitly best-effort after the repository commit in both admin mutation
  lanes, so audit-log degradation can no longer falsely surface a committed
  balance mutation as an API/server-action failure
- Admin prompt-entitlement mutations on `/admin/users/[uid]` now also treat
  admin audit logging as best-effort after persistence commits, preventing
  false UI failure reports when entitlement state was already durably updated
- Assessment credit diagnostics now support opt-in low-noise structured tracing
  through `ZOOTOPIA_ASSESSMENT_CREDIT_DIAGNOSTICS` and
  `NEXT_PUBLIC_ZOOTOPIA_ASSESSMENT_CREDIT_DIAGNOSTICS`, with correlation IDs
  across mutation, recompute, live publish, credit reads, shell refresh, studio
  updates, and exhausted-state recalculation
- Admin credit diagnostics now include actor email at the request boundary,
  explicit admin-log failure events, and route-level live-publish result/failure
  events, while Assessment Studio now emits a canonical `summary_applied`
  client diagnostic so header apply and studio apply can both be correlated
- Assessment credit live delivery now uses owner-scoped private Supabase
  realtime channels (`assessment-credit:owner:{uid}`) as the primary
  cross-tab/cross-instance invalidation lane, and protected-shell subscribers
  now apply provider access tokens to Realtime auth before joining those
  private channels
- Assessment credit browser payloads remain invalidation-only envelopes
  (event/correlation metadata only, no client-applied balance summary payload)
- Successful assessment credit commits from `POST /api/assessment` now publish
  the same owner-scoped invalidation signal used by admin mutations, improving
  same-user multi-tab reconciliation without turning realtime into a balance
  truth source
- User/admin/confirm-email auth completion flows now preserve the Supabase
  browser session after successful Auth.js session establishment so private
  Realtime channel authorization can remain healthy in protected pages; the
  live server trust boundary is `Auth.js cookie -> auth() ->
  apps/web/lib/server/session.ts persisted-user rehydration -> role/status/
  profile gating`, not the browser Supabase session
- `/api/auth/bootstrap` and `/api/auth/admin/bootstrap` are intentionally
  deprecated compatibility bridges that now return `410` and are not part of
  the active login/auth-completion runtime
- Protected sign-out now treats `/api/auth/logout` workspace cleanup and
  browser-side Supabase sign-out as best-effort support steps so Auth.js cookie
  invalidation and protected-shell teardown still complete when helper cleanup
  degrades
- `/admin` dashboard now performs its own page-level `requireAdminUser()`
  assertion in addition to the admin layout/proxy guard so the primary admin
  landing page matches the redundant server-side protection already used by
  sibling admin pages
- Assessment credit persistence now uses a hybrid bridge: the live repository
  transaction paths still maintain legacy compatibility documents, but now
  mirror committed account, daily-ledger, grant, prompt-entitlement, and
  mutation-history state into structured relational credit tables inside the
  same ACID boundary (`assessment_credit_accounts`,
  `assessment_daily_credits`, `assessment_credit_grants`,
  `assessment_credit_mutations`)
- Canonical credit reads now reconcile structured credit tables against the
  legacy compatibility mirror by timestamp instead of trusting only one store,
  which closes the old “log says success but persisted balance stays stale”
  drift risk during the migration bridge
- Structured credit tables now also have a dedicated historical backfill
  migration from legacy `zc_entities` credit collections, and mutable credit
  rows converge by `updated_at` so older account/ledger/grant rows no longer
  need a future mutation before the relational schema reflects the current
  owner-scoped truth
- `assessment_credit_grants` now has an owner/date composite index aligned with
  the repository read shape (`WHERE owner_uid = ? ORDER BY created_at DESC`)
  used by owner/admin credit detail surfaces
- Protected shell and Assessment Studio now share one TanStack Query cache key
  for `/api/assessment/credits`, and the Assessment page/studio no longer seed
  or patch that shared balance cache from page props, mutation responses, or
  client-calculated deltas
- Protected workspace navigation now includes `/credits` as the primary
  dedicated owner-facing global-credit details route (with `/assessment/credits`
  preserved as a compatibility redirect), and it reads the canonical shared
  `/api/assessment/credits` query path and keeps refresh behavior on shared
  invalidation/refetch instead of local balance patching
- `/api/assessment/credits/details` now provides the owner-scoped canonical
  credit detail read model (account + summary + grants + mutation history +
  computedAt) for the protected credit details page, while the visible total
  balance remains owned by the shared `/api/assessment/credits` TanStack query
- The protected header balance badge now links directly to `/credits`
  without changing balance ownership; the page itself uses the shared summary
  query for totals and a separate owner-scoped details query only for source
  breakdown and mutation history
- Admin credit mutation history now records admin email, correlation ID, route
  source, and commit status in the shared contract and in the structured
  mutation table, and the admin PATCH route now returns the credit trace header
  even on early authorization/self-mutation failures
- Assessment credit summary shape already distinguishes the user-facing balance
  sources needed for truthful UX (`dailyRemainingCount`,
  `manualCreditsAvailable`, `grantCreditsAvailable`, `extraCreditsAvailable`,
  `remainingCount`), so the latest balance-presentation pass did not extend the
  backend contract and instead kept `/api/assessment/credits` as the single
  canonical read model
- Assessment create requests now require a route-level `Idempotency-Key`, and
  Assessment Studio persists a short-lived per-tab logical-attempt key across
  duplicate submits / refresh retries so one successful generation attempt
  cannot fan out into multiple credit deductions while reserve/commit/release
  authority stays fully server-owned
- Legacy assessment-credit custom browser events and the SSE stream route have
  been removed; protected credit refresh fallback now stays within TanStack
  Query invalidation (focus/visibility/path transitions + timed revalidation)
- Protected header credit badge now keeps a compact always-visible balance value
  beside the wallet icon and includes an adjacent non-mutating plus action that
  opens an Arabic urgent-credit help popover (Elmahdy Abdallah contact guidance)
  with outside-click/Escape close behavior
- Assessment prompt feature now uses a durable admin-managed per-user
  entitlement gate (`enabled|disabled`) with server-authoritative enforcement,
  while preserving the existing env-password unlock mechanism for entitled
  users and keeping admin permanently exempt
- Assessment prompt unlock success state now keeps backend unlock authority
  unchanged while rendering celebratory Arabic success copy (with explicit
  developer attribution) and refined success-banner styling after verified
  password acceptance only
- Assessment setup now supports an env-driven UI-only lock profile for
  normal users: question count cap, allowed question-type catalog, and allowed
  output-language catalog. Locked options remain visible but disabled (not
  hidden), a locked-types Arabic note appears under question types, and admins
  retain full setup access via explicit role bypass
- Assessment setup non-admin model-replacement panel now uses compact Arabic
  rights ownership copy and a subtle donation/support hint with small icons,
  preserving premium visual balance in the top setup row across breakpoints
- Assessment setup non-admin rights panel now prioritizes single-line Arabic
  rights rendering through responsive clamp sizing, tighter tracking, and a
  slightly wider desktop/tablet control rail while keeping text inside the
  existing panel surface
- Assessment page hero and detached preview header now share a compact Arabic
  reassurance note component with a subtle `😊` accent, rendered in the
  existing header whitespace (assessment top block + preview logo/QR rail
  under-row) with tone-specific styling and wrap-safe small typography so
  layout balance remains stable across light/dark themes and narrow viewports
- Admin user detail page now prioritizes scanability with a concise identity
  summary, dedicated access/status flag group, and collapsible observability
  disclosures (server metadata, client metadata, device capability, and raw
  verbose snapshot) while preserving all existing backend-owned data/actions
- Admin user detail credit operations now keep credit/prompt feedback scoped to
  the Credits & Usage section, keep storage/deletion feedback scoped to Admin
  Controls, render Credit Grants below the main credit workspace, and use
  wrapped min-height credit action controls to avoid overlap/crowding on
  narrow breakpoints without changing backend mutation authority
- Global maintenance mode now supports an env-controlled proxy guard that
  redirects non-admin auth/public/site/protected traffic to `/maintenance`,
  while preserving admin bypass and `/admin/login` availability
- Admin overview/users API routes now explicitly declare dynamic runtime cache
  behavior (`dynamic = "force-dynamic"`, `revalidate = 0`) in addition to
  response-level no-store headers
- Protected header and settings surfaces now share a compact local avatar policy
  (`admin` role override → managed `gender` asset → initial fallback)
- Request-locale bootstrap now supports env-seeded first-load language via
  `ZOOTOPIA_DEFAULT_LANGUAGE` (`en|ar`, invalid/missing fails closed to `en`)
- Home page stat counters now render in a lower workspace snapshot section so
  the hero title lane remains clean across breakpoints
- Home protected page spacing is now slightly tighter on small screens with a
  centered fold-discovery cue that keeps subtle motion and honors reduced-motion
  preferences
- Home protected page platform-story CTA now keeps its elevated interaction cue
  visible in touch-first production contexts, and its roadmap viewer renders
  through a viewport-owned portal with a more visible close control so the Home
  hero's overflow/blur shell cannot clip the popup on Vercel/mobile browsers
- Shared file/result card titles now use extension-aware smart middle-ellipsis
  formatting on tiny screens, preserving meaningful start + visible tail/extension
  (for example: `lecture-one-beers-law...2026.pdf`) while keeping full raw titles
  on `sm+` breakpoints
- Shared file/result card lanes now use tighter mobile smart-title budgets so
  tiny-screen names remain compact without dropping visible extensions; generated
  summary snippets on History and Assessment cards now clamp to 3 lines on
  tiny screens and release clamping on `sm+` for wider-layout readability
- Home generated-card and content rails now enforce stricter tiny-screen width
  guards (`min-w-0`/`max-w-full`) at the page root and main content grid so
  protected Home surfaces stay fully inside viewport bounds on very narrow devices
- Journey page light-mode surfaces/text now use coherent high-contrast daytime
  styling
- Upload hero now presents a single premium intake surface with unified internal
  layering and a restrained upper-left depth hover reaction, replacing the prior
  redundant route-level title shell
- Assessment hero/premium surfaces now use slightly higher translucency while
  preserving contrast for controls and text
- Detached assessment preview/result pages now place their interactive export
  controls in a dedicated full-width action panel beneath metadata; preview
  footers now render through a dedicated preview-only component while the
  Fast and Pro PDF lanes each own their own server-side renderer file
  (`assessment-fast-print-renderer.ts` / `assessment-pro-print-renderer.ts`),
  and now preserve the old shared renderer content inside lane-owned files
  without hidden final-renderer coupling; Pro keeps the absolute-base-url /
  non-interactive capture contract while Fast stays browser-print-first
- Assessment preview/result/export normalization now recovers choice-based
  question options from alternative provider/legacy shapes (`choices`,
  `options`, `answerOptions`, `answerChoices`, `alternatives`, and keyed
  `A/B/C/D`-style objects) before grouped rendering, so mixed AI payloads no
  longer silently drop options in preview, Markdown, DOCX, Fast PDF, or Pro PDF
- Choice recovery now resolves MCQ answers from explicitly-correct recovered
  choices before falling back to generic placeholder answers, while leaving
  rationale, numbering, tags, and non-choice question behavior intact
- Assessment generation prompt instructions now require every question object to
  be complete, with ordered `choices[]` for choice-based questions and `choices:
  []` for non-choice questions, reducing provider-side omissions and partial MCQ
  payloads
- Detached assessment preview/result/export metadata no longer render the model
  label/value, but backend generation metadata still stores model/provider data
  for internal tracking and repository truth
- Assessment footer attribution now renders a leading `💻` and trailing `❤️`
  across preview/result/export surfaces, and the visual footer lanes use shared
  inline SVG emoji fallbacks so browser print and Puppeteer PDF capture no
  longer depend on host color-emoji font availability
- Web workspace `typecheck` and `build` now clear stale `.next/dev` cache state
  before invoking Next typegen/build so malformed dev-only route helper files do
  not poison later TypeScript or production build runs
- Web workspace `dev` startup now performs a narrow `.next/dev` integrity check
  before `next dev`, automatically resetting half-written Turbopack dev output
  when manifests or the generated SSR runtime chunk are missing so routes like
  `/contact` recover without requiring a manual full `.next` wipe
- Repository line-ending governance is now reinforced for Windows workspaces:
  tracked source/config/log text remains LF via `.gitattributes`, VS Code
  workspace defaults enforce LF saves, and transient `apps/web/.next-dev-test*.log`
  artifacts are ignored so routine `git add .` flows stay warning-free
- Assessment Pro PDF export now traces the hoisted Chromium Brotli payload from
  the workspace root for Vercel, uses explicit `maxDuration = 120`, and emits
  stage-classified internal diagnostics without exposing raw failure details to
  the user-facing response
- Assessment Pro PDF static-section layout now reserves explicit per-page
  footer height budget with deterministic print sizing and tighter card spacing
  so page footers remain fully contained on their own page (no bleed/overlap)
- The active Pro PDF lane at `/api/assessment/export/pdf/pro/[id]` keeps
  Puppeteer/Chromium as the primary export engine, keeps deterministic
  page-grouping inside the Pro lane (2 questions on page 1, 3 on later
  question pages), and now hands the fixed-height cover body down into the
  page-1 shell so the two opening cards consume the reserved body space above
  the compact footer instead of leaving a detached blank gap
- The active Pro renderer/export lane version is now
  `pro-2026-04-17-deterministic-pagination-v6`, so Pro artifact reuse only
  occurs for artifacts stamped with that exact version tag; older cached Pro
  artifacts are forced to regenerate through the current hardened renderer path
- Local workspace verification is currently healthy again for the web app:
  `npm run lint:web`, `npm run typecheck:web`, and `npm run build:web` all
  succeeded on 2026-04-17; older ledger entries describing missing
  `next`/`eslint` binaries are historical environment notes, not current truth
- Assessment prompt-lock password input now includes a local eye/eye-off
  visibility toggle in the locked prompt UI without changing server lock logic
- Assessment prompt-lock UI now consumes the server-owned entitlement state so
  non-entitled users do not see a misleading password form; unlock failures now
  distinguish wrong password, missing entitlement, and server misconfiguration
  with clearer Arabic feedback while unlock authority remains backend-owned
- Admin credit mutations on `/admin/users/[uid]` are currently initiated by the
  detail-page server-action UI while the admin PATCH route remains an active
  secondary mutation lane; both paths reuse the same repository mutation
  authority and keep recipient balance server-owned through
  `/api/assessment/credits`
- Assessment Studio and the protected header now read the same shared TanStack
  credit query without studio-owned cache seeding or mutation-response cache
  writes, so balance visibility stays on one canonical client path
- Protected header balance now formats the canonical shared `remainingCount`
  with locale-aware digits and uses a source-aware tooltip derived from the same
  shared summary object (daily vs extra availability, plus manual/grant source
  hints) without introducing any client-side balance math
- Assessment Studio credit guidance now derives a small source-aware display
  model from the canonical summary so the panel can distinguish daily-only,
  mixed daily+extra, extra-only, exhausted, disabled, and admin-exempt states
  while preserving the shared TanStack query as the only client-visible balance
  owner
- Assessment credit client diagnostics now capture the display state plus the
  exact summary fields that drive wording (`dailyAvailable`, `extraAvailable`,
  `manualCreditsAvailable`, `grantCreditsAvailable`) so runtime tracing can
  explain why a given header/panel message was shown without leaking secure
  mutation authority into the browser
- Assessment Studio exhausted-credit, success, mount, and realtime-driven
  reconciliation now converge through shared query invalidation/refetch against
  canonical `/api/assessment/credits`, with loading/reconciling UI used instead
  of provisional balance patching
- Assessment/admin credit failure classification is now centralized in
  `apps/web/lib/server/assessment-platform-errors.ts` and reused by the
  assessment create route, admin credits API route, and admin user detail
  server-action query-code mapping
- The same platform-error file now also owns the common touched-flow operational
  error registry for assessment auth, admin access, invalid JSON, user lookup,
  credit-state read, and canonical credit-summary read failures so those routes
  no longer depend on scattered local error strings
- Admin user-management overview/detail surfaces now run without floating/fade
  motion and use stronger light-mode button/surface contrast while dark mode
  retains the existing visual language
- Protected Home/Assessment/Infographic/Settings/Upload/History wrappers now
  share a lighter translucency baseline so background ambience reads through
  consistently without reducing form/control legibility
- Assessment workspace generation cards now hide the visible model-name row,
  while backend metadata persistence remains unchanged (`generation.meta.modelLabel`
  is still stored for internal tracking)
- Assessment/History generated summaries now render controlled card excerpts
  with a trailing `...` continuation hint plus full-text hover titles, and
  Home/Upload/History card action lanes now use vertical-first mobile stacking
  to reduce narrow-screen crowding without altering desktop/tablet card rhythm
- Shared file/result name lanes keep wrap-first clamped title rendering
  (`line-clamp`) with full-text hover title hints across Upload/Home/History/
  Assessment card surfaces, while detached assessment preview/result header
  title + summary now render full text with wrap-safe behavior (no clamp/
  truncation) across breakpoints
- Tiny-screen generated-card lanes now enforce viewport-fit no-swipe behavior:
  protected-shell content no longer allows assessment-path horizontal auto-scroll,
  shared generated-card/action wrappers use stricter `min-w-0`/`w-full`/
  `max-w-full` shrink guards, selected action rails keep vertical-first mobile
  stacking until `md`, and mobile action controls use compact sizing without
  changing desktop/tablet interaction patterns
- Exhausted daily-credit messaging now includes localized EN/AR escalation
  guidance to contact developer/admin Elmahdy Abdallah for private or urgent
  credits, rendered only in the exhausted-credit state
- Public auth pages now include tighter mobile overlap guards and restrained
  overlay/shadow density for cleaner stacking behavior
- Email verification via Supabase with Resend provider support
- Admin panel includes user management, storage management, governance dashboard
- Database uses Supabase Postgres with RLS policies
- Migration management via scripts/supabase/apply-migrations.mjs
- 18 migration files tracked in supabase/migrations (latest include
  `20260416210000_assessment_credit_mutations.sql`,
  `20260417103000_assessment_credit_prompt_entitlement.sql`, and
  `20260418123000_active_normal_user_session_capacity.sql`)

================================================================================
3. PROJECT PURPOSE & PLATFORM DIRECTION
================================================================================

2026-04-18: SHARED CARD MOBILE COMPACTNESS REFINEMENT (TITLE BUDGETS + SUMMARY CLAMP)
────────────────────────────────────────────────────────────────────────────

ROOT CAUSE:
1. Shared file/result card surfaces already used mobile-only smart titles, but
   several narrow-screen lanes still felt too wide because smart-title budgets
   were tuned for larger tiny-screen widths.
2. Generated summary snippets on key card surfaces were clamped as
   `line-clamp-2 sm:line-clamp-3`, which made mobile excerpts too short while
   still clipping on wider breakpoints.
3. Detached result-view source-document labels always rendered through the
   smart-title helper, so larger screens could not recover the full raw label.

FILES CHANGED:
- apps/web/app/(protected)/(completed)/page.tsx
- apps/web/app/(protected)/(completed)/history/page.tsx
- apps/web/components/upload/upload-workspace.tsx
- apps/web/components/assessment/assessment-studio.tsx
- apps/web/components/assessment/assessment-preview-shell.tsx
- apps/web/components/assessment/assessment-result-viewer.tsx
- apps/web/components/document/document-context-card.tsx
- zootopia-club-next-ledger.txt

MIGRATIONS:
- No migration added.
- No migration changed.

REASON:
- Deliver the requested UI-only responsive follow-up for narrow screens across
  shared result/file card patterns without touching backend/auth/session logic.
- Keep extension visibility for compact mobile names while preserving full raw
  title/text behavior on wider breakpoints.

TECHNICAL DETAILS:
1. Mobile smart-title compactness
   - Reduced mobile smart-title max-length budgets across Home, History,
     Upload, Assessment Studio, detached Preview shell, shared Document
     Context card, and Result Viewer source-document badge.
   - Mobile smart-title spans now use compact inline rendering (`sm:hidden`
     with inline-block alignment), while `sm+` still renders the full raw value.

2. Summary clamp behavior
   - History assessment summary cards and Assessment Studio generation summary
     cards now use `line-clamp-3 sm:line-clamp-none`.
   - This keeps a controlled 3-line preview on tiny screens and restores full
     excerpt visibility on wider layouts.

3. Detached result-view source document lane
   - Result viewer source-document badge now renders compact smart title on
     tiny screens only and full raw source-document label on `sm+`.
   - Added hover `title` attribute to preserve full-value inspection parity.

WHAT WAS INTENTIONALLY PRESERVED:
- Backend/API/auth/session/repository/storage authority (unchanged).
- Existing premium card hierarchy, dark/light behavior, localization, and
  action-lane structure.
- Existing extension-aware smart-title strategy and desktop/tablet raw-title
  rendering pattern.

RISKS / FOLLOW-UP:
1. Low UI risk: tighter tiny-screen title budgets intentionally trade more
   middle text for stronger compactness.
2. Recommend an optional visual QA sweep on extra-small mobile widths to
   confirm preferred readability tradeoffs for very long multilingual titles.

VERIFICATION:
Diagnostics verification:
✓ `get_errors` on all changed files — PASS.

Command verification:
✓ `npm run lint` — PASS (2 pre-existing warnings only:
  `assessment-studio.tsx` aria-disabled on form role,
  `confirm-email-panel.tsx` missing `flow` dependency in `useEffect`).
✓ `npm run typecheck` — PASS.
✓ `npm run build` — PASS.
✓ `npm run build:vercel` — PASS.

Zootopia Club is an AI-powered platform that provides:

1. Document Upload & Processing
   - Users upload documents (PDF, images, text files)
   - Server-side extraction and processing pipeline
   - Configurable retention policies per storage type

2. AI Assessment Generation
   - Users generate assessments from uploaded documents
   - AI models analyze content and produce structured assessments
   - Credit-limited system (daily caps)
   - Export to PDF, JSON, and other formats

3. Infographic Generation
   - AI-powered infographic creation from document content
   - Admin-only feature (currently gated)
   - Multiple modes and styles

4. Admin Governance
   - User management and oversight
   - Storage management and cleanup
   - System governance controls
   - Audit logging

Platform Direction:
- Migrated from Firebase to Supabase (complete)
- Monorepo structure prepared for future API extraction
- Provider-agnostic AI layer (Google + Alibaba/DashScope)
- Enterprise-grade security with RLS, admin allowlists, rate limiting

================================================================================
4. ARCHITECTURE STYLE
================================================================================

Pattern:        Next.js App Router with layered server architecture
Style:          Monorepo with workspace isolation
Session:        Stateless JWT (Auth.js / NextAuth v5)
Database:       Supabase Postgres (provider-managed)
Storage:        Supabase Storage buckets (provider-managed)
AI:             Multi-provider orchestration (Google AI + DashScope)

Layered Architecture:
```
Route Handlers (apps/web/app/api/)
  → Session/Auth Layer (lib/server/session.ts)
  → Repository Layer (lib/server/repository.ts)
  → AI Execution Layer (lib/server/ai/execution.ts)
  → Storage Layer (lib/server/supabase-blob-storage.ts)
  → Database Layer (lib/server/zootopia-postgres-adapter.ts)
```

Boundaries:
- Frontend: Client components in (site), (public), (protected)/(completed)
- Backend: Route handlers, lib/server/*, auth.ts, proxy.ts
- Shared: packages/shared-* (types, config, utils)
- Infrastructure: supabase/migrations/, scripts/, vercel.json
- Provider-Managed: Supabase (Auth, Postgres, Storage), Vercel (Hosting), Resend (Email)

Same-Origin vs Provider-Managed:
- Next.js app runs on Vercel (same-origin for all /api/* routes)
- Supabase Auth: cross-origin provider (browser → Supabase CDN)
- Supabase Postgres: server-side only (connection string, not browser-accessible)
- Supabase Storage: cross-origin provider (browser + server)
- Google AI API: cross-origin provider (server-side calls)
- DashScope API: cross-origin provider (server-side calls)
- Resend/SMTP: server-side only

================================================================================
5. MONOREPO STRUCTURE
================================================================================

Root: zootopia-club-next/
├── apps/
│   ├── web/                    # Next.js 16.2.2 application (PRIMARY/ACTIVE)
│   │   ├── app/                # App Router routes and layouts
│   │   │   ├── (protected)/    # Authenticated user workspace
│   │   │   ├── (public)/       # Public auth pages
│   │   │   ├── (site)/         # Public marketing/info pages
│   │   │   └── api/            # API route handlers
│   │   ├── components/         # React components
│   │   ├── lib/                # Library code (client + server)
│   │   │   └── server/         # Server-side services
│   │   ├── messages/           # AI prompt templates / i18n messages
│   │   ├── public/             # Static assets
│   │   ├── scripts/            # Build/postbuild scripts
│   │   ├── auth.ts             # Auth.js configuration
│   │   ├── proxy.ts            # Auth.js proxy helpers
│   │   ├── next.config.ts      # Next.js configuration
│   │   └── package.json        # @zootopia/web manifest
│   └── api/                    # Future API extraction target (INACTIVE)
│       └── (placeholder only — no active code)
├── packages/
│   ├── shared-config/          # Constants, routes, env keys, feature flags
│   ├── shared-types/           # TypeScript interfaces, DTOs
│   └── shared-utils/           # Pure utility functions
├── scripts/
│   └── supabase/               # Migration management scripts
├── supabase/
│   └── migrations/             # SQL migration files
├── docs/                       # Architecture documentation
│   └── auth-email/             # Auth email templates
├── Guide-Files/                # Agent/CLI guides (reference only)
├── skills/                     # Agent skill definitions
│   ├── supabase/
│   └── supabase-postgres-best-practices/
├── package.json                # Root workspace manifest
├── tsconfig.json               # Root TypeScript config
├── tsconfig.base.json          # Shared TypeScript base config
├── eslint.config.mjs           # ESLint configuration
├── .env.example                # Environment variable template
├── .env.local                  # Local environment (gitignored)
└── zootopia-club-next-ledger.txt  # This file

================================================================================
6. DEPENDENCY & RUNTIME BASELINE
================================================================================

Root Dependencies:
- @supabase/ssr: ^0.10.2
- @supabase/supabase-js: ^2.103.0
- framer-motion: 12.38.0
- lucide-react: ^1.7.0
- pnpm: ^10.33.0
- @tailwindcss/postcss: ^4
- tailwindcss: ^4
- typescript: ^5
- eslint: ^9
- eslint-config-next: 16.2.2

@zootopia/web Dependencies (additional):
- next: 16.2.2
- next-auth: ^5.0.0-beta.30
- react: 19.2.4
- react-dom: 19.2.4
- @tanstack/react-query: ^5.99.0
- @radix-ui/react-slot: ^1.2.4
- radix-ui: ^1.4.3
- class-variance-authority: ^0.7.1
- clsx: ^2.1.1
- tailwind-merge: ^3.5.0
- tw-animate-css: ^1.4.0
- shadcn: ^4.2.0
- motion: ^12.38.0
- docx: ^9.5.1
- exceljs: ^4.4.0
- libphonenumber-js: ^1.12.41
- react-phone-number-input: ^3.4.16
- qrcode: ^1.5.4
- nodemailer: ^8.0.4
- postgres: ^3.4.5
- puppeteer-core: ^24.35.0
- @sparticuz/chromium: ^143.0.0

Node Engine: >= 20.9.0
Module Type: ESM (apps/web has "type": "module")

================================================================================
7. ACTIVE DEPLOYMENT MODEL
================================================================================

Primary Target: Vercel
- Build command: npm run build:vercel (→ next build)
- Start command: npm run start:vercel (→ next start)
- Node max old space: 16384 MB for build
- Postbuild: scripts/normalize-standalone.mjs

Local Development:
- Dev command: npm run dev (→ npm run dev:local → next dev --turbopack)
- Start command: npm run start:local (→ next start)

Supabase:
- Provider-managed Postgres, Auth, Storage
- Migrations applied via scripts/supabase/apply-migrations.mjs
- Uses Supabase Management API for migration application
- Project ID from SUPABASE_PROJECT_ID env var

Email:
- Resend provider for email verification (Supabase-managed)
- SMTP relay for contact form (optional)

Legacy/Reference-Only (NOT active):
- Firebase (fully migrated away)
- Cloud Run
- Netlify
- Docker/Cloud Build
- Single-app server layouts

================================================================================
8. ENVIRONMENT STRATEGY
================================================================================

Environment Variables (from .env.example):

Required:
- AUTH_SECRET: Auth.js session crypto (generate with: npx auth secret)
- NEXT_PUBLIC_BASE_URL: Canonical app origin for server callbacks/internal fetches
- NEXTAUTH_URL: Optional Auth.js origin fallback when canonical base URL is unset
- NEXT_PUBLIC_SUPABASE_URL: Supabase project URL (browser + server)
- NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY: Supabase anon key (browser)
- NEXT_PUBLIC_SUPABASE_ANON_KEY: Browser auth alias (compatibility)
- SUPABASE_URL: Server auth config (can reuse NEXT_PUBLIC_SUPABASE_URL)
- SUPABASE_SERVICE_ROLE_KEY: Server admin key for Supabase
- SUPABASE_DATABASE_URL: Postgres connection string (pooler URI)
- GOOGLE_AI_API_KEY: Google AI provider key
- DASHSCOPE_API_KEY: DashScope (Alibaba/Qwen) provider key

Admin & Governance:
- ZOOTOPIA_ADMIN_EMAILS: Comma-separated admin email allowlist
- ZOOTOPIA_MAINTENANCE_SECRET: Optional maintenance endpoint secret
- ZOOTOPIA_MAINTENANCE_MODE_ENABLED: Optional global maintenance-mode toggle
  (`1|true|yes` enables; missing/invalid values fail closed to disabled)

Session & Credits:
- ZOOTOPIA_SESSION_TTL_SECONDS: Session TTL (default 3600)
- ZOOTOPIA_DEFAULT_DAILY_ASSESSMENT_CREDITS: Daily AI credit cap (default 3)
- ZOOTOPIA_ASSESSMENT_CREDIT_RENEWAL_WINDOW_HOURS: Credit renewal window in UTC hours
  (default 24, valid range 1..168)
- ZOOTOPIA_ASSESSMENT_PROMPT_LOCK_ENABLED: Optional lock toggle for normal-user
  assessment prompt access (true/false)
- ZOOTOPIA_ASSESSMENT_PROMPT_UNLOCK_PASSWORD: Server-only unlock password for
  normal-user assessment prompt access
- NEXT_PUBLIC_ZOOTOPIA_UI_LOCK_ENABLED: Optional Assessment setup UI lock toggle
  for normal users (defaults to true when unset/invalid)
- NEXT_PUBLIC_ZOOTOPIA_UI_LOCK_MAX_QUESTION_COUNT_USER: Normal-user maximum
  question count ceiling in setup UI (default 40)
- NEXT_PUBLIC_ZOOTOPIA_UI_LOCK_USER_ALLOWED_QUESTION_TYPES: Comma-separated
  normal-user allowed question types in setup UI (default mcq)
- NEXT_PUBLIC_ZOOTOPIA_UI_LOCK_USER_ALLOWED_OUTPUT_LANGUAGES: Comma-separated
  normal-user allowed output languages in setup UI (default en)
- NOTE: The NEXT_PUBLIC_ZOOTOPIA_UI_LOCK_* values are intentionally UI-only
  product gating and do not replace backend authorization/validation
- ZOOTOPIA_DEFAULT_THEME_MODE: Initial theme fallback (`dark` or `light`, invalid falls back to `dark`)
- ZOOTOPIA_DEFAULT_LANGUAGE: Initial locale fallback (`en` or `ar`, invalid falls back to `en`)

Tool Default Models:
- ZOOTOPIA_DEFAULT_MODEL_ASSESSMENT: server-authoritative default for assessment
- ZOOTOPIA_DEFAULT_MODEL_INFOGRAPHIC: server-authoritative default for infographic
- Missing, invalid, unsupported, or cross-tool values fall back to the built-in
  tool default instead of leaking across tool scopes

Redirects:
- NEXT_PUBLIC_ZOOTOPIA_AUTH_USER_DEFAULT_REDIRECT: Post-login redirect (default /)
- NEXT_PUBLIC_ZOOTOPIA_AUTH_ADMIN_DEFAULT_REDIRECT: Admin post-login redirect (default /)

Email Verification:
- ZOOTOPIA_EMAIL_VERIFICATION_RESEND_MODE: provider (default)
- ZOOTOPIA_EMAIL_VERIFICATION_COOLDOWN_SECONDS: 30
- ZOOTOPIA_EMAIL_VERIFICATION_MAX_ATTEMPTS: 5
- ZOOTOPIA_EMAIL_VERIFICATION_WINDOW_MINUTES: 60
- ZOOTOPIA_EMAIL_VERIFICATION_IP_MAX_ATTEMPTS: 20
- ZOOTOPIA_EMAIL_VERIFICATION_IP_WINDOW_MINUTES: 60
- ZOOTOPIA_EMAIL_VERIFICATION_HASH_SALT: Salt for attempt hashing

Storage Retention:
- ZOOTOPIA_UPLOAD_RETENTION_MINUTES: 15 (documents/uploads)
- ZOOTOPIA_RESULT_RETENTION_MINUTES: 1440 (1 day, assessment results)
- ZOOTOPIA_EXPORT_RETENTION_MINUTES: 15 (export artifacts)
- ZOOTOPIA_UPLOAD_RETENTION_MODE: expiry
- ZOOTOPIA_RESULT_RETENTION_MODE: expiry
- ZOOTOPIA_EXPORT_RETENTION_MODE: expiry

AI Configuration:
- DASHSCOPE_BASE_URL: https://dashscope-us.aliyuncs.com/compatible-mode/v1
- DASHSCOPE_COMPATIBLE_BASE_URL: (legacy alias)
- ALIBABA_MODEL_STUDIO_BASE_URL: (legacy alias)
- GOOGLE_AI_MODEL: (optional override)
- GOOGLE_AI_ADVANCED_MODEL: (optional override)
- QWEN_MODEL: (optional override)

SMTP/Contact (optional):
- SMTP_HOST: smtp.gmail.com (default)
- SMTP_PORT: 587 (default)
- SMTP_SECURE: false (default)
- SMTP_USER, SMTP_PASS, EMAIL_FROM, CONTACT_FORM_TO

PDF Generation (optional):
- ASSESSMENT_PDF_BROWSER_EXECUTABLE_PATH: Local Chrome/Chromium override
- PUPPETEER_EXECUTABLE_PATH: Puppeteer Chrome override

Legacy (commented out, not used):
- FIREBASE_* variables (all legacy Firebase config)
- ZOOTOPIA_ADMIN_LOGIN_PASSWORD (deprecated admin password gate)
- ZOOTOPIA_VERIFICATION_RESEND_* (legacy aliases, still accepted)
- ZOOTOPIA_STORAGE_RETENTION_* (legacy global fallback)
- DATABASE_URL (optional alias)

Environment File Strategy:
- .env.example: Template (committed)
- .env.local: Local development secrets (gitignored)
- Vercel: Environment variables configured in Vercel dashboard
- Supabase: Provider-managed, no env file needed

================================================================================
9. ROUTE MAP
================================================================================

Route Groups:
- (protected): Requires authentication + profile completion
- (public): Public pages (login, forgot/reset password, confirm email)
- (site): Public marketing/info pages

Complete Route Tree:

Root:
  /                           → Home dashboard (protected, completed profile)

Protected Routes (requires auth):
  /upload                     → Document upload workspace
  /assessment                 → AI assessment generation studio
  /credits                    → Global credit details page (primary)
  /assessment/credits         → Compatibility redirect to `/credits`
  /infographic                → Infographic generation (admin-only)
  /history                    → Upload & assessment history viewer
  /settings                   → User profile settings
  /admin                      → Admin dashboard
  /admin/users/[uid]          → User detail management
  /admin/storage              → Storage management dashboard
  /admin/governance           → Governance controls (if present)

Public Routes (no auth):
  /login                      → Login page
  /forgot-password            → Password reset email request page
  /reset-password             → Password recovery completion page
  /confirm-email              → Email confirmation page
  /maintenance                → Maintenance-mode holding page
  /coming-soon                → Feature preview placeholder

Site Routes (public marketing):
  /about                      → About page (if present)
  /contact                    → Contact page (if present)
  /privacy                    → Privacy policy (if present)
  /terms                      → Terms of service (if present)

API Routes:
  /api/auth/[...nextauth]     → Auth.js handler
  /api/auth/bootstrap         → Deprecated compatibility bridge (returns 410)
  /api/auth/admin/bootstrap   → Deprecated compatibility bridge (returns 410)
  /api/auth/admin/resolve-identifier → Admin identifier lookup / rate-limited
  /api/auth/confirm-email/resend  → Email verification resend
  /api/auth/logout            → Workspace cleanup + logout audit trail
  /api/auth/me                → Authoritative server session snapshot
  /api/auth/password/forgot   → Request password reset email (regular users)
  /api/auth/password/recovery → Validate/complete recovery token flow
  /api/auth/password/change   → In-account password change (regular users)
  /api/assessment             → POST: Create assessment
  /api/assessment/[id]        → GET: Get single assessment
  /api/assessment/credits     → GET: Daily credit status
  /api/assessment/credits/details → GET: Owner-scoped credit detail view
  /api/assessment/credits/page-unlock → POST: Credit page unlock
  /api/assessment/results/[id] → GET: Owner-scoped assessment result
  /api/assessment/prompt-unlock → POST: Verify unlock password for assessment prompt
  /api/assessment/export/*    → GET: Export as PDF/JSON
  /api/assessment/linked-document → GET: Resolve linked document
  /api/uploads                → POST: File upload
  /api/uploads/[id]           → GET/DELETE: Upload operations
  /api/infographic            → POST: Generate infographic
  /api/infographic/[id]       → GET: Infographic detail
  /api/admin/overview         → GET: Admin dashboard summary
  /api/admin/internal/verification-resend-governance → GET: Verification resend governance diagnostics
  /api/admin/users            → GET: List users (admin)
  /api/admin/users/export     → GET: User export (admin)
  /api/admin/users/[uid]      → GET/DELETE: User management (admin)
  /api/admin/users/[uid]/credits → GET/PATCH: Credit state + mutation lane (admin)
  /api/admin/users/[uid]/role → PATCH: Role mutation (admin)
  /api/admin/users/[uid]/status → PATCH: Status mutation (admin)
  /api/admin/storage/cleanup  → POST: Storage cleanup (admin)
  /api/contact                → POST: Contact form (SMTP relay)
  /api/health                 → GET: Runtime health snapshot
  /api/users/me/profile       → PATCH: Owner-scoped profile updates
  /api/internal/maintenance/expired-uploads → POST: Secret-gated upload cleanup sweep

================================================================================
10. ROUTE OWNERSHIP CLARIFICATION
================================================================================

Frontend-Owned (Client Pages):
- apps/web/app/(protected)/(completed)/page.tsx — Home dashboard
- apps/web/app/(protected)/(completed)/upload/page.tsx — Upload workspace
- apps/web/app/(protected)/(completed)/assessment/page.tsx — Assessment studio
- apps/web/app/(protected)/(completed)/credits/page.tsx — Global credit details
- apps/web/app/(protected)/(completed)/assessment/credits/page.tsx — Compatibility redirect to `/credits`
- apps/web/app/(protected)/(completed)/infographic/page.tsx — Infographic page
- apps/web/app/(protected)/(completed)/history/page.tsx — History viewer
- apps/web/app/(protected)/settings/page.tsx — Settings page
- apps/web/app/(protected)/admin/page.tsx — Admin dashboard
- apps/web/app/(protected)/admin/users/[uid]/page.tsx — User management
- apps/web/app/(protected)/admin/storage/page.tsx — Storage management
- apps/web/app/(public)/login/page.tsx — Login page
- apps/web/app/(public)/forgot-password/page.tsx — Forgot-password page
- apps/web/app/(public)/reset-password/page.tsx — Reset-password page
- apps/web/app/(public)/confirm-email/page.tsx — Email confirmation
- apps/web/app/(public)/maintenance/page.tsx — Maintenance-mode page
- apps/web/app/(site)/** — Marketing pages

Backend-Owned (API Routes):
- apps/web/app/api/auth/** — Auth.js and email verification
- apps/web/app/api/assessment/** — Assessment CRUD and export
- apps/web/app/api/assessments — Assessment listing
- apps/web/app/api/documents/** — Document operations
- apps/web/app/api/uploads/** — Upload operations
- apps/web/app/api/infographic — Infographic generation
- apps/web/app/api/admin/** — Admin operations
- apps/web/app/api/contact — Contact form relay
- apps/web/app/api/internal/maintenance/expired-uploads — Internal maintenance sweep endpoint

Shared Ownership:
- apps/web/auth.ts — Auth.js config (server-side, but affects client sessions)
- apps/web/proxy.ts — Auth.js proxy helpers (server-side)
- apps/web/lib/server/** — Server services (backend authority)
- apps/web/lib/** (non-server) — Client utilities (frontend authority)

================================================================================
11. BACKEND RUNTIME & SERVICE OWNERSHIP
================================================================================

Server-Side Services (apps/web/lib/server/):

Database Layer:
- zootopia-postgres-adapter.ts — Postgres connection and query helpers
  Owns: Database connection pool, query execution, type-safe adapters

Repository Layer:
- repository.ts — Data access abstraction
  Owns: CRUD operations for all entities, transaction boundaries

Storage Layer:
- supabase-blob-storage.ts — Supabase Storage bucket operations
  Owns: Upload, download, delete, URL generation, bucket management
- storage-retention-config.ts — Retention policy configuration
  Owns: Per-scope retention parsing from env vars, expiry calculation

Document Processing:
- document-runtime.ts — Document upload and processing pipeline
  Owns: File validation, metadata extraction, storage persistence

Assessment System:
- assessment-retention.ts — Assessment lifecycle and cleanup
  Owns: Assessment expiry, result cleanup, credit management

AI Execution:
- ai/execution.ts — AI model orchestration
  Owns: Provider selection, prompt construction, response parsing

Email & Verification:
- verification-resend-governance.ts — Email verification rate limiting
  Owns: Cooldown tracking, attempt counting, IP-based limits

Session Management:
- session.ts — Session utilities
  Owns: Session validation, user context extraction

Admin Services:
- Admin-specific repository and governance helpers
  Owns: Admin queries, audit logging, user management

Runtime Flow:
1. Request arrives at API route handler
2. Handler validates session via session.ts
3. Handler calls repository.ts for data access
4. Repository uses zootopia-postgres-adapter.ts for database queries
5. For uploads: supabase-blob-storage.ts handles storage
6. For AI: ai/execution.ts orchestrates model calls
7. Response is returned with proper error handling

================================================================================
12. AUTH & SESSION ARCHITECTURE
================================================================================

Framework: Auth.js (NextAuth v5)
Strategy: JWT (stateless sessions)
Identity Provider: Supabase Auth (email/password only)

Credential Providers:
1. user-credentials — Regular user login
2. admin-credentials — Admin login (separate provider)

Public User Sign-Up Flow:
1. Client posts email/password to `/api/auth/signup`
2. Server reserves persisted admission capacity (account + IP windows) before calling Supabase Auth
3. Server calls Supabase sign-up with server-owned confirm-email callback URL
4. If confirmation is required, client is redirected to `/confirm-email`
5. If environment ever returns an immediately confirmed session, client restores the helper Supabase session
   and then continues through the normal Auth.js login bootstrap path

User Login Flow:
1. Client calls `/api/auth/login/admission` to reserve persisted login capacity
2. Client signs in via Supabase browser client → receives idToken
2. Client calls signIn("user-credentials", { idToken, ...deviceMetadata })
3. Server (auth.ts → authorizeUserCredentials):
   a. Runtime check: Supabase admin runtime + allowlist must be configured
   b. Rate limit: 20 requests/minute per IP
   c. Token verification: verifySupabaseAccessToken() via Supabase getUser()
   d. Recency check: auth_time must be within 5 minutes
   e. Admin block: If email in ZOOTOPIA_ADMIN_EMAILS, reject with ADMIN_LOGIN_REQUIRED
   f. Provider check: Must be password provider
  g. Email confirmation check: token/user must be confirmed (fail-closed AUTH_EMAIL_NOT_CONFIRMED)
  h. User upsert: upsertUserFromAuth() creates/updates profile in Postgres
  i. Status check: User must be active (not suspended)
4. JWT callback populates token with uid, role, status, profile completion, and confirmation claims
5. Session callback syncs token to session object and projects emailVerified from JWT claims
6. Client receives session cookie

Admin Login Flow:
1. Admin signs in via Supabase browser client
2. Client calls signIn("admin-credentials", { idToken })
3. Server checks email against ZOOTOPIA_ADMIN_EMAILS allowlist
4. If not in allowlist, reject
5. If in allowlist, proceed with same verification as user flow, including email-confirmation enforcement
6. JWT populated with role/status/profile and confirmation claims

Regular User Password Lifecycle (2026-04-14 hardening):
1. Forgot-password request enters `/api/auth/password/forgot` (email validation + IP/account throttling)
2. Admin/allowlisted identities are blocked from user reset lane (`ADMIN_PASSWORD_FLOW_UNSUPPORTED`)
3. Recovery callback flow finalizes token/session on `/reset-password`, then `/api/auth/password/recovery` validates token identity server-side
4. Password recovery completion records security metadata (`recovery_reset`) without storing raw password/hash data
5. In-account password changes run through `/api/auth/password/change` with current-password verification and policy enforcement
6. Settings password change enforces an immediate re-authentication boundary after success

Session Configuration:
- Strategy: JWT (no database sessions)
- Max age: ZOOTOPIA_SESSION_TTL_SECONDS (default 3600 = 1 hour)
- Cookie: httpOnly, secure, sameSite: lax
- Supabase browser auth session now intentionally persists and auto-refreshes
  only to keep private Realtime channel authorization healthy in protected UI;
  it does not grant route/API authority outside the server-owned
  `auth() -> persisted user lookup -> role/status/profile` trust chain

Authoritative Trust Boundary:
- `auth()` validates the Auth.js cookie first
- `apps/web/lib/server/session.ts` rehydrates the live persisted user by `uid`
- Active status, role, and profile-completion checks run from that live user
  record before protected routes, APIs, and admin pages continue
- `apps/web/proxy.ts` is early route shaping only and is not the sole
  authorization boundary

Logout / Session Invalidation:
- `/api/auth/logout` performs best-effort workspace cleanup and audit logging
- `next-auth/react signOut({ redirect: false })` is the decisive client-side
  Auth.js cookie invalidation step
- Browser `supabase.auth.signOut()` is best-effort follow-up cleanup for
  browser-side provider continuity and private Realtime auth only
- `ProtectedShell` removes private channels when Supabase auth clears or the
  protected subtree unmounts

Compatibility Bridges:
- `/api/auth/bootstrap` and `/api/auth/admin/bootstrap` now return `410` and
  are preserved only as safe inactive compatibility endpoints

Key Files:
- apps/web/auth.ts — Auth.js configuration
- apps/web/proxy.ts — Auth.js proxy helpers
- apps/web/lib/auth-*.ts — Auth utilities
- apps/web/lib/server/session.ts — Server session utilities

================================================================================
13. ADMIN AUTHORITY MODEL
================================================================================

Admin Access Control:
- Email allowlist: ZOOTOPIA_ADMIN_EMAILS environment variable
- Separate credential provider: admin-credentials
- Admin users cannot log in via user-credentials provider (blocked)
- Regular users cannot access admin routes (role check)

Admin Routes:
- /api/admin/** — All require admin role
- /admin/** — All require admin role + active status

Admin Capabilities:
- User management: view, update, suspend users
- Storage management: view stats, run cleanup
- Governance controls: system configuration
- Audit logging: view admin action logs
- Assessment override: bypass credit limits (if implemented)

Admin Panel Pages:
- /admin — Dashboard with system overview
- /admin/users/[uid] — User detail and management
- /admin/storage — Storage statistics and cleanup
- /admin/governance — Governance controls (if present)

Admin Server-Side Guards:
- Role check in API route handlers
- Email allowlist check in auth.ts
- Separate admin credential provider
- Admin-specific redirect after login
- `/admin` dashboard now also performs its own page-level `requireAdminUser()`
  assertion so the landing page does not rely on layout/proxy coverage alone

================================================================================
14. PROFILE COMPLETION & USER GATING MODEL
================================================================================

Profile Completion Gating:
- Users must complete their profile before accessing protected features
- Incomplete profiles are redirected to /settings to finish setup
- Profile completion status stored in JWT session token
- Gating applied at:
  - Route level (middleware or layout guards)
  - API level (server-side checks in route handlers)

Gating Rules:
1. User logs in → session created
2. Session checked for profile completion
3. If incomplete → redirect to /settings
4. If complete → access to protected routes
5. Admin users bypass profile completion gating

Profile Fields (typical):
- Display name
- Phone number (validated with libphonenumber-js)
- Preferences (theme, locale)
- Any required onboarding fields

Implementation:
- Profile completion check in session token
- Server-side guards in API routes
- Client-side guards in protected layouts
- Redirect logic in auth flow

================================================================================
15. UPLOAD / DOCUMENT / STORAGE FLOW
================================================================================

Upload Flow:
1. User selects file on /upload page
2. Client validates file type and size
3. File sent to POST /api/uploads
4. Server validates file, extracts metadata
5. File stored in Supabase Storage bucket
6. Metadata saved to Postgres via repository
7. Upload record created with owner_uid
8. Response includes upload ID and metadata

Document Processing:
- document-runtime.ts handles processing pipeline
- Metadata extraction (file type, size, name)
- Storage path generation
- Retention policy application

Storage Retention:
- Uploads/documents: 15 minutes (configurable via ZOOTOPIA_UPLOAD_RETENTION_MINUTES)
- Mode: expiry (automatic deletion after retention period)
- Configurable per scope via env vars

Storage Operations:
- supabase-blob-storage.ts handles all storage operations
- Upload, download, delete, URL generation
- Bucket management and cleanup
- Admin storage cleanup endpoint: POST /api/admin/storage/cleanup

File Types Supported:
- PDF documents
- Images (PNG, JPG, etc.)
- Text files
- Other document formats

================================================================================
16. ASSESSMENT RUNTIME / RESULT / EXPORT FLOW
================================================================================

Assessment Generation Flow:
1. User navigates to /assessment
2. User selects document and configures assessment options
3. Client sends POST /api/assessment
4. Server performs checks:
   a. Authentication check
   b. Profile completion check
   c. Admin bypass (if admin)
   d. Idempotency check
   e. Credit reservation check (daily limit)
   f. Tool-scoped default-model resolution and request model validation
  g. Assessment-prompt lock enforcement for normal users when prompt text is submitted
5. AI generation via ai/execution.ts
6. Assessment result saved to Postgres
7. Admin log entry created
8. Response includes assessment data

Assessment Model Defaults:
- Backend default model resolves through `apps/web/lib/server/ai/default-models.ts`
- Env key: `ZOOTOPIA_DEFAULT_MODEL_ASSESSMENT`
- Frontend initial selected model is passed from the server page using the same
  resolver, but backend request validation still re-normalizes and re-validates
  the submitted model id
- Fallback when env is missing/invalid/unsupported: `gemini-2.5-flash-lite`

Credit System:
- Daily credit limit: ZOOTOPIA_DEFAULT_DAILY_ASSESSMENT_CREDITS (default 3)
- Renewal window: ZOOTOPIA_ASSESSMENT_CREDIT_RENEWAL_WINDOW_HOURS (default 24h UTC)
- GET /api/assessment/credits returns current status with explicit no-store headers
- Owner-scoped private Supabase Realtime channel topics
  (`assessment-credit:owner:{uid}`) are the live invalidation lane for
  near-real-time credit freshness
- Protected shell channel setup applies current Supabase session access tokens
  to `supabase.realtime.setAuth(...)` and re-syncs on auth state change before
  subscribing, keeping private-channel auth aligned with token refresh/sign-out
- Successful `POST /api/assessment` commits now publish an owner-scoped
  invalidation signal after the durable credit commit so other tabs for the
  same user refetch canonical `/api/assessment/credits`
- Protected shell and Assessment Studio consume one shared TanStack Query key
  for `/api/assessment/credits`
- The active credit flow has no studio/page `initialData`, no direct
  `setQueryData`, and no mutation-response/realtime-payload balance patching in
  the shared client cache; invalidation/refetch is the only live refresh path
- Credit reservation before AI generation
- Credit consumed on success, released on failure
- Protected shell credit badge now follows TanStack invalidation + periodic
  revalidation fallback when realtime is unavailable or reconnecting

Assessment Retrieval:
- GET /api/assessment/[id] — Single assessment
- GET /api/assessments — List user assessments
- GET /api/assessment/linked-document — Resolve linked document context

Export Flow:
- GET /api/assessment/export/* — Export assessment
- Formats: PDF, JSON, Markdown, DOCX
- Locale and theme variants supported
- `/api/assessment/export/pdf/[id]` is compatibility-only and redirects to the
  explicit Fast or Pro lane using canonical runtime origin
- `/api/assessment/export/pdf/fast/[id]` stays the lightweight owner-scoped
  browser-print HTML lane and now owns
  `apps/web/lib/server/assessment-fast-print-renderer.ts`
- `/api/assessment/export/pdf/pro/[id]` is the owner-scoped Puppeteer lane for
  real downloadable PDFs, runs on Node.js with `maxDuration = 120`, owns
  `apps/web/lib/server/assessment-pro-print-renderer.ts`, and now traces the
  hoisted workspace-root `@sparticuz/chromium/bin/**/*` payload so Vercel
  lambdas receive the packaged browser binary even when npm workspaces do not
  create `apps/web/node_modules`
- Pro PDF failures are now logged with internal stage classification
  (`route-context`, `artifact-load`, `qr-build`, `html-render`, `pdf-buffer`,
  `artifact-store`, `generation-save`, `audit-log`) while the UI still returns
  the same professional generic failure message
- PDF generation uses Puppeteer (puppeteer-core + @sparticuz/chromium)
- Export retention: 15 minutes (configurable)

Detached Preview/Footer Ownership:
- `/assessment/preview/[id]` and `/assessment/results/[id]` still share
  `AssessmentPreviewShell` and `AssessmentResultViewer` for page chrome/body.
- Preview-only footer ownership now branches inside
  `apps/web/components/assessment/assessment-result-viewer.tsx` by `view`.
- Preview route uses
  `apps/web/components/assessment/assessment-preview-page-footer.tsx`.
- Result route keeps `apps/web/components/assessment/assessment-file-footer.tsx`
  as its shared footer owner.
- Assessment preview/result/export grouping is now owned by normalized
  `questionSections` from `apps/web/lib/server/assessment-preview.ts`; extra
  provider-returned question types are tolerated in raw generation output but
  filtered out of final grouped display/export surfaces.
- Legacy assessment records now recover omitted choice arrays from
  `rawModelResult.responseJson.questions[*]` during normalization, and the
  preview/export layer also tolerates alternate per-question choice field names
  before rendering grouped cards.
- Preview/result/export metadata rows intentionally omit the model label/value
  even though `generation.meta.modelLabel` remains persisted for internal use.
- Type-specific preview/result card ownership now lives in
  `apps/web/components/assessment/renderers/*` with the grouped page shell in
  `apps/web/components/assessment/assessment-grouped-view.tsx` and the switch
  boundary in `apps/web/components/assessment/question-renderer-switch.tsx`.
- Fast export HTML/footer ownership now lives in
  `apps/web/lib/server/assessment-fast-print-renderer.ts`.
- Pro export HTML/footer ownership now lives in
  `apps/web/lib/server/assessment-pro-print-renderer.ts`.

Assessment Retention:
- Results: 1440 minutes (1 day, configurable)
- Mode: expiry (automatic cleanup)
- assessment-retention.ts handles lifecycle management

================================================================================
17. INFOGRAPHIC FLOW
================================================================================

Infographic Generation:
- POST /api/infographic — Generate infographic
- Admin-only feature (currently gated)
- Non-admin users see "coming soon" on /infographic page
- Backend now validates infographic model selection against infographic-scoped
  catalog entries before execution

Flow:
1. User/admin navigates to /infographic
2. Non-admin → sees coming-soon message
3. Admin → can configure and generate
4. AI processes document content for infographic data
5. Infographic rendered on client
6. Export options available

Modes:
- Multiple infographic modes supported
- English language guide available (Guide-Files/Zootopia_Infographic_Modes_English_Guide.md)

Infographic Model Defaults:
- Backend default model resolves through `apps/web/lib/server/ai/default-models.ts`
- Env key: `ZOOTOPIA_DEFAULT_MODEL_INFOGRAPHIC`
- Frontend initial selected model is passed from the server page using the same
  resolver, but backend still validates submitted model ids
- Fallback when env is missing/invalid/unsupported or cross-tool:
  `google-balanced`
- Invalid infographic request model ids now fail closed with
  `INFOGRAPHIC_MODEL_UNSUPPORTED`, and execution has a second infographic-scoped
  guard to prevent accidental fallback into assessment defaults

================================================================================
18. DATABASE / MIGRATIONS / STORAGE / PERSISTENCE NOTES
================================================================================

Database: Supabase Postgres (provider-managed)

Migrations (supabase/migrations/):
1. 20260410120000_zootopia_platform_entities.sql
  - Base zc_entities table + storage bucket bootstrap
2. 20260410180000_phase1_postgres_parity_tables.sql
  - Phase 1 parity relational tables
3. 20260410180001_phase1_storage_bucket.sql
  - Additional phase-1 storage bucket wiring
4. 20260410213000_phase1_owner_rls_and_storage_policies.sql
  - Owner-scoped RLS and storage policy hardening
5. 20260412000000_phase2_internal_table_hardening.sql
  - Internal table hardening
6. 20260412103000_email_verification_resend_governance.sql
  - Resend governance persistence tables/functions
7. 20260412164000_internal_operational_table_lockdown.sql
  - Internal operational table lockdown
8. 20260413193000_fix_zc_entities_created_at_and_index.sql
  - Idempotent repair for missing created_at metadata column + supporting index
9. 20260414103000_storage_owner_policies_unified_user_root.sql
  - Rebuilds storage object owner policies to allow canonical users/{uid}/... paths while keeping legacy compatibility
10. 20260414153000_assessment_artifact_metadata_index_and_document_extension.sql
  - Adds assessment artifact metadata query indexes + backfills assessmentArtifactMetadata mirror rows
  - Backfills missing document fileExtension metadata and adds file-extension query index
11. 20260414230000_user_password_security_events.sql
  - Adds `user_password_security_events` append-only telemetry table for password reset/change events
  - Adds `user_password_security_state` latest-state table with backend-only deny-all RLS for anon/authenticated
12. 20260416193000_assessment_credit_private_realtime_authorization.sql
  - Adds owner-authenticated private-channel SELECT policy on `realtime.messages`
    for `assessment-credit:owner:{auth.uid}` topic namespace

Migration Application:
- scripts/supabase/apply-migrations.mjs
- Uses postgres npm client against SUPABASE_DATABASE_URL / DATABASE_URL
- Maintains public._migration_history for canonical timestamp-safe replay tracking
- Applies pending migrations sequentially inside per-file transactions

Current Metadata Persistence Notes:
- Assessment generations still persist canonical `artifacts` map payloads.
- `assessmentArtifactMetadata` now mirrors each artifact into independently queryable
  owner-scoped records (for retention/admin/reporting queries).
- Upload documents now persist `fileExtension` and `contentSha256` fields for
  durable format/digest metadata lookup.

NPM Scripts:
- npm run supabase:migrations:list — List migrations
- npm run supabase:migrations:validate — Validate migration order
- npm run supabase:migrations:status — Check migration status
- npm run supabase:check — Validate + list
- npm run db:apply — Apply migrations
- npm run db:apply:dry — Dry run
- npm run db:status — Check status

Storage:
- Supabase Storage buckets (provider-managed)
- Bucket names and paths configured in supabase-blob-storage.ts
- Retention policies applied per scope
- Admin cleanup endpoint for manual cleanup

RLS Policies:
- Admin: full access to all records
- User: access only to own records (owner_uid match)
- Policies defined in initial migration and hardened in later migrations

================================================================================
19. AI / MODEL / PROVIDER ORCHESTRATION
================================================================================

Providers:
1. Google AI (Gemini)
   - API Key: GOOGLE_AI_API_KEY
   - Model: configurable via GOOGLE_AI_MODEL, GOOGLE_AI_ADVANCED_MODEL
   - Used for: assessment generation, infographic data extraction

2. DashScope (Qwen/Alibaba)
   - API Key: DASHSCOPE_API_KEY
   - Base URL: DASHSCOPE_BASE_URL (default: https://dashscope-us.aliyuncs.com/compatible-mode/v1)
   - Model: configurable via QWEN_MODEL
   - Legacy aliases: DASHSCOPE_COMPATIBLE_BASE_URL, ALIBABA_MODEL_STUDIO_BASE_URL

Orchestration:
- ai/execution.ts handles provider selection and execution
- Provider selection based on task type and availability
- Prompt construction from messages/ directory templates
- Response parsing and validation
- Error handling and fallback logic

Tool Model Catalog & Defaults:
- Real tool scopes currently present in the repo: `assessment`, `infographic`
- Shared catalog lives in `packages/shared-config/src/models.ts`
- Built-in assessment default: `gemini-2.5-flash-lite`
- Built-in infographic default: `google-balanced`
- Server-authoritative per-tool env resolver lives in
  `apps/web/lib/server/ai/default-models.ts`
- Provider runtime env overrides (`GOOGLE_AI_MODEL`,
  `GOOGLE_AI_ADVANCED_MODEL`, `QWEN_MODEL`) remain separate from tool-default
  selection and still resolve provider-side model ids after tool selection

Prompt Templates:
- Stored in apps/web/messages/ directory
- Organized by task type
- May include i18n variants
- Assessment prompt orchestration now explicitly requires complete per-question
  payloads, full ordered choice arrays for choice-based questions, empty
  `choices` arrays for non-choice questions, explicit answers, and rationale
  when available so normalization/rendering contracts stay aligned

PDF Generation:
- Uses Puppeteer (puppeteer-core)
- @sparticuz/chromium for serverless Chrome
- Vercel Pro PDF tracing must include the workspace-root Chromium Brotli payload
  because this repo hoists the package to root `node_modules`
- Local executable override: ASSESSMENT_PDF_BROWSER_EXECUTABLE_PATH
- PUPPETEER_EXECUTABLE_PATH for local development

================================================================================
20. REDIRECT / CALLBACK / CONFIRMATION BEHAVIOR
================================================================================

Post-Login Redirects:
- User default: NEXT_PUBLIC_ZOOTOPIA_AUTH_USER_DEFAULT_REDIRECT (default /)
- Admin default: NEXT_PUBLIC_ZOOTOPIA_AUTH_ADMIN_DEFAULT_REDIRECT (default /)
- Using NEXT_PUBLIC_ prefix for server/client alignment in Next App Router

Email Confirmation:
- /confirm-email page handles email verification
- Supabase sends confirmation email via Resend
- User clicks link → redirected to /confirm-email
- Finalization now verifies provider-confirmed email state before any success UI
- Only confirmed callbacks proceed to redirect back to `/login` or `/admin/login`
- Confirm-email success intentionally cleans up the helper Supabase session so
  Auth.js remains the only entry path into protected routes

Password Recovery Callbacks:
- `/forgot-password` requests reset emails through `/api/auth/password/forgot` with privacy-preserving success responses
- `/reset-password` validates callback tokens, enforces user-only lane ownership, and completes password updates with password-security metadata persistence

Auth Flow Redirects:
- Login success → default redirect or profile completion check
- Profile incomplete → redirect to /settings
- Admin login → admin default redirect
- Email confirmation → `/login` or `/admin/login`, then explicit Auth.js login

Runtime-Origin Authority (2026-04-12 hardening):
- Server-only redirects/callback URLs now use getServerRuntimeOrigin() instead of request origin
- Canonical resolution order: NEXT_PUBLIC_BASE_URL → NEXTAUTH_URL → VERCEL_URL → localhost
- Protocol-less env values are normalized (https by default, http for localhost variants)
- Confirm-email resend callback and PDF export compatibility redirect no longer trust request host
- Pro PDF Puppeteer asset base URL now uses canonical runtime origin (host-header spoof resistant)

================================================================================
21. EMAIL VERIFICATION & RESEND GOVERNANCE
================================================================================

Email Provider:
- Supabase Auth manages email sending via Resend (provider mode)
- SMTP relay available for contact form (optional)

Verification Resend Governance:
- verification-resend-governance.ts handles rate limiting
- Cooldown: 30 seconds (ZOOTOPIA_EMAIL_VERIFICATION_COOLDOWN_SECONDS)
- Account max attempts: 5 per window (ZOOTOPIA_EMAIL_VERIFICATION_MAX_ATTEMPTS)
- Account window: 60 minutes (ZOOTOPIA_EMAIL_VERIFICATION_WINDOW_MINUTES)
- IP max attempts: 20 per window (ZOOTOPIA_EMAIL_VERIFICATION_IP_MAX_ATTEMPTS)
- IP window: 60 minutes (ZOOTOPIA_EMAIL_VERIFICATION_IP_WINDOW_MINUTES)
- Hash salt: ZOOTOPIA_EMAIL_VERIFICATION_HASH_SALT

API Endpoint:
- GET /api/auth/confirm-email/resend?email=... — Return governance snapshot for resend availability polling
- POST /api/auth/confirm-email/resend — Trigger resend with governance

Confirm-Email Query/UX Hardening (2026-04-12):
- Confirm-email page now resolves duplicate query keys using first non-empty value (for example email=&email=user@example.com)
- Resend GET route now resolves duplicate email params using first non-empty value before validation
- Resend action label remains truthful per email session: starts as "Send verification email" and only flips to
  "Resend verification email" after a real provider-accepted send has been observed

Resend Governance Correctness Hardening (2026-04-13):
- Provider-rejected resend attempts are now rolled back so account/IP attempt budgets are not permanently consumed
  when upstream delivery was never accepted.
- Rollback remains best-effort: route still returns provider-classified errors if rollback itself fails.
- Confirm-email panel now disables submit while governance is initializing and during governance-blocked states
  (cooldown/account/ip/unavailable), preventing misleading early submits.
- Reserve flow now marks reservation-accepted sends so the same request is not immediately rejected by its own
  newly-created cooldown; cooldown still applies to subsequent attempts.

Email Templates:
- docs/auth-email/templates/zootopia-confirm-email-template-supabase.html
- Custom branded confirmation email template

Legacy Aliases (still accepted):
- ZOOTOPIA_VERIFICATION_RESEND_* (all variants)

Public Auth Admission Governance (2026-04-18):
- auth-admission-governance.ts enforces persisted account/IP admission windows
  for sign-up and sign-in across instances
- Window: 15 minutes by default (`ZOOTOPIA_AUTH_ADMISSION_WINDOW_SECONDS`)
- Sign-up account max attempts: 3 (`ZOOTOPIA_AUTH_SIGNUP_ACCOUNT_MAX_ATTEMPTS`)
- Sign-up IP max attempts: 15 (`ZOOTOPIA_AUTH_SIGNUP_IP_MAX_ATTEMPTS`)
- Sign-in account max attempts: 12 (`ZOOTOPIA_AUTH_SIGNIN_ACCOUNT_MAX_ATTEMPTS`)
- Sign-in IP max attempts: 60 (`ZOOTOPIA_AUTH_SIGNIN_IP_MAX_ATTEMPTS`)
- Hash salt: `ZOOTOPIA_AUTH_ADMISSION_HASH_SALT`
- API: `POST /api/auth/signup` and `POST /api/auth/login/admission`
- Both routes return Retry-After with admission-window-aware retry guidance
  when shaping public auth load

================================================================================
22. SECURITY ASSUMPTIONS & TRUST BOUNDARIES
================================================================================

Trust Boundaries:
- Browser → Supabase Auth: trusted provider (cross-origin)
- Browser → Next.js API: same-origin (trusted)
- Next.js Server → Supabase Postgres: server-side only (connection string)
- Next.js Server → Supabase Storage: server + browser (provider-managed)
- Next.js Server → Google AI API: server-side only (API key)
- Next.js Server → DashScope API: server-side only (API key)
- Next.js Server → SMTP: server-side only (credentials)

Security Measures:
- JWT sessions (stateless, httpOnly cookies)
- Rate limiting on auth endpoints (20 req/min per IP)
- Admin email allowlist (ZOOTOPIA_ADMIN_EMAILS)
- RLS policies on all database tables
- Profile completion gating
- Credit limits on AI generation
- Email verification rate limiting
- Token recency checks (auth_time within 5 minutes)
- Provider checks (password only)
- Status checks (active vs suspended)

Sensitive Data:
- AUTH_SECRET: Session encryption key
- SUPABASE_SERVICE_ROLE_KEY: Full admin access to Supabase
- SUPABASE_DATABASE_URL: Database connection string
- GOOGLE_AI_API_KEY: Google AI provider key
- DASHSCOPE_API_KEY: DashScope provider key
- SMTP credentials: Email relay credentials
- ZOOTOPIA_ADMIN_EMAILS: Admin allowlist

Never Expose to Browser:
- SUPABASE_SERVICE_ROLE_KEY
- SUPABASE_DATABASE_URL
- GOOGLE_AI_API_KEY
- DASHSCOPE_API_KEY
- SMTP credentials
- AUTH_SECRET
- Any private keys or credentials

================================================================================
23. IMPORTANT FOLDERS & FILES
================================================================================

Root Configuration:
- package.json — Root workspace manifest
- tsconfig.json — Root TypeScript configuration
- tsconfig.base.json — Shared TypeScript base config
- eslint.config.mjs — ESLint configuration
- .env.example — Environment variable template
- .env.local — Local environment (gitignored)
- AGENTS.md — Agent instructions

Application (apps/web/):
- auth.ts — Auth.js configuration
- proxy.ts — Auth.js proxy helpers
- next.config.ts — Next.js configuration
- app/ — App Router routes and layouts
- components/ — React components
- components/assessment/assessment-preview-page-footer.tsx — preview-route-only
  footer surface for `/assessment/preview/[id]` (kept separate from result/export)
- lib/ — Library code
- lib/server/ — Server-side services
- messages/ — AI prompt templates / i18n messages
- public/ — Static assets
- scripts/ — Build/postbuild scripts
- package.json — @zootopia/web manifest

Shared Packages:
- packages/shared-config/ — Constants, routes, env keys, feature flags
- packages/shared-types/ — TypeScript interfaces, DTOs
- packages/shared-utils/ — Pure utility functions

Database & Migrations:
- supabase/migrations/ — SQL migration files
- scripts/supabase/ — Migration management scripts

Documentation:
- docs/ — Architecture documentation
- docs/auth-email/ — Auth email templates
- Guide-Files/ — Agent/CLI guides (reference only)
- zootopia-club-next-ledger.txt — This file

================================================================================
24. SENSITIVE & HIGH-RISK FILES
================================================================================

Critical Secret Files:
- .env.local — Contains all local secrets (gitignored)
- apps/web/auth.ts — Contains auth logic, session configuration
- apps/web/lib/server/zootopia-postgres-adapter.ts — Database connection handling
- apps/web/lib/server/supabase-blob-storage.ts — Storage operations with service key

High-Risk API Routes:
- /api/admin/** — Admin operations (user management, storage cleanup)
- /api/assessment — Assessment generation (credit consumption)
- /api/uploads — File upload (storage consumption)
- /api/auth/confirm-email/resend — Email resend (rate-limited)

High-Risk Server Files:
- apps/web/lib/server/ai/execution.ts — AI provider calls (API key usage)
- apps/web/lib/server/session.ts — Session validation
- apps/web/lib/server/repository.ts — Data access (all entities)

Migration Files:
- supabase/migrations/*.sql — Database schema changes (irreversible if destructive)
- scripts/supabase/apply-migrations.mjs — Migration application

Build Scripts:
- apps/web/scripts/normalize-standalone.mjs — Postbuild normalization

Agent Instruction Files:
- AGENTS.md — Contains agent behavior rules
- Guide-Files/*.txt, *.md — Reference guides (not runtime code)

================================================================================
25. IMPORTANT COMMANDS & SCRIPTS
================================================================================

Development:
- npm run dev — Start local development server (with Turbopack)
- npm run dev:local — Same as dev
- npm run dev:web — Same as dev

Build:
- npm run build — Build all workspaces
- npm run build:local — Build all workspaces locally
- npm run build:vercel — Build for Vercel deployment
- npm run build:web — Build web app only
- npm run rebuild:web — Clean and rebuild web app

Start:
- npm run start — Start local production server
- npm run start:local — Start local production server
- npm run start:web — Start web app
- npm run start:vercel — Start web app (Vercel mode)

Lint & Type Check:
- npm run lint — Lint all workspaces
- npm run lint:web — Lint web app
- npm run typecheck — Type check all workspaces
- npm run typecheck:web — Type check web app
- npm run check — Lint + typecheck + build
- npm run check:web — Lint + typecheck + build web app

Clean:
- npm run clean:web — Remove .next build cache

Database:
- npm run supabase:migrations:list — List migrations
- npm run supabase:migrations:validate — Validate migration order
- npm run supabase:migrations:status — Check migration status
- npm run supabase:check — Validate + list migrations
- npm run db:apply — Apply migrations
- npm run db:apply:dry — Dry run migrations
- npm run db:status — Check migration status

Auth:
- npx auth secret — Generate AUTH_SECRET

================================================================================
26. CURRENT FEATURE SET
================================================================================

Implemented & Active:
- [x] User authentication (email/password via Supabase)
- [x] Admin authentication (separate provider, email allowlist)
- [x] Email verification with resend governance
- [x] Regular-user forgot-password and recovery reset flow
- [x] In-account regular-user password change with forced re-authentication handoff
- [x] Unified regular-user password policy enforcement across sign-up/reset/settings
- [x] Profile completion gating
- [x] Document upload with retention policies
- [x] AI assessment generation with credit limits
- [x] Env-driven Assessment setup UI lock profile for normal users (count/type/language)
  with visible disabled options, Arabic locked-types note, and explicit admin bypass
- [x] Near-real-time server-authoritative assessment credit synchronization
- [x] Dedicated protected global credits page at `/credits` with `/assessment/credits` compatibility redirect
- [x] Assessment history viewer
- [x] Assessment export (PDF, JSON)
- [x] User settings page
- [x] Admin dashboard
- [x] Admin user management
- [x] Admin storage management
- [x] Infographic page (admin-only, others see coming-soon)
- [x] Dark/light mode support
- [x] Localization support
- [x] Responsive design (mobile to ultra-wide)
- [x] Supabase Postgres database with RLS
- [x] Supabase Storage with retention policies
- [x] Multi-provider AI orchestration (Google + DashScope)
- [x] Session management with JWT
- [x] Rate limiting on auth endpoints
- [x] Admin audit logging
- [x] Admin-managed per-user assessment prompt entitlement grant/revoke controls
- [x] Env-controlled global maintenance mode with proxy-enforced non-admin redirects
- [x] Secret-gated internal expired-upload maintenance cleanup endpoint

Partially Implemented:
- [~] Infographic generation (UI present, AI flow may be incomplete)
- [~] Contact form (SMTP relay configured, may need activation)
- [~] Governance controls (admin page may be partial)

Not Implemented / Placeholder:
- [ ] @zootopia/api extraction (placeholder only)
- [ ] Firebase integration (fully migrated away)

================================================================================
27. DEFERRED / DISABLED / LEGACY AREAS
================================================================================

Legacy/Deprecated:
- Firebase integration: Fully migrated to Supabase. Legacy env vars commented out.
- ZOOTOPIA_ADMIN_LOGIN_PASSWORD: Deprecated admin password gate. Replaced by allowlist.
- ZOOTOPIA_VERIFICATION_RESEND_*: Legacy aliases for email verification. Still accepted.
- ZOOTOPIA_STORAGE_RETENTION_*: Legacy global retention fallback. Still accepted.
- FIREBASE_* env vars: Commented out, not used by current runtime.

Reference-Only:
- Guide-Files/: Historical agent guides, deployment guides, classification guides.
  These are reference material, not active runtime code.
- docs/legacy-reference/: Legacy documentation.
- apps/code-backup.txt: Historical code backup.

Inactive:
- apps/api/: Placeholder for future API extraction. No active code.
- Single-app server layouts (server.ts, server/*): Not present in current repo.
- Docker/Cloud Build/Netlify: Not present in current repo.

Deferred:
- Infographic AI generation: UI present, may need AI flow completion.
- Contact form: SMTP configured, may need activation.
- Governance controls: May need completion.

================================================================================
28. FUTURE AGENT GUIDANCE
================================================================================

For Future AI Agents and Human Developers:

1. Always read this ledger first before making changes.
2. Treat it as the primary historical memory of the project.
3. Compare the ledger against the real current codebase before editing.
4. Never rebuild from scratch unless explicitly requested.
5. Never redesign the architecture unless explicitly requested.
6. Never remove working features unless explicitly requested.
7. Make only minimal, surgical, backward-compatible changes.
8. Read all relevant files fully before editing.
9. Trace imports, exports, route ownership, shared contracts, and state flow.
10. Preserve the current live runtime ownership.
11. Keep backend authority on the backend.
12. Never expose secrets, API keys, credentials, or privileged logic to the browser.
13. Always check official documentation for every framework, library, or platform.
14. Prefer official conventions over ad-hoc patterns.
15. Keep shared state separate from shared page UI.
16. Preserve dark mode, light mode, localization, and current design language.
17. Always consider responsive behavior across all screen sizes.
18. Run lint, typecheck, and build after meaningful changes.
19. Refresh this ledger thoroughly after every task.
20. Record all meaningful changes, decisions, risks, and verification results.

Architecture Boundaries to Respect:
- Frontend: apps/web/app/(site)/**, apps/web/app/(public)/**, client components
- Backend: apps/web/app/api/**, apps/web/lib/server/**, auth.ts, proxy.ts
- Shared: packages/shared-*/**
- Infrastructure: supabase/**, scripts/**, vercel.json
- Provider-Managed: Supabase, Vercel, Resend, Google AI, DashScope

Sensitive Areas Requiring Extra Care:
- auth.ts: Session and authentication configuration
- lib/server/**: Server-side business logic
- supabase/migrations/**: Database schema changes
- .env.local: Secrets (never commit)
- API routes with admin access
- AI provider configuration
- Storage retention policies

================================================================================
29. HISTORICAL CHANGE LOG
================================================================================

2026-04-19: PROTECTED HOME TOP SCROLL-GUIDANCE STRIP (ARABIC-ONLY, PREMIUM)
────────────────────────────────────────────────────────────────────────────

ROOT CAUSE:
1. Protected Home already had rich sections, but the primary scroll invitation
   appeared lower inside the hero/status lane and was easy to miss on first view.
2. The requested UX needed an early, elegant, non-intrusive top-level cue that
   encourages users to continue downward and discover the full page.

FILES CHANGED:
- apps/web/app/(protected)/(completed)/page.tsx
- zootopia-club-next-ledger.txt

MIGRATIONS:
- No migration added.
- No migration changed.

REASON:
- Deliver a minimal, protected Home UI-only enhancement that adds a premium
  top scroll-invitation strip without touching backend/auth/session/permissions/
  routing authority/business logic.

TECHNICAL DETAILS:
1. Placement and hierarchy
   - Added a new RTL guidance strip at the very top of the Home page container,
     immediately before the existing Zone 1 hero section.
   - Kept the element lightweight and non-blocking so current Home structure,
     cards, and flow remain intact.

2. Arabic-only copy
   - Added polished Arabic guidance text:
     `لا تفوّت أي تفصيل — مرّر لأسفل واكتشف كل ما يميز المنصة`
   - No English text was introduced inside this new guidance element.

3. Downward cue + motion
   - Used a downward hand emoji indicator (`👇`) plus `ChevronDown` icon.
   - Applied subtle hint motion on the chevron only:
     `animate-[bounce_2.3s_ease-in-out_infinite] motion-reduce:animate-none`.
   - Motion remains lightweight and respects reduced-motion preferences.

4. Theme and responsiveness
   - Matched existing premium translucent/glow language with light/dark-safe
     border/background/text tokens.
   - Used wrap-safe centered layout (`flex-wrap`, `text-center`, adaptive
     small-text sizing) to keep readability stable across mobile/tablet/desktop/
     ultrawide widths.

PRESERVED INTENTIONALLY:
- No backend/API/auth/session/permissions/routing-authority/business-logic
  modifications.
- No popups, no scroll hijacking, no blocking behavior.
- Existing Home sections and interaction patterns remain unchanged.

VERIFICATION:
Diagnostics:
✓ `get_errors` on changed file — PASS.

Command verification:
✓ `npm run lint --workspace @zootopia/web` — PASS.
✓ `npm run typecheck --workspace @zootopia/web` — PASS.
✓ `npm run build --workspace @zootopia/web` — PASS.
✓ `npm run build:vercel --workspace @zootopia/web` — PASS.

Code-trace UI verification:
✓ New top strip exists before Home hero section and renders Arabic-only copy.
✓ Downward cue uses `👇` + animated `ChevronDown` with reduced-motion fallback.
✓ Theme classes provide readable light/dark contrast.
✓ Layout classes maintain responsive wrapping/alignment on narrow and wide viewports.

Runtime note:
~ No authenticated browser screenshot session was available in this environment,
  so visual confirmation is code-trace based plus successful lint/type/build/
  build:vercel passes.

2026-04-19: HOME PAGE TEASER CTA DARK MODE FIX
────────────────────────────────────────────────────────────────────────────

ROOT CAUSE:
1. `apps/web/components/home/lecture-summary-coming-soon-teaser.tsx` had a CTA button
   with `dark:bg-cyan-300/80` and `dark:text-slate-950`.
2. This bright background in dark mode appeared too white-ish against the dark aesthetic
   and made the text unreadable for users who expected a dark-themed button.

FILES CHANGED:
- apps/web/components/home/lecture-summary-coming-soon-teaser.tsx
- zootopia-club-next-ledger.txt

MIGRATIONS:
- No migration added.
- No migration changed.

REASON:
- Apply a minimal UI-only fix targeting dark mode to make the button text clearly 
  readable while matching the teaser's general look and feel.

TECHNICAL DETAILS:
- Changed the background styling for the "افتح صفحة الصور والنماذج" CTA button
  from `dark:bg-cyan-300/80` to `dark:bg-cyan-950/50`.
- Changed button text color from `dark:text-slate-950` to `dark:text-cyan-50`.
- Changed button hover background from `dark:hover:bg-cyan-200/90` to `dark:hover:bg-cyan-900/60`.
- Changed button border from implicitly inherited/cyan-200 to `dark:border-cyan-800/60`.

2026-04-18: WEB LINT WARNING CLEANUP (ASSESSMENT STUDIO + CONFIRM EMAIL PANEL)
────────────────────────────────────────────────────────────────────────────

ROOT CAUSE:
1. `apps/web/components/assessment/assessment-studio.tsx` set
   `aria-disabled` on a native `<form>`, but `form` already has an implicit
   role and does not support `aria-disabled` in the lint accessibility rule.
2. `apps/web/components/auth/confirm-email-panel.tsx` used `flow` inside a
   `useEffect` body while omitting it from the dependency array, which raised
   the React Hooks exhaustive-deps warning.

FILES CHANGED:
- apps/web/components/assessment/assessment-studio.tsx
- apps/web/components/auth/confirm-email-panel.tsx
- zootopia-club-next-ledger.txt

MIGRATIONS:
- No migration added.
- No migration changed.

REASON:
- Apply a minimal, backward-compatible lint cleanup pass that preserves current
  UX behavior, accessibility intent, and auth/redirect flow semantics.

TECHNICAL DETAILS:
1. Assessment Studio accessibility fix
   - Removed invalid `aria-disabled` from the `<form>` element.
   - Added `aria-describedby` on the form when platform lock is active,
     pointing to the existing lock guidance copy.
   - Added stable id `assessment-platform-capacity-lock-message` to the lock
     body text so assistive technologies can associate form state with the
     visible lock explanation.
   - Existing lock behavior remained unchanged (same lock banner, same submit
     blocking conditions, same backend-authoritative enforcement).

2. Confirm-email hook dependency fix
   - Added `flow` to the finalize `useEffect` dependency array.
   - Preserved current rerun protections via existing refs
     (`finalizationStartedRef`, `redirectStartedRef`) so no duplicate
     finalization/redirect path was introduced.

WHAT WAS INTENTIONALLY PRESERVED:
- No backend/auth/session/permission/business logic changes.
- No route contract changes.
- No UI redesign or control-flow rewrite.

VERIFICATION:
Diagnostics verification:
✓ `get_errors` on changed files — PASS.

Command verification:
✓ `npm run lint --workspace @zootopia/web` — PASS (0 warnings, 0 errors).

2026-04-18: ASSESSMENT RESULT-VIEWER FULL TITLE + SUMMARY WRAP EXCEPTION (UI-ONLY)
────────────────────────────────────────────────────────────────────────────

ROOT CAUSE:
1. Detached assessment preview/result header title still used mobile smart-title
   truncation plus `line-clamp` limits, so long generated titles were not fully
   visible on small screens.
2. Detached header summary still used `line-clamp-3` on narrow screens, so
   complete summary text was not always visible.
3. The requested behavior required a targeted exception for this surface only,
   without changing compact clamp behavior on other shared card lanes.

FILES CHANGED:
- apps/web/components/assessment/assessment-preview-shell.tsx
- zootopia-club-next-ledger.txt

MIGRATIONS:
- No migration added.
- No migration changed.

REASON:
- Deliver a minimal, surgical UI-only refinement so assessment result-viewer
  title and summary always render in full while preserving existing premium
  styling, spacing, dark/light behavior, and actions.

TECHNICAL DETAILS:
1. Title truncation/clamp removal
   - Removed `buildSmartTitleDisplay` usage for `preview.title` in detached
     preview/result header rendering.
   - Removed `line-clamp-*` classes from the `<h1>` title node.
   - Title now renders full `preview.title` with wrap-safe classes:
     `break-words` + `[overflow-wrap:anywhere]`.

2. Summary clamp removal
   - Removed `line-clamp-3 sm:line-clamp-none` from summary body.
   - Summary now uses wrap-safe classes:
     `assessment-file-summary__body break-words [overflow-wrap:anywhere]`.

3. Scope containment
   - Preserved existing detached surface hierarchy, metadata/action rails, and
     theme treatment.
   - No backend/auth/session/permission/query/business logic changes.
   - No shared-card clamp behavior outside this detached assessment surface was
     modified.

VERIFICATION:
Static/code verification:
✓ `get_errors` on `assessment-preview-shell.tsx` — PASS.
✓ Confirmed removed patterns are absent in the file:
  `buildSmartTitleDisplay(preview.title)` and title/summary `line-clamp` classes.
✓ Confirmed wrap-safe classes are present on title and summary nodes.

Command verification:
✓ `npm run lint --workspace @zootopia/web` — PASS
  (2 pre-existing warnings only:
  `assessment-studio.tsx` aria-disabled on form role,
  `confirm-email-panel.tsx` missing `flow` dependency in `useEffect`).
✓ `npm run typecheck --workspace @zootopia/web` — PASS.
✓ `npm run build --workspace @zootopia/web` — PASS.
✓ `npm run build:vercel --workspace @zootopia/web` — PASS.

Viewport-behavior verification:
✓ Small/narrow screens: title/summary no longer contain clamp/truncate classes;
  wrap utilities enforce downward wrapping within the existing container.
✓ Large screens: existing detached header layout, spacing, and typography scale
  were preserved while full title/summary remain visible.

2026-04-18: EXEMPT LOGIN-ADMISSION BYPASS + TRUTHFUL ADMISSION-UNAVAILABLE MESSAGING
────────────────────────────────────────────────────────────────────────────

ROOT CAUSE:
1. `POST /api/auth/login/admission` still reserved the generic public
   auth-admission window before checking the active normal-user exempt/admin
   bypass, so exempt identities could still be blocked by admission-store
   degradation even though the decisive Auth.js/session capacity paths already
   treated them as exempt.
2. `AUTH_ACTIVE_USER_ADMISSION_UNAVAILABLE` was normalized into
   `AUTH_RATE_LIMITED`, so the public login UI rendered the 15-minute pacing/
   queue copy for a temporary admission-state availability problem.
3. When a real capacity-full denial lost its live count snapshot, the fallback
   login copy also dropped to the retry-later pacing message instead of staying
   on truthful capacity-full wording.
4. `ZOOTOPIA_ADMIN_EMAILS` parsing was still stricter than the exempt-email
   parser, so accidental env quote wrapping or newline/semicolon separators
   could silently break admin matching and the admin-implied bypass path.

FILES CHANGED:
- apps/web/app/api/auth/login/admission/route.ts
- apps/web/lib/auth-failure.ts
- apps/web/components/auth/auth-feedback.ts
- apps/web/components/auth/login-panel.tsx
- apps/web/lib/server/admin-auth.ts
- apps/web/messages/en.json
- apps/web/messages/ar.json
- zootopia-club-next-ledger.txt

MIGRATIONS:
- No migration added.
- No migration changed.

REASON:
- Ensure exempt/admin identities bypass public login admission before any
  blocking admission decision is returned.
- Keep normal users fail-closed while letting exempt/admin identities fail open
  when the admission-state store is temporarily unavailable.
- Preserve truthful user-facing messaging by separating real capacity-full from
  temporary admission availability degradation.
- Keep Auth.js as the trust boundary and avoid redesigning the surrounding auth
  architecture.

TECHNICAL DETAILS:
1. Login admission ordering hardening
   - `POST /api/auth/login/admission` now evaluates active normal-user
     admission first.
   - Capacity-full responses now return immediately with
     `AUTH_ACTIVE_USER_CAPACITY_FULL`.
   - Exempt/admin identities now return `accepted: true` immediately and skip
     `reserveAuthAdmissionAttempt(...)`, so the generic public auth-admission
     store can no longer re-gate them before Auth.js authorize runs.

2. Distinct auth failure normalization
   - `apps/web/lib/auth-failure.ts` now preserves
     `AUTH_ACTIVE_USER_ADMISSION_UNAVAILABLE` as its own normalized code
     instead of collapsing it into `AUTH_RATE_LIMITED`.

3. Truthful UI mapping
   - `apps/web/components/auth/auth-feedback.ts` now maps
     `AUTH_ACTIVE_USER_ADMISSION_UNAVAILABLE` to dedicated temporary
     availability copy.
   - Capacity-full fallback copy now stays on a capacity-full message even when
     the exact active/limit snapshot is unavailable.
   - `apps/web/components/auth/login-panel.tsx` now surfaces the temporary
     admission-unavailable state during blocked-login polling instead of
     silently leaving stale queue/capacity copy on screen.

4. Admin env parsing hardening
   - `apps/web/lib/server/admin-auth.ts` now tolerates one accidental outer
     quote pair plus newline/semicolon delimiters so admin allowlist parsing
     stays aligned with the exempt-email parser used by active-capacity
     governance.

5. Auth.js/session/logout preservation
   - Re-traced `apps/web/auth.ts`, `apps/web/lib/server/session.ts`, and
     `apps/web/app/api/auth/logout/route.ts`.
   - No logic change was required there because:
     - user authorize already calls the decisive
       `reserveOrRenewActiveNormalUserSessionLease(...)`
     - exempt identities already fail open inside that lease helper
     - admin authorize/session rehydration already best-effort release stale
       leases instead of renewing them
     - logout already reads raw `auth()` and releases the lease without routing
       through renewal-gated session rehydration

WHAT WAS INTENTIONALLY PRESERVED:
- Auth.js cookie -> server session rehydration remains the only protected-route
  trust boundary.
- Normal-user active-capacity governance remains server-authoritative and
  fail-closed for non-exempt identities.
- Allowlisted admins still must use the dedicated admin lane; this patch stops
  public login preflight from mislabeling them as queued, but it does not turn
  `/login` into a second admin entrance.
- Existing sign-up, confirm-email, profile-completion, repository, and owner-
  scope architecture remained unchanged.

RISKS / FOLLOW-UP:
1. Full live Auth.js admin sign-in and live exempt authenticated session
   rehydration still require dedicated QA credentials to prove beyond code
   tracing.
2. Real capacity-full browser proof still requires a controlled occupied-seat
   fixture or concurrent-session test setup; it was not forced in this
   environment.
3. The client diagnosis stage can still read as
   `AUTH_STAGE_E_SESSION_HYDRATION` for some preflight-originated failures
   because the login panel currently buckets submit failures at that later
   surface; this patch targeted the raw code truth and UI messaging, not a
   broader stage-taxonomy redesign.

VERIFICATION:
Parallel investigation:
✓ Completed the requested multi-agent trace lanes before editing and
  consolidated the findings against the live codebase.

Code-trace verification:
✓ Verified exempt/admin bypass already exists in the decisive
  Auth.js authorize, session rehydration, and logout release paths.
✓ Verified the misleading queue message came from auth-failure normalization
  collapsing `AUTH_ACTIVE_USER_ADMISSION_UNAVAILABLE` into
  `AUTH_RATE_LIMITED`.
✓ Verified the public login admission route was the only decisive remaining
  bypass gap.

Command verification:
✓ `npm run lint` — PASS (2 pre-existing warnings only:
  `assessment-studio.tsx` aria-disabled on form role, and
  `confirm-email-panel.tsx` missing `flow` dependency in `useEffect`)
✓ `npm run typecheck` — PASS
✓ `npm run build` — PASS
✓ `npm run build:vercel` — PASS

Live runtime proof (final patched build on `127.0.0.1:3020`):
✓ `GET /login` — `200`
✓ `GET /admin/login` — `200`
✓ `POST /api/auth/login/admission` with required exempt email
  `elmahdyabdulla208@gmail.com` — `200`, `accepted: true`,
  `capacity.exempt: true`, `reason: "EXEMPT"`
✓ `POST /api/auth/login/admission` with allowlisted admin email
  `alahlyeagle@gmail.com` — `200`, `accepted: true`,
  `capacity.exempt: true`, `reason: "EXEMPT"`
✓ `POST /api/auth/login/admission` with non-exempt
  `normal-user@example.com` — `503`,
  `AUTH_ACTIVE_USER_ADMISSION_UNAVAILABLE`,
  `"Secure login admission is temporarily unavailable. Please wait a moment and try again."`
✓ Headless browser submit on `/login` with non-exempt
  `normal-user@example.com` matched the temporary
  admission-unavailable UI message, not the 15-minute queue text
✓ Headless browser submit on `/login` with required exempt email
  `elmahdyabdulla208@gmail.com` reached the invalid-credentials UI path,
  which proves the exempt identity bypassed the public queue/admission gate
  and continued to the downstream Supabase/Auth.js lane instead

Runtime proof still not completed live:
~ No dedicated credential-backed admin Auth.js sign-in was executed.
~ No dedicated credential-backed exempt Auth.js sign-in + protected-session
  rehydration cycle was executed.
~ No controlled live capacity-full occupancy fixture was created in this
  environment.

2026-04-18: SMART TITLE END-VISIBILITY POLISH + HOME TINY-SCREEN VIEWPORT CONTAINMENT
────────────────────────────────────────────────────────────────────────────

ROOT CAUSE:
1. Wrap/clamp hardening kept cards stable, but long generated/file/result titles
   could still hide the most useful trailing identity cues on tiny screens
   (year/version suffixes and file extensions).
2. Multiple card surfaces needed one shared, extension-aware middle-ellipsis
   strategy so users can distinguish similarly prefixed titles faster without
   waiting for desktop breakpoints.
3. Home protected surfaces still needed explicit root/grid width-guard tightening
   to fully close tiny-screen viewport-leak edge cases.

CHANGES:
1. Added shared smart-title utility in
   `apps/web/lib/smart-title-display.ts`.
   - Introduced `buildSmartTitleDisplay(...)` with extension detection,
     configurable width budgets, and middle-ellipsis rendering.

2. Applied mobile-first smart-title rendering (desktop/tablet keep full title)
   across shared generated/file/result card surfaces in:
   `apps/web/app/(protected)/(completed)/page.tsx`
   `apps/web/app/(protected)/(completed)/history/page.tsx`
   `apps/web/components/upload/upload-workspace.tsx`
   `apps/web/components/document/document-context-card.tsx`
   `apps/web/components/assessment/assessment-preview-shell.tsx`
   `apps/web/components/assessment/assessment-studio.tsx`
   `apps/web/components/assessment/assessment-result-viewer.tsx`

3. Re-checked and hardened Home tiny-screen containment in
   `apps/web/app/(protected)/(completed)/page.tsx`.
   - Strengthened root/content rails with `min-w-0` + `max-w-full` guards so
     very narrow devices cannot push Home generated cards beyond viewport width.

FILES CHANGED:
- apps/web/lib/smart-title-display.ts
- apps/web/app/(protected)/(completed)/page.tsx
- apps/web/app/(protected)/(completed)/history/page.tsx
- apps/web/components/upload/upload-workspace.tsx
- apps/web/components/document/document-context-card.tsx
- apps/web/components/assessment/assessment-preview-shell.tsx
- apps/web/components/assessment/assessment-studio.tsx
- apps/web/components/assessment/assessment-result-viewer.tsx
- zootopia-club-next-ledger.txt

MIGRATIONS:
- No migration added.
- No migration changed.

REASON:
- Deliver the requested UI-only polish so long names display a larger useful
  visible portion while preserving ending context/file extension.
- Keep premium design language intact and desktop behavior stable.
- Ensure Home page cards/containers remain fully viewport-contained on very
  small screens.

TECHNICAL DETAILS:
- `buildSmartTitleDisplay(...)` trims only when needed, detects plausible
  extensions via last-dot token validation, and renders `prefix...suffix`
  where suffix keeps ending context (including full extension when present).
- Surface-level overrides use compact budgets (for example max length ~40-42)
  tuned for tiny-screen card density.
- Rendering pattern is breakpoint-safe: smart title on mobile via
  `sm:hidden`, full raw title on `sm+` via `hidden sm:inline`.
- Scope remained UI-only; no backend/API/auth/session/repository/storage
  authority was modified.

RISKS / FOLLOW-UP:
- Low UI risk: tiny-screen titles now intentionally trade exact middle text for
  faster visual identification of beginning + ending markers.
- Full raw title remains available through desktop rendering and existing
  tooltip/title attributes where present.

VERIFICATION:
Post-edit command verification:
✓ `npm run lint --workspace @zootopia/web` — PASS
✓ `npm run typecheck --workspace @zootopia/web` — PASS
✓ `npm run build --workspace @zootopia/web` — PASS
✓ `npm run build:vercel` — PASS

2026-04-18: TINY-SCREEN GENERATED-CARD VIEWPORT-FIT HARDENING (NO HORIZONTAL SWIPE)
────────────────────────────────────────────────────────────────────────────

ROOT CAUSE:
1. Protected shell main content still permitted assessment-path horizontal
   auto-scroll, so child card fixes alone could not fully prevent viewport
   bleed on tiny screens.
2. Shared export/history action rails still had intrinsic-width pressure from
   metadata, badges, and button groups; on narrow devices this could force
   card width beyond viewport bounds.
3. Several action groups switched to horizontal at `sm`, which is too early
   for the smallest phone widths.
4. Generated title/summary surfaces were wrap-safe but not consistently
   clamp-first/compact on tiny screens, leaving some cards visually dominant.

CHANGES:
1. Removed assessment-path horizontal auto-scroll in
   `apps/web/components/layout/protected-shell.tsx`
   - Protected workspace content now hides horizontal overflow by default,
     eliminating shell-level swipe bleed from generated-card lanes.

2. Hardened generated-card viewport constraints in
   `apps/web/app/(protected)/(completed)/history/page.tsx`
   `apps/web/app/(protected)/(completed)/page.tsx`
   `apps/web/components/assessment/assessment-studio.tsx`
   `apps/web/components/upload/upload-workspace.tsx`
   - Added tighter width/shrink guards and stronger mobile title/summary clamp
     behavior so long generated text remains in-card on tiny screens.

3. Hardened shared action/export shrink behavior in
   `apps/web/app/globals.css`
   `apps/web/components/assessment/assessment-export-actions.tsx`
   `apps/web/components/history/history-document-actions.tsx`
   - Strengthened wrapper shrink rules (`min-w-0`/`w-full`/`max-w-full`).
   - Kept vertical-first stacking on tiny screens and delayed horizontal switch
     to `md` where required.
   - Reduced mobile control sizing while preserving desktop/tablet rhythm.

4. Tightened detached preview/result mobile clamping in
   `apps/web/components/assessment/assessment-preview-shell.tsx`
   - Reduced title dominance and strengthened summary clamp behavior on
     small screens without changing backend metadata ownership.

FILES CHANGED:
- apps/web/components/layout/protected-shell.tsx
- apps/web/app/(protected)/(completed)/history/page.tsx
- apps/web/app/(protected)/(completed)/page.tsx
- apps/web/components/assessment/assessment-studio.tsx
- apps/web/components/upload/upload-workspace.tsx
- apps/web/app/globals.css
- apps/web/components/assessment/assessment-export-actions.tsx
- apps/web/components/history/history-document-actions.tsx
- apps/web/components/assessment/assessment-preview-shell.tsx
- zootopia-club-next-ledger.txt

REASON:
- Complete the UI-only tiny-screen follow-up so generated cards no longer
  produce horizontal swipe/overflow in protected surfaces.
- Keep existing premium design language while making mobile controls more
  compact and generated title/summary previews less dominant.

TECHNICAL DETAILS:
- Applied only minimal class/CSS hardening (`min-w-0`, `w-full`, `max-w-full`,
  `overflow-hidden`, controlled clamp sizing, and breakpoint-safe stacking).
- No backend/API/auth/session/repository/migration logic changed.
- No dependency changes or architecture redesign.

RISKS / FOLLOW-UP:
- Low UI risk: tighter mobile clamps may shorten visible preview text sooner,
  but hover/full-view paths still preserve full content access.
- Targeted QA confirmed no horizontal overflow in tested route combinations,
  but this sandbox's generation attempt returned
  `ASSESSMENT_PROVIDER_AUTH_FAILED` (503), so generated export-toolbar-heavy
  fixtures were limited in this run.

VERIFICATION:
Post-edit command verification:
✓ `npm run lint --workspace @zootopia/web` — PASS
✓ `npm run typecheck --workspace @zootopia/web` — PASS
✓ `npm run build --workspace @zootopia/web` — PASS
✓ `npm run build:vercel` — PASS

Targeted tiny-screen QA verification:
✓ `node .tmp/mobile-card-overflow-supabase-qa.mjs` (temporary helper removed
  after run)
  - combinations: 24 (route/theme/viewport)
  - route failures: 0
  - horizontal overflow failures: 0
  - export stacking failures: 0
  - compact button failures: 0
  - upload attempt: status 201 (ok)
  - generation attempt: status 503 (`ASSESSMENT_PROVIDER_AUTH_FAILED`)
  - report: `output/playwright/mobile-card-overflow/2026-04-18T00-32-52-709Z/mobile-card-overflow-report.json`

2026-04-17: SHARED GENERATED-CARD WRAP HARDENING + PREVIEW TITLE FIX + LIGHT-MODE BADGE CONTRAST
────────────────────────────────────────────────────────────────────────────

ROOT CAUSE:
1. Multiple generated artifact surfaces still rendered long titles, filenames,
   summaries, and metadata without consistent wrap-safe guards (`break-words`,
   overflow-wrap) even when parent cards were responsive.
2. Some card lanes previously relied on truncation or unconstrained inline text,
   which could push content beyond card edges or viewport width on narrow screens.
3. The Assessment Studio question-count badge used `text-accent`, and in light
   mode `--accent` resolves to a very light cream tone that reduced text contrast.
4. Detached preview/result header title and summary blocks needed stronger
   wrap-safe behavior for long generated result names and summaries.

CHANGES:
1. Hardened History generated cards in
   `apps/web/app/(protected)/(completed)/history/page.tsx`
   - Added wrap-safe classes for document filenames, assessment titles, summaries,
     and metadata spans (`break-words` + `[overflow-wrap:anywhere]`).
   - Preserved existing card hierarchy, actions, and theme language.

2. Hardened Home generated cards in
   `apps/web/app/(protected)/(completed)/page.tsx`
   - Added `min-w-0` guards to recent document content lane.
   - Switched long generated assessment title from truncation to wrap-safe text
     within the existing card layout.

3. Hardened Upload generated-document rows in
   `apps/web/components/upload/upload-workspace.tsx`
   - Replaced truncation on latest-document filename with wrap-safe rendering.
   - Added wrap-safe handling for active-document filename above action rows.

4. Hardened detached Preview/Result header wrapping in
   `apps/web/components/assessment/assessment-preview-shell.tsx` and
   `apps/web/app/globals.css`
   - Added `min-w-0` + wrap-safe title classes on preview/result `<h1>`.
   - Added wrap-safe behavior to summary body via shared CSS rule
     (`overflow-wrap: anywhere`).
   - Added wrap-safe handling for composition badges.
   - Increased light-mode composition badge text contrast (`text-cyan-800` /
     `text-emerald-800`) while preserving dark-mode classes.

5. Hardened detached result metadata and badge wrapping in
   `apps/web/components/assessment/assessment-result-viewer.tsx`
   - Added wrap-safe classes for metadata values and first-page info badges
     (`questionCountLabel`, source document label).

6. Fixed light-mode question-count badge contrast in
   `apps/web/components/assessment/assessment-studio.tsx`
   - Updated history-card question-count badge text from `text-accent` to
     `text-accent-strong dark:text-accent` so light mode stays readable while
     dark mode behavior remains unchanged.
   - Added wrap-safe classes for assessment title/summary and metadata chips in
     the same generated-history card lane.

FILES CHANGED:
- apps/web/app/(protected)/(completed)/history/page.tsx
- apps/web/app/(protected)/(completed)/page.tsx
- apps/web/components/upload/upload-workspace.tsx
- apps/web/components/assessment/assessment-preview-shell.tsx
- apps/web/components/assessment/assessment-result-viewer.tsx
- apps/web/components/assessment/assessment-studio.tsx
- apps/web/app/globals.css
- zootopia-club-next-ledger.txt

REASON:
- Complete a shared-card hardening pass so long generated artifact text remains
  readable inside card boundaries across Assessment, History, Upload, Home, and
  detached Preview/Result surfaces.
- Resolve the remaining light-mode question-count badge contrast issue without
  disturbing existing dark-mode styling.

TECHNICAL DETAILS:
- Focused on minimal, backward-compatible class/CSS adjustments only.
- No backend/API/auth/session/repository/permission logic was changed.
- No feature removal, architecture redesign, or dependency changes were made.
- Action controls and existing visual hierarchy were preserved.

RISKS / FOLLOW-UP:
- Low UI risk: wrapping behavior now prioritizes full text readability over
  single-line truncation in generated-card lanes.
- Recommend visual QA sweep across small mobile/mobile/tablet/laptop/desktop/
  ultra-wide once full browser runtime is available.

VERIFICATION:
Baseline (before edits):
✓ `npm install` — PASS
✓ `npm run lint` — PASS
✓ `npm run typecheck` — PASS
✗ `npm run build` — FAIL (network-restricted Google Fonts fetch from `fonts.googleapis.com`)
✗ `npm run build:vercel` — FAIL (same Google Fonts network restriction)

Post-edit:
✓ `npm run lint` — PASS
✓ `npm run typecheck` — PASS
✗ `npm run build` — FAIL (same external Google Fonts fetch restriction in sandbox)
✗ `npm run build:vercel` — FAIL (same external Google Fonts fetch restriction in sandbox)

Responsive verification notes:
~ Code-level wrap/overflow verification completed for shared generated-card lanes
  and detached preview/result title+summary+badge lanes.
~ Live browser viewport matrix verification remains blocked here by build-time
  external-font fetch failures unrelated to this UI patch.

2026-04-17: ADMIN CREDIT CONTROL HARDENING - POST-COMMIT FALLBACK STATE
────────────────────────────────────────────────────────────────────────────

ROOT CAUSE:
1. Admin credit mutations on `/admin/users/[uid]` and `/api/admin/users/[uid]/credits`
   already committed durably inside `applyAdminAssessmentCreditMutation(...)`, but the
   repository helper performed a second read afterward through
   `getAdminAssessmentCreditStateForUser(...)`.
2. If that post-commit reread degraded or returned null after the write had already landed,
   the helper threw `ASSESSMENT_CREDIT_STATE_UNAVAILABLE` and surfaced a false failure to the
   admin UI/API.
3. Because the live admin page uses the server-action lane and only refreshes on success,
   a false failure could hide an already-committed mutation and encourage a duplicate retry,
   which is unsafe because the admin credit mutation path has no idempotency token.

CURRENT VERIFIED STATE:
1. Confirmed `apps/web/lib/server/repository.ts` already contains the repository-layer hardening
   for this risk in the current codebase.
   - `buildAdminAssessmentCreditCommittedFallbackState(...)` is present.
   - `applyAdminAssessmentCreditMutation(...)` already captures a committed fallback snapshot.
   - A degraded post-commit reread already returns the committed fallback state instead of
     surfacing a false mutation failure.
   - The repository already emits the focused diagnostic event
     `assessment_credit_post_commit_read_degraded`.

FILES CHANGED:
- zootopia-club-next-ledger.txt

MIGRATIONS:
- No migration added.
- No migration changed.
- Verified current migration inventory is 16 files, including:
  `20260416193000_assessment_credit_private_realtime_authorization.sql`,
  `20260416210000_assessment_credit_mutations.sql`, and
  `20260417103000_assessment_credit_prompt_entitlement.sql`.

REASON:
- Refresh the ledger to the current truth after a full credit-control audit.
- Document that the current codebase already mitigates the narrow false-failure risk where a
  durable admin credit commit could appear failed after the fact.
- Keep Auth.js/server authority, repository-owned mutation logic, canonical `/api/assessment/credits`
  reads, and private Supabase invalidation behavior unchanged.

TECHNICAL DETAILS:
- The active admin credit UI path is the server-action lane on `/admin/users/[uid]`; the API
  route remains a compatible backend lane but is not the in-repo caller today.
- Admin credit persistence remains transaction-backed in `applyAdminAssessmentCreditMutation(...)`.
  Legacy mirrors and structured tables still update together through
  `syncStructuredAssessmentCreditState(...)`.
- Persisted mutation history remains authoritative and still records actor identity, target owner,
  action, before/after snapshots, correlation ID, route source, and commit status.
- Realtime remains owner-scoped and invalidation-only on topic
  `assessment-credit:owner:{uid}`. No balance truth moved into browser payloads.
- Assessment credit subtraction/consumption logic was re-verified by code path:
  reservation in `reserveAssessmentDailyCreditAttempt(...)`, release in
  `releaseAssessmentDailyCreditReservation(...)`, and durable one-credit commit in
  `saveAssessmentGenerationWithCreditCommit(...)`.

RUNTIME / DEPLOYMENT NOTES:
1. Local integrated development
   - Frontend: Next.js App Router in `apps/web`, protected shell uses React Query and browser
     Supabase helper for private realtime auth only.
   - Backend: Auth.js/session checks, repository transactions, admin mutation server actions,
     and API routes all execute in the same local app runtime.
   - Same-origin: app pages + API routes are same-origin; Supabase Auth/Realtime remain
     provider-managed cross-origin services.
  - Shared verified behavior: the repository fallback hardening applies here directly.

2. Vercel + Supabase (current active path)
   - Frontend: Vercel-served `apps/web` pages and protected shell subscribe to the private
     Supabase broadcast topic after browser session auth is applied.
   - Backend: Auth.js/JWT remains the server trust boundary; admin credit mutations still commit
     on the server before any live update is published.
   - Same-origin vs provider-managed: app pages/routes are same-origin on Vercel, while Supabase
     Auth/Database/Realtime are provider-managed.
  - Shared verified behavior: the same repository fallback avoids false failure across
    multi-instance production reads after commit.

3. Legacy/reference paths still present in code/docs
   - `/api/admin/users/[uid]/credits` remains a supported backend lane.
   - `/assessment/credits` remains a compatibility redirect to `/credits`.
   - `apps/web/lib/middleware.ts` still appears unused/stale and was not activated or edited.

PRESERVED INTENTIONALLY:
- Auth.js as the server trust boundary.
- Admin authority on the backend only.
- Canonical user-visible credit totals from server-owned read paths.
- Private Supabase channel delivery as owner-scoped invalidation only.
- Hybrid legacy + structured credit persistence architecture.
- Existing assessment reservation/commit/release semantics.

RISKS / FOLLOW-UP:
- The hybrid legacy/structured credit read model is still timestamp-reconciled, so mirror drift
  remains a longer-term follow-up risk outside this surgical fix.
- `assessment_credit_mutations` still has no explicit owner-read RLS policy; that is acceptable
  for the current server-only access pattern but would matter for future direct client reads.
- Authenticated browser proof for admin mutation, targeted realtime delivery, and end-user credit
  refresh is still recommended once dedicated QA credentials are available.

VERIFICATION:
Code-traced and architecture-traced in this pass:
✓ Admin UI -> server action -> repository -> transaction -> mutation history -> live publish ->
  canonical read model path traced and reconciled.
✓ Owner-scoped enforcement verified for normal-user reads, admin-only mutation routes, and private
  realtime topic authorization.
✓ Assessment consumption path re-verified logically for reservation, release, and durable commit.

Command verification:
✓ `npm run lint:web` — PASS
✓ `npm run typecheck:web` — PASS
✓ `npm run supabase:migrations:validate` — PASS (`Validated 16 migration file(s).`)
✓ `npm run build:web` — PASS
✓ `npm run build:vercel` — PASS

Runtime proof status:
~ Fresh authenticated browser proof was not performed in this sandbox because no dedicated
  admin/user QA credentials or live fixture accounts were provided for end-to-end mutation tests.
~ Existing saved runtime artifacts still support owner-lane separation and admin self-mutation
  rejection, but this pass's new fix was verified primarily by code path plus full build/lint/type
  validation rather than live browser mutation playback.

2026-04-17: SHARED RESULT / FILE CARD TITLE CONSTRAINT HARDENING
────────────────────────────────────────────────────────────────────────────

ROOT CAUSE:
1. The shared result/file card surfaces already used wrap-first + line-clamp title styling in
   several places, but a few card rows still left the title column on an unconstrained flex basis
   when badges or action rails sat beside it.
2. In those mixed-content rows, the title text could keep too much intrinsic width before the clamp
   budget applied, which made very long file/result names the remaining narrow-screen and side-rail
   overflow risk.

FILES CHANGED:
- apps/web/app/(protected)/(completed)/page.tsx
- apps/web/app/(protected)/(completed)/history/page.tsx
- apps/web/components/assessment/assessment-studio.tsx
- apps/web/components/document/document-context-card.tsx
- apps/web/components/upload/upload-workspace.tsx
- zootopia-club-next-ledger.txt

MIGRATIONS:
- No migration added.
- No migration changed.

REASON:
- Complete the UI-only shared-card follow-up without redesigning the card system.
- Keep long file/result titles inside their cards by letting the existing wrap/clamp/ellipsis
  styling receive a reliable width budget on the remaining shared surfaces.

TECHNICAL DETAILS:
- Home recent-upload cards now give the document text rail `sm:flex-1` and the title element
  `min-w-0 w-full max-w-full` so the filename can shrink cleanly beside the status chips.
- History linked-document cards now give the text column `xl:flex-1`, which preserves the premium
  side-by-side desktop composition while ensuring long file names yield space to the action rail.
- Assessment Studio recent-generation cards now give the left content rail `xl:flex-1`, allowing
  long generated-result names to wrap and clamp before the right-side actions are crowded.
- Upload workspace recent-upload and active-document cards now give the text rail a flex width and
  the title itself `min-w-0 w-full max-w-full`, so long names stay inside both the compact recent
  card and the larger continue-to-assessment CTA surface.
- The shared `DocumentContextCard` now gives the text block `sm:flex-1` and the highlighted file
  title `min-w-0 w-full max-w-full`, keeping the shared assessment/infographic context pattern
  contained when a long filename sits next to the status badge.
- No backend metadata, storage, auth/session, or permissions logic changed.

PRESERVED INTENTIONALLY:
- Existing premium card design language, gradients, spacing, and action hierarchy.
- Existing wrap-first + line-clamp + ellipsis approach where it was already correct.
- Existing responsive stacking behavior and dark/light mode presentation.
- Existing backend/server authority and all non-UI behavior.

RISKS / FOLLOW-UP:
- This pass hardened the shared card title rails that still had width-budget drift; if any future
  new card surface renders file/result names outside these shared patterns, it should copy the same
  `min-w-0` + flex-basis rules rather than reintroducing plain intrinsic-width text.
- Fresh browser screenshots were not generated in this pass, so runtime validation remains
  code-traced plus lint/type/build proof rather than new visual snapshots.

VERIFICATION:
Code/UI verification:
✓ Re-traced the shared file/result title surfaces on Home, History, Assessment Studio, Upload, and
  Document Context cards to confirm the smallest safe edit surface.
✓ Verified the updated title nodes now use constrained flex rails plus wrap/clamp-safe width rules
  so long titles stay in-card and end with ellipsis when clamped.

Command verification:
✓ `npm run lint:web` — PASS
✓ `npm run typecheck:web` — PASS
✓ `npm run build:web` — PASS
✓ `npm run build:vercel` — PASS

Verification note:
~ An initial parallel run of `build:web` and `build:vercel` caused `build:web` to hit Next's
  existing "Another next build process is already running" lock. `build:web` was rerun alone and
  passed cleanly, so the final verified state is green.

2026-04-18: PUBLIC AUTH ADMISSION HARDENING + CONFIRM-EMAIL REDIRECT-TO-LOGIN
────────────────────────────────────────────────────────────────────────────

ROOT CAUSE:
1. Regular-user sign-up still ran browser-direct through `supabase.auth.signUp()` in
   `apps/web/components/auth/login-panel.tsx`, so the public auth edge had no shared
   cross-instance admission control before provider user creation started.
2. The existing auth-edge throttling for regular login/signup was either client-local or
   process-local, which is too weak for concurrent production-style bursts across multiple
   instances.
3. Confirm-email success still bootstrapped Auth.js immediately from the callback page, which
   bypassed the explicit "verify email -> go back to login -> log in -> profile gating" path
   required by the current runtime.
4. Duplicate sign-up attempts could still surface as generic upstream failures instead of a
   truthful "account already exists" path.

FILES CHANGED:
- apps/web/lib/server/auth-admission-governance.ts
- apps/web/app/api/auth/login/admission/route.ts
- apps/web/app/api/auth/signup/route.ts
- apps/web/components/auth/login-panel.tsx
- apps/web/components/auth/confirm-email-panel.tsx
- apps/web/components/auth/auth-feedback.ts
- apps/web/lib/auth-failure.ts
- apps/web/messages/en.json
- apps/web/messages/ar.json
- supabase/migrations/20260418110000_auth_admission_governance.sql
- zootopia-club-next-ledger.txt

MIGRATIONS:
- Added `20260418110000_auth_admission_governance.sql`
  - Creates backend-only `public.auth_admission_governance`
  - Stores sign-in/sign-up admission windows for account/IP buckets
  - Enables RLS and deny-all policies for `anon` and `authenticated`

REASON:
- Make new-user registration reliable without rebuilding auth.
- Add a small production-grade public-auth admission layer that matches the
  current Auth.js + Supabase runtime.
- Keep email verification isolated per account and return users to the login
  surface after successful confirmation.
- Preserve profile completion gating, owner-scoped persistence, admin
  separation, and backend authority.

TECHNICAL DETAILS:
1. Public auth admission control
   - Added `auth-admission-governance.ts`, modeled after the existing
     verification resend governance table/transaction pattern.
   - Uses persisted account/IP windows with row locking, shared across
     instances, instead of per-process memory only.
   - Default shaping window is 15 minutes with env-tunable per-flow attempt
     budgets and `Retry-After`/`X-RateLimit-Reset` headers.

2. Sign-up hardening
   - Added `POST /api/auth/signup` as the server-owned sign-up entry point.
   - Route validates email/password, reserves admission capacity, builds the
     confirm-email callback URL from `getServerRuntimeOrigin()`, and calls
     Supabase with a transient publishable-key server client.
   - Duplicate existing-account responses now normalize to
     `AUTH_ACCOUNT_ALREADY_EXISTS` with truthful user guidance.
   - If Supabase ever returns an immediately usable session, the client now
     restores the helper browser session and still enters through Auth.js.

3. Login hardening
   - Added `POST /api/auth/login/admission` and wired `login-panel.tsx` to
     preflight login capacity before browser `signInWithPassword()`.
   - User-facing overload copy now explicitly asks users to retry in about
     15 minutes when public auth traffic is being paced.

4. Confirm-email flow
   - `confirm-email-panel.tsx` still finalizes the Supabase callback and
     verifies provider-confirmed email state, but no longer bootstraps Auth.js
     from the callback page.
   - Success now performs best-effort local Supabase session cleanup and
     redirects to `/login` or `/admin/login` with `confirmed=1&email=...`.
   - `login-panel.tsx` now reads that redirect state, pre-fills the email, and
     shows the post-confirmation sign-in prompt.

5. Shared auth diagnostics / UX
   - Extended `auth-failure.ts` with `AUTH_ACCOUNT_ALREADY_EXISTS`.
   - Updated `auth-feedback.ts` and EN/AR messages so regular-user rate limiting,
     duplicate-account guidance, and post-confirmation guidance are centralized
     instead of scattered raw strings.

RUNTIME / DEPLOYMENT NOTES:
1. Local integrated development
   - The original checkout in `C:\zootopia-club-next` remained ACL-locked for
     existing tracked files, so this pass was implemented and verified in a
     writable sibling clone at `C:\zootopia-club-next-codex-auth` on the same
     repository HEAD (`378c7a0063d7de2e3e75cf5d94be3409df64d46b`).
   - Same-origin app/API ownership and Supabase provider integration remained unchanged.

2. Vercel + Supabase
   - Auth.js/JWT remains the server trust boundary.
   - Browser Supabase auth remains helper-only for password sign-in token
     acquisition, confirmation finalization, and private Realtime auth.
   - The new admission layer is backend-owned and compatible with multi-instance
     deployments because it uses persisted Postgres windows.

PRESERVED INTENTIONALLY:
- Auth.js as the only protected-route/session trust boundary.
- Admin authority on the backend only.
- Existing profile-completion redirect model (`/settings` until complete).
- Existing owner-scoped upload/document/result/export persistence and retrieval.
- Existing admin governance, user-management visibility, and protected-shell behavior.
- Existing confirm-email resend governance and branded public auth design language.

RISKS / FOLLOW-UP:
- Fresh browser proof with real user credentials is still recommended to confirm
  the exact Supabase provider responses for:
  1. successful unconfirmed signup
  2. duplicate existing-account signup
  3. expired/used confirmation links
- Admission budgets are intentionally conservative defaults and may need tuning
  per deployment once real auth traffic is observed.
- This pass did not add an admin UI for admission-governance inspection; the
  persisted table is backend-observable now, but operational tooling could be
  added later if needed.

VERIFICATION:
Code-path verification:
✓ Re-traced regular sign-up, login, confirm-email, profile-gating, owner-scope,
  and admin-boundary code paths before editing.
✓ Verified confirm-email success now redirects to login/admin-login instead of
  bootstrapping protected access directly.
✓ Verified protected/profile-completion, owner-scoped persistence, and admin
  server-enforced paths were preserved and not redesigned in this pass.

Command verification:
✓ `npm install` — PASS
✓ `npm run lint:web` — PASS
✓ `npm run typecheck:web` — PASS
✓ `npm run build:web` — PASS
✓ `npm run build:vercel` — PASS
✓ `npm run supabase:migrations:validate` — PASS (`Validated 17 migration file(s).`)

Runtime proof status:
~ No live end-to-end browser signup/login/confirmation run was performed because
  no dedicated QA credentials or safe live test account workflow were available
  in this environment.
~ New signup/login overload behavior and duplicate-account handling were
  verified by route logic, auth error normalization, route manifest inclusion,
  migration validation, and full lint/type/build/build:vercel passes.

2026-04-18: ACTIVE NORMAL-USER SESSION CAPACITY GOVERNANCE (SERVER-AUTHORITATIVE)
────────────────────────────────────────────────────────────────────────────

ROOT CAUSE:
1. Public auth admission previously paced traffic without a strict concurrent
   active-user cap for normal users, so bursts could still admit more active
   sessions than intended.
2. Admission preflight alone is not decisive under race conditions; authoritative
   slot reservation/renewal must happen in trusted server flows.
3. The login UX needed a truthful live "capacity full" counter that reflects the
   current backend snapshot instead of static messaging.

FILES CHANGED:
- supabase/migrations/20260418123000_active_normal_user_session_capacity.sql
- apps/web/lib/server/active-normal-user-session-governance.ts
- apps/web/app/api/auth/login/admission/route.ts
- apps/web/auth.ts
- apps/web/lib/server/session.ts
- apps/web/app/api/auth/logout/route.ts
- apps/web/components/auth/login-panel.tsx
- apps/web/lib/auth-failure.ts
- apps/web/messages/en.json
- apps/web/messages/ar.json
- .env.example
- zootopia-club-next-ledger.txt

MIGRATIONS:
- Added `20260418123000_active_normal_user_session_capacity.sql`
  - Creates `public.active_normal_user_sessions` for active normal-user leases.
  - Adds indexes for lease expiry and normalized email lookups.
  - Enables RLS and deny-all policies for `anon` and `authenticated` so only
    backend SQL clients can read/write this governance table.

REASON:
- Replace wait-only pacing with a backend-authoritative concurrent active
  normal-user capacity model.
- Enforce the cap at trusted server boundaries while preserving admin and
  explicit exempt-account bypass behavior.
- Keep login UX accurate by exposing safe capacity snapshots to the login page.

TECHNICAL DETAILS:
1. New server-only capacity governance module
   - Introduced `active-normal-user-session-governance.ts` with bounded env
     parsing and normalized exemption logic.
   - Added a required exempt email (`elmahdyabdulla208@gmail.com`) plus env
     extension via `ZOOTOPIA_ACTIVE_NORMAL_USER_EXEMPT_EMAILS`.
   - Added transactional advisory-lock control using
     `pg_advisory_xact_lock(381104202)` to serialize capacity decisions across
     concurrent requests and instances.
   - Admission model now purges expired leases, reads active count, and either
     reserves/renews or denies based on configured limit.

2. Decisive server enforcement points
   - `POST /api/auth/login/admission` now evaluates normal-user capacity after
     existing auth-admission checks and returns 429 when full.
   - `GET /api/auth/login/admission` now returns live capacity snapshot for the
     login page polling loop.
   - Auth.js `authorize` in `apps/web/auth.ts` now calls
     `reserveOrRenewActiveNormalUserSessionLease` as the decisive gate before
     issuing normal-user authenticated state.
   - `session.ts` renews the lease in verified non-admin server session
     rehydration, and fail-closes non-exempt sessions when renewal cannot be
     trusted.
   - `logout` route performs best-effort lease release to free slots quickly.

3. Client UX and failure normalization
   - `login-panel.tsx` now parses capacity snapshot headers/details, blocks
     submit while full, and polls the admission GET endpoint for live counts.
   - Added capacity-aware status/button copy in EN/AR messages.
   - `auth-failure.ts` now normalizes active-user-capacity failure codes so race
     denials from server-authoritative paths map to stable UX behavior.

4. Environment contract additions
   - `ZOOTOPIA_ACTIVE_NORMAL_USER_LIMIT` (default 3)
   - `ZOOTOPIA_ACTIVE_NORMAL_USER_SESSION_MINUTES` (default 15)
   - `ZOOTOPIA_ACTIVE_NORMAL_USER_EXEMPT_EMAILS` (comma-separated)

PRESERVED INTENTIONALLY:
- Auth.js server trust boundary for protected runtime ownership.
- Backend authority for admission and session decisions.
- Admin bypass behavior and allowlist-based exemption model.
- Existing auth admission governance and sign-up/login architecture.
- Existing profile-completion gating and protected-shell behavior.
- Existing localization and design language, with only targeted messaging
  additions for capacity states.

RISKS / FOLLOW-UP:
- Runtime capacity behavior should still be browser-validated with multiple
  concurrent normal-user sessions plus one exempt/admin account to verify:
  1. strict cap denial for normal users,
  2. exempt bypass and non-counting behavior,
  3. lease expiry release behavior after inactivity window.
- This pass intentionally kept messaging simple and did not add an admin
  inspection UI for current active lease rows.

VERIFICATION:
Code-path verification:
✓ Re-traced trusted auth/session/login/logout paths after edits to confirm
  decisive slot control occurs on backend server boundaries.
✓ Verified exempt/admin paths are always allowed and normal-user paths fail
  closed on uncertain decisive renewal states.
✓ Verified login UI now receives and refreshes live capacity snapshot data.

Diagnostics verification:
✓ `get_errors` on all changed files — PASS (no language-service errors).

Command verification:
✓ `npm run lint --workspace @zootopia/web` — PASS
✓ `npm run typecheck --workspace @zootopia/web` — PASS
✓ `npm run build --workspace @zootopia/web` — PASS
✓ `npm run build:vercel --workspace @zootopia/web` — PASS
✓ `npm run supabase:migrations:validate` — PASS (`Validated 18 migration file(s).`)

Runtime proof status:
~ No dedicated live concurrent-session QA run was executed in this environment.
~ Enforcement and UX behavior were validated by server-path tracing, static
  diagnostics, migration validation, and full lint/type/build/build:vercel
  command passes.

2026-04-18: ADMIN-TO-USER CREDIT CONTROL + OWNER-SCOPE AUTHORITY AUDIT HARDENING
────────────────────────────────────────────────────────────────────────────

ROOT CAUSE:
1. The production credit-control architecture already kept mutation authority
  on the backend repository path, but one active admin detail server-action
  lane (`/admin/users/[uid]`) did not append dedicated admin logs for inline
  role/status mutations.
2. This created an audit-governance parity gap between API-route mutations
  (which already logged role/status changes) and same-page server-action
  mutations.
3. The full system required architecture-first revalidation to prove admin
  target UID integrity, owner-scope isolation, canonical credit read ownership,
  private realtime invalidation scope, and repository boundary preservation.

FILES CHANGED:
- apps/web/app/(protected)/admin/users/[uid]/page.tsx
- zootopia-club-next-ledger.txt

MIGRATIONS:
- No migration added.
- No migration changed.
- Existing migration inventory and order revalidated (`18` files).

REASON:
- Harden admin governance metadata completeness without redesigning runtime
  architecture.
- Keep server-action and API-route mutation lanes consistent for operator
  forensics and post-incident attribution.
- Preserve strict backend authority, owner scoping, and canonical read models.

WHAT WAS FIXED:
1. Added best-effort admin audit logging for inline role/status mutations in
  `apps/web/app/(protected)/admin/users/[uid]/page.tsx`:
  - `runInlineRoleMutation(...)` now imports and calls `appendAdminLog(...)`
    after successful `setUserRole(...)`.
  - `runInlineStatusMutation(...)` now imports and calls `appendAdminLog(...)`
    after successful `setUserStatus(...)`.
  - Logged metadata now includes server-action source marker:
    `source: "admin-user-detail-server-action"`.
2. Preserved existing best-effort semantics for observability operations so
  authoritative commits are never rolled back by logging failures.

WHAT WAS PRESERVED:
- Admin/user role and status mutation authority remains repository-owned in
  `setUserRole(...)` / `setUserStatus(...)`.
- Self-mutation protections remained unchanged and enforced in both API and
  server-action lanes.
- Credit mutation authority remained in
  `applyAdminAssessmentCreditMutation(...)` only.
- Canonical credit totals remain server-owned through
  `/api/assessment/credits` read path.
- Realtime remained invalidation-only; no balance truth moved to browser.
- Owner-scoped uploads/results/artifacts/storage path model remained unchanged.

OWNER-SCOPING ENFORCEMENT (CREDITS, UPLOADS, RESULTS, METADATA):
1. Credits:
  - User lanes read only via session UID through
    `getAssessmentDailyCreditsSummaryForUser({ uid, role })`.
  - Admin mutation targets are explicit `uid` route params, resolved server-side,
    and persisted with target `ownerUid` + actor metadata.
2. Uploads/Documents:
  - Upload create/finalize/delete and download routes derive owner from session
    (`user.uid`) and use owner-filtered repository methods (`*ForOwner`).
  - Storage keys are enforced by `assertOwnerScopedStoragePath(...)` with
    canonical `users/{ownerUid}/...` namespacing.
3. Assessments/Results/Exports:
  - Owner read routes use `getAssessmentGenerationForOwner(...)` and reject
    mismatched owner UIDs.
4. Artifact metadata:
  - Artifact mirror sync remains transaction-coupled to generation persistence
    through `syncAssessmentArtifactMetadataForGeneration(...)`.

ADMIN GOVERNANCE (SERVER-ENFORCED):
1. Admin routes continue to require `getAdminSessionUser()`/`requireAdminUser()`.
2. Role/status/credit/prompt/storage operations remain backend-only, never
  client-authoritative.
3. Self-mutation protections remain active in role/status/credits/delete flows.
4. Role elevation continues to enforce allowlist governance for admin role.
5. New inline role/status audit entries now match API mutation observability
  expectations on the active page server-action lane.

REPOSITORY BOUNDARY STATUS:
1. Sensitive persistence remains centralized in `apps/web/lib/server/repository.ts`:
  - credits: reserve/commit/release/admin mutation
  - role/status/prompt entitlement mutations
  - owner-scoped document/generation reads
  - audit log append helper
2. Route handlers remain orchestration/auth layers and do not introduce ad-hoc
  SQL/write bypasses for sensitive credit or owner data mutations.
3. No client-side second authority was introduced.

BACKEND AUTHORITY STATUS:
- Privileged operations remain server-owned:
  - admin credit mutation
  - prompt entitlement mutation
  - role/status mutation
  - audit log writes
  - realtime publication
  - upload/result/artifact persistence
- Browser code still triggers flows only; it does not own balance truth,
  artifact truth, or mutation authority.

PRIVATE SUPABASE CHANNEL DELIVERY (CURRENT CONTRACT):
1. Topic namespace stays owner-scoped private:
  `assessment-credit:owner:{uid}`.
2. Server publishing uses `publishAssessmentCreditLiveUpdate(...)` with
  `private: true` channel config.
3. Browser subscription applies Supabase access token via
  `supabaseClient.realtime.setAuth(...)` before channel join.
4. Payload remains invalidation envelope only (`eventId`, `emittedAt`, `traceId`);
  no balance numbers are trusted from realtime payload.
5. Canonical refresh path remains query invalidation + server re-read.

DATABASE PERSISTENCE GUARANTEES:
1. Admin credit mutations remain transaction-backed in
  `applyAdminAssessmentCreditMutation(...)`.
2. Before/after snapshots and mutation metadata persist durably in mutation
  history structures and structured mirror tables.
3. Assessment credit consumption remains reserve -> commit -> release aware
  with transaction-coupled final commit in
  `saveAssessmentGenerationWithCreditCommit(...)`.
4. Post-commit reread degradation fallback remains in place, preventing false
  failed responses after committed writes.

CREDIT SUBTRACTION / CONSUMPTION VERIFICATION:
1. Reserve path verified in `reserveAssessmentDailyCreditAttempt(...)`.
2. Durable commit path verified in
  `saveAssessmentGenerationWithCreditCommit(...)`.
3. Failure/retry release path verified in
  `releaseAssessmentDailyCreditReservation(...)` and route cleanup paths.
4. Idempotency guard remains active for assessment create requests with
  `Idempotency-Key` and repository idempotency lock documents.

RUNTIME / DEPLOYMENT NOTES (SEPARATED):
1. Local integrated development
  - Frontend: Next.js protected/admin pages and client query invalidation.
  - Backend: same-origin App Router APIs + server actions + repository layer.
  - Auth/session: Auth.js cookie -> server session rehydration by UID.
  - Same-origin vs provider-managed: app routes same-origin, Supabase
    provider-managed for Auth/Realtime/Storage.
  - This change applies identically in local runtime.

2. Vercel + Supabase (active path)
  - Frontend: Vercel-hosted pages; client subscribes to private channel.
  - Backend: server actions + API handlers on Vercel continue to own mutations.
  - Auth/session: Auth.js trust boundary unchanged.
  - Env-sensitive areas: Auth.js secret, Supabase runtime/service credentials,
    admin allowlist, credit config envs.
  - This change is shared and path-agnostic (server-action logging only).

3. Legacy/reference assumptions
  - Legacy docs/compatibility flows remain non-authoritative references.
  - No legacy path activation or architectural rework was introduced.

RISKS / FOLLOW-UP:
1. Live multi-user browser proof for admin mutation + target-user realtime
  invalidation delivery remains recommended with dedicated QA credentials.
2. Existing best-effort observability operations (audit/realtime) remain
  intentionally non-blocking after durable commits.
3. No additional migration was required because hardening was governance-path
  metadata parity, not schema drift.

VERIFICATION:
Architecture/code-trace verification:
✓ Completed 8-lane parallel exploration and consolidated findings before edits.
✓ Re-traced exact end-to-end credit flow:
  admin UI/server action/API -> repository transaction -> mutation history ->
  realtime invalidation publish -> canonical `/api/assessment/credits` read.
✓ Re-traced owner-scoped upload/result/artifact flow and confirmed session UID
  ownership enforcement plus path assertions.

Diagnostics verification:
✓ `get_errors` on changed file(s) — PASS.

Command verification:
✓ `npm run lint` — PASS
✓ `npm run typecheck` — PASS
✓ `npm run build` — PASS
✓ `npm run build:vercel` — PASS
✓ `npm run supabase:migrations:validate` — PASS (`Validated 18 migration file(s).`)

Runtime proof status:
~ No fresh live authenticated browser concurrency run was executed in this
  environment for admin-target mutation and private-channel delivery.
~ Behavior was verified by code-path tracing and full lint/type/build/migration
  validation passes after the surgical governance patch.

2026-04-18: AUDIT ONLY — ACTIVE NORMAL-USER SESSION RENEWAL + LOGOUT RELEASE TRACE
────────────────────────────────────────────────────────────────────────────

SCOPE:
- `apps/web/lib/server/session.ts`
- `apps/web/app/api/auth/logout/route.ts`
- Traced supporting paths in `apps/web/auth.ts`,
  `apps/web/lib/server/active-normal-user-session-governance.ts`,
  `apps/web/components/auth/sign-out-button.tsx`, and
  `apps/web/components/settings/password-change-panel.tsx`

CURRENT VERIFIED BEHAVIOR:
1. Renewal
   - Normal-user capacity is reserved decisively during Auth.js credentials
     authorize in `apps/web/auth.ts`.
   - Protected-session rehydration in `apps/web/lib/server/session.ts`
     re-loads persisted user truth first, then renews the active normal-user
     lease for non-admin users, and fail-closes when the renewal decision
     cannot be trusted.
2. Logout release
   - Client sign-out flows call `POST /api/auth/logout` before Auth.js
     `signOut()`, so the release route runs while the Auth.js cookie is still
     present when the helper request succeeds.
   - The logout route performs best-effort lease release, then workspace
     cleanup, then session audit logging.

CONCRETE RISK:
1. Lease release is currently coupled to the renewal-gated session helper.
   - `apps/web/app/api/auth/logout/route.ts` reads the user through
     `getAuthenticatedSessionUser()`, which routes through
     `getVerifiedSessionContext()` in `apps/web/lib/server/session.ts`.
   - That helper renews the active normal-user lease and returns `null` on
     persisted-user lookup failure, inactive status, lease-renewal failure, or
     capacity denial.
   - When that happens during logout, the route skips
     `releaseActiveNormalUserSessionLease(...)` entirely, so an existing lease
     row can remain until TTL expiry even though the browser still proceeds to
     clear the Auth.js session cookie.
   - Impact: a temporary false occupancy/capacity hold is possible for the
     affected normal-user slot until the lease expires or another trusted
     server path cleans it up.

FILES CHANGED:
- `zootopia-club-next-ledger.txt`

VERIFICATION:
- Code-path audit only; no runtime code changes were made.
- No lint/type/build run was needed for this audit-only ledger refresh.

2026-04-18: BUILD FIX — LEGACY DAILY-CREDIT OWNER UID NARROWING + DAY-RECORD PREDICATE ALIGNMENT
────────────────────────────────────────────────────────────────────────────

ROOT CAUSE:
1. The legacy daily-credit day-list mapper in
   `apps/web/lib/server/repository.ts` read `documentSnapshot.data().ownerUid`
   through repeated property access where snapshot fields are typed as
   `unknown`, so the Next.js build TypeScript pass failed with:
   `Type 'unknown' is not assignable to type 'string'`.
2. After fixing that guard, TypeScript then surfaced a latent contract mismatch
   in two `AssessmentDailyCreditLedgerDayRecord` filter predicates: the mapper
   parameter type carried a normalized full ledger record while the predicate
   target type uses `Partial<AssessmentDailyCreditLedgerDocument>`.

CHANGES:
1. In `listLegacyAssessmentDailyCreditLedgerRecordsForDay(...)`:
   - Captured `documentSnapshot.data()` once into `documentData`.
   - Added a local owner UID guard that narrows safely:
     `const ownerUid = typeof documentData.ownerUid === "string" ? ... : ""`.
   - Reused `documentData` for the normalize input record cast.
2. In both day-list map paths that feed
   `AssessmentDailyCreditLedgerDayRecord` filters:
   - Kept runtime behavior unchanged.
   - Aligned TypeScript contract by returning `record` as
     `Partial<AssessmentDailyCreditLedgerDocument>`.

FILES CHANGED:
- apps/web/lib/server/repository.ts
- zootopia-club-next-ledger.txt

MIGRATIONS:
- No migration added.
- No migration changed.

REASON:
- Resolve the exact production build blocker reported by the repository
  TypeScript phase while preserving existing credit-ledger runtime behavior.
- Keep the fix surgical and fully backward-compatible.

TECHNICAL DETAILS:
- The normalization path remains unchanged: legacy and structured rows still
  converge through `normalizeAssessmentDailyCreditLedger(...)`.
- No auth/session/credit authority logic changed.
- No API contracts changed.

RISKS / FOLLOW-UP:
- Low risk: this patch is type-safety alignment in mapper glue code only.
- If the day-record alias is later tightened from `Partial` to full ledger
  ownership, all mapper return sites should be updated consistently in one pass.

VERIFICATION:
Diagnostics:
✓ `get_errors` on `apps/web/lib/server/repository.ts` — PASS.

Command verification:
✓ `npm run build` — PASS (root workspace build, including Next.js TypeScript pass in `@zootopia/web`).

2026-04-18: EXEMPT-EMAIL ENV PARSING HARDENING (ACTIVE-CAPACITY + PLATFORM DAILY LOCK INPUT)
────────────────────────────────────────────────────────────────────────────

ROOT CAUSE / GAP:
1. `ZOOTOPIA_ACTIVE_NORMAL_USER_EXEMPT_EMAILS` already supported comma-separated values,
   trimming, lowercasing, malformed-entry rejection, and required fallback inclusion of
   `elmahdyabdulla208@gmail.com`.
2. However, a common real-world env formatting mistake remained: wrapping the full list
   in quotes or using newline/semicolon separators could cause valid entries to be parsed as
   malformed tokens and dropped.
3. Because this exemption list is reused by both:
   - active normal-user capacity exclusion, and
   - global daily platform usage UI-lock exemption,
   parser fragility here could produce surprising non-exempt behavior after env edits.

FILES CHANGED:
- apps/web/lib/server/active-normal-user-session-governance.ts
- zootopia-club-next-ledger.txt

REASON:
- Keep the existing architecture and enforcement model intact while making exemption env
  parsing more operationally robust and backward-compatible.
- Reduce config-footgun risk without redesigning any auth/session/credit authority paths.

TECHNICAL DETAILS:
1. Added `unwrapSingleQuotedEnvValue(...)` helper:
   - removes one accidental outer quote pair (`"..."` or `'...'`) and re-trims value.
2. Updated exempt-email parser tokenization:
   - now splits on `[,\n;]+` so comma, newline, and semicolon-delimited env lists are accepted.
3. Preserved existing security/behavior controls:
   - required fallback exempt email remains enforced
   - values still normalize with trim + lowercase
   - malformed entries still log warning and are ignored
   - deduplication still enforced with `Set`
4. No authority changes:
   - Auth.js trust boundary unchanged
   - backend governance logic unchanged
   - UI lock remains a read-only client reflection of server-owned summary state.

WHAT WAS VERIFIED (CODE-TRACE):
1. Exempt-email env parsing path:
   - `readConfiguredExemptEmails(...)` remains the canonical parser.
2. Active normal-user capacity exclusion path:
   - exclusion list still feeds capacity-count queries and admission/reservation checks.
3. Global platform daily usage lock exemption path:
   - repository daily usage summary still marks admin + exempt-email users as bypassed,
     and UI lock only applies when server summary reports `locked: true` for applicable users.
4. Upload + Assessment UI lock surfaces:
   - Upload entry lock continues to disable dropzone/input for locked non-exempt users.
   - Assessment generate path continues to disable submit and show lock alert when server
     summary reports platform lock.

INTENTIONALLY PRESERVED:
- Existing active normal-user capacity architecture and advisory-lock flow.
- Existing admin permanent exemption behavior.
- Existing required exempt email guarantee (`elmahdyabdulla208@gmail.com`).
- Existing env names and defaults, including
  `ZOOTOPIA_PLATFORM_GLOBAL_DAILY_CREDIT_LIMIT` default behavior.
- Existing UI-only lock policy (no client-owned authority introduced).

RISKS / FOLLOW-UP:
1. Live runtime validation with concrete accounts is still recommended for final production
   confidence:
   - admin account,
   - env-listed exempt account,
   - normal non-exempt account.
2. Build commands in this environment continue to fail when Google Fonts are unreachable;
   this remains an environment/network limitation rather than a code regression.

VERIFICATION:
Code and command verification:
✓ `npm run lint` — PASS (existing pre-existing a11y warning only in assessment form aria-disabled).
✓ `npm run typecheck` — PASS.
~ `npm run build` — FAIL due to external Google Fonts fetch connectivity in Next build runtime.
~ `npm run build:vercel` — FAIL due to external Google Fonts fetch connectivity in Next build runtime.

Runtime proof status:
~ No live authenticated browser multi-account runtime test was executed in this environment.
~ Verification here is code-trace + static checks + command outcomes above.

2026-04-18: FIRST-TIME SIGNUP ONBOARDING + ACTIVE-CAPACITY WAITING UX HARDENING
────────────────────────────────────────────────────────────────────────────

ROOT CAUSE:
1. Active-capacity enforcement stayed correctly server-authoritative, but the
   public login surface could still show the same generic capacity-full copy
   to both existing users and newly-registered users.
2. For first-time users who already completed account creation (and then hit
   active-seat limits), this could feel like registration failed instead of a
   separate admission-wait state.
3. Confirm-email success redirects back to login already carried `confirmed=1`
   and `email`, but did not preserve an explicit sign-up onboarding marker that
   the login UI could use for friendlier post-registration wait guidance.

CHANGES:
1. Login onboarding-aware capacity status logic
   - Updated `apps/web/components/auth/login-panel.tsx` to track a
     first-signup onboarding email context and render dedicated waiting copy
     when that exact email is blocked by active-capacity admission.
   - Added onboarding-specific status builders for:
     - capacity full after successful registration,
     - capacity available after onboarding wait,
     - fallback wait message when a capacity snapshot is temporarily missing.
   - Capacity blocked button label now distinguishes normal wait vs
     first-signup wait context.

2. Confirm-email redirect marker for sign-up flow
   - Updated `apps/web/components/auth/confirm-email-panel.tsx` so successful
     sign-up confirmation redirects include `onboarding=signup` when flow is
     `sign_up`.
   - Login panel consumes this marker to preserve friendly first-time guidance
     without changing backend authority.

3. Localized onboarding-specific messaging
   - Added EN/AR message keys in:
     - `apps/web/messages/en.json`
     - `apps/web/messages/ar.json`
   - New copy explicitly explains:
     - account creation succeeded,
     - active-platform capacity is a separate admitted-usage gate,
     - waiting status is temporary and refreshes automatically,
     - next-step guidance when a slot becomes available.

FILES CHANGED:
- apps/web/components/auth/login-panel.tsx
- apps/web/components/auth/confirm-email-panel.tsx
- apps/web/messages/en.json
- apps/web/messages/ar.json
- zootopia-club-next-ledger.txt

MIGRATIONS:
- No migration added.
- No migration changed.

REASON:
- Preserve a smooth, understandable first-registration experience while keeping
  the active-capacity trust boundary entirely server-owned.
- Distinguish clearly between:
  1) account creation / first-time registration, and
  2) active platform capacity admission for workspace usage.

TECHNICAL DETAILS:
1. Capacity authority is unchanged:
   - server login admission endpoint and Auth.js authorize reserve/renew logic
     still decide capacity.
2. The new onboarding marker is UI-routing context only (`onboarding=signup`)
   and does not grant access or bypass capacity checks.
3. Sign-up remains non-hostile:
   - account creation and email confirmation guidance proceed normally,
   - capacity wait messaging is now explicit and reassuring for first-time
     users instead of generic failure-like wording.

PRESERVED INTENTIONALLY:
- Auth.js server trust boundary.
- Backend-only capacity enforcement and seat reservation semantics.
- Existing sign-up, confirm-email, and login route ownership.
- Existing admission-governance shaping behavior.

RISKS / FOLLOW-UP:
1. Live browser proof is still recommended with dedicated test identities to
   validate the full first-time path:
   - sign-up -> confirm-email -> first login while capacity full,
   - auto-refresh into available-slot state,
   - successful first admitted sign-in.

VERIFICATION:
Diagnostics verification:
✓ `get_errors` on changed files — PASS.

Command verification:
✓ `npm run lint --workspace @zootopia/web` — PASS (existing pre-existing warning in unrelated `assessment-studio.tsx`).
✓ `npm run typecheck --workspace @zootopia/web` — PASS.
✓ `npm run build --workspace @zootopia/web` — PASS.

2026-04-18: AUTH ADMISSION ENV CONTRACT + WINDOW-AWARE RETRY MESSAGING ALIGNMENT
────────────────────────────────────────────────────────────────────────────

ROOT CAUSE:
1. Runtime auth-admission controls were already server-authoritative in
  `apps/web/lib/server/auth-admission-governance.ts`, but
  `.env.example` did not document the canonical
  `ZOOTOPIA_AUTH_ADMISSION_*` and sign-in/sign-up attempt-budget env keys.
2. Public auth pacing copy in routes/localized login feedback still carried
  fixed "about 15 minutes" wording while the admission window is env-driven
  (`ZOOTOPIA_AUTH_ADMISSION_WINDOW_SECONDS`).
3. One signup admission fallback path still hardcoded `Retry-After: 900`
  instead of reusing the configured admission window.

CHANGES:
1. Env contract refresh in `.env.example`:
  - added `ZOOTOPIA_AUTH_ADMISSION_MODE`
  - added `ZOOTOPIA_AUTH_ADMISSION_WINDOW_SECONDS`
  - added `ZOOTOPIA_AUTH_ADMISSION_HASH_SALT`
  - added `ZOOTOPIA_AUTH_SIGNIN_ACCOUNT_MAX_ATTEMPTS`
  - added `ZOOTOPIA_AUTH_SIGNIN_IP_MAX_ATTEMPTS`
  - added `ZOOTOPIA_AUTH_SIGNUP_ACCOUNT_MAX_ATTEMPTS`
  - added `ZOOTOPIA_AUTH_SIGNUP_IP_MAX_ATTEMPTS`
  - documented mode/range behavior and salt-stability guidance.
2. Login admission API message alignment:
  - `apps/web/app/api/auth/login/admission/route.ts` now builds retry copy
    from `getAuthAdmissionConfig().windowSeconds` (minute-rounded), removing
    fixed-duration drift.
3. Signup API message + fallback header alignment:
  - `apps/web/app/api/auth/signup/route.ts` now builds retry copy from the
    same configured admission window.
  - signup reservation-failure fallback now sets `Retry-After` from
    configured `windowSeconds` instead of fixed `900`.
4. Localized login retry body truthfulness refresh:
  - `apps/web/messages/en.json` no longer claims a fixed 15-minute delay and
    now references waiting for the current admission window reset.
  - `apps/web/messages/ar.json` updated with the same window-neutral meaning.

FILES CHANGED:
- .env.example
- apps/web/app/api/auth/login/admission/route.ts
- apps/web/app/api/auth/signup/route.ts
- apps/web/messages/en.json
- apps/web/messages/ar.json
- zootopia-club-next-ledger.txt

MIGRATIONS:
- No migration added.
- No migration changed.

PRESERVED INTENTIONALLY:
- Existing server-owned auth/session authority model.
- Existing signup -> confirm-email -> login onboarding/capacity flow.
- Existing auth admission store semantics (account/IP windows + Retry-After).

VERIFICATION:
Diagnostics:
✓ `get_errors` on changed files — PASS.

Command verification (repo root):
✓ `npm run lint` — PASS.
✓ `npm run typecheck` — PASS.
✓ `npm run build` — PASS.
✓ `npm run build:vercel` — PASS.

Notes:
- Build logs show expected warning when `ZOOTOPIA_AUTH_ADMISSION_HASH_SALT`
  is unset; this is existing guardrail behavior and not a regression.

2026-04-18: SESSION TTL VS ACTIVE-NORMAL-USER LEASE SEPARATION AUDIT (HARDENING)
────────────────────────────────────────────────────────────────────────────

ROOT CAUSE:
1. Runtime ownership was already correctly separated, but a few comments/env
  lines still used generic "TTL" phrasing that could be misread as coupling
  Auth.js session lifetime with active normal-user lease duration.

AUDIT FINDINGS (CODE-TRACE):
1. Global session TTL authority is isolated:
  - `apps/web/lib/server/session-config.ts` is the single parser/owner for
    `ZOOTOPIA_SESSION_TTL_SECONDS` with bounded fallback behavior.
  - `apps/web/auth.ts` applies this value only to Auth.js JWT `session.maxAge`.
2. Active normal-user lease authority is isolated:
  - `apps/web/lib/server/active-normal-user-session-governance.ts` is the
    owner for `ZOOTOPIA_ACTIVE_NORMAL_USER_SESSION_MINUTES` and lease
    reserve/renew/release behavior.
3. Admin/exempt isolation remains enforced:
  - Admin and exempt identities bypass normal-user capacity and trigger
    best-effort stale lease cleanup; they are not blocked by normal-user
    capacity persistence degradation.
4. Renewal/release flow remains server-authoritative and decoupled:
  - Decisive normal-user reserve/renew in Auth.js `authorize`.
  - Ongoing renew in trusted verified session rehydration path.
  - Logout release in `/api/auth/logout` using raw Auth.js session parsing.
5. Expiry semantics stay independent:
  - Auth.js session expiry is controlled by `ZOOTOPIA_SESSION_TTL_SECONDS`.
  - Active seat occupancy is controlled by lease expiry and query filtering on
    `lease_expires_at > NOW()` with expired-lease purging.

CHANGES:
1. Clarified global TTL ownership comment:
  - `apps/web/lib/server/session-config.ts`
2. Clarified lease-vs-session separation comment:
  - `apps/web/lib/server/active-normal-user-session-governance.ts`
3. Clarified Auth.js config boundary comment near `session.maxAge`:
  - `apps/web/auth.ts`
4. Removed ambiguous "TTL expiry" wording in logout lease-release comment:
  - `apps/web/app/api/auth/logout/route.ts`
5. Clarified env contract separation for operators:
  - `.env.example`

FILES CHANGED:
- .env.example
- apps/web/lib/server/session-config.ts
- apps/web/lib/server/active-normal-user-session-governance.ts
- apps/web/auth.ts
- apps/web/app/api/auth/logout/route.ts
- zootopia-club-next-ledger.txt

MIGRATIONS:
- No migration added.
- No migration changed.

PRESERVED INTENTIONALLY:
- Auth.js trust boundary and server-authoritative session verification.
- Active normal-user capacity governance authority and advisory-lock flow.
- Admin/exempt non-consumption of normal-user seats.
- Existing env keys, defaults, and runtime semantics.

VERIFICATION:
Diagnostics:
✓ `get_errors` not required after command-level full validation because lint,
  typecheck, and both builds completed successfully.

Command verification:
✓ `npm run lint --workspace @zootopia/web` — PASS.
✓ `npm run typecheck --workspace @zootopia/web` — PASS.
✓ `npm run build --workspace @zootopia/web` — PASS.
✓ `npm run build:vercel --workspace @zootopia/web` — PASS.

Notes:
- `build:vercel` logs still show existing guardrail warning for unset
  `ZOOTOPIA_AUTH_ADMISSION_HASH_SALT`; not introduced by this audit.
- This pass is comment/env-contract hardening only (no runtime behavior change).

2026-04-18: .ENV.EXAMPLE SOURCE-TRUTH REFACTOR (MIXED/PUBLIC/SERVER CONTRACT)
────────────────────────────────────────────────────────────────────────────

ROOT CAUSE:
1. `.env.example` had accumulated high-value env notes over time but remained
  difficult to operate as a strict contract because ownership boundaries were
  not explicitly separated into mixed/public/server domains.
2. Several active runtime env keys used in server helpers were either missing
  or under-documented in the contract (notably production durability fallback,
  password-security hash salt, and client diagnostics toggle).
3. Existing comments were accurate in many places, but accepted-value parsers,
  clamp ranges, and alias/fallback chains were not consistently visible in one
  clear operational layout.

CHANGES:
1. Rewrote `.env.example` into explicit ownership sections:
  - `[MIXED]` runtime-crossing keys
  - `[PUBLIC]` browser-safe UI shaping keys
  - `[SERVER]` server-authoritative secrets/governance keys
2. Added/clarified source-truth parser contracts directly in comments:
  - accepted boolean tokens per parser (`1|true|yes|on`, etc. where applicable)
  - min/max clamp windows for admission, verification resend, sessions, limits

2026-04-18: LECTURE-SUMMARY PREMIUM POLISH (IMAGE-FIRST HERO + INSTRUMENTATION SAMPLE)
────────────────────────────────────────────────────────────────────────────

ROOT CAUSE:
1. The protected `/lecture-summary` page already had rich Arabic content, but
  visual scanning still leaned text-first before the main preview image.
2. The page only showcased one sample image (`coming-soon.png`), while
  `apps/web/public/coming-soon-2.png` existed without intentional UI usage.
3. The home teaser needed a small content-level alignment pass so its voice and
  value proposition stayed consistent with the refined lecture-summary surface.

FILES CHANGED:
- apps/web/app/(protected)/(completed)/lecture-summary/page.tsx
- apps/web/components/home/lecture-summary-coming-soon-teaser.tsx
- zootopia-club-next-ledger.txt

MIGRATIONS:
- No migration added.
- No migration changed.

REASON:
- Deliver the requested premium UI refinement only for the protected
  lecture-summary page and its linked home teaser.
- Keep routing/auth/session/permission architecture unchanged.
- Ensure all visible download actions use real image assets (no fake text-file
  fallback behavior).

TECHNICAL DETAILS:
1. Stronger opening headline + expressive accent
  - Added top Arabic opening headline:
    `انتهى عصر التلخيص اليدوي` with a tasteful soft emoji accent (`🥹`).
  - Kept rich explanatory Arabic copy and improved hierarchy spacing for faster
    scanability.

2. Image-first hero composition for `coming-soon.png`
  - Kept `coming-soon.png` as the dominant opening visual element.
  - Promoted the image card earlier in the hero flow and strengthened visual
    treatment (larger frame, stronger depth, cleaner caption chip).
  - Preserved responsive image sizing and non-distorting rendering with the
    existing `next/image` approach and tuned `sizes` hints.

3. Added dedicated instrumentation-summary section using `coming-soon-2.png`
  - Introduced a new polished section labeled as an instrumentation summary
    showcase and added clear Arabic copy including:
    `هذه صورة ملخص لمادة الإنسترو`.
  - Added real preview + direct-download CTAs for `coming-soon-2.png`.
  - Download now uses:
    `download={LECTURE_SUMMARY_INSTRUMENTATION_IMAGE_DOWNLOAD_NAME}`
    with `href="/coming-soon-2.png"`.

4. Download behavior hardening
  - Kept/expanded real-image download CTAs for `coming-soon.png`.
  - Added direct real-image download CTA for `coming-soon-2.png`.
  - Confirmed no `.txt` placeholder download behavior in the lecture-summary
    page path.

5. Home teaser alignment
  - Preserved existing teaser architecture (localStorage dismissal +
    `APP_ROUTES.lectureSummary` linking).
  - Added a matching opening line with tasteful emoji accent and refreshed copy
    to reflect the two real sample images now available on the target page.

PRESERVED INTENTIONALLY:
- `requireCompletedUser(APP_ROUTES.lectureSummary)` protection on the page.
- Backend/auth/session/permissions ownership and trust boundaries.
- Existing route constants and home teaser routing contract.
- Existing premium design language, dark/light support, RTL direction, and
  responsive class strategy.

VERIFICATION:
Diagnostics:
✓ `get_errors` on changed UI files — PASS.

Command verification:
✓ `npm run lint` — PASS.
✓ `npm run typecheck` — PASS.
✓ `npm run build` — PASS.
✓ `npm run build:vercel` — PASS.

Runtime route verification:
✓ `GET /api/health` on local runtime — `200`.
✓ `GET /lecture-summary` (unauthenticated, manual redirect) — `307` with
  `Location: /login` (protected-route contract preserved).

Requirement-specific verification:
✓ Opening Arabic headline and tasteful emoji are present.
✓ `coming-soon.png` remains rendered as the dominant hero visual.
✓ `coming-soon-2.png` is now integrated in a dedicated polished section.
✓ Direct download button for the instrumentation-summary image points to the
  real image asset.
✓ Lecture-summary image download CTAs are image-only (no fake `.txt` path).
✓ Home teaser still links correctly via `APP_ROUTES.lectureSummary`.

Notes:
- Build logs still show the existing warning for unset
  `ZOOTOPIA_AUTH_ADMISSION_HASH_SALT`; this pre-existed and is unrelated to
  this UI pass.
- Full authenticated visual QA across mobile/desktop viewports was validated at
  code-level responsive class ownership in this pass; live authenticated
  browser-playback screenshots were not generated in this environment.
  - stable-hash-salt guidance for deterministic governance lookups
  - route sanitization behavior for auth redirect env keys
  - base URL normalization and fallback chain behavior
3. Added active env keys that were missing from the contract:
  - `ZOOTOPIA_ALLOW_PRODUCTION_MEMORY_FALLBACK`
  - `NEXT_PUBLIC_ZOOTOPIA_ASSESSMENT_CREDIT_DIAGNOSTICS`
  - `ZOOTOPIA_PASSWORD_SECURITY_HASH_SALT`
  - `NEXTAUTH_SECRET` as explicit compatibility alias
4. Preserved compatibility notes and fallback aliases without changing runtime:
  - Supabase publishable key alias
  - verification resend legacy aliases
  - DashScope base URL legacy aliases
  - Postgres URL fallback alias (`DATABASE_URL`)
5. Replaced hardcoded personal admin-email sample values with neutral example
  placeholder to keep template reusable and safe for distribution.

FILES CHANGED:
- .env.example
- zootopia-club-next-ledger.txt

MIGRATIONS:
- No migration added.
- No migration changed.

PRESERVED INTENTIONALLY:
1. No runtime code paths were changed.
2. No env-key names were renamed or removed from runtime contracts.
3. Existing fallback/alias behavior remained documented as compatibility-only,
  with canonical keys clearly marked.
4. Session TTL vs active-capacity lease separation remained explicit and
  unchanged in behavior.

VERIFICATION:
Code-trace/source-truth verification:
✓ Re-traced env readers across auth/session/admission/capacity/Supabase/
  storage retention/assessment locks/provider runtime/admin helpers/maintenance
  route/contact relay before applying the rewrite.

Diagnostics:
✓ `get_errors` on `.env.example` — PASS.

Command verification:
~ No lint/typecheck/build rerun for this pass because only documentation
  contract files changed (`.env.example` + ledger), with no TypeScript/runtime
  implementation edits.

2026-04-18: FULL QUEUE/ADMISSION/CAPACITY SYSTEM AUDIT + ADMIN EMAIL VALIDATION HARDENING
────────────────────────────────────────────────────────────────────────────

ROOT CAUSE:
A comprehensive architecture-level audit of the full queue/admission/capacity
system was requested to verify correctness end-to-end: env parsing, exempt/admin
bypass ordering, signup/confirm-email compatibility, Auth.js consistency, DB
governance, error taxonomy, and user-facing messaging.

One real hardening gap was found:
  ZOOTOPIA_ADMIN_EMAILS parsing in admin-auth.ts was missing format validation.
  The exempt-email parser in active-normal-user-session-governance.ts already
  validated each entry with regex /^[^\s@]+@[^\s@]+\.[^\s@]+$/ and logged a
  warning for malformed entries, but the admin email parser only trimmed and
  lowercased without any format check. A typo like admin@@example.com would
  silently become a valid admin entry and also feed the exempt-capacity
  exclusion list, potentially creating a phantom admin identity that could
  never actually authenticate.

FILES CHANGED:
- apps/web/lib/server/admin-auth.ts
- zootopia-club-next-ledger.txt

MIGRATIONS:
- No migration added.
- No migration changed.

REASON:
Harden admin email parsing to match the exempt-email parser so both use the
same validation contract: split, trim, lowercase, validate, warn, drop invalid.
Close the gap where a typo in ZOOTOPIA_ADMIN_EMAILS could silently persist as a
malformed admin allowlist entry with no runtime warning.

TECHNICAL DETAILS:
1. Admin email validation added
   - Added ADMIN_EMAIL_FORMAT_REGEX = /^[^\s@]+@[^\s@]+\.[^\s@]+$/ constant
     at module level in admin-auth.ts.
   - readConfiguredAdminEmailsFromEnv() now calls .flatMap() after the existing
     .map(normalizeIdentifier).filter(Boolean) chain.
   - Each entry that fails the regex emits a console.warn and is dropped from
     the returned allowlist (identical pattern to exempt-email handling).
   - Quote-handling, delimiter tolerance, and lowercase normalization unchanged.
   - Deduplication by getAllowlistedAdminEmails() (new Set()) is unchanged.
   - Updated comment to reflect validation alignment is now complete.

AUDIT FINDINGS (all system areas reviewed, no other bugs found):

A. Queue/capacity system
   - Server-authoritative via advisory-locked DB transactions. Correct.
   - Only non-exempt users count: readActiveNormalUserCount() uses
     email != ALL(excludedEmails) in the SQL query.
   - Expired rows excluded via WHERE lease_expires_at > NOW() plus proactive
     purge at start of each capacity transaction.
   - Active count is truthful. Count is sanitized to non-negative.

B. Environment variables
   - All 13 queue/admission/capacity env vars correctly documented in .env.example.
   - ZOOTOPIA_ACTIVE_NORMAL_USER_LIMIT, _SESSION_MINUTES, _EXEMPT_EMAILS: robustly
     parsed with clamping, quote-unwrapping, delimiter tolerance, and validation.
   - ZOOTOPIA_ADMIN_EMAILS: now fully aligned with exempt parser (see fix above).
   - AUTH_ADMISSION_CONFIG is cached at module load in admission/route.ts and
     signup/route.ts. Env changes require restart/redeploy. Documented behavior.

C. Exempt/admin bypass ordering
   - Admission route POST: capacity decision evaluated first, exempt early return
     happens BEFORE reserveAuthAdmissionAttempt() is called. Correct.
   - Auth.js authorizeUserCredentials: calls reserveOrRenewActiveNormalUserSession
     Lease() where exempt check is the first operation. Correct.
   - Session rehydration: admin role calls releaseActiveNormalUserSessionLease()
     best-effort; exempt email passes through the lease function which returns
     immediately with allowed=true.
   - No location found where an exempt user can be blocked before exemption is applied.

D. Signup + confirm-email + onboarding
   - POST /api/auth/signup performs admission rate-limiting but NO capacity check.
     Account creation is not the same as active session admission. Correct.
   - Confirm-email success redirects to /login?confirmed=1&email=...&onboarding=signup.
   - Login panel reads ?onboarding=signup and renders onboarding-aware capacity
     messaging if the platform is full when the new user tries to log in.
   - Users are never confused about whether signup failed vs. capacity being full.

E. Auth.js / session / logout consistency
   - authorizeUserCredentials and getVerifiedSessionContext both call
     reserveOrRenewActiveNormalUserSessionLease with the same logic, fail-closed
     for non-exempt normal users.
   - authorizeAdminCredentials calls releaseActiveNormalUserSessionLease to clean
     up stale user-mode leases. Admin sessions are never capacity-gated.
   - Logout reads raw auth() cookie and calls releaseActiveNormalUserSessionLease
     best-effort so lease release cannot block the logout response.
   - No logical contradictions between authorize and session rehydration.

F. Error taxonomy + UI messaging
   - 14 distinct AUTH_* error codes. None collapse incorrectly.
   - AUTH_ACTIVE_USER_CAPACITY_FULL and AUTH_ACTIVE_USER_ADMISSION_UNAVAILABLE
     are distinct raw codes, distinct normalized codes, and distinct UI states.
   - AUTH_RATE_LIMITED is distinct from both: different source, different message,
     no count interpolation.
   - Auto-refresh polling every 5 seconds on capacity-blocked states.
   - EN/AR message sets complete and coherent for all states including onboarding.
   - Admin login does not handle capacity codes. Appropriate.

G. Database / migration
   - public.active_normal_user_sessions table: correct structure, proper PK,
     constraints. RLS: deny-all for both anon and authenticated roles.
   - No accidental browser access possible.
   - Indexes on lease_expires_at and email. Adequate for max-100-user scale.
   - Advisory lock key 381104202 serializes all capacity decisions across instances.
   - No RLS drift or policy gaps found.

H. Logic review summary
   - No false queue messages for non-capacity states.
   - No stale counters. No misordered checks.
   - Design note: lease TTL (default 15min) is intentionally decoupled from
     Auth.js session TTL (default 60min). This is documented behavior, not a bug.

PRESERVED INTENTIONALLY:
- Auth.js as the server trust boundary.
- Server-authoritative queue/admission logic.
- Admin authority and exempt-account behavior.
- Existing error normalization architecture.
- All working features across signup, confirm-email, login, session, logout.

VERIFICATION:
Code-trace verification:
✓ 10 parallel exploration agents traced all system areas before any changes.
✓ All agents consolidated before implementing the fix.
✓ Only one real hardening gap identified (admin email validation).
✓ All other system areas verified correct by code tracing.

Command verification:
✓ npm run typecheck:web — PASS
✓ npm run build:web — PASS (Compiled successfully in 10.2s)
✓ npm run lint:web — PASS (no errors; 2 pre-existing warnings unchanged)

Runtime proof:
~ No live concurrent-session QA run was executed in this environment.
~ Verification is code-traced plus lint/type/build command passes.
~ Outstanding live QA gap: dedicated credential-backed admin login, exempt user
  session rehydration cycle, and controlled capacity-full occupancy fixture.

2026-04-18: ENV-DRIVEN PASSWORD MIN-LENGTH (DEFAULT 10) + SHARED IN-FIELD PASSWORD TOGGLE STANDARDIZATION
────────────────────────────────────────────────────────────────────────────

ROOT CAUSE:
Regular-user password policy was still carrying a hardcoded min-length contract
in multiple app surfaces (frontend minLength attrs, localized hint/error copy,
and backend callsites that imported shared validation directly). The previous
baseline remained 12 in those paths, which prevented true environment-driven
control and made policy messaging drift-prone.

Password visibility toggles were also implemented with repeated local state and
different UI placements across panels (some in header rows, some missing), so
password-entry UX behavior was not standardized.

FILES CHANGED:
- .env.example
- packages/shared-utils/src/validation.ts
- apps/web/lib/password-policy.ts
- apps/web/app/api/auth/signup/route.ts
- apps/web/app/api/auth/password/change/route.ts
- apps/web/components/ui/password-visibility-input.tsx (new)
- apps/web/components/auth/login-panel.tsx
- apps/web/components/auth/reset-password-panel.tsx
- apps/web/components/settings/password-change-panel.tsx
- apps/web/components/auth/admin-login-panel.tsx
- apps/web/components/assessment/global-credits-unlock-gate.tsx
- apps/web/components/assessment/assessment-studio.tsx
- zootopia-club-next-ledger.txt

MIGRATIONS:
- No migration added.
- No migration changed.

REASON:
Establish one canonical runtime-tunable policy source for regular-user password
minimum length with default 10, while preserving server authority and existing
auth/session/admission/security architecture. Remove scattered hardcoded UI
assumptions and keep all user-facing copy synchronized with the active policy.

TECHNICAL DETAILS:
1. Canonical env contract introduced (default 10)
   - Added NEXT_PUBLIC_ZOOTOPIA_PASSWORD_MIN_LENGTH=10 to .env.example.
   - Documented as the canonical regular-user password min length for signup,
     reset-password, and in-account password-change flows.
   - Contract normalization/clamp behavior documented and implemented as 8..128.

2. Shared validator made min-length configurable (with safe defaults)
   - packages/shared-utils validateUserPasswordPolicy() now accepts optional
     minLength and normalizes it through a bounded helper.
   - Default shared fallback changed from 12 to 10 to align with requested
     platform default when no explicit override is passed.
   - PASSWORD_TOO_SHORT error text now reflects the resolved minLength value.

3. App policy wrapper became the single app-side authority
   - apps/web/lib/password-policy.ts now:
     - exposes PASSWORD_POLICY_MIN_LENGTH_ENV_KEY
       (NEXT_PUBLIC_ZOOTOPIA_PASSWORD_MIN_LENGTH)
     - resolves and clamps min length at runtime via getPasswordPolicyMinLength()
     - wraps shared validation through validateUserPasswordPolicy() so all app
       callsites can inherit env-driven behavior
     - renders localized hint/error messages dynamically from resolved minLength
       (EN + AR) instead of fixed "12" text.

4. Backend callsites rewired to env-aware wrapper
   - signup route and password change route switched imports from direct
     @zootopia/shared-utils validator to @/lib/password-policy wrapper.
   - This removes backend drift risk where route validation could bypass app env
     policy resolution.

5. Frontend minLength hardcoding removed from password policy surfaces
   - login sign-up password + confirm fields now use getPasswordPolicyMinLength().
   - reset-password new + confirm fields now use getPasswordPolicyMinLength().
   - settings password-change new + confirm fields now use
     getPasswordPolicyMinLength().
   - Browser-native minLength attrs now match server validation policy.

6. Password-eye toggle standardized with one reusable in-input component
   - Added shared PasswordVisibilityInput component that:
     - keeps toggle icon inside the input field
     - owns visibility state per field
     - supports localized show/hide labels
     - preserves disabled semantics/accessibility labels
   - Reused in:
     - login panel password/confirm inputs
     - reset-password panel new/confirm inputs
     - settings password-change current/new/confirm inputs
     - admin login password input
     - global credits unlock gate password input
     - assessment studio prompt unlock password input

7. Behavioral boundaries preserved
   - No auth trust-boundary move: server actions/routes remain authoritative.
   - No session/admission/capacity/security architecture changes.
   - No route ownership or permission model changes.
   - No database schema or migration changes.

SCATTERED HARDCODED ASSUMPTIONS REMOVED:
- Fixed "12 characters" hints/errors in password-policy copy removed and replaced
  with resolved minLength interpolation.
- Fixed minLength={12} attrs in key regular-user password inputs removed.
- Direct backend imports that skipped app-level env policy resolution removed.
- Repeated per-panel eye-toggle state and ad-hoc placement replaced by shared
  in-input component behavior.

VERIFICATION:
Code-trace verification:
✓ Re-traced all touched password validation callsites and confirmed they now
  resolve through apps/web/lib/password-policy.ts in app surfaces.
✓ Confirmed env key contract exists in .env.example and policy copy now renders
  dynamic min-length text.
✓ Confirmed shared password visibility component is reused across the targeted
  auth/settings/assessment unlock password fields.

Diagnostics:
✓ get_errors on all changed files — PASS (no file diagnostics).

Command verification:
✓ npm run lint --workspace @zootopia/web — PASS
✓ npm run typecheck — PASS
✓ npm run build — PASS
✓ npm run build:vercel — PASS
~ Existing non-blocking operational warning remains unchanged during build:
  missing auth-admission hash salt warning (pre-existing, outside this scope).

Runtime proof:
~ No interactive browser QA pass executed in this environment for this patch.
~ Verification is code-traced plus lint/typecheck/build/build:vercel passes.

2026-04-18: PROTECTED ARABIC-FIRST LECTURE-SUMMARY COMING-SOON TEASER + DEDICATED PAGE + REAL SAMPLE DOWNLOAD
────────────────────────────────────────────────────────────────────────────

ROOT REQUEST:

2026-04-19: PROTECTED HOME REFINEMENT — COMING-SOON SIDEBAR LINK + ARABIC VISION CARD STACK
────────────────────────────────────────────────────────────────────────────

ROOT CAUSE:
1. The protected `/lecture-summary` page was reachable from Home teaser context,
   but users who dismissed or missed the teaser did not have a persistent sidebar
   navigation path to revisit it later.
2. The protected Home surface did not yet include the requested elegant,
   vertically stacked, Arabic-first platform-story/community/future-vision cards.

FILES CHANGED:
- apps/web/components/layout/shell-nav.tsx
- apps/web/app/(protected)/(completed)/page.tsx
- zootopia-club-next-ledger.txt

MIGRATIONS:
- No migration added.
- No migration changed.

REASON:
- Apply a minimal, backward-compatible protected UI/content refinement that:
  1) adds a safe persistent sidebar route to `/lecture-summary`, and
  2) introduces the requested premium Arabic card stack on Home,
  without changing backend/auth/session/permission/business logic.

TECHNICAL DETAILS:
1. Protected sidebar link addition
   - File: `apps/web/components/layout/shell-nav.tsx`
   - Added icon mapping for `APP_ROUTES.lectureSummary`.
   - Added protected nav item:
     `{ href: APP_ROUTES.lectureSummary, label: "Coming Soon" }`
   - Inserted beside existing informational links to avoid nav clutter and keep
     a sensible information-architecture position.

2. Protected Home Arabic premium card stack
   - File: `apps/web/app/(protected)/(completed)/page.tsx`
   - Added `homeVisionCards` data model with four Arabic-only cards:
     1) رحلة بناء المنصة
     2) عايز اسمك وصورتك ورأيك هنا؟
     3) ادعم تحويل المنصة إلى تطبيق
     4) عندك فكرة تتمناها في المنصة؟
   - Added a dedicated Home section (`dir="rtl"`) with a vertical stack layout
     (`space-y-*`) so cards appear one under another on all breakpoints.
   - Used expressive Lucide icons + tasteful emoji accents:
     - Rocket + 🛠️
     - MessageCircleMore + 🖼️
     - HandCoins + 📱
     - Lightbulb + 💡
   - Preserved existing premium dark/light visual language with translucent
     surfaces, subtle glow layers, balanced typography, and wrap-safe Arabic text.

PRESERVED INTENTIONALLY:
- No backend/API/auth/session/permissions/database/business-logic edits.
- No routing/behavior changes beyond adding a sidebar link to an existing
  protected route.
- No redesign of existing Home card architecture outside the new requested section.

VERIFICATION:
Diagnostics:
✓ `get_errors` on changed files — PASS (no errors).

Command verification:
✓ `npm run lint --workspace @zootopia/web` — PASS.
✓ `npm run typecheck --workspace @zootopia/web` — PASS.
✓ `npm run build --workspace @zootopia/web` — PASS.
✓ `npm run build:vercel --workspace @zootopia/web` — PASS.

Code-trace requirement verification:
✓ Sidebar now includes exact label `Coming Soon` targeting `APP_ROUTES.lectureSummary` (`/lecture-summary`).
✓ New Home cards are Arabic-only visible content and vertically stacked.
✓ Icon + emoji treatments are present per card.
✓ Responsive layout guards remain in place (`min-w-0`, wrap-safe text,
  adaptive spacing/typography classes) for mobile and desktop behavior.

Runtime note:
~ No authenticated visual browser session was available in this environment, so
  sidebar click-through and final visual polish were verified by code trace plus
  successful lint/type/build/build:vercel.
- Add a minimal-risk protected teaser for a coming intelligent lecture-summary
  tool, targeted as a first-visit/new-user discovery surface.
- Add a dedicated protected coming-soon page with Arabic-first visible copy,
  explicit premium donation rationale, official contact handoff, and a working
  downloadable sample summary file.
- Preserve backend/session/auth/admission authority boundaries and avoid
  architecture redesign.

FILES CHANGED:
- packages/shared-config/src/routes.ts
- apps/web/app/(protected)/(completed)/page.tsx
- apps/web/components/home/lecture-summary-coming-soon-teaser.tsx (new)
- apps/web/app/(protected)/(completed)/lecture-summary/page.tsx (new)
- apps/web/public/samples/lecture-summary-sample-ar.txt (new)
- zootopia-club-next-ledger.txt

MIGRATIONS:
- No migration added.
- No migration changed.

TECHNICAL DETAILS:
1. New protected route constant and page ownership
   - Added `APP_ROUTES.lectureSummary = "/lecture-summary"`.
   - Implemented `apps/web/app/(protected)/(completed)/lecture-summary/page.tsx`.
   - Page remains fully inside protected/completed route ownership and calls
     `requireCompletedUser(APP_ROUTES.lectureSummary)` to preserve existing
     server-side access gate semantics.

2. New-user teaser behavior on protected Home
   - Added `LectureSummaryComingSoonTeaser` to
     `apps/web/app/(protected)/(completed)/page.tsx` directly under existing
     platform-story CTA section, keeping navigation/sidebar unchanged.
   - Teaser is intentionally client-local and non-authoritative:
     dismissal is persisted via localStorage key
     `zootopia:lecture-summary:teaser:dismissed:v1`.
   - This keeps UX contextual and low-noise without introducing new backend
     state, auth/session mutation, or cross-device authority assumptions.

3. Dedicated Arabic-first coming-soon experience
   - New page copy is intentionally Arabic-first for both teaser and route
     surface, centered on the upcoming intelligent lecture-summary capability.
   - Added clear product-intent blocks (what the tool will do, expected output,
     and readiness framing) without exposing fake runtime behavior.

4. Donation/contact/download CTA integration (official paths only)
   - Donation CTA links to `APP_ROUTES.donation`.
   - Support/help CTA links to `APP_ROUTES.contact` (official existing path).
   - Download CTA links to a real static file in public:
     `/samples/lecture-summary-sample-ar.txt`.

5. Real sample artifact added
   - Created `apps/web/public/samples/lecture-summary-sample-ar.txt` with an
     actual structured Arabic summary sample and review questions.
   - Route-level download uses a direct anchor + `download` attribute to keep
     delivery deterministic and avoid introducing temporary API handlers.

ARCHITECTURE / SAFETY GUARANTEES:
- No backend route logic changed.
- No auth/session/admission/capacity code changed.
- No role/permission authority changes.
- No DB schema/RLS/migration changes.
- No protected-shell/nav architecture replacement.

RISKS / FOLLOW-UP:
1. Teaser dismissal is browser-local by design.
   - This is intentional for low-risk contextual UX and avoids new server state.
   - If future product policy requires cross-device persistence, add a dedicated
     server-owned preference field with explicit migration and authority review.

VERIFICATION:
Diagnostics verification:
✓ `get_errors` on all touched code files — PASS.

Command verification:
✓ `npm run lint` — PASS.
✓ `npm run typecheck` — PASS.
✓ `npm run build` — PASS.
✓ `npm run build:vercel` — PASS.

Runtime notes:
~ Build logs still include the pre-existing operational warning:
  `ZOOTOPIA_AUTH_ADMISSION_HASH_SALT is unset`.
~ Warning is unchanged by this feature and outside this UI-only scope.

2026-04-18: LECTURE-SUMMARY UI POLISH PASS (IMAGE-FIRST HERO + TEASER GIFT ICON + REAL IMAGE DOWNLOAD)
────────────────────────────────────────────────────────────────────────────

ROOT CAUSE / DESIGN ISSUE:
- The protected `lecture-summary` page was functionally correct but visually
  text-dominant: the preview image existed yet did not lead the page hierarchy.
- Text richness was present, but grouping/scanning rhythm was flatter than the
  surrounding premium surfaces.
- The download CTA still pointed to a text sample file, creating mismatch with
  user expectation for a visual coming-soon preview.
- The linked home teaser needed a stronger premium accent connection to the
  page, specifically a visible gift marker.

FILES CHANGED:
- apps/web/app/(protected)/(completed)/lecture-summary/page.tsx
- apps/web/components/home/lecture-summary-coming-soon-teaser.tsx
- zootopia-club-next-ledger.txt

MIGRATIONS:
- No migration added.
- No migration changed.

TECHNICAL DETAILS:
1. Image-first hero conversion on protected page
   - Elevated `/coming-soon.png` to a dominant hero block with larger frame,
     richer shadow depth, and top-level placement before detail cards.
   - Updated image dimensions to real asset ratio (`1408x768`) and responsive
     `sizes` tuned for mobile/tablet/desktop.
   - Added direct preview-open affordance on the hero image itself.

2. Text presentation improvement without reducing content richness
   - Preserved core Arabic-first meaning and expanded structure into cleaner
     premium cards:
     - feature-details card with icon-led points,
     - dedicated preview/download explainer card,
     - support-impact card with checklisted operational value points.
   - Improved hierarchy with stronger section headings, badges, spacing, and
     scan-friendly card rhythm.

3. Download behavior fix (real image, no txt placeholder)
   - Replaced txt-based download target with the real preview image source:
     `LECTURE_SUMMARY_PREVIEW_IMAGE_SRC = "/coming-soon.png"`.
   - Added explicit open-preview action and image-download action using:
     `download="zootopia-lecture-summary-preview.png"`.
   - Removed old lecture-summary txt download usage from the page.

4. Home teaser premium refinement + gift icon
   - Added visible gift accent treatment in
     `lecture-summary-coming-soon-teaser.tsx` using `Gift` icon in:
     - badge lane (`هدية تجربة قريباً`),
     - primary CTA icon chip.
   - Kept teaser behavior/routing unchanged (same localStorage dismissal,
     same protected-page link).

5. Architecture/safety boundaries preserved
   - No backend/auth/session/permissions changes.
   - No route ownership changes; protected gate remains via
     `requireCompletedUser(APP_ROUTES.lectureSummary)`.
   - No unrelated page redesign.

VERIFICATION:
Diagnostics verification:
✓ `get_errors` on changed files — PASS.

Behavior/code-trace verification:
✓ Protected lecture-summary route still built and listed in production build.
✓ Hero image now primary visual lane (larger, top-level, dedicated preview frame).
✓ Content richness preserved and reorganized into premium cards (no emptying pass).
✓ Home teaser contains a clearly visible premium gift icon treatment.
✓ Download/open actions now target `/coming-soon.png` (no txt placeholder path).
✓ Responsive classes verified for mobile/desktop composition (`sm:`/`lg:` grids,
  tuned `sizes`, adaptive spacing).

Command verification:
✓ `npm run lint` — PASS.
✓ `npm run typecheck` — PASS.
✓ `npm run build` — PASS.
✓ `npm run build:vercel` — PASS.

Runtime notes:
~ Existing warning remains unchanged during build:
  `ZOOTOPIA_AUTH_ADMISSION_HASH_SALT is unset`.
~ Warning is pre-existing and outside this UI refinement scope.

2026-04-18: HOME TEASER DARK-MODE CTA CONTRAST FIX + SHARED PLATFORM SLOGAN ON HOME AND LECTURE-SUMMARY
────────────────────────────────────────────────────────────────────────────

ROOT CAUSE / DESIGN ISSUE:
- The Home lecture-summary teaser CTA is a one-off raw `Link` in
  `lecture-summary-coming-soon-teaser.tsx`, not the shared `Button` primitive.
- Because that CTA used fixed local classes and no explicit dark-mode text rule,
  its dark-theme readability depended on the static one-off class stack instead
  of an intentional theme override.
- The requested platform slogan did not exist on either the protected Home hero
  or the protected `/lecture-summary` hero, so the platform lacked a shared
  compact branding line across those two related discovery surfaces.

FILES CHANGED:
- apps/web/components/branding/platform-slogan-note.tsx (new)
- apps/web/components/home/lecture-summary-coming-soon-teaser.tsx
- apps/web/app/(protected)/(completed)/page.tsx
- apps/web/app/(protected)/(completed)/lecture-summary/page.tsx
- zootopia-club-next-ledger.txt

MIGRATIONS:
- No migration added.
- No migration changed.

TECHNICAL DETAILS:
1. Dark-mode-only Home teaser CTA text fix
   - Added an explicit `dark:text-slate-950` override to the primary teaser CTA
     link in `apps/web/components/home/lecture-summary-coming-soon-teaser.tsx`.
   - This keeps light mode unchanged while hardening text contrast when the
     protected shell is in dark mode.
   - No routing, dismissal behavior, badge layout, or CTA structure changed.

2. Shared slogan component introduced
   - Added `PlatformSloganNote` in
     `apps/web/components/branding/platform-slogan-note.tsx`.
   - The component renders the exact Arabic line:
     `شعارنا: زوتوبيا كلوب - السبق لنا، والتميّز عنواننا`
   - The component uses one compact, wrap-safe layout with:
     - a small premium `Crown` icon chip,
     - responsive small typography,
     - `overflow-wrap:anywhere` protection for narrow screens,
     - a tiny tone switch (`adaptive` vs `hero-dark`) so Home and
       `/lecture-summary` stay visually aligned without forcing one rigid style
       across different hero backgrounds.

3. Home slogan placement
   - Mounted `PlatformSloganNote` inside the protected Home title stack in
     `apps/web/app/(protected)/(completed)/page.tsx`, directly below the main
     hero title.
   - Placement was intentionally kept inside the existing title lane so the hero
     gains brand reinforcement without adding another full-width card or
     disturbing current page hierarchy.

4. `/lecture-summary` slogan placement
   - Mounted `PlatformSloganNote tone="hero-dark"` inside the lecture-summary
     hero intro block in
     `apps/web/app/(protected)/(completed)/lecture-summary/page.tsx`.
   - Placement sits with the existing hero badges/title/lead copy so the slogan
     visually matches Home while still feeling native to the permanently dark
     lecture-summary hero surface.

5. Architecture / safety preserved
   - No backend changes.
   - No auth/session/permission changes.
   - No route ownership changes.
   - No navigation or redirect behavior changes.
   - No dependency changes.

VERIFICATION:
Code-trace verification:
✓ Confirmed Home still mounts the teaser in the protected hero sequence and now
  mounts the shared slogan directly under the title stack.
✓ Confirmed `/lecture-summary` still calls
  `requireCompletedUser(APP_ROUTES.lectureSummary)` and now mounts the same
  slogan component inside the hero intro.
✓ Confirmed the CTA contrast fix is isolated to the Home teaser link class list.
✓ Confirmed the slogan component is wrap-safe and responsive by class design
  (`max-w-full`, compact text sizing, `overflow-wrap:anywhere`, adaptive hero
  tone classes).

Command verification:
✓ `npm run lint` — PASS
✓ `npm run typecheck` — PASS
✓ `npm run build` — PASS
✓ `npm run build:vercel` — PASS

Runtime verification:
✓ Browser automation against the production server confirmed the app boots and
  protected routing remains intact:
  - `http://127.0.0.1:3000/` redirected to `/login`
  - `http://127.0.0.1:3000/lecture-summary` redirected to `/login`
~ No authenticated browser session was available in this environment, so the
  protected Home and `/lecture-summary` UI surfaces could not be visually
  inspected live after login.
~ Therefore, the dark-mode teaser CTA readability, Home slogan rendering,
  lecture-summary slogan rendering, and mobile/desktop visual fit are verified
  by code trace plus successful lint/type/build/build:vercel, not by an
  authenticated browser screenshot session.

Runtime notes:
~ Build/start logs continue to show the pre-existing operational warning:
  `ZOOTOPIA_AUTH_ADMISSION_HASH_SALT is unset`.
~ Warning is unchanged by this UI-only refinement and remains outside this task.

2026-04-19: AUTH ONBOARDING + ACCOUNT LIFECYCLE HARDENING FOR PROFILE-GATED ADMISSION
────────────────────────────────────────────────────────────────────────────

ROOT CAUSES:
- The decisive active-user capacity lease was being reserved and renewed before
  the backend-confirmed profile-completion gate, so freshly registered users
  could be counted by queue/admission logic too early.
- Public login admission preflight could surface active-capacity blocking before
  the decisive Auth.js/session authority ran, which risked confusing first-time
  onboarding with active-seat governance.
- Sign-up admission reservations had no rollback path when downstream provider
  or configuration failures occurred after the reservation was accepted.
- Password reset completion left the user on the reset form instead of handing
  them back to login with a clear completion message.

FILES CHANGED:
- apps/web/app/api/auth/signup/route.ts
- apps/web/app/api/users/me/profile/route.ts
- apps/web/auth.ts
- apps/web/components/auth/login-panel.tsx
- apps/web/components/auth/reset-password-panel.tsx
- apps/web/components/settings/profile-settings-form.tsx
- apps/web/lib/server/active-normal-user-session-governance.ts
- apps/web/lib/server/auth-admission-governance.ts
- apps/web/lib/server/session.ts
- packages/shared-types/src/user.ts
- zootopia-club-next-ledger.txt

MIGRATIONS:
- No migration added.
- No migration changed.

TECHNICAL DETAILS:
1. Capacity now starts only after required profile completion
   - Added `shouldApplyActiveNormalUserCapacity(...)` in
     `apps/web/lib/server/active-normal-user-session-governance.ts`.
   - Auth.js credential authorization in `apps/web/auth.ts` now consults the
     normalized profile-completion truth before reserving the decisive active
     lease.
   - Session rehydration in `apps/web/lib/server/session.ts` now releases any
     stale lease for profile-incomplete, admin, or exempt identities instead of
     renewing occupancy.
   - Result: signup success and early login remain distinct from later active
     capacity admission, and pre-completion users stay outside queue counting.

2. Profile completion is now the exact server-owned admission transition point
   - `apps/web/app/api/users/me/profile/route.ts` now detects the transition from
     `profileCompleted=false` to `profileCompleted=true`.
   - On that transition, the route evaluates active-capacity admission by email
     and returns a new `completionTransition` payload describing:
     - whether the user just became queue-eligible,
     - whether capacity is available, full, or temporarily unavailable,
     - and the current capacity snapshot when available.
   - `packages/shared-types/src/user.ts` now exposes the typed response contract
     for that transition payload.

3. Settings/profile UX now explains the admission transition clearly
   - `apps/web/components/settings/profile-settings-form.tsx` now treats profile
     completion as the moment the user enters normal admission control.
   - After a successful completion transition:
     - capacity available: show a success message, then redirect normally;
     - capacity full: show a warning message, then return the user to login with
       `profileTransition=1&profileTransitionState=capacity_full`;
     - admission unavailable: show a warning message, then return the user to
       login with `profileTransition=1&profileTransitionState=admission_unavailable`.
   - The form continues to write only through `/api/users/me/profile` and never
     sends a user id from the client.

4. Login no longer confuses fresh onboarding with active-seat blocking
   - `apps/web/components/auth/login-panel.tsx` still uses login admission
     preflight for pacing and shaping, but it no longer hard-blocks on
     `AUTH_ACTIVE_USER_CAPACITY_FULL` or
     `AUTH_ACTIVE_USER_ADMISSION_UNAVAILABLE`.
   - Those two outcomes are now deferred to the decisive Auth.js/session
     authority so incomplete-profile users are not blocked before they ever
     become queue-eligible.
   - The login screen now also displays localized EN/AR status messaging for:
     - password reset completion,
     - post-profile-completion queue transition,
     - sign-up rate limiting,
     - and temporary sign-up unavailability.

5. Sign-up admission reservations now roll back on downstream failure
   - Added `rollbackAuthAdmissionAttempt(...)` in
     `apps/web/lib/server/auth-admission-governance.ts`.
   - `apps/web/app/api/auth/signup/route.ts` now rolls back the accepted sign-up
     admission reservation when:
     - the sign-up Supabase client is unavailable, or
     - Supabase sign-up returns a duplicate/provider failure.
   - This reduces false pressure on account/IP shaping windows during retries and
     makes burst handling more truthful under contention.

6. Password reset now returns the user cleanly to login
   - `apps/web/components/auth/reset-password-panel.tsx` now redirects to login
     after a successful reset with `passwordReset=1` and prefilled email when
     available.
   - `apps/web/components/auth/login-panel.tsx` consumes that query state and
     shows a clear success message before sign-in.

7. Owner-scoped behavior remains strict
   - Profile persistence still flows through the authenticated self route only.
   - Queue eligibility now keys off server-owned profile completion truth rather
     than client timing.
   - Auth.js remains the trust boundary for decisive sign-in/session authority.
   - No client-side fake queue truth was introduced.

CONCURRENCY / 3000+ SIGN-UP ASSESSMENT:
- Code-traced proof:
  - Public auth shaping is already backed by
    `apps/web/lib/server/auth-admission-governance.ts`, which uses durable
    Postgres governance rows plus row-level update flow to coordinate attempt
    windows across concurrent requests.
  - The new rollback path prevents post-reservation provider failures from
    artificially exhausting those shaping windows during heavy retry storms.
  - Queue/capacity counting is now deferred until profile completion, so first
    sign-up/login bursts do not consume active-user seats prematurely.
- Remaining live-scale gap:
  - I did not run a true 3000+ concurrent sign-up load test in this environment.
  - Therefore 3000+ tolerance is partially proven by code trace and build
    verification, not fully validated by live traffic simulation.

VERIFICATION:
Code-trace verification:
✓ Confirmed Auth.js authorization now checks profile completion before active
  lease reservation and releases stale leases for non-counted identities.
✓ Confirmed session rehydration now mirrors the same profile-gated capacity rule.
✓ Confirmed profile completion is now the server-owned transition point that
  emits admission-state feedback to the client.
✓ Confirmed login preflight no longer hard-blocks on capacity-full/admission-
  unavailable outcomes before decisive server auth.
✓ Confirmed sign-up reservations now have a rollback path after downstream
  provider/configuration failure.
✓ Confirmed reset success now returns the user to login with explicit status
  messaging.

Command verification:
✓ `npm run lint --workspace @zootopia/web` — PASS
✓ `npm run typecheck --workspace @zootopia/web` — PASS
✓ `npm run build:web` — PASS
✓ `npm run build:vercel --workspace @zootopia/web` — PASS

Runtime verification:
✓ `GET /login` on the local production server returned `200`.
✓ `GET /forgot-password` returned `200`.
✓ `GET /reset-password` returned `200`.
✓ `GET /confirm-email` returned `200`.
✓ Anonymous `GET /settings` redirected to `/login`.
✓ Invalid signup payload returned
  `400 SIGNUP_EMAIL_INVALID`.
✓ Invalid login-admission payload returned
  `400 LOGIN_EMAIL_INVALID`.
✓ Anonymous profile update returned
  `401 UNAUTHENTICATED`.
~ Local production runtime could not fully exercise live auth/session mail flows
  because the standalone server reported missing runtime requirements:
  - Auth.js `secret`
  - Supabase admin runtime / database persistence requirements
~ Local password-reset request runtime proof therefore stopped at the truthful
  environment failure response:
  `503 PASSWORD_RESET_UNAVAILABLE`.

RISKS / FOLLOW-UPS:
~ `ZOOTOPIA_AUTH_ADMISSION_HASH_SALT` is still unset in build/runtime logs, so
  account/IP governance buckets may not stay stable across deploys.
~ Live verification of duplicate sign-up handling, confirm-email token flows,
  password-reset token expiry, and true 3000+ concurrent sign-up traffic still
  requires an environment with production auth secrets, mail delivery, and a
  controlled load harness.

2026-04-19: SUPABASE CREDIT BRIDGE AUDIT + RELATIONAL BACKFILL HARDENING
────────────────────────────────────────────────────────────────────────────

ROOT CAUSE / AUDIT OUTCOME:
- The live Supabase/Auth.js integration, owner-scoped storage paths, admin
  credit mutation authority, and owner-scoped credit invalidation delivery were
  already professionally server-owned in the current codebase.
- The concrete architecture gap was historical schema drift inside the hybrid
  credit bridge: older `zc_entities` credit account / daily-ledger / grant /
  mutation-history rows could remain the only or newer copy until a fresh write
  touched that owner again.
- Repository reads already compensated by reconciling legacy and structured
  state by timestamp, but the relational schema itself could lag behind the
  effective owner-scoped truth.

FILES CHANGED:
- supabase/migrations/20260419110000_credit_bridge_relational_backfill.sql
- zootopia-club-next-ledger.txt

MIGRATIONS:
- Added `supabase/migrations/20260419110000_credit_bridge_relational_backfill.sql`

TECHNICAL DETAILS:
1. Full credit-bridge relational backfill
   - Added a new migration that reads legacy `zc_entities` collections:
     - `assessmentCreditAccounts`
     - `assessmentDailyCredits`
     - `assessmentCreditGrants`
     - `assessmentCreditMutationHistory`
   - The migration hydrates the canonical relational tables:
     - `public.assessment_credit_accounts`
     - `public.assessment_daily_credits`
     - `public.assessment_credit_grants`
     - `public.assessment_credit_mutations`

2. Safe convergence rules
   - Accounts, daily ledgers, and grants now use timestamp-gated upserts:
     the legacy row can refresh the structured row only when the legacy mirror
     is newer by `updated_at`.
   - Mutation history stays append-only and inserts only missing relational rows
     (`ON CONFLICT DO NOTHING`) because those records are immutable events.
   - Result: the migration repairs stale structured credit rows without
     overwriting newer relational truth.

3. Query-shape index hardening
   - Added `assessment_credit_grants_owner_uid_created_idx` on
     `public.assessment_credit_grants (owner_uid, created_at DESC)`.
   - This matches the repository grant-history/admin-detail read path and keeps
     owner-scoped grant inspection aligned with the actual SQL access pattern.

4. What was intentionally preserved
   - No auth rewrite.
   - No Auth.js trust-boundary change.
   - No client-side authority expansion.
   - No storage-path redesign.
   - No RLS weakening.
   - No removal of legacy mirrors or working hybrid reads.
   - No unrelated UI or route changes.

AUDIT FINDINGS PRESERVED AS CURRENT REALITY:
- `apps/web/auth.ts` + `apps/web/lib/server/session.ts` still keep Auth.js as
  the decisive server trust boundary; Supabase browser auth/session remains a
  helper path, not the protected-data authority.
- Owner-scoped document/result/export reads still flow through owner-bound
  repository loaders plus storage-path assertions, not client-supplied owner ids.
- Admin credit mutations remain backend-authoritative and self-target mutation
  remains forbidden.
- Credit live updates remain invalidation-only private realtime topics
  (`assessment-credit:owner:{uid}`), not a second client balance truth.

VERIFICATION:
Code-trace verification:
✓ Confirmed protected auth/session authority remains
  `Auth.js cookie -> auth() -> apps/web/lib/server/session.ts -> persisted user`.
✓ Confirmed admin credit mutation writes still originate in
  `applyAdminAssessmentCreditMutation(...)` and remain backend-owned.
✓ Confirmed upload/result/export storage paths remain owner-scoped under
  `users/{uid}/...` and still require server-side ownership checks.
✓ Confirmed the new migration is additive and does not remove or bypass the
  current hybrid compatibility mirrors.

Command verification:
✓ `npm run supabase:migrations:validate` — PASS
✓ `npm run supabase:migrations:list` — PASS
✓ `npm run lint` — PASS
✓ `npm run typecheck` — PASS
✓ `npm run build` — PASS
✓ `npm run build:vercel` — PASS

Runtime verification:
~ No live Supabase database connection was available in this environment, so the
  new migration was validated by static review plus migration-order checks, not
  by execution against a real Supabase instance.
~ No authenticated multi-user runtime session was available here, so owner-scope
  isolation, private realtime delivery, admin mutation delivery, and storage
  artifact access remain code-traced rather than live dual-user proven.

REMAINING RISKS / FOLLOW-UP:
~ `supabase/migrations/20260410180001_phase1_storage_bucket.sql` still contains
  the old `zootopia-app` bucket creation while runtime and later migrations use
  `zootopia-private`; the current app path is correct, but the historical bucket
  migration remains documented drift and was not deleted without live project
  inspection.
~ Because the repository still intentionally reads both legacy and structured
  credit stores during the migration bridge, complete removal of legacy credit
  mirrors should wait for live database inspection and a dedicated cleanup pass.
~ Build logs still show the pre-existing operational warning:
  `ZOOTOPIA_AUTH_ADMISSION_HASH_SALT is unset`.
